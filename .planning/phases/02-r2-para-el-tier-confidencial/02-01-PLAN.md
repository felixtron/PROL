---
phase: 02-r2-para-el-tier-confidencial
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - pnpm-lock.yaml
  - apps/web/lib/r2.ts
  - apps/web/lib/env.ts
  - .env.example
  - .env.production.example
  - docker-compose.prod.yml
  - .gitignore
autonomous: true
requirements: [R2-01, R2-04]
parallel_safe: false  # Ola 1 tiene un solo plan a propósito: los cuatro planes de
                      # esta fase tocan los mismos módulos centrales y en la fase 1
                      # dos ejecutores concurrentes se pisaron el índice de git.

must_haves:
  truths:
    - "Una petición firmada desde `lib/r2.ts` escribe y lee un objeto real en el bucket `ibizadata`."
    - "Todo objeto que escribe PROL cae bajo `prol/`; el listado del bucket muestra `empresas/` y `leads/` intactos al lado."
    - "Arrancar con `R2_BUCKET` puesta y alguna de las otras tres credenciales ausente revienta el arranque con un mensaje en español que nombra las que faltan."
    - "Sin ninguna variable R2 la app arranca igual — es el caso de desarrollo y CI, y es el rollback de R2-04."
    - "`r2Head` existe, está exportada y documenta por qué no se usa todavía, para que nadie la borre como código muerto."
  artifacts:
    - path: "apps/web/lib/r2.ts"
      provides: "Cliente R2 genérico: r2Put, r2Get, r2Head, assertR2Env, isR2Configured, missingR2Env"
      contains: "aws4fetch"
      min_lines: 100
    - path: "apps/web/lib/env.ts"
      provides: "Fail-fast de arranque ante configuración R2 incompleta"
      contains: "R2_BUCKET"
    - path: "apps/web/package.json"
      provides: "Dependencia aws4fetch"
      contains: "aws4fetch"
    - path: ".env.example"
      provides: "Las cuatro variables R2 documentadas, vacías"
      contains: "R2_SECRET_ACCESS_KEY"
    - path: "docker-compose.prod.yml"
      provides: "Las cuatro variables R2 en el entorno del contenedor"
      contains: "R2_BUCKET"
  key_links:
    - from: "apps/web/lib/env.ts"
      to: "apps/web/lib/r2.ts"
      via: "import { missingR2Env }"
      pattern: "from \"@/lib/r2\""
    - from: "apps/web/lib/r2.ts"
      to: "aws4fetch"
      via: "new AwsClient({ service: \"s3\", region: \"auto\" })"
      pattern: "new AwsClient"
---

<objective>
Añadir el cliente de Cloudflare R2 y cerrar el hueco de configuración que la
investigación encontró: hoy `R2_BUCKET` presente con credenciales incompletas no
falla al arrancar, sólo al primer archivo real.

Purpose: dejar la frontera de transporte lista y probada contra el bucket real
**antes** de tocar `document-storage.ts`. Este plan no cambia dónde se guarda ni
un solo archivo — la aplicación sigue escribiendo a disco al terminarlo.
Output: `apps/web/lib/r2.ts` funcionando contra `ibizadata`, el arranque que
distingue las tres configuraciones posibles, y las cuatro variables declaradas
en los tres archivos de entorno del repo.
</objective>

<execution_context>
@/Users/flx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-r2-para-el-tier-confidencial/02-CONTEXT.md
@.planning/phases/02-r2-para-el-tier-confidencial/02-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

@apps/web/lib/cloudflare-stream.ts
@apps/web/lib/env.ts
@.env.example

<interfaces>
<!-- Contratos exactos, transcritos del código real. No hace falta explorar. -->

**El molde** (`apps/web/lib/cloudflare-stream.ts`, líneas 1-10). Constante de base
a nivel de módulo + `getCredentials()` que lanza en español + cada export llama a
`getCredentials()` primero y lanza si `!res.ok`:
```typescript
const CF_API_BASE = "https://api.cloudflare.com/client/v4";

function getCredentials() {
  const accountId = process.env.CLOUDFLARE_ACCOUNT_ID;
  const apiToken = process.env.CLOUDFLARE_STREAM_API_TOKEN;
  if (!accountId || !apiToken) {
    throw new Error("Credenciales de Cloudflare Stream no configuradas");
  }
  return { accountId, apiToken };
}
```

**El contrato que NO se puede romper** (`apps/web/lib/env.ts`, líneas 11-14, cita
literal del archivo):
> "Variables que deben existir para que la app sea funcional en cualquier
> despliegue real. No incluye llaves de servicios opcionales (IA, Stripe,
> Cloudflare Stream, Resend) — esas están gateadas por feature flags o rutas
> específicas y se validan en su propio módulo si se usan."

Es decir: las variables R2 **no entran en `CriticalEnvSchema`**. El chequeo de
coherencia va aparte, dentro de la misma función, y la lógica de "qué falta" vive
en `lib/r2.ts`.

**Forma actual de `assertCriticalServerEnv`** (`apps/web/lib/env.ts`, líneas 26-51),
con los números de línea donde hay que insertar:
```typescript
let validated = false;                                       // 26

export function assertCriticalServerEnv(): void {            // 28
  if (validated) return;                                     // 29
  if (process.env.NEXT_PHASE === "phase-production-build") return;  // 35
  validated = true;                                          // 37
  // ← AQUÍ va el bloque nuevo (después de 37, ANTES del safeParse de 39).
  //   Si fuera después, el `return` de la línea 40 lo saltaría en el caso feliz.
  const result = CriticalEnvSchema.safeParse(process.env);   // 39
  if (result.success) return;                                // 40
  // ... construcción del mensaje ...
  if (process.env.NODE_ENV === "production") throw new Error(message);  // 47-49
  console.warn(`[env] ${message}`);                          // 50
}
```
Se invoca desde `apps/web/lib/auth.ts` línea 11, a nivel de módulo: **eso es el
arranque**. `middleware.ts` no importa `auth.ts` y no hay ninguna ruta con
`runtime = "edge"` en el repo (verificado por grep), así que meter `aws4fetch` en
este grafo de imports es seguro.

**El patrón de log estructurado de la casa**
(`apps/web/lib/compliance-dispatch.ts`, líneas 46-66): objeto con
`ts`/`level`/`component`/`msg` + campos; `JSON.stringify` en producción, texto
legible en desarrollo.

**Constructor de `aws4fetch`** (verificado vía Context7, `/mhart/aws4fetch`,
v1.0.20, cero dependencias):
```javascript
new AwsClient({
  accessKeyId, secretAccessKey,
  service: 's3',        // pre-especificar evita el parseo del hostname
  region: 'auto',
  retries: 3,           // por defecto reintenta con backoff exponencial
  initRetryMs: 50,
})
// aws.fetch() NO lanza por códigos de estado HTTP — sólo por errores de red o
// de construcción. El bucle interno sólo reintenta en status >= 500 o 429.
```
</interfaces>
</context>

<constraints>
- **`region: "auto"` y `service: "s3"` van explícitos** en el constructor del
  `AwsClient`. Decisión cerrada: no dependemos de la autodetección por hostname
  para `<account>.r2.cloudflarestorage.com`, que la documentación no cubre al 100%.
- **`lib/r2.ts` no sabe nada de PROL.** Ni del prefijo `prol/`, ni de `evidence`,
  ni de `templates`. Es un cliente de R2, igual que `cloudflare-stream.ts` es un
  cliente de la API de Cloudflare. Toda la política vive en `document-storage.ts`
  (plan 02-02).
- **Ninguna operación de borrado.** No implementar `r2Delete` ni nada equivalente,
  "por si acaso" tampoco. El bucket `ibizadata` está compartido con el CRM de
  IBIZA en producción (prefijos `empresas/` y `leads/`). Tampoco tocar reglas de
  ciclo de vida ni versionado del bucket.
- **Cero cambios de comportamiento en el almacenamiento.** Este plan NO toca
  `apps/web/lib/document-storage.ts`. Al terminarlo la app sigue escribiendo a
  disco exactamente como hoy.
- **Nunca commitear valores reales de credenciales.** Los cuatro `R2_*` ya están
  en el `.env` local, que está gitignored. En los `.env*.example` van vacíos.
- **La puerta de lint sale con exit 1 estando sana**: `✖ 81 problems (0 errors,
  81 warnings)`. No "arreglar" esas 81. Lo que sí importa: el código nuevo no
  debe añadir ninguna advertencia (nada de `any`, nada sin usar dentro del módulo).
</constraints>

<tasks>

<task type="auto">
  <name>Tarea 1: dependencia aws4fetch y módulo apps/web/lib/r2.ts</name>
  <files>apps/web/package.json, pnpm-lock.yaml, apps/web/lib/r2.ts</files>
  <action>
**1. Instalar la dependencia** (el paquete de `apps/web` se llama `@prol/web`):

```bash
pnpm --filter @prol/web add aws4fetch
```

Debe quedar como `"aws4fetch": "^1.0.20"` en `dependencies`, con semver directo
como el resto (`stripe@^20.4.1`, `resend@^6.9.4`). No hay `catalog:` en este
monorepo. Cero dependencias transitivas: si el lockfile crece con algo más que
`aws4fetch`, parar y reportar.

**2. Crear `apps/web/lib/r2.ts`.** Cabecera:

```typescript
// Cliente de Cloudflare R2 (S3-compatible) para el tier confidencial.
//
// Genérico a propósito: sabe de R2 y de nada más. Qué prefijo lleva el bucket,
// qué claves son válidas y cuándo se usa R2 en vez del disco son política de
// PROL y viven en `lib/document-storage.ts` — igual que `cloudflare-stream.ts`
// sabe de la API de Cloudflare y no del dominio.
//
// Sólo servidor. Se firma SigV4 con `aws4fetch` (~6 KB, cero dependencias) en
// vez de `@aws-sdk/client-s3` (~2 MB en ~40 paquetes): `.npmrc` documenta los
// timeouts del VPS contra el registry, y esto son dos operaciones, no un SDK.

import { AwsClient } from "aws4fetch";
```

Contenido, en este orden:

```typescript
const R2_ENV_KEYS = [
  "R2_ACCOUNT_ID",
  "R2_ACCESS_KEY_ID",
  "R2_SECRET_ACCESS_KEY",
  "R2_BUCKET",
] as const;

/** Cuáles de las cuatro variables faltan. Vacío = configuración completa. */
export function missingR2Env(): string[] {
  return R2_ENV_KEYS.filter((key) => !process.env[key]);
}

/**
 * Las cuatro presentes. Es lo único que decide si el backend de almacenamiento
 * es R2 o disco (`STORAGE_BACKEND` en `lib/document-storage.ts`).
 */
export function isR2Configured(): boolean {
  return missingR2Env().length === 0;
}

/** Lanza en español si falta cualquiera de las cuatro. */
export function assertR2Env(): void {
  getCredentials();
}

function getCredentials() {
  const accountId = process.env.R2_ACCOUNT_ID;
  const accessKeyId = process.env.R2_ACCESS_KEY_ID;
  const secretAccessKey = process.env.R2_SECRET_ACCESS_KEY;
  const bucket = process.env.R2_BUCKET;
  if (!accountId || !accessKeyId || !secretAccessKey || !bucket) {
    throw new Error(
      `Credenciales de R2 no configuradas: faltan ${missingR2Env().join(", ")}`,
    );
  }
  return { accountId, accessKeyId, secretAccessKey, bucket };
}
```

Cliente cacheado a nivel de módulo (a diferencia de `cloudflare-stream.ts`, que
no cachea nada porque cada `fetch` suelto es barato de rearmar: aquí `aws4fetch`
deriva material de firma SigV4 y conviene reutilizarlo entre peticiones):

```typescript
let cached: { accessKeyId: string; client: AwsClient } | null = null;

function getClient(accessKeyId: string, secretAccessKey: string): AwsClient {
  if (cached && cached.accessKeyId === accessKeyId) return cached.client;
  const client = new AwsClient({
    accessKeyId,
    secretAccessKey,
    // Explícitos a propósito: no dependemos de la autodetección por hostname
    // para `<account>.r2.cloudflarestorage.com`. R2 no tiene regiones y su
    // endpoint S3 espera "auto".
    service: "s3",
    region: "auto",
    // Por defecto reintenta muchas más veces. Estas llamadas ocurren dentro de
    // una petición HTTP de un usuario: preferimos fallar en segundos a dejar
    // una subida colgada minutos.
    retries: 3,
    initRetryMs: 50,
  });
  cached = { accessKeyId, client };
  return client;
}

function objectUrl(accountId: string, bucket: string, key: string): string {
  // Path-style sobre el endpoint de cuenta. Las barras de `key` van literales:
  // son separadores de la clave del objeto, no caracteres a codificar.
  return `https://${accountId}.r2.cloudflarestorage.com/${bucket}/${key}`;
}
```

Log estructurado, calcado del patrón de `compliance-dispatch.ts` con
`component: "r2"`:

```typescript
function log(
  level: "warn" | "error",
  msg: string,
  fields: Record<string, unknown> = {},
) {
  const record = { ts: new Date().toISOString(), level, component: "r2", msg, ...fields };
  if (process.env.NODE_ENV === "production") {
    console[level === "error" ? "error" : "warn"](JSON.stringify(record));
  } else {
    console[level === "error" ? "error" : "warn"](`[${level}] [r2] ${msg}`, fields);
  }
}
```

Las tres operaciones:

```typescript
/** Escribe un objeto. Lanza si R2 responde algo que no sea 2xx. */
export async function r2Put(
  key: string,
  body: Buffer,
  contentType: string,
): Promise<void> {
  const { accountId, accessKeyId, secretAccessKey, bucket } = getCredentials();
  const res = await getClient(accessKeyId, secretAccessKey).fetch(
    objectUrl(accountId, bucket, key),
    {
      method: "PUT",
      headers: { "Content-Type": contentType || "application/octet-stream" },
      body: new Uint8Array(body),
    },
  );
  if (!res.ok) {
    const text = await res.text().catch(() => "");
    throw new Error(`R2 PUT ${res.status}: ${text.slice(0, 300)}`);
  }
}

/**
 * Lee un objeto. Devuelve `null` si no existe.
 *
 * `aws4fetch` NO lanza por códigos de estado: un 403 por token sin permiso y un
 * 500 por R2 caído llegan aquí igual que un 404. Devolvemos `null` en los tres
 * casos para conservar el contrato de `readPrivateFile`, pero los que no son 404
 * se loguean: sin ese rastro, una credencial rota se ve exactamente igual que
 * "el archivo no existe", y el síntoma sería "todas las evidencias devuelven
 * 404 a la vez".
 */
export async function r2Get(key: string): Promise<Buffer | null> {
  const { accountId, accessKeyId, secretAccessKey, bucket } = getCredentials();
  const res = await getClient(accessKeyId, secretAccessKey).fetch(
    objectUrl(accountId, bucket, key),
    { method: "GET" },
  );
  if (res.status === 404) return null;
  if (!res.ok) {
    log("error", "GET fallido", { status: res.status, key });
    return null;
  }
  return Buffer.from(await res.arrayBuffer());
}

/**
 * Metadatos de un objeto sin descargarlo.
 *
 * NO SE USA EN ESTA FASE, y es deliberado. `readPrivateFile` necesita los bytes
 * de todas formas, así que un HEAD antes de cada GET sería un viaje de red de
 * más: un GET que recibe 404 ya distingue "no existe" sin ayuda (en disco `stat`
 * es barato y local, aquí no). Existe porque la fase 6 —subida directa por URL
 * firmada— tiene que comprobar el tamaño real de lo que subió el navegador
 * antes de confirmar la evidencia, y ese es su único consumidor previsto.
 *
 * No borrar por "código muerto".
 */
export async function r2Head(
  key: string,
): Promise<{ size: number; contentType: string | null } | null> {
  const { accountId, accessKeyId, secretAccessKey, bucket } = getCredentials();
  const res = await getClient(accessKeyId, secretAccessKey).fetch(
    objectUrl(accountId, bucket, key),
    { method: "HEAD" },
  );
  if (res.status === 404) return null;
  if (!res.ok) {
    log("error", "HEAD fallido", { status: res.status, key });
    return null;
  }
  const length = res.headers.get("content-length");
  return {
    size: length ? Number(length) : 0,
    contentType: res.headers.get("content-type"),
  };
}
```

**Qué NO añadir:** ninguna función de borrado, ninguna de listado (el listado que
hace falta para verificar el prefijo vive en el script desechable de la tarea 3),
ninguna de presigna (es la fase 6), y ninguna referencia a `prol/`.
  </action>
  <verify>
    <automated>
# La dependencia entró y es la única nueva
grep -n 'aws4fetch' apps/web/package.json
git diff pnpm-lock.yaml | grep -c '^+ *aws4fetch' || true
git diff --stat pnpm-lock.yaml

# Superficie exacta del módulo: las tres operaciones + los tres helpers de entorno
grep -n '^export ' apps/web/lib/r2.ts

# Nada de borrado, nada de listado, nada de prefijo de PROL
grep -niE 'delete|listobjects|list-type|prol/' apps/web/lib/r2.ts && echo "ERROR: superficie prohibida" || echo "OK: sin borrado, listado ni prefijo"

# Región y servicio explícitos
grep -n 'region: "auto"' apps/web/lib/r2.ts
grep -n 'service: "s3"' apps/web/lib/r2.ts

pnpm exec turbo run check-types
    </automated>
  </verify>
  <done>
`apps/web/lib/r2.ts` exporta exactamente `missingR2Env`, `isR2Configured`,
`assertR2Env`, `r2Put`, `r2Get`, `r2Head` y nada más. El `AwsClient` se construye
con `service: "s3"` y `region: "auto"` explícitos. El archivo no menciona `prol/`,
no borra y no lista. `aws4fetch` está en `apps/web/package.json` y no arrastró
dependencias transitivas. `check-types` limpio.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: fail-fast de arranque ante configuración R2 incompleta</name>
  <files>apps/web/lib/env.ts</files>
  <action>
Este es el hueco que encontró la investigación: `R2_BUCKET` presente con
credenciales incompletas **no falla al arrancar**, sólo al primer archivo real —
y para entonces una evidencia ya se está escribiendo (o perdiendo) en el disco
efímero del contenedor. El warning que proponía el CONTEXT (`!R2_BUCKET &&
!PRIVATE_UPLOAD_DIR`) no lo detecta, porque `R2_BUCKET` sí existe.

**1. Import.** Añadir al grupo de imports locales (después de `import { z } from
"zod";`, que es dependencia externa):

```typescript
import { missingR2Env } from "@/lib/r2";
```

**2. El bloque.** Insertar en `assertCriticalServerEnv()` **justo después de
`validated = true;` (línea 37) y ANTES del `safeParse` de la línea 39**. El orden
importa: la línea 40 hace `return` en el caso feliz, así que cualquier cosa
puesta después se saltaría siempre.

```typescript
  // Coherencia del almacenamiento confidencial. No entra en `CriticalEnvSchema`
  // a propósito —el comentario de arriba dice que las llaves de servicios
  // opcionales se validan en su propio módulo—, pero sí tiene que reventar el
  // arranque: `R2_BUCKET` presente con credenciales incompletas arranca, pasa
  // el healthcheck, y sólo falla al primer archivo real. Para entonces las
  // evidencias ya se estarían escribiendo en el disco efímero del contenedor,
  // que es exactamente el agujero que la fase 1 acaba de cerrar. Degradar a
  // disco en silencio sería peor que no arrancar.
  //
  // La asimetría es deliberada: `R2_BUCKET` es el interruptor. Sin ella la app
  // usa disco y las otras tres sobran — ése es el rollback de R2-04, un solo
  // cambio de variable. Con ella, las cuatro son obligatorias.
  if (process.env.R2_BUCKET) {
    const missing = missingR2Env();
    if (missing.length > 0) {
      throw new Error(
        `Configuración de R2 incompleta: R2_BUCKET está definida pero faltan ${missing.join(", ")}. ` +
          `Compléta(las) en el entorno o quita R2_BUCKET para volver al disco local.`,
      );
    }
  } else if (
    process.env.NODE_ENV === "production" &&
    !process.env.PRIVATE_UPLOAD_DIR
  ) {
    // Ni bucket ni volumen: los archivos confidenciales van a un directorio que
    // desaparece al recrear el contenedor. Espeja el aviso de
    // `warnedAboutPrivateDir` en `lib/upload-paths.ts`; aquí no hace falta su
    // propio flag anti-repetición porque `validated` ya protege toda la función.
    console.warn(
      JSON.stringify({
        level: "warn",
        component: "storage",
        msg: "Sin R2_BUCKET ni PRIVATE_UPLOAD_DIR: las evidencias van a un directorio no persistente",
      }),
    );
  }
```

**Por qué el throw no lleva guarda de `NODE_ENV`:** una configuración parcial de
R2 no es nunca intencional. En desarrollo y en CI **no hay ninguna** variable R2
puesta, así que ese camino ni se toca; el único modo de disparar el throw es
haber puesto el bucket y haberse dejado una llave, que es un error en cualquier
entorno. El `NEXT_PHASE === "phase-production-build"` de la línea 35 ya protege
el `next build`, que corre sin variables de runtime.

**Qué NO cambia:** `CriticalEnvSchema` (no se le añade ni un campo), el orden del
`safeParse`, el mensaje de error de las variables críticas, el singleton
`validated`, y la firma de la función.
  </action>
  <verify>
    <automated>
# El bloque está donde toca: después de `validated = true`, antes del safeParse
grep -n 'validated = true' apps/web/lib/env.ts
grep -n 'R2_BUCKET' apps/web/lib/env.ts
grep -n 'CriticalEnvSchema.safeParse' apps/web/lib/env.ts
# La línea del R2_BUCKET debe ser MENOR que la del safeParse

# CriticalEnvSchema intacto: sigue con exactamente 4 campos y ninguno R2
sed -n '/^const CriticalEnvSchema/,/^});/p' apps/web/lib/env.ts | grep -c 'R2_' | grep -q '^0$' && echo "OK: el esquema crítico no menciona R2"

grep -n 'missingR2Env' apps/web/lib/env.ts

pnpm exec turbo run check-types

# Prueba de arranque REAL: bucket presente, cuenta ausente => el arranque revienta
# nombrando lo que falta. (El servidor de desarrollo hereda el resto del .env.)
R2_ACCOUNT_ID= pnpm --filter @prol/web dev > /tmp/prol-env-failfast.log 2>&1 &
sleep 25
curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3000/login
grep -n 'Configuración de R2 incompleta' /tmp/prol-env-failfast.log
grep -n 'R2_ACCOUNT_ID' /tmp/prol-env-failfast.log
pkill -f 'next dev' || true

# Arranque sano con las cuatro variables del .env local: sin ese error
pnpm --filter @prol/web dev > /tmp/prol-env-ok.log 2>&1 &
sleep 25
curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3000/login   # 200
grep -c 'Configuración de R2 incompleta' /tmp/prol-env-ok.log | grep -q '^0$' && echo "OK: arranque sano"
pkill -f 'next dev' || true
    </automated>
  </verify>
  <done>
`assertCriticalServerEnv()` lanza, con `R2_BUCKET` puesta y `R2_ACCOUNT_ID`
ausente, un error en español que nombra `R2_ACCOUNT_ID`, y el log del servidor lo
muestra. Con las cuatro variables del `.env` local el arranque es limpio y
`/login` responde 200. `CriticalEnvSchema` sigue con sus cuatro campos y sin
ninguna variable R2. `check-types` limpio.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: declarar las variables en los tres entornos y probar el bucket real</name>
  <files>.env.example, .env.production.example, docker-compose.prod.yml, .gitignore</files>
  <action>
**1. `.env.example`** — añadir al final, después del bloque de
`PRIVATE_UPLOAD_DIR` (línea 69), con los valores **vacíos**:

```
# Cloudflare R2 — almacenamiento de los archivos confidenciales (evidencias y
# plantillas por empresa). Las CUATRO van juntas: R2_BUCKET es el interruptor y,
# si está sin las otras tres, la app NO arranca — mejor eso que escribir
# evidencias en un disco efímero creyendo que van al bucket.
# Vacías = la app guarda en PRIVATE_UPLOAD_DIR. Ése es el rollback: quitar
# R2_BUCKET y reiniciar, sin desplegar código.
# El bucket está COMPARTIDO con otro producto: PROL sólo escribe bajo `prol/`,
# y nunca borra ni toca reglas de ciclo de vida. Ver DEPLOY.md.
# NUNCA commitear valores reales: en producción se aplican por SSH al env del
# contenedor (/etc/containers/env/prol-web-1.env).
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET=""
```

**2. `.env.production.example`** — mismo bloque (comentario abreviado a las tres
primeras líneas), después de `PRIVATE_UPLOAD_DIR="/app/private-uploads"` (línea 56),
también con valores vacíos.

**3. `docker-compose.prod.yml`** — en el mapa `environment` del servicio `web`,
justo después de `PRIVATE_UPLOAD_DIR` (línea 37):

```yaml
      # Cloudflare R2 (tier confidencial). Las cuatro juntas o ninguna: con
      # R2_BUCKET y sin las otras tres el contenedor no arranca. Sin ninguna,
      # los archivos van a PRIVATE_UPLOAD_DIR.
      R2_ACCOUNT_ID: ${R2_ACCOUNT_ID:-}
      R2_ACCESS_KEY_ID: ${R2_ACCESS_KEY_ID:-}
      R2_SECRET_ACCESS_KEY: ${R2_SECRET_ACCESS_KEY:-}
      R2_BUCKET: ${R2_BUCKET:-}
```

Recordatorio de la fase 1: producción **no** corre este compose (usa quadlets de
podman con el env en `/etc/containers/env/prol-web-1.env`), pero el compose es la
receta para reconstruir desde cero y se mantiene sincronizado por convención.

**4. `.gitignore`** — el backend de disco escribe en `<cwd>/private-uploads`
cuando `PRIVATE_UPLOAD_DIR` está vacía, y con `pnpm --filter @prol/web dev` ese
cwd es `apps/web`. Ese directorio **no está ignorado hoy** (`.gitignore` sólo
tiene `apps/web/public/uploads/`), así que en cuanto los planes 02-02 y 02-03
suban un archivo de prueba aparecería en `git status` y podría acabar commiteado.
Añadir al bloque "Uploads (local dev)" (después de la línea 45):

```
# Archivos confidenciales en desarrollo: evidencias y plantillas de prueba.
# Nunca deben entrar al repositorio.
private-uploads/
```

**5. Humo real contra el bucket, con script desechable.** El repo no tiene test
runner y añadirlo está fuera de alcance, así que la comprobación de que las
credenciales firman y de que las claves caen bajo `prol/` se hace con un script
que **no se commitea**. Usa `aws4fetch` directamente (un `.mjs` no puede importar
el `.ts` de `lib/r2.ts`); el envoltorio TypeScript se prueba de punta a punta en
el plan 02-02, a través del servidor real.

Crear `apps/web/scripts/_tmp-r2-smoke.mjs` (colocado ahí para que la resolución
de módulos encuentre `apps/web/node_modules/aws4fetch`):

```javascript
import { randomUUID } from "node:crypto";
import { AwsClient } from "aws4fetch";

const { R2_ACCOUNT_ID: acct, R2_ACCESS_KEY_ID: id, R2_SECRET_ACCESS_KEY: secret, R2_BUCKET: bucket } = process.env;
if (!acct || !id || !secret || !bucket) { console.error("Faltan variables R2"); process.exit(1); }

const aws = new AwsClient({ accessKeyId: id, secretAccessKey: secret, service: "s3", region: "auto" });
const base = `https://${acct}.r2.cloudflarestorage.com/${bucket}`;
const key = `prol/_smoke/${randomUUID()}.txt`;
const payload = `humo ${new Date().toISOString()}`;
```

El script debe hacer, en este orden, e imprimir el resultado de cada paso:

1. **Guarda del prefijo**: `if (!key.startsWith("prol/")) throw ...`.
2. **PUT** de `payload` en `key` con `Content-Type: text/plain`. Exigir `res.ok`.
3. **GET** de `key`. Exigir 200 y que el cuerpo sea **idéntico** a `payload`.
4. **HEAD** de `key`. Exigir 200 y `content-length === Buffer.byteLength(payload)`.
5. **GET de una clave inexistente** (`prol/_smoke/${randomUUID()}.txt`). Exigir
   exactamente `404` — es la comprobación de que `aws4fetch` no lanza por status
   y de que "no existe" es distinguible.
6. **Listado bajo el prefijo**: `GET ${base}?list-type=2&prefix=prol/&max-keys=200`.
   Parsear los `<Key>` del XML y exigir que **todas** empiecen por `prol/`.
   Imprimir cuántas hay.
7. **Listado de la raíz con delimitador**: `GET ${base}?list-type=2&delimiter=%2F&max-keys=200`.
   Imprimir los `<Prefix>` de `<CommonPrefixes>`. Sirve para dejar constancia en
   el SUMMARY de que `empresas/`, `leads/` y `prol/` conviven y de que PROL sólo
   escribió en el suyo.

Salir con código 1 en cuanto cualquier paso falle. **No borrar nada**: el objeto
de humo se queda en el bucket, porque en esta fase no existe ninguna operación de
borrado y no la vamos a improvisar aquí. Son unas decenas de bytes bajo
`prol/_smoke/`; anotarlo en el SUMMARY.

Ejecutar y borrar:

```bash
node --env-file=.env apps/web/scripts/_tmp-r2-smoke.mjs
rm apps/web/scripts/_tmp-r2-smoke.mjs
```

Copiar la salida completa al SUMMARY: es la única evidencia que queda de que las
credenciales funcionan y de que el bucket compartido está donde se cree.
  </action>
  <verify>
    <automated>
# Las cuatro variables declaradas, vacías, en los tres archivos de entorno
grep -n 'R2_ACCOUNT_ID\|R2_ACCESS_KEY_ID\|R2_SECRET_ACCESS_KEY\|R2_BUCKET' .env.example .env.production.example docker-compose.prod.yml

# Ningún valor real filtrado a un archivo versionado
grep -rnE 'R2_(ACCOUNT_ID|ACCESS_KEY_ID|SECRET_ACCESS_KEY|BUCKET)=("")?$' .env.example .env.production.example
git diff .env.example .env.production.example docker-compose.prod.yml | grep -E '^\+.*R2_.*=.+' | grep -vE '=\s*""|:-\}|\$\{' && echo "ERROR: posible credencial commiteada" || echo "OK: sin valores reales"

# private-uploads ignorado
grep -n 'private-uploads' .gitignore

# Humo contra el bucket real
node --env-file=.env apps/web/scripts/_tmp-r2-smoke.mjs
# Esperado: PUT ok, GET 200 con cuerpo idéntico, HEAD con tamaño correcto,
# GET inexistente = 404, todas las claves bajo `prol/`, y los CommonPrefixes
# mostrando `empresas/`, `leads/` y `prol/`.

rm -f apps/web/scripts/_tmp-r2-smoke.mjs
git status --porcelain | grep '_tmp-r2-smoke' && echo "ERROR: el script sigue ahí" || echo "OK: script desechable eliminado"

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep 'problems'
pnpm exec turbo run build
    </automated>
  </verify>
  <done>
Los tres archivos de entorno declaran las cuatro variables con valor vacío y sin
ninguna credencial real en el diff. `.gitignore` ignora `private-uploads/`. El
script de humo completó los siete pasos contra `ibizadata`: escribió y leyó un
objeto bajo `prol/_smoke/`, un GET a una clave inexistente devolvió 404 sin
lanzar, el listado con `prefix=prol/` no encontró ni una clave fuera del prefijo,
y el listado de la raíz muestra `prol/` conviviendo con los prefijos del otro
producto. El script ya no existe. `check-types` limpio, `lint` en `✖ 81 problems
(0 errors, 81 warnings)`, `build` verde.
  </done>
</task>

</tasks>

<verification>
Puertas transversales del milestone (obligatorias antes de cerrar el plan):

```bash
pnpm exec turbo run check-types                    # limpio
pnpm exec turbo run lint 2>&1 | grep 'problems'    # "✖ 81 problems (0 errors, 81 warnings)"
pnpm exec turbo run build                          # verde
```

**Atención con `lint`:** `apps/web` corre eslint con `--max-warnings 0`, así que
en la línea base **sale con exit 1** mostrando exactamente `✖ 81 problems
(0 errors, 81 warnings)`. Eso es lo esperado, no un fallo. Este plan añade código:
el número **no puede subir** de 81 ni aparecer ningún error.

Comprobaciones específicas del plan:
- `git diff --stat` toca exactamente los ocho archivos de `files_modified` y ninguno más.
- `apps/web/lib/document-storage.ts` **no aparece** en el diff. Este plan no cambia
  dónde se guarda ningún archivo.
- `git status --porcelain` no menciona ningún `_tmp-` ni `private-uploads`.
</verification>

<success_criteria>
1. `apps/web/lib/r2.ts` firma peticiones reales contra `ibizadata`: PUT, GET, HEAD
   y un 404 limpio para una clave inexistente, demostrado por la salida del script
   de humo recogida en el SUMMARY.
2. El listado del bucket confirma que ninguna clave escrita por PROL cae fuera de
   `prol/`, y que los prefijos del otro producto siguen ahí.
3. Arrancar con `R2_BUCKET` y sin `R2_ACCOUNT_ID` revienta con un mensaje en
   español que nombra la variable ausente; con las cuatro, el arranque es limpio.
4. `lib/r2.ts` no contiene ninguna operación de borrado, ningún listado y ninguna
   mención a `prol/`: la política vive en `document-storage.ts`, no aquí.
5. `r2Head` está exportada y documenta por qué no tiene consumidor todavía.
6. Las cuatro variables están declaradas y vacías en `.env.example`,
   `.env.production.example` y `docker-compose.prod.yml`; ninguna credencial real
   entró al repositorio.
7. `check-types` limpio, `lint` en 0 errores y ≤81 advertencias, `build` verde.
</success_criteria>

<output>
Al terminar, crear
`.planning/phases/02-r2-para-el-tier-confidencial/02-01-SUMMARY.md`.

Registrar en el SUMMARY:
- La salida literal de los siete pasos del script de humo (los listados incluidos):
  es la única evidencia que queda, porque el script no se conserva.
- La clave exacta del objeto de humo que quedó en el bucket (`prol/_smoke/…`) y el
  recordatorio de que en esta fase no hay borrado, así que se queda ahí.
- El mensaje literal del fallo de arranque con configuración R2 incompleta, para
  que el plan de despliegue sepa qué buscar en los logs del contenedor.
- Si el número de advertencias de lint cambió respecto a 81, cuál es la nueva
  línea base y por qué.
</output>

