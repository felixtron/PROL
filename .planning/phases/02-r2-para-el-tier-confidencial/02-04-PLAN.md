---
phase: 02-r2-para-el-tier-confidencial
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - DEPLOY.md
# Tiene checkpoints: no se toca el VPS sin aprobación explícita.
autonomous: false
requirements: [R2-01, R2-04]
parallel_safe: false  # Ola 4 tiene un solo plan. Además toca producción: nada
                      # concurrente mientras corre.

must_haves:
  truths:
    - "El usuario aprueba explícitamente, con alcance, riesgo, rollback y verificación delante, antes de que nada toque el VPS."
    - "La imagen desplegada contiene el código de la fase 2 y el desfase pendiente de la fase 1."
    - "Las cuatro variables R2 están en el env del contenedor de producción, aplicadas por SSH y nunca commiteadas."
    - "El contenedor arranca sin el error de configuración de R2 y la aplicación responde con normalidad."
    - "El rollback está probado como procedimiento y documentado: quitar `R2_BUCKET` y reiniciar, o retaggear la imagen anterior."
  artifacts:
    - path: "DEPLOY.md"
      provides: "Sección 7c con el estado real de producción tras el despliegue"
      contains: "7c"
  key_links:
    - from: "/etc/containers/env/prol-web-1.env"
      to: "contenedor prol-web-1"
      via: "systemctl daemon-reload && systemctl restart prol-web-1.service"
      pattern: "prol-web-1.service"
---

<objective>
Poner en producción el cambio de backend: desplegar la imagen con el código de la
fase 2 y aplicar las cuatro variables R2 al env del contenedor, por SSH.

Purpose: sin este paso la fase queda demostrada sólo en local. Con él, las
evidencias que se escriban en producción van al bucket en vez de a un volumen que
hoy nadie respalda con versionado.
Output: producción corriendo con `STORAGE_BACKEND = "r2"`, verificada, y
`DEPLOY.md` §7c reflejando el estado real.

**Este plan toca el VPS. No empieza hasta que el usuario lo apruebe.**
</objective>

<execution_context>
@/Users/flx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-r2-para-el-tier-confidencial/02-CONTEXT.md
@.planning/phases/02-r2-para-el-tier-confidencial/02-01-SUMMARY.md
@.planning/phases/02-r2-para-el-tier-confidencial/02-02-SUMMARY.md
@.planning/phases/02-r2-para-el-tier-confidencial/02-03-SUMMARY.md

@DEPLOY.md

<interfaces>
<!-- Hechos de producción verificados el 2026-09-01. No hace falta explorar. -->

**Estado de producción antes de este plan** (de STATE.md):
- Desplegado `64f7476` en `panel-prosuite-2`.
- El módulo de gestión documental está **en producción y apagado**:
  `documents_enabled = false` en Academia Digital MX, IBIZA Consultores y
  Mecanica G3. Con el módulo apagado, nadie puede subir una evidencia por la
  interfaz.
- El volumen `prol_prol_private` existe, está montado en `/app/private-uploads`
  y **está vacío**: la migración es un no-op.
- **Desfase repo ↔ producción**: `64f7476` NO incluye el tipado de `formSnapshot`
  (`b697b3b`, `ab975e2`), commiteado después. Este despliegue lo arrastra.
- Esquema ya aplicado con `db push` (105 sentencias). La fase 2 **no cambia el
  esquema de Prisma**, así que no debería hacer falta ningún `db push` nuevo —
  hay que confirmarlo con el `migrate diff` en modo preview, no darlo por hecho.

**Ruta canónica de despliegue** (`DEPLOY.md` líneas 364-388). El host no tiene
`git`, así que el árbol va por `git archive` a un directorio limpio y `/opt/prol`
no se toca:
```bash
SHA=$(git rev-parse --short HEAD)
git archive --format=tar.gz HEAD | ssh <host> "mkdir -p /opt/prol-deploy-$SHA && tar -xz -C /opt/prol-deploy-$SHA"
# en el host
podman build -t localhost/prol-web:$SHA /opt/prol-deploy-$SHA
podman tag localhost/prol-web:$SHA localhost/prol-web:latest
systemctl restart prol-web-1.service
```

**Alias SSH:** el archivo usa `panel-prosuite-2` en unas secciones y
`propodvps2` en la línea 371. Confirmar cuál resuelve antes de nada
(`ssh -G <alias>` o un `ssh <alias> true`), y anotar en el SUMMARY cuál es el
bueno para que la próxima vez no haya que averiguarlo.

**Env del contenedor:** `/etc/containers/env/prol-web-1.env`, modo 600, root.
**No** `/opt/prol/.env`, que el host no lee. Molde exacto de la §7b:
```bash
printf "PRIVATE_UPLOAD_DIR=%s\n" "/app/private-uploads" >> /etc/containers/env/prol-web-1.env
systemctl daemon-reload && systemctl restart prol-web-1.service
```

**Trampa conocida** (STATE.md, Blockers): el VPS corre un
`docker-compose.prod.yml` divergente del repo, sin commitear allí. **No usar la
sección "Re-deploy" de DEPLOY.md**, que hace `git pull` en el host: rompería. La
ruta canónica de arriba no toca `/opt/prol`.

**Rollbacks disponibles, en orden de menor a mayor impacto:**
1. Sólo el backend: `sed -i '/^R2_BUCKET=/d' /etc/containers/env/prol-web-1.env`
   + `systemctl restart prol-web-1.service`. Vuelve al disco sin desplegar código.
2. La imagen entera: `podman tag localhost/prol-web:64f7476 localhost/prol-web:latest`
   + `systemctl restart prol-web-1.service`.

**Qué buscar en los logs si algo falla:** el mensaje literal de arranque por
configuración R2 incompleta está recogido en el SUMMARY del plan 02-03.
</interfaces>
</context>

<constraints>
- **Nada toca el VPS antes del checkpoint de la tarea 1.** Es una regla del
  proyecto, no una preferencia de este plan.
- **Las credenciales van por SSH, nunca por commit.** No entran en
  `docker-compose.prod.yml` (que además no es lo que corre allí), ni en ningún
  archivo versionado, ni en el cuerpo de un SUMMARY. En el SUMMARY se anota que se
  aplicaron y cuántas son, no sus valores.
- **No activar `documents_enabled` en ningún tenant.** Es una decisión de producto
  fuera del alcance de esta fase. Consecuencia asumida: el camino de escritura en
  producción queda sin ejercitar hasta que el módulo se encienda.
- **Ninguna operación de borrado en el bucket**, ni regla de ciclo de vida, ni
  versionado: está compartido con datos de producción de otro producto.
- **No usar la sección "Re-deploy" de DEPLOY.md** (`git pull` en el host).
- Si el `migrate diff` en preview devuelve alguna sentencia, **parar** y reportar:
  la fase 2 no cambia el esquema, así que cualquier diferencia es otra cosa y
  merece una decisión aparte.
</constraints>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Tarea 1: aprobación del despliegue</name>
  <decision>
¿Se despliega ahora la fase 2 a producción, o se difiere al final del milestone?
  </decision>
  <context>
Antes de tocar el VPS, esto es lo que hay sobre la mesa.

**Alcance.** El despliegue arrastra tres cosas, no una:
1. El cambio de backend de la fase 2 (`lib/r2.ts`, el interruptor en
   `document-storage.ts`, el fail-fast de arranque).
2. El desfase pendiente de la fase 1: el tipado de `formSnapshot` (`b697b3b`,
   `ab975e2`), que producción todavía no tiene.
3. Las cuatro variables R2 en el env del contenedor.

**Riesgo.**
- *Bajo en el camino de datos*: el módulo documental está apagado en los tres
  tenants y el volumen privado está vacío. Ninguna evidencia real cambia de sitio,
  porque no hay ninguna.
- *El riesgo real es el arranque*: el código nuevo revienta el boot si `R2_BUCKET`
  está sin las otras tres. Si el `printf` al env se corta a medias, el contenedor
  no levanta. Mitigación: aplicar las cuatro en una sola operación y comprobar el
  conteo antes de reiniciar.
- *Riesgo heredado*: es el primer despliegue desde `64f7476`, así que también
  entra el cambio de la fase 1 que producción no tiene.

**Rollback.** Dos niveles, los dos probados como procedimiento:
1. Quitar `R2_BUCKET` y reiniciar → vuelve al disco, sin desplegar código.
2. Retaggear `localhost/prol-web:64f7476` como `latest` y reiniciar → vuelve el
   código anterior.
Sin cambios de esquema, así que no hace falta restaurar ningún dump.

**Verificación posterior.** Contenedor arriba, `/api/health` en 200, sin el error
de configuración de R2 en `journalctl`, login funcionando, y las cuatro variables
presentes en el entorno del contenedor (contadas, no impresas).

**Lo que este despliegue NO demuestra:** que una evidencia real se escriba en el
bucket desde producción. Con `documents_enabled = false` no hay forma de subir una
por la interfaz, y encenderlo es una decisión de producto que esta fase no toma.
El camino de escritura queda demostrado en local (plan 02-02) y verificado contra
el bucket real.
  </context>
  <options>
    <option id="desplegar-ahora">
      <name>Desplegar ahora</name>
      <pros>Producción deja de depender de un volumen sin versionado para el tier confidencial. Cierra el desfase de la fase 1. El momento es el mejor posible: no hay datos que perder.</pros>
      <cons>Un despliegue más, con la ventana de corte del reinicio. Arrastra código de la fase 1 que producción no ha visto.</cons>
    </option>
    <option id="diferir">
      <name>Diferir al final del milestone</name>
      <pros>Un solo despliegue para todo v1.1, con menos ventanas de corte. Las fases 3-7 van a volver a tocar esta zona.</pros>
      <cons>El desfase repo ↔ producción crece y el despliegue final acumula más superficie de fallo. Si alguien enciende `documents_enabled` antes, las evidencias van al volumen efímero.</cons>
    </option>
    <option id="solo-variables">
      <name>Sólo aplicar las variables, sin desplegar imagen</name>
      <pros>Deja el env listo para el despliegue que venga.</pros>
      <cons>No hace nada útil por sí solo —la imagen actual no tiene el código R2— y deja el env en un estado que el código viejo ignora y el nuevo exige completo. Confunde más de lo que adelanta.</cons>
    </option>
  </options>
  <action>
Presentar al usuario el bloque de contexto y las tres opciones de arriba, y
**esperar su respuesta**. No ejecutar ningún comando contra el VPS —ni de lectura—
antes de tenerla: la regla del proyecto es pausar antes de tocar producción, con
alcance, riesgo, rollback y verificación delante.

Según la respuesta:
- `desplegar-ahora` → continuar con la tarea 2.
- `diferir` → saltar a la tarea 4 y dejar la §7c marcada como no aplicada, con la
  fecha y la condición para retomarlo.
- `solo-variables` → no ejecutar la tarea 2. Explicar por qué no adelanta nada
  (la imagen en producción no tiene el código R2, así que las variables quedarían
  inertes hasta el siguiente despliegue) y volver a preguntar entre
  `desplegar-ahora` y `diferir`.
  </action>
  <resume-signal>Responde: desplegar-ahora, diferir o solo-variables</resume-signal>
</task>

<task type="auto">
  <name>Tarea 2: desplegar la imagen y aplicar las variables por SSH</name>
  <files>(sin cambios en el repositorio; operación sobre el VPS)</files>
  <action>
**Sólo si la tarea 1 resolvió `desplegar-ahora`.** Si fue `diferir`, saltar a la
tarea 4 y dejar la §7c marcada como no aplicada.

**1. Comprobaciones previas, en local.**
```bash
pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep 'problems'   # "✖ 81 problems (0 errors, 81 warnings)"
pnpm exec turbo run build
git status --porcelain                            # limpio
SHA=$(git rev-parse --short HEAD)
```
Confirmar el alias SSH que resuelve (`panel-prosuite-2` o `propodvps2`) y anotarlo.

**2. Estado actual del host, antes de tocar nada.** Guardar para el SUMMARY:
```bash
ssh <host> 'podman images --format "{{.Repository}}:{{.Tag}}" | grep prol-web | head'
ssh <host> 'systemctl is-active prol-web-1.service'
ssh <host> 'grep -c "^R2_" /etc/containers/env/prol-web-1.env || echo 0'   # esperado: 0
ssh <host> 'podman volume inspect prol_prol_private --format "{{.Mountpoint}}"'
```
Y confirmar que el volumen privado sigue vacío (si no lo estuviera, **parar**: haría
falta correr la migración de la §7c antes de cambiar el backend, y eso cambia el
plan).

**3. Preview del esquema. No debe haber diferencias.** Usar el `migrate diff` en
modo preview de DEPLOY.md (líneas 356-361). Si devuelve alguna sentencia, **parar
y reportar**: la fase 2 no toca Prisma.

**4. Construir y desplegar por la ruta canónica.** Es la de las líneas 368-378, la
que **no** toca `/opt/prol`:
```bash
git archive --format=tar.gz HEAD | ssh <host> "mkdir -p /opt/prol-deploy-$SHA && tar -xz -C /opt/prol-deploy-$SHA"
ssh <host> "podman build -t localhost/prol-web:$SHA /opt/prol-deploy-$SHA"
```
**Todavía no mover `latest` ni reiniciar.**

**5. Aplicar las cuatro variables, en una sola operación.** Leerlas del `.env`
local (gitignored) y escribirlas al env del contenedor. Nunca imprimir los valores
en la salida del terminal ni en el SUMMARY.
```bash
# Un solo append: si se corta a medias, el contenedor no arranca.
ssh <host> 'cat >> /etc/containers/env/prol-web-1.env' <<EOF
R2_ACCOUNT_ID=<valor>
R2_ACCESS_KEY_ID=<valor>
R2_SECRET_ACCESS_KEY=<valor>
R2_BUCKET=<valor>
EOF
ssh <host> 'chmod 600 /etc/containers/env/prol-web-1.env && grep -c "^R2_" /etc/containers/env/prol-web-1.env'
```
El conteo tiene que dar **exactamente 4** antes de seguir. Si da otra cosa —líneas
duplicadas de un intento anterior, o menos de cuatro—, arreglarlo antes de
reiniciar: con el bucket puesto y una llave a medias, el contenedor no levanta.

**6. Mover `latest` y reiniciar.**
```bash
ssh <host> "podman tag localhost/prol-web:$SHA localhost/prol-web:latest && systemctl daemon-reload && systemctl restart prol-web-1.service"
```

**7. Verificar el arranque.**
```bash
ssh <host> 'systemctl is-active prol-web-1.service'
ssh <host> 'journalctl -u prol-web-1.service -n 80 --no-pager'
```
Buscar en el log: **ninguna** aparición de "Configuración de R2 incompleta"
(el mensaje literal está en el SUMMARY del plan 02-03) ni del aviso de
almacenamiento no persistente.

```bash
curl -s -o /dev/null -w '%{http_code}\n' https://prol.prosuite.pro/api/health   # 200
ssh <host> 'podman exec prol-web-1 sh -c "env | grep -c ^R2_"'                  # 4, sin imprimir valores
```

**8. Si algo falla**, aplicar el rollback del nivel que corresponda y reportar:
- El contenedor no arranca → mirar el log; si es la configuración R2, corregir el
  env; si no, retaggear `64f7476` como `latest` y reiniciar.
- Arranca pero la app va mal → retaggear `64f7476`.
En cualquier caso, **no** dejar producción a medias sin decirlo.
  </action>
  <verify>
    <automated>
ssh <host> 'systemctl is-active prol-web-1.service'                       # active
curl -s -o /dev/null -w '%{http_code}\n' https://prol.prosuite.pro/api/health   # 200
ssh <host> 'podman exec prol-web-1 sh -c "env | grep -c ^R2_"'            # 4
ssh <host> 'journalctl -u prol-web-1.service -n 200 --no-pager | grep -c "Configuración de R2 incompleta"'   # 0
ssh <host> 'journalctl -u prol-web-1.service -n 200 --no-pager | grep -ci "error" || true'
ssh <host> "podman images --format '{{.Repository}}:{{.Tag}}' | grep prol-web"   # el SHA nuevo y el anterior, ambos tagueados
# El repositorio no cambió y no se filtró ninguna credencial
git status --porcelain
git diff | grep -iE 'R2_(ACCESS|SECRET|ACCOUNT|BUCKET)=[^"]' && echo "ERROR: credencial en el diff" || echo "OK"
    </automated>
  </verify>
  <done>
`prol-web-1.service` está activo con la imagen del SHA nuevo, `/api/health`
responde 200, el entorno del contenedor tiene exactamente cuatro variables `R2_`,
y `journalctl` no muestra el error de configuración de R2. La imagen anterior
sigue tagueada para poder retaggear. No entró ninguna credencial al repositorio.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 3: confirmación humana de que producción sigue sana</name>
  <what-built>
Producción corriendo con el backend de almacenamiento en R2: imagen nueva
desplegada por la ruta canónica de quadlets, cuatro variables R2 aplicadas al env
del contenedor por SSH, servicio activo y `/api/health` en 200.

El riesgo real de este despliegue era el arranque —el código nuevo revienta el
boot ante una configuración R2 parcial— y las comprobaciones automáticas ya lo
descartaron. Lo que falta es lo que ninguna comprobación automática cubre: que la
aplicación se ve y se usa con normalidad.
  </what-built>
  <how-to-verify>
1. Abre https://prol.prosuite.pro e inicia sesión con tu cuenta habitual.
2. Comprueba que el panel carga y que se ve el contenido de siempre: cursos,
   alumnos, lo que uses a diario.
3. Descarga algo que ya funcionara antes del despliegue —un certificado, un PDF de
   resultados— para confirmar que el camino de archivos **públicos** no se movió.
   Esta fase no tocó ese tier, así que tiene que ir igual.
4. Confirma que el módulo de gestión documental **sigue sin aparecer**: está
   apagado a propósito en los tres tenants y este despliegue no lo enciende.

Si algo va mal, dilo y se retaggea la imagen `64f7476` y se reinicia: producción
vuelve al estado anterior en menos de un minuto.

**Lo que este despliegue deja sin demostrar, y conviene tenerlo presente:** que
una evidencia real se escriba en el bucket desde producción. Con el módulo apagado
no hay forma de subir una por la interfaz. Ese camino está demostrado en local
contra el bucket real (plan 02-02) y se ejercitará en producción cuando se
encienda `documents_enabled`, que es una decisión de producto aparte.
  </how-to-verify>
  <action>
Presentar al usuario los pasos de verificación de arriba y **esperar su
respuesta**. No continuar con la tarea 4 hasta tenerla.

Si responde "aprobado", seguir. Si describe algo mal, evaluar cuál de los dos
rollbacks corresponde —quitar `R2_BUCKET` y reiniciar, o retaggear
`localhost/prol-web:64f7476` como `latest` y reiniciar—, proponerlo antes de
ejecutarlo, y registrar en el SUMMARY qué falló y qué se hizo.
  </action>
  <resume-signal>Escribe "aprobado" o describe qué viste mal</resume-signal>
</task>

<task type="auto">
  <name>Tarea 4: dejar DEPLOY.md contando la verdad</name>
  <files>DEPLOY.md</files>
  <action>
Actualizar la sección 7c que creó el plan 02-03 para que refleje el estado real
tras este plan. Es el mismo hábito que la §7b, donde la nota de estado dice
exactamente cuándo se aplicó y qué no se había hecho hasta entonces — esa nota es
la que evitó que la fase 1 diera por buena una receta que nunca se había ejecutado.

**Si el despliegue se hizo**, sustituir el bloque de estado por:

```markdown
> **Estado: APLICADO en `panel-prosuite-2` el <fecha>.** Imagen `<sha>`. Las
> cuatro variables R2 estan en `/etc/containers/env/prol-web-1.env` (600, root),
> aplicadas por SSH. El volumen `prol_prol_private` seguia **vacio** al desplegar,
> asi que la migracion no llego a ejecutarse en produccion: la receta de abajo
> sigue sin verificar contra el host. El modulo documental continua **apagado**
> (`documents_enabled = false` en los tres tenants), asi que el camino de
> escritura en produccion todavia no se ha ejercitado.
```

Añadir además, si se descubrió, cuál de los dos alias SSH (`panel-prosuite-2` o
`propodvps2`) es el que resuelve, para que la próxima persona no lo averigüe otra
vez.

**Si el despliegue se difirió**, dejar la nota diciéndolo y por qué, con la fecha
y la condición para retomarlo. Un "no aplicado" explícito vale más que el silencio:
el agujero de respaldo de la fase 1 duró tres meses y medio precisamente porque
nadie escribió que la receta no se había ejecutado.

Respetar el estilo **sin acentos** del archivo.
  </action>
  <verify>
    <automated>
grep -n '7c\.' DEPLOY.md
grep -n 'Estado:' DEPLOY.md | head
git diff --name-only    # exactamente DEPLOY.md
git diff DEPLOY.md | grep -iE 'R2_(ACCESS|SECRET|ACCOUNT|BUCKET)=[A-Za-z0-9]' && echo "ERROR: credencial en el diff" || echo "OK: sin credenciales"
    </automated>
  </verify>
  <done>
`DEPLOY.md` §7c dice si el despliegue se aplicó o se difirió, con fecha y SHA si
se aplicó, y deja constancia explícita de que la receta de migración en el host
sigue sin ejecutarse y de que el módulo continúa apagado. El diff toca sólo
`DEPLOY.md` y no contiene ninguna credencial.
  </done>
</task>

</tasks>

<verification>
```bash
ssh <host> 'systemctl is-active prol-web-1.service'                              # active
curl -s -o /dev/null -w '%{http_code}\n' https://prol.prosuite.pro/api/health    # 200
ssh <host> 'podman exec prol-web-1 sh -c "env | grep -c ^R2_"'                   # 4
ssh <host> 'journalctl -u prol-web-1.service -n 200 --no-pager | grep -c "Configuración de R2 incompleta"'   # 0
```

Comprobaciones del repositorio:
- `git diff --name-only` devuelve exactamente `DEPLOY.md`.
- Ninguna credencial real en el diff ni en el SUMMARY.
- La imagen anterior (`64f7476`) sigue tagueada en el host: sin ella no hay
  rollback de código.
</verification>

<success_criteria>
1. El usuario aprobó el despliegue con alcance, riesgo, rollback y verificación
   delante, antes de que nada tocara el VPS.
2. Si se desplegó: el servicio está activo con la imagen nueva, `/api/health`
   responde 200, el contenedor tiene las cuatro variables R2 y el log no muestra
   el error de configuración incompleta.
3. Un humano confirmó que la aplicación se ve y se usa con normalidad, y que el
   tier público de archivos sigue funcionando.
4. Las credenciales se aplicaron por SSH y no aparecen en ningún archivo
   versionado ni en ningún SUMMARY.
5. `DEPLOY.md` §7c dice la verdad sobre lo que se aplicó y lo que no, incluida la
   receta de migración que sigue sin ejecutarse en el host.
6. El rollback está disponible en sus dos niveles y documentado.
</success_criteria>

<output>
Al terminar, crear
`.planning/phases/02-r2-para-el-tier-confidencial/02-04-SUMMARY.md`.

Registrar en el SUMMARY:
- La decisión del checkpoint y su motivo.
- Si se desplegó: el SHA, la fecha, el alias SSH que resuelve, el estado previo
  del host (imágenes tagueadas, conteo de variables `R2_` antes y después) y la
  salida de las comprobaciones de arranque.
- **Nunca** los valores de las credenciales: sólo que se aplicaron cuatro.
- Lo que queda pendiente y por qué: la receta de migración en el host sin ejecutar
  (volumen vacío), el módulo documental apagado, y el camino de escritura en
  producción sin ejercitar hasta que se encienda.
- El comando exacto de los dos rollbacks, listo para copiar y pegar si hiciera
  falta en las próximas horas.
</output>
