---
phase: 02-r2-para-el-tier-confidencial
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/web/lib/document-storage.ts
autonomous: true
requirements: [R2-01, R2-03]
parallel_safe: false  # Ola 2 tiene un solo plan a propósito: este toca el archivo
                      # central del que cuelgan los cinco call sites. En la fase 1
                      # dos ejecutores concurrentes se pisaron el índice de git.

must_haves:
  truths:
    - "Una evidencia subida con R2 activo aparece como objeto en el bucket bajo `prol/evidence/<uuid>.<ext>`."
    - "La `fileKey` que devuelve `storePrivateFile` sigue siendo `<subdir>/<uuid>.<ext>`, sin prefijo: la base no sabe qué backend hay debajo."
    - "Esa misma evidencia se descarga por `/files/evidence/<id>` con los bytes idénticos a los subidos."
    - "Un miembro de otra empresa recibe 403 y una petición sin sesión recibe 401, exactamente igual que antes."
    - "Las validaciones de MIME, tamaño y archivo vacío siguen ocurriendo antes de cualquier llamada de red."
    - "Con `R2_BUCKET` presente y alguna credencial ausente la aplicación arranca, pero `storePrivateFile` rechaza la subida con 503, registrando en el log las variables que faltan, y no escribe nada en disco."
  artifacts:
    - path: "apps/web/lib/document-storage.ts"
      provides: "storePrivateFile y readPrivateFile con backend conmutable y prefijo del bucket compartido"
      contains: "STORAGE_BACKEND"
      min_lines: 150
  key_links:
    - from: "apps/web/lib/document-storage.ts"
      to: "apps/web/lib/r2.ts"
      via: "import { isR2Configured, missingR2Env, r2Get, r2Put }"
      pattern: "from \"@/lib/r2\""
    - from: "storePrivateFile (rama r2)"
      to: "objeto en el bucket bajo prol/"
      via: "r2Put(sharedBucketKey(fileKey), bytes, file.type)"
      pattern: "r2Put\\(sharedBucketKey"
    - from: "readPrivateFile (rama r2)"
      to: "objeto en el bucket bajo prol/"
      via: "r2Get(sharedBucketKey(fileKey))"
      pattern: "r2Get\\(sharedBucketKey"
    - from: "storePrivateFile (configuración R2 parcial)"
      to: "rechazo explícito de la escritura, sin degradar a disco"
      via: "missingR2Env() evaluado al cargar el módulo"
      pattern: "missingR2Env"
---

<objective>
Reescribir los cuerpos de `storePrivateFile` y `readPrivateFile` para que
escriban y lean de R2 cuando las credenciales están completas, y del disco
cuando no, sin tocar sus firmas ni ninguno de sus cinco call sites.

Purpose: es el núcleo de la fase, y es barato porque `fileKey` ya es una clave
opaca `<subdir>/<uuid>.<ext>` — una clave de objeto R2 válida sin transformación.
La frontera de cambio cabe en un archivo.
Output: el interruptor de almacenamiento en un solo sitio, el prefijo `prol/`
contenido dentro de la frontera de almacenamiento, y los criterios 1 y 3 de la
fase demostrados contra el servidor real con sesiones reales.

Aquí vive además la mitad que importa de la enmienda del CONTEXT del 2026-09-01:
con `R2_BUCKET` puesta y alguna credencial ausente, `storePrivateFile` **rechaza
la escritura** en vez de degradar a disco en silencio. El arranque no se toca —
`lib/env.ts` sólo avisa (plan 02-01)—, porque una errata en una variable del
módulo documental no puede dejar sin servicio a cursos, evaluaciones ni
certificados. Y no degradar es lo que impide esparcir evidencias por
almacenamiento efímero, que es el agujero que la fase 1 acaba de cerrar.
</objective>

<execution_context>
@/Users/flx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-r2-para-el-tier-confidencial/02-CONTEXT.md
@.planning/phases/02-r2-para-el-tier-confidencial/02-RESEARCH.md
@.planning/phases/02-r2-para-el-tier-confidencial/02-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

@apps/web/lib/document-storage.ts
@apps/web/lib/r2.ts
@apps/web/app/files/evidence/[id]/route.ts
@apps/web/app/api/upload/evidence/route.ts

<interfaces>
<!-- Contratos exactos, transcritos del código real. No hace falta explorar. -->

**Lo que exporta `apps/web/lib/r2.ts`** (creado en el plan 02-01):
```typescript
export function missingR2Env(): string[];
export function isR2Configured(): boolean;
export function assertR2Env(): void;
export async function r2Put(key: string, body: Buffer, contentType: string): Promise<void>;   // lanza si !res.ok
export async function r2Get(key: string): Promise<Buffer | null>;                              // null en 404; loguea y null en 403/5xx
export async function r2Head(key: string): Promise<{ size: number; contentType: string | null } | null>;  // sin consumidor hasta la fase 6
```

**Las tres firmas que NO pueden cambiar** (`apps/web/lib/document-storage.ts`):
```typescript
export async function storePrivateFile(
  file: File, subdir: PrivateSubdir, allowed: Record<string, string>,
): Promise<StoreResult | StoreError>;
export async function readPrivateFile(fileKey: string): Promise<Buffer | null>;
export function privateFileResponse(buffer: Buffer, file: { fileName: string; mimeType: string }): Response;
```

**Los cinco call sites** (verificados por grep exhaustivo en todo `apps/web`).
Ninguno reconstruye una ruta a partir de `fileKey`: lo reciben opaco y lo pasan
íntegro. **Ninguno se edita en esta fase.**

| Archivo | Línea | Llamada |
|---|---|---|
| `app/files/evidence/[id]/route.ts` | 52, 55 | `readPrivateFile(evidence.fileKey)` → `privateFileResponse(...)` |
| `app/files/company-document/[id]/route.ts` | 41, 44 | `readPrivateFile(doc.fileKey)` → `privateFileResponse(...)` |
| `app/files/manual-document/[id]/route.ts` | 54, 57 | `readPrivateFile(doc.baseFileKey)` → `privateFileResponse(...)` |
| `app/api/upload/evidence/route.ts` | 24 | `storePrivateFile(file, "evidence", EVIDENCE_EXT_BY_MIME)` |
| `app/api/upload/document-template/route.ts` | 23 | `storePrivateFile(file, "templates", TEMPLATE_EXT_BY_MIME)` |

**Autorización de `/files/evidence/[id]/route.ts`** (líneas 39-64) — no se toca, y
es la que produce los códigos del criterio 3:
```typescript
if (!evidence?.fileKey || evidence.deletedAt) return 404;
await assertDocumentsEnabled(evidence.assignment.tenantId, user.role);
const allowed = isManualReviewer(user)
  ? user.role === "SUPER_ADMIN" || user.tenantId === evidence.assignment.tenantId
  : user.companyId === evidence.assignment.companyId;
if (!allowed) return 403;               // ← "otra empresa"
// catch: message === "Unauthorized" → 401   ← "sin sesión"
```
`isManualReviewer(user)` es `REVIEW_ROLES.has(user.role)` — personal del tenant.
Un `STUDENT` cae en la rama `user.companyId === assignment.companyId`.

**Fixture que ya existe en la base local** (creado en el plan 01-03, `localhost:5435`,
contenedor `prol-db`). La cadena completa `Manual → Chapter → Section →
EvidenceRequirement → Assignment → Activity` está fabricada; el seed **no** la crea:
```
assignmentId: cmtj66ac20009t30l32b3yipo
activityId:   cmtj66acc000bt30lckqtuh0d
sectionId:    cmtj66abo0005t30l4dkcn9ix
evidenceId (matriz de riesgos, legacy): cmtj938ve00053arl0omi0yrd
```
Hay dos evidencias con `form_snapshot` (`1|f` y `2|t`) que **no hay que romper**.

**Usuarios del seed** (contraseña `password123` para todos):
| Correo | Rol | Empresa |
|---|---|---|
| `maria.garcia@academiadigitalmx.com` | PROFESSOR | — (revisora del tenant) |
| `carlos.mendoza@gmail.com` | STUDENT | Acme Corp |
| `ana.rodriguez@outlook.com` | STUDENT | Acme Corp |
| `admin@prol.prosuite.pro` | TENANT_ADMIN | — |
| `super@prol.prosuite.pro` | SUPER_ADMIN | — |

**Ojo:** el seed sólo crea **una** empresa (Acme Corp) y mete a los dos estudiantes
dentro. Para el 403 hay que fabricar un usuario ajeno (tarea 3).

**Sesión por API, precedente del plan 01-04** — Better Auth con endpoint de
correo/contraseña. El plugin de captcha sólo se activa si `TURNSTILE_SECRET_KEY`
está presente, y **no está en el `.env` local**, así que el login por `curl`
funciona:
```bash
curl -s -c /tmp/prol-cookies.txt -X POST http://localhost:3000/api/auth/sign-in/email \
  -H 'Content-Type: application/json' \
  -d '{"email":"carlos.mendoza@gmail.com","password":"password123"}'
```
</interfaces>
</context>

<constraints>
- **Cero ediciones** en `privateFileResponse`, en las tres rutas
  `apps/web/app/files/*/[id]/route.ts`, en las dos de `apps/web/app/api/upload/*`
  y en `packages/db/prisma/schema.prisma`. Si algo de eso aparece en el diff, el
  plan está mal ejecutado.
- **El prefijo `prol/` NO entra nunca en `fileKey`.** La base sigue guardando
  `<subdir>/<uuid>.<ext>`. Si se filtrara se romperían tres cosas a la vez: las
  filas existentes dejarían de resolver, el rollback a disco de R2-04 se rompería
  (las rutas de disco no llevan `prol/`), y la base dejaría de ser agnóstica al
  backend, que es justo lo que hace barata toda la fase.
- **La rama de disco no se elimina ni se degrada.** Es el rollback de R2-04 y el
  camino de desarrollo y CI. `resolvePrivateUploadDir` y `PRIVATE_SUBDIR` de
  `lib/upload-paths.ts` se quedan como están.
- **La configuración R2 parcial no degrada a disco en silencio.** Con `R2_BUCKET`
  puesta y alguna credencial ausente, la **escritura** se rechaza. La **lectura**
  sí puede seguir yendo a disco: devolver `null` cuando no hay nada ya está
  contemplado y las tres rutas `/files/*` lo traducen a 404, así que
  `readPrivateFile` no lleva ninguna guarda nueva.
- **Nada en este plan puede impedir el arranque.** El rechazo vive en la ruta de
  escritura, no en el arranque ni en el import del módulo.
- **Las validaciones existentes se mantienen, en el mismo orden.** MIME, tamaño y
  vacío en `storePrivateFile` ocurren **antes** de cualquier llamada de red. Las
  cuatro comprobaciones de forma de clave en `readPrivateFile` (`parts.length !== 2`,
  allowlist de subdir, `..`, `\\`) se quedan: ahora previenen inyección de claves
  en el bucket, que es el mismo valor por otro motivo.
- **Ninguna operación de borrado en R2**, ni aquí ni en los scripts de prueba. El
  bucket está compartido con datos de producción de otro producto.
- **Nada de subida directa por URL firmada**: es la fase 6.
- La puerta de lint sale con **exit 1 estando sana**: `✖ 81 problems (0 errors,
  81 warnings)`. No es un fallo.
</constraints>

<tasks>

<task type="auto">
  <name>Tarea 1: backend conmutable y prefijo del bucket compartido en document-storage.ts</name>
  <files>apps/web/lib/document-storage.ts</files>
  <action>
**1. Cabecera.** El archivo abre hoy con un comentario que dice "Acceso a disco".
Actualizarlo para que documente los dos backends y el interruptor — es el único
sitio de la aplicación donde vive esa decisión, y así lo pidió el CONTEXT.
Conservar los párrafos que siguen siendo ciertos (el contrato puro de
`document-files.ts`, y que lo que se guarda en la base es la `fileKey` y nunca una
URL pública). Añadir, con este contenido:

```
// La aplicación tiene DOS backends de almacenamiento y los va a tener para
// siempre: Cloudflare R2 cuando las cuatro credenciales están completas, y el
// disco privado cuando no. El interruptor vive aquí y en ningún otro sitio.
```

**2. Imports.** Añadir al grupo de imports locales, respetando el orden de la casa
(Node, externas, `@prol/*`, `@/*`):

```typescript
import { isR2Configured, missingR2Env, r2Get, r2Put } from "@/lib/r2";
```

`missingR2Env` entra para el rechazo por configuración parcial del punto 4: la
lista de nombres de variables vive en `lib/r2.ts` y no se duplica aquí.
`r2Head` **no** se importa: no tiene consumidor hasta la fase 6, y su docstring en
`lib/r2.ts` explica por qué existe.

**3. Constantes de módulo**, después de los imports y antes de `StoreResult`:

```typescript
/**
 * Backend activo, decidido una sola vez por proceso al primer import.
 *
 * Interruptor único de toda la aplicación. Exige las CUATRO variables: con menos,
 * vale `"disk"`. Quitar `R2_BUCKET` del entorno y reiniciar devuelve la app al
 * disco sin desplegar código — ése es el rollback de R2-04, y la razón de que la
 * rama de disco no se borre nunca.
 */
const STORAGE_BACKEND: "r2" | "disk" = isR2Configured() ? "r2" : "disk";

/**
 * Configuración a medias: `R2_BUCKET` puesta y alguna de las otras tres ausente.
 * Vacío en los dos estados sanos (ninguna variable R2, o las cuatro).
 *
 * `lib/env.ts` ya lo avisó al arrancar, pero **sólo avisó**: la aplicación levanta
 * con normalidad a propósito, porque una errata en una variable del módulo
 * documental no puede dejar sin servicio a cursos, evaluaciones y certificados.
 * Quien lo hace cumplir es la ruta de escritura de aquí abajo. En este estado
 * `STORAGE_BACKEND` vale `"disk"`, y escribir una evidencia en el disco efímero
 * del contenedor creyendo que va al bucket es exactamente el agujero que la fase 1
 * cerró: entre tumbar la plataforma y esparcir evidencias, no hacer ninguna de las
 * dos y rechazar la subida.
 *
 * Se lee una sola vez al cargar el módulo, igual que `STORAGE_BACKEND`: los dos
 * describen el mismo entorno de proceso y no pueden discrepar a media vida.
 */
const R2_PARTIAL_ENV: string[] = process.env.R2_BUCKET ? missingR2Env() : [];

/**
 * El bucket de R2 está COMPARTIDO con otro producto en producción, que guarda lo
 * suyo bajo `empresas/` y `leads/`. Todo lo que escriba o lea PROL cuelga de
 * `prol/`, y el token tiene permiso sobre todo el bucket: por eso en esta fase no
 * existe ninguna operación de borrado, ni regla de ciclo de vida, ni versionado.
 *
 * Este prefijo NO entra nunca en `fileKey`. La base sigue guardando la clave
 * opaca `<subdir>/<uuid>.<ext>`. Si se filtrara: (1) las filas que ya existen
 * dejarían de resolver, (2) el rollback a disco se rompería, porque las rutas de
 * disco no llevan `prol/`, y (3) la base dejaría de ser agnóstica al backend, que
 * es la propiedad que hace barato tener dos.
 */
const R2_KEY_PREFIX = "prol/";

/**
 * Traduce una `fileKey` de la base a la clave real dentro del bucket compartido.
 *
 * Es una guarda de runtime, no una prueba automatizada, y es deliberado: el repo
 * no tiene test runner y añadirlo está declarado fuera de alcance. A cambio, esto
 * corre en cada lectura y cada escritura reales, que es más cobertura de la que
 * daría un test unitario. Si alguna vez llega una `fileKey` con el prefijo ya
 * puesto —el modo de fallo que más miedo da—, `parts.length` valdría 3 y esto
 * lanza en vez de escribir en `prol/prol/...`.
 */
function sharedBucketKey(fileKey: string): string {
  const parts = fileKey.split("/");
  const [subdir, name] = parts;
  if (
    parts.length !== 2 ||
    (subdir !== "evidence" && subdir !== "templates") ||
    !name ||
    fileKey.includes("..")
  ) {
    throw new Error(`Clave de archivo inválida para R2: ${fileKey}`);
  }
  const key = `${R2_KEY_PREFIX}${fileKey}`;
  if (!key.startsWith(R2_KEY_PREFIX)) {
    throw new Error(`Clave fuera del prefijo ${R2_KEY_PREFIX}: ${key}`);
  }
  return key;
}
```

**4. `storePrivateFile`.** Las validaciones de las líneas 46-62 (MIME, 25 MB,
vacío) **no se tocan**: siguen ejecutándose antes de cualquier llamada de red.
Sustituir el bloque de las líneas 64-70 y ajustar el `return`:

```typescript
  // Configuración a medias: no se escribe NADA, ni a R2 ni al disco. Es la otra
  // mitad de la enmienda del CONTEXT del 2026-09-01 —la app arranca, pero no
  // esparce evidencias por almacenamiento efímero— y va aquí, después de las
  // validaciones de siempre, para no alterar el orden de sus mensajes.
  if (R2_PARTIAL_ENV.length > 0) {
    console.error(
      `[storage] Subida rechazada por configuración de R2 incompleta: faltan ${R2_PARTIAL_ENV.join(", ")}`,
    );
    // El detalle (qué variables faltan) va al log, donde lo lee quien puede
    // arreglarlo. A quien sube el archivo no le sirve de nada saber nombres de
    // variables de entorno, así que el mensaje visible se queda en el qué y el
    // qué hacer. Deliberado: no lo "mejores" volviendo a nombrarlas aquí.
    return {
      ok: false,
      status: 503,
      error:
        "El almacenamiento de evidencias no está configurado correctamente. El archivo no se ha guardado; avisa a un administrador.",
    };
  }

  const storedName = `${crypto.randomUUID()}.${ext}`;
  const fileKey = `${subdir}/${storedName}`;
  const bytes = Buffer.from(await file.arrayBuffer());

  if (STORAGE_BACKEND === "r2") {
    // Fuera del try: si esto lanza es un bug de forma de clave, no un fallo de
    // red, y no debe disfrazarse de "vuelve a intentarlo".
    const key = sharedBucketKey(fileKey);
    try {
      await r2Put(key, bytes, file.type);
    } catch (err) {
      console.error("[r2] PUT falló al guardar un archivo privado:", err);
      return {
        ok: false,
        status: 502,
        error: "No se pudo guardar el archivo. Vuelve a intentarlo.",
      };
    }
  } else {
    const targetDir = resolvePrivateUploadDir(subdir);
    await mkdir(targetDir, { recursive: true });
    await writeFile(join(targetDir, storedName), bytes);
  }

  return {
    ok: true,
    file: {
      fileKey,
      fileName: safeFilename(file.name),
      fileSize: file.size,
      mimeType: file.type,
    },
  };
```

Tres detalles que importan:
- `fileKey` vale exactamente `${subdir}/${storedName}`, igual que la línea 75 de
  hoy. La forma de lo que se guarda en la base no cambia ni un carácter.
- El fallo de red devuelve un `StoreError` en vez de lanzar, para respetar el
  docstring de la función ("devuelve un resultado en vez de lanzar porque cada
  mensaje de error va tal cual al usuario"). El `502` sale por
  `NextResponse.json({ error }, { status })` en las dos rutas de subida, que ya
  pintan `result.error` al usuario. La rama de disco conserva su comportamiento
  actual (un fallo de `fs` lanza y la ruta responde 500); no se toca.
- **El rechazo por configuración parcial nombra las variables que faltan SÓLO en el log, y es
  deliberado**, en el mismo estilo que `getCredentials()` de `lib/r2.ts` y de
  `cloudflare-stream.ts`. El mensaje lo ve quien está subiendo: son nombres de
  variables, nunca valores, y sin ellos el aviso sería "no se pudo guardar" a secas
  y nadie sabría qué arreglar. `503` y no `502`: el servicio está mal configurado,
  no ha fallado una llamada de red — de hecho no llega a haberla. La lista sale de
  `missingR2Env()`, así que los nombres siguen viviendo en un solo sitio.

**5. `readPrivateFile`.** Las líneas 91-95 (las cuatro comprobaciones de forma de
clave) **no se tocan**. Insertar la rama R2 justo después de ellas, dejando el
código de disco de las líneas 97-107 intacto debajo:

```typescript
  if (STORAGE_BACKEND === "r2") {
    // `sharedBucketKey` no puede lanzar aquí: las comprobaciones de arriba ya
    // garantizaron la forma. El try/catch conserva el contrato "esta función
    // devuelve null, nunca lanza", del que dependen las tres rutas /files/*.
    try {
      return await r2Get(sharedBucketKey(fileKey));
    } catch (err) {
      console.error("[r2] GET falló al leer un archivo privado:", err);
      return null;
    }
  }

  const dir = resolvePrivateUploadDir(subdir);
  // ... resto igual que hoy, sin cambios ...
```

**`readPrivateFile` NO lleva guarda de configuración parcial**, y es deliberado
(enmienda del CONTEXT): en ese estado `STORAGE_BACKEND` vale `"disk"`, así que la
lectura va al disco y, si no hay nada, devuelve `null` — que es exactamente lo que
las tres rutas `/files/*` ya traducen a 404. Fallar la lectura no protegería nada,
rompería el camino de rollback y convertiría un estado degradado en uno caído.

**6. `privateFileResponse`: cero ediciones.** No importa `node:fs`, no depende de
ningún backend, sólo envuelve un `Buffer` ya cargado.

**Nota honesta que ya está asumida** (no hay que arreglarla aquí): el archivo
entero pasa por el heap de Node en los dos backends, porque `readPrivateFile`
devuelve un `Buffer` completo y `privateFileResponse` lo envuelve en
`new Uint8Array(buffer)`. Ya es así con disco: no es una regresión. La
optimización (redirect 302 a URL firmada tras autorizar) es de una fase posterior.
  </action>
  <verify>
    <automated>
# El interruptor, el estado parcial y el prefijo existen, y el prefijo vive SÓLO aquí
grep -n 'STORAGE_BACKEND' apps/web/lib/document-storage.ts
grep -n 'R2_PARTIAL_ENV' apps/web/lib/document-storage.ts

# El rechazo por configuración parcial reutiliza missingR2Env: los nombres de las
# cuatro variables no se duplican en este archivo
grep -n 'missingR2Env' apps/web/lib/document-storage.ts
grep -nE '"R2_(ACCOUNT_ID|ACCESS_KEY_ID|SECRET_ACCESS_KEY)"' apps/web/lib/document-storage.ts && echo "ERROR: lista de variables duplicada" || echo "OK: los nombres viven en lib/r2.ts"
grep -n 'R2_KEY_PREFIX\|sharedBucketKey' apps/web/lib/document-storage.ts
grep -rn '"prol/"' apps/web/lib apps/web/app 2>/dev/null | grep -v '\.next'
# Debe aparecer en document-storage.ts y en ningún otro archivo

# Las dos ramas llaman a R2 a través de la guarda, nunca con la fileKey cruda
grep -n 'r2Put(sharedBucketKey\|r2Get(sharedBucketKey' apps/web/lib/document-storage.ts
grep -nE 'r2(Put|Get)\((?!sharedBucketKey)' apps/web/lib/document-storage.ts || echo "OK: ninguna llamada a R2 sin la guarda"

# La rama de disco sigue entera
grep -n 'resolvePrivateUploadDir\|mkdir\|writeFile\|readFile\|stat' apps/web/lib/document-storage.ts

# La forma del fileKey no cambió
grep -n 'const fileKey = `${subdir}/${storedName}`' apps/web/lib/document-storage.ts

# Nada de borrado ni de presigna
grep -niE 'r2Delete|delete|presign|signed' apps/web/lib/document-storage.ts && echo "REVISAR" || echo "OK: sin borrado ni presigna"

# El diff toca UN archivo y ninguno de los intocables
git status --porcelain
git diff --name-only | grep -E 'files/|api/upload/|schema.prisma' && echo "ERROR: se tocó un archivo prohibido" || echo "OK: intocables intactos"

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep 'problems'
    </automated>
  </verify>
  <done>
`document-storage.ts` declara `STORAGE_BACKEND` una vez a nivel de módulo a partir
de `isR2Configured()`, y las dos funciones tienen sus dos ramas. Toda llamada a
`r2Put`/`r2Get` pasa por `sharedBucketKey`. La cadena `"prol/"` no aparece en
ningún otro archivo del repo. `fileKey` sigue siendo `<subdir>/<uuid>.<ext>`. Las
validaciones de MIME/tamaño/vacío y las cuatro comprobaciones de forma de clave
están donde estaban. `storePrivateFile` rechaza la escritura con 503 cuando
`R2_PARTIAL_ENV` no está vacío, registrando en el log las variables que faltan
y que sale de `missingR2Env()`; `readPrivateFile` no gana ninguna guarda nueva. El
diff toca exactamente un archivo. `check-types` limpio y
`lint` en `✖ 81 problems (0 errors, 81 warnings)`.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: criterio 1 — la evidencia aparece en el bucket, se descarga por /files/*, y los tres estados de configuración se comportan</name>
  <files>(sin cambios de código; scripts desechables que NO se commitean)</files>
  <action>
El repo no tiene suite de pruebas, así que el criterio 1 de la fase se demuestra
contra el servidor real, con sesión real, igual que hizo el plan 01-04.

**Preparación.**

1. Confirmar que el módulo documental está habilitado para el tenant del fixture.
   `assertDocumentsEnabled` lo exige para todo el que no sea SUPER_ADMIN:
   ```sql
   select t.id, t.slug, t.documents_enabled
   from tenants t
   join company_manual_assignments a on a.tenant_id = t.id
   where a.id = 'cmtj66ac20009t30l32b3yipo';
   ```
   Si sale `f`, ponerlo a `true` para el tenant local y anotarlo en el SUMMARY.
   (El nombre real de la tabla de asignaciones se confirma en `schema.prisma` por
   su `@@map`; si difiere, usar el que corresponda — no cambiar el esquema.)

2. Arrancar el servidor con **R2 activo** (las cuatro variables ya están en el
   `.env` local):
   ```bash
   pnpm --filter @prol/web dev > /tmp/prol-dev-r2.log 2>&1 &
   ```
   Confirmar en el log que no aparece ningún error de configuración de R2.

3. Sesión de un miembro de Acme Corp:
   ```bash
   curl -s -c /tmp/cookies-acme.txt -X POST http://localhost:3000/api/auth/sign-in/email \
     -H 'Content-Type: application/json' \
     -d '{"email":"carlos.mendoza@gmail.com","password":"password123"}'
   ```

**Demostración.**

4. Fabricar dos archivos de prueba con contenido conocido y calcular su `sha256`:
   uno PDF y uno PNG (MIME distintos, para que pasen la allowlist por caminos
   distintos). Deben superar los 10 bytes: menos que eso los rechaza
   `storePrivateFile` por "Archivo vacío".

5. Subirlos por la ruta real y capturar la respuesta:
   ```bash
   curl -s -b /tmp/cookies-acme.txt -F "file=@/tmp/evidencia.pdf;type=application/pdf" \
     http://localhost:3000/api/upload/evidence
   ```
   La respuesta es el objeto `StoredFile`. **Comprobar dos cosas sobre `fileKey`:**
   - empieza por `evidence/` y tiene exactamente dos segmentos;
   - **no** contiene `prol/`. Si lo contuviera, el prefijo se habría filtrado a lo
     que va a guardarse en la base, que es el fallo que la fase entera trata de
     evitar. Abortar si aparece.

6. Comprobar que el objeto está en el bucket, bajo el prefijo. Script desechable
   `apps/web/scripts/_tmp-r2-check.mjs` (mismo patrón que el humo del plan 02-01:
   `AwsClient` con `service: "s3"`, `region: "auto"`), que recibe las `fileKey`
   por `argv` y para cada una hace un **HEAD** a `prol/<fileKey>`, exigiendo 200 y
   que `content-length` coincida con el tamaño del archivo local. Ejecutar con
   `node --env-file=.env`. Después borrarlo.

   Comprobar además que **no** existe un objeto en `<fileKey>` sin prefijo (HEAD
   a la clave cruda debe dar 404): confirma que nada se escribió fuera de `prol/`.

7. Comprobar que el directorio de disco **no** recibió nada: con R2 activo,
   `apps/web/private-uploads/evidence/` no debe existir o no debe contener los
   archivos nuevos.

8. Crear la fila `Evidence` que hace descargable el archivo. Script desechable en
   `packages/db` (que sí tiene `tsx`), siguiendo el precedente de la fase 1:
   ```bash
   pnpm --filter @prol/db exec tsx prisma/_tmp-evidence-fixture.ts
   ```
   El script crea una `Evidence` por cada archivo subido, sobre la actividad y la
   asignación del fixture (`activityId: cmtj66acc000bt30lckqtuh0d`,
   `assignmentId: cmtj66ac20009t30l32b3yipo`), con `fileKey`, `fileName`,
   `fileSize` y `mimeType` **tal cual los devolvió la ruta de subida**, la
   `version` siguiente a la mayor existente para esa actividad, y el usuario que
   subió. Imprime los ids. **No tocar las dos evidencias de `form_snapshot`
   existentes** (`1|f` y `2|t`): son el banco de regresión de la fase 1.

9. Descargar por la ruta real y comparar bytes:
   ```bash
   curl -s -b /tmp/cookies-acme.txt -D /tmp/headers.txt \
     -o /tmp/bajado.pdf http://localhost:3000/files/evidence/<id>
   ```
   Exigir: HTTP 200, `Content-Type` igual al MIME subido, `Content-Disposition:
   attachment`, y **`sha256` idéntico al del archivo original**. Repetir con el PNG.

10. Guardar los ids de evidencia y las `fileKey` en un sitio recuperable (van al
    SUMMARY): el plan 02-03 los reutiliza para el criterio 2 y el rollback.

**Los tres estados de configuración, en la ruta de escritura.** Los pasos 4-9
acaban de demostrar el estado (b). Faltan los otros dos, y el (c) es el que decidió
la enmienda del CONTEXT del 2026-09-01: hay que demostrarlo, no asumirlo.

11. **(a) Sin ninguna variable R2 → backend disco: arranca y escribe bien.**
    ```bash
    pkill -f 'next dev'
    R2_ACCOUNT_ID= R2_ACCESS_KEY_ID= R2_SECRET_ACCESS_KEY= R2_BUCKET= \
      pnpm --filter @prol/web dev > /tmp/prol-dev-disco.log 2>&1 &
    sleep 25
    ```
    Volver a iniciar sesión (servidor nuevo) y subir un tercer archivo por
    `/api/upload/evidence`. Exigir: **200** con su `StoredFile`, el archivo
    presente en `apps/web/private-uploads/evidence/`, y **ninguna** línea de log de
    `component: "r2"` — sin bucket la aplicación no habla con R2.

12. **(c) `R2_BUCKET` presente y una credencial ausente → arranca, y la escritura
    se rechaza nombrando la que falta.** Éste es el estado nuevo:
    ```bash
    pkill -f 'next dev'
    R2_ACCESS_KEY_ID= pnpm --filter @prol/web dev > /tmp/prol-dev-parcial.log 2>&1 &
    sleep 25
    curl -s -o /dev/null -w 'login=%{http_code}\n' http://localhost:3000/login
    ```
    Exigir, en este orden:
    - `/login` responde **200**: la aplicación está arriba. Si esto fuera un fallo
      de arranque, la implementación no sigue la decisión del CONTEXT — parar y
      volver al plan 02-01.
    - El log muestra el aviso `Configuración de R2 incompleta` nombrando
      `R2_ACCESS_KEY_ID` (lo dejó el plan 02-01).
    - Subir un archivo por `/api/upload/evidence` devuelve **503** y un cuerpo cuyo
      `error` **nombra `R2_ACCESS_KEY_ID`**. Copiarlo literal al SUMMARY: el plan
      02-04 lo necesita.
    - **Nada se escribió a disco.** Contar los archivos de
      `apps/web/private-uploads/evidence/` antes y después de la subida rechazada y
      exigir el mismo número. Ésta es la comprobación que de verdad importa: "no
      degrada a disco en silencio" es toda la razón de ser del rechazo.
    - Nada se escribió al bucket, obviamente: sin la llave no se puede firmar.

13. Dejar el servidor otra vez con las cuatro variables (estado (b)) antes de pasar
    a la tarea 3, y anotar en el SUMMARY la tabla de los tres estados:

    | Variables R2 | ¿Arranca? | Backend | Escritura |
    |---|---|---|---|
    | ninguna | sí | disco | 200, el archivo aparece en `private-uploads/` |
    | las cuatro | sí | R2 | 200, el objeto aparece bajo `prol/` |
    | `R2_BUCKET` + falta alguna | **sí** | disco | **503; el log nombra las que faltan; nada escrito** |

Borrar los dos scripts desechables al terminar. **No borrar los objetos del
bucket** — en esta fase no existe operación de borrado.
  </action>
  <verify>
    <automated>
# Subida: la fileKey no lleva prefijo y tiene la forma de siempre
# (sustituir <fileKey> por el valor real capturado)
echo "$FILEKEY" | grep -E '^evidence/[0-9a-f-]{36}\.(pdf|png)$'
echo "$FILEKEY" | grep -q 'prol/' && echo "ERROR: el prefijo se filtró a fileKey" || echo "OK: fileKey sin prefijo"

# El objeto está en el bucket bajo prol/ y NO fuera de él
node --env-file=.env apps/web/scripts/_tmp-r2-check.mjs "$FILEKEY"
# Esperado: HEAD prol/<fileKey> => 200 con el tamaño correcto
#           HEAD <fileKey>      => 404

# Con R2 activo el disco no recibió nada (comprobar ANTES del paso 11, que sí
# escribe a disco a propósito para demostrar el estado (a))
ls apps/web/private-uploads/evidence/ 2>/dev/null | wc -l   # 0 o el directorio no existe

# Descarga por la ruta real, bytes idénticos
curl -s -b /tmp/cookies-acme.txt -o /tmp/bajado.pdf -w '%{http_code}\n' \
  http://localhost:3000/files/evidence/$EVIDENCE_ID          # 200
shasum -a 256 /tmp/evidencia.pdf /tmp/bajado.pdf             # los dos hashes iguales

# --- Los tres estados de configuración, en la ruta de escritura ---

# (a) sin variables R2: la app no habló con el bucket y el archivo cayó en disco
grep -c 'component":"r2"\|\[r2\]' /tmp/prol-dev-disco.log | grep -q '^0$' && echo "OK (a): no hablo con el bucket"
ls apps/web/private-uploads/evidence/ | wc -l                 # 1: el archivo del paso 11

# (c) configuración parcial: la app ARRANCA, avisa, y la escritura se rechaza
curl -s -o /dev/null -w 'login=%{http_code}\n' http://localhost:3000/login   # 200 <- no es un fallo de arranque
grep -n 'Configuración de R2 incompleta' /tmp/prol-dev-parcial.log
ANTES=$(ls apps/web/private-uploads/evidence/ | wc -l)
curl -s -b /tmp/cookies-acme.txt -F "file=@/tmp/evidencia.pdf;type=application/pdf" \
  -w '\nstatus=%{http_code}\n' http://localhost:3000/api/upload/evidence
# Esperado: status=503. El "error" del cuerpo es genérico a propósito (no expone
# nombres de variables al usuario); quien las nombra es el log de arriba.
DESPUES=$(ls apps/web/private-uploads/evidence/ | wc -l)
[ "$ANTES" = "$DESPUES" ] && echo "OK (c): el rechazo no toco el disco" || echo "ERROR: degrado a disco"

# Las dos evidencias de la fase 1 siguen intactas
psql "$DATABASE_URL" -c "select version, (form_snapshot::jsonb ? 'snapshotVersion') from evidences where form_snapshot is not null order by version;"
# Esperado: 1|f y 2|t

rm -f apps/web/scripts/_tmp-r2-check.mjs packages/db/prisma/_tmp-evidence-fixture.ts
git status --porcelain | grep '_tmp-' && echo "ERROR: quedan scripts desechables" || echo "OK: limpio"
    </automated>
  </verify>
  <done>
Dos evidencias subidas con R2 activo existen como objetos en el bucket bajo
`prol/evidence/<uuid>.<ext>`, con el tamaño correcto; las mismas claves sin
prefijo devuelven 404. Sus `fileKey` en la base son `<subdir>/<uuid>.<ext>` sin
prefijo. `/files/evidence/<id>` devuelve 200 y bytes con `sha256` idéntico al
original para las dos. El directorio de disco no recibió nada mientras R2 estuvo
activo. Los tres estados de configuración quedan demostrados: sin variables R2 la
subida va a disco con 200; con las cuatro va al bucket con 200; y con `R2_BUCKET` y
`R2_ACCESS_KEY_ID` ausente la aplicación **arranca** (`/login` en 200), el log trae
el aviso, la subida devuelve **503** —con `R2_ACCESS_KEY_ID` nombrada en el log, no en la respuesta— y el número de
archivos en disco no cambia. Las dos evidencias de `form_snapshot` de la fase 1
siguen intactas. Los scripts desechables ya no existen.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: criterio 3 — 403 desde otra empresa, 401 sin sesión</name>
  <files>(sin cambios de código; script desechable que NO se commitea)</files>
  <action>
La autorización no la toca este plan, y ése es justo el punto: hay que demostrar
que **sigue funcionando igual** después de cambiar el backend. Se usan las
evidencias creadas en la tarea 2, sobre el mismo servidor con R2 activo.

**1. Sin sesión → 401.**
```bash
curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3000/files/evidence/<id>
```
Debe ser `401` (`requireUser()` lanza `"Unauthorized"` y el `catch` de la ruta lo
traduce). Comprobar además que el cuerpo es `"No autenticado"`.

**2. Otra empresa → 403.** El seed sólo crea una empresa y mete a los dos
estudiantes dentro, así que hay que fabricar el usuario ajeno. Script desechable
`packages/db/prisma/_tmp-outsider.ts` que:
- crea un `User` con rol `STUDENT`, correo `ajeno@ejemplo.test`, `companyId: null`
  y el mismo `tenantId` del fixture — con `companyId` distinto al de la asignación
  cae en la rama `user.companyId === assignment.companyId`, que es falsa, y la ruta
  devuelve 403 sin haber tocado el archivo;
- crea su cuenta de credenciales de Better Auth **reutilizando la función
  `hashPassword` de `packages/db/prisma/seed.ts` (líneas 12-20)**, no una copia
  escrita a mano: si el hash no coincide con la configuración de scrypt de Better
  Auth, el login falla y el experimento no prueba nada;
- imprime el id creado.

Luego:
```bash
curl -s -c /tmp/cookies-ajeno.txt -X POST http://localhost:3000/api/auth/sign-in/email \
  -H 'Content-Type: application/json' -d '{"email":"ajeno@ejemplo.test","password":"password123"}'
curl -s -b /tmp/cookies-ajeno.txt -o /dev/null -w '%{http_code}\n' \
  http://localhost:3000/files/evidence/<id>
```
Debe ser `403`. Si sale 200, el login no funcionó o el usuario quedó en la empresa
equivocada: revisar antes de dar nada por bueno, porque un 403 por un login roto
no demuestra nada.

**3. Control positivo — la empresa dueña sigue viendo el archivo.** Repetir con
`/tmp/cookies-acme.txt`: `200`. Sin este control, los dos anteriores serían
compatibles con "la ruta está rota para todo el mundo".

**4. Control de revisor.** Con `maria.garcia@academiadigitalmx.com` (PROFESSOR del
tenant, rama `isManualReviewer`): `200`. Cubre el otro brazo del `if` de
autorización.

**5. Limpieza.** Borrar el usuario ajeno y su cuenta en un `finally`, y borrar el
script. La base local debe quedar como estaba salvo las evidencias nuevas, que el
plan 02-03 necesita.

Recoger la tabla de resultados (usuario → código) en el SUMMARY: es la evidencia
del criterio 3.
  </action>
  <verify>
    <automated>
# Los cuatro casos, sobre la misma evidencia y el mismo servidor con R2 activo
echo "sin sesion:"; curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3000/files/evidence/$EVIDENCE_ID
echo "otra empresa:"; curl -s -b /tmp/cookies-ajeno.txt -o /dev/null -w '%{http_code}\n' http://localhost:3000/files/evidence/$EVIDENCE_ID
echo "empresa duena:"; curl -s -b /tmp/cookies-acme.txt -o /dev/null -w '%{http_code}\n' http://localhost:3000/files/evidence/$EVIDENCE_ID
echo "revisor del tenant:"; curl -s -b /tmp/cookies-maria.txt -o /dev/null -w '%{http_code}\n' http://localhost:3000/files/evidence/$EVIDENCE_ID
# Esperado: 401, 403, 200, 200

# El usuario ajeno se borró
psql "$DATABASE_URL" -c "select count(*) from users where email = 'ajeno@ejemplo.test';"   # 0

rm -f packages/db/prisma/_tmp-outsider.ts
git status --porcelain | grep '_tmp-' && echo "ERROR: quedan scripts desechables" || echo "OK: limpio"

# Cierre del servidor de desarrollo
pkill -f 'next dev' || true

# Puertas transversales
pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep 'problems'
pnpm exec turbo run build
    </automated>
  </verify>
  <done>
Sobre la misma evidencia servida desde R2: sin sesión 401, miembro de otra empresa
403, miembro de la empresa dueña 200, revisor del tenant 200. El usuario ajeno se
borró de la base. Los scripts desechables no existen. `check-types` limpio, `lint`
en `✖ 81 problems (0 errors, 81 warnings)`, `build` verde.
  </done>
</task>

</tasks>

<verification>
Puertas transversales del milestone (obligatorias antes de cerrar el plan):

```bash
pnpm exec turbo run check-types                    # limpio
pnpm exec turbo run lint 2>&1 | grep 'problems'    # "✖ 81 problems (0 errors, 81 warnings)"
pnpm exec turbo run build                          # verde
```

`lint` sale con **exit 1** en la línea base sana. No es un fallo y no hay que
"arreglar" esas 81 advertencias; lo que no puede pasar es que suba el número o
aparezca un error.

Comprobaciones específicas del plan:
- `git diff --name-only` devuelve exactamente `apps/web/lib/document-storage.ts`.
- Ningún archivo bajo `apps/web/app/files/`, `apps/web/app/api/upload/` ni
  `packages/db/prisma/schema.prisma` aparece en el diff.
- `grep -rn '"prol/"' apps/web` (excluyendo `.next`) devuelve una sola coincidencia,
  en `document-storage.ts`.
- `git status --porcelain` no menciona ningún `_tmp-` ni nada bajo `private-uploads/`.
</verification>

<success_criteria>
1. Una evidencia nueva aparece como objeto en el bucket bajo
   `prol/evidence/<uuid>.<ext>` y se descarga por `/files/evidence/<id>` con los
   bytes idénticos. (Criterio 1 de la fase.)
2. La `fileKey` guardada no lleva el prefijo, y la misma clave sin prefijo no
   existe como objeto en el bucket: el prefijo no salió de la frontera de
   almacenamiento.
3. Otra empresa recibe 403 y una petición sin sesión recibe 401, con controles
   positivos que descartan "la ruta está rota para todos". (Criterio 3 de la fase.)
4. El interruptor `STORAGE_BACKEND` existe en un único sitio y la rama de disco
   sigue completa, lista para el rollback que demuestra el plan 02-03.
5. Con `R2_BUCKET` puesta y una credencial ausente la aplicación **arranca**, la
   subida se rechaza con 503 —la variable que falta se nombra en el log—, y el disco no recibe
   nada: los tres estados de configuración quedan en la tabla del SUMMARY.
6. Cero ediciones en `privateFileResponse`, en las tres rutas `/files/*`, en las
   dos de `/api/upload/*` y en el esquema de Prisma.
7. `check-types` limpio, `lint` en 0 errores y ≤81 advertencias, `build` verde.
</success_criteria>

<output>
Al terminar, crear
`.planning/phases/02-r2-para-el-tier-confidencial/02-02-SUMMARY.md`.

Registrar en el SUMMARY:
- **Los ids de evidencia y las `fileKey` creados en la tarea 2.** El plan 02-03 los
  necesita para el criterio 2 y para el rollback; sin ellos hay que rehacer el
  fixture.
- La tabla de códigos del criterio 3 (usuario → HTTP), incluidos los controles
  positivos.
- **La tabla de los tres estados de configuración** del paso 13, y el `error`
  literal del 503 de la subida rechazada: el plan 02-04 lo necesita para saber qué
  significa encontrarse ese estado en producción.
- Si hubo que activar `documents_enabled` en el tenant local, decirlo.
- Las claves exactas que quedaron escritas en el bucket compartido (recordatorio:
  en esta fase no hay borrado, así que se quedan).
- El contenido o la idea de los scripts desechables, por si el plan 02-03 o la
  fase 6 necesitan repetir el montaje.
</output>
