---
phase: 04-puente-html-pdf
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - DEPLOY.md
  - .planning/REQUIREMENTS.md
# Dos checkpoints: la firma humana de lo que se ve, y la decisión de desplegar.
autonomous: false
requirements: [PDF-01, PDF-02, PDF-04]
parallel_safe: false  # Ola 4 con un solo plan.

must_haves:
  truths:
    - "Una persona ha mirado el PDF y ha dicho si el encabezado, el pie, la rejilla de las tablas y la vista previa están bien — o consta explícitamente que no lo miró."
    - "La decisión de desplegar o diferir la toma el usuario, con alcance, riesgo, rollback y verificación delante."
    - "Si se despliega, producción queda verificada de forma automatizada y el registro dice exactamente qué NO demuestra ese despliegue."
    - "El despliegue va sólo a la instancia de Ibiza; PROL se queda a propósito en su SHA anterior y el registro dice que las dos instancias divergen."
    - "PDF-01, PDF-02 y PDF-04 quedan en REQUIREMENTS.md con la evidencia que realmente existe, declarada por partes."
  artifacts:
    - path: "DEPLOY.md"
      provides: "Sección 7f con el registro del despliegue de la fase 4 (o la constancia de que se difirió)"
      contains: "7f"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PDF-01, PDF-02 y PDF-04 con su estado y su evidencia"
  key_links:
    - from: "checkpoint de verificación humana"
      to: "criterios 1, 2 y 4 de la fase"
      via: "cuatro preguntas concretas con respuesta registrada literal"
      pattern: "resume-signal"
---

<objective>
Cerrar la fase: que una persona mire el PDF y firme lo que ve, que el usuario decida
si esto va a producción, y que el registro quede honesto en los dos casos.

Purpose: los criterios 1, 2 y 4 tienen evidencia de servidor completa después de
04-02 y 04-03 —páginas, filas íntegras, rejilla detectada por geometría, hashes— pero
ninguno de ellos sustituye a alguien mirando el documento. En esta fase y en la 3.1
hubo cuatro checkpoints de verificación humana y sólo uno se ejerció de verdad; el
resto se aprobaron en blanco y se registraron como tales. Este plan pregunta poco y
concreto para que ejercerlo cueste dos minutos.
Output: el registro de la verificación humana (ejercida o no, tal como ocurra), la
decisión de despliegue con su ejecución o su aplazamiento, y los tres requisitos
cerrados en REQUIREMENTS.md.
</objective>

<execution_context>
@/Users/flx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-puente-html-pdf/04-CONTEXT.md
@.planning/phases/04-puente-html-pdf/04-01-SUMMARY.md
@.planning/phases/04-puente-html-pdf/04-02-SUMMARY.md
@.planning/phases/04-puente-html-pdf/04-03-SUMMARY.md
@DEPLOY.md

<interfaces>
**Esta fase no cambia el esquema.** Ni una columna, ni un enum, ni un índice:
`packages/db/prisma/schema.prisma` no se ha tocado en ningún plan (comprobarlo con
`git diff --stat "$BASE"..HEAD -- packages/db/prisma/schema.prisma`, tiene que
salir vacío). El único cambio en `packages/db` es la fixture de desarrollo
(`seed-documents.ts`), que **no se ejecuta en producción**. Por tanto: **no hay
`db push`, no hace falta dump previo por esquema**, y el despliegue es sólo imagen.

**La migración v1.2 es requisito previo de este plan.** Entre la planificación de
esta fase y su ejecución, cuatro commits (`597f22f`, `d842ecf`, `9ca296c`, `55df7c2`)
cambiaron cómo se despliega este proyecto. El host pasa a alojar **dos instancias
sobre la misma imagen**, distinguidas sólo por el entorno:

| | Ibiza | PROL |
|---|---|---|
| Dominio | `https://ibizaonline.ibizaconsultores.mx` | `https://prol.prosuite.pro` |
| Units | `ibiza-web` / `ibiza-db` | `prol-web` / `prol-db` |
| Red | `ibiza-internal` | `prol-internal` |
| Entorno | `/etc/containers/env/ibiza-web.env` | `/etc/containers/env/prol-web.env` |
| Tag móvil | `localhost/prol-web:ibiza` | `localhost/prol-web:prol` |
| `DEFAULT_TENANT_SLUG` | `ibiza-online` | vacío (multi-tenant) |

**Esta fase despliega SÓLO a `ibiza`** (decisión del usuario, 2026-09-03). Es donde
vive el único tenant con el módulo documental encendido, así que es la única
instancia donde la descarga de PDF sirve para algo hoy.

**Estado real comprobado el 2026-09-04, antes de ejecutar la fase:** Ibiza está en pie
y sana (`ibiza-web` `healthy` sobre `prol-web:55df7c2`, `/api/health` y `/sign-in` en
200, y el redirect legacy `ibiza-online.prol.prosuite.pro` → 301 funcionando).
**PROL no está levantado todavía**: sin unit `prol-web.service`, sin tag
`localhost/prol-web:prol`, `prol-web-1` detenida a propósito y `prol.prosuite.pro` en
502. Es un paso pendiente de la migración, confirmado como tal por el usuario, y
**queda fuera del alcance de esta fase**. Este plan no lo arregla ni lo espera: se
limita a registrarlo.

**Comprobar primero que la separación ya terminó**; si no, este plan no puede
desplegar y la tarea 2 se presenta con `diferir` como única opción viable:
```bash
ssh propodvps2 "systemctl is-active ibiza-web.service"
curl -s -o /dev/null -w '%{http_code}\n' https://ibizaonline.ibizaconsultores.mx/api/health
```

**Ruta canónica de despliegue: `scripts/deploy.sh ibiza`.** Ya no se teclea la
secuencia a mano; el script encapsula árbol limpio → `git archive` → build →
respaldo → SQL pendiente CON CONFIRMACIÓN → migrar → mover el tag de ESTA instancia →
reiniciar → esperar `healthy`, y revierte solo si no llega. Si durante la transición
las units o la red conservan los nombres viejos (`prol-web-1`, `prol-db-1`,
`prol_prol-internal`), se fuerzan con `PROL_DEPLOY_UNIT_WEB`, `PROL_DEPLOY_UNIT_DB`
y `PROL_DEPLOY_NETWORK`.

**Rollback:** mover el tag de la instancia a un SHA anterior, que es exactamente
`scripts/deploy.sh ibiza <sha-anterior>`. **Ya no existe un `latest` compartido**:
tocarlo desplegaría a las dos instalaciones a la vez, que es justo lo que la
separación evita.

**El SHA base se lee, no se supone.** `9bf55ee` era la imagen en producción cuando se
escribió este plan; la migración v1.2 se despliega antes que esta fase, así que ya no
lo será. Tanto el alcance como el rollback salen de leer el tag en vivo:
```bash
BASE=$(ssh propodvps2 "podman image inspect localhost/prol-web:ibiza --format '{{index .RepoTags 0}}'" | sed 's/.*://')
git log --oneline "$BASE"..HEAD
```

**`APP_URL` y `APP_DOMAIN` son obligatorias desde `597f22f`.** `lib/env.ts` las exige
y **el contenedor no arranca sin ellas**; las viejas `NEXT_PUBLIC_*` no valen de
respaldo, porque Next las hornea en la imagen durante el build. Comprobarlo **antes**
de desplegar, no descubrirlo con el servicio caído:
```bash
ssh propodvps2 "grep -cE '^(APP_URL|APP_DOMAIN)=' /etc/containers/env/ibiza-web.env"   # 2
```

`panel-prosuite-2` y `propodvps2` son el mismo host (`195.26.255.71`).

**Lo que un despliegue de esta fase NO puede demostrar.** El módulo documental está
encendido sólo para `ibiza-online` —el tenant que tras la separación vive en la
instancia de Ibiza— y nunca se ha usado: al cerrar la fase 3.1 `manual_assignments` y
`evidences` estaban en 0 filas. Si `manual_documents` también está en 0, **no existe
ningún documento cuyo PDF descargar en producción**, y hay que decirlo así en el
registro en vez de insinuar una verificación que no cabe. Comprobarlo, no suponerlo,
y contra la base de Ibiza:
```bash
ssh propodvps2 "DBU=\$(podman exec ibiza-db printenv POSTGRES_USER); DBN=\$(podman exec ibiza-db printenv POSTGRES_DB); podman exec ibiza-db psql -U \$DBU -d \$DBN -tAc \"select count(*) from manual_documents\""
```
El usuario y la base se leen del entorno del contenedor en vez de escribir `-U prol -d
prol`: la separación puede haberlos renombrado, y el `HealthCmd` del quadlet los lleva
a mano precisamente por eso (ver `9ca296c`).

**Deuda heredada, abierta desde el plan 03-08 y arrastrada por 03.1-06:** nadie ha
descargado un certificado o un PDF de resultados en producción tras los dos últimos
despliegues. Si se despliega, es la ocasión natural de saldarla — y sólo se da por
saldada con el clic.

**Precedente de redacción del registro:** DEPLOY.md §7e (fase 3.1). Copia esa
estructura: estado, qué se aplicó, tabla de verificación automatizada, qué NO
demuestra, rollback.
</interfaces>
</context>

<constraints>
- **Una aprobación en blanco no es una verificación.** Si el usuario responde
  «aprobado» sin contestar las preguntas, se registra literalmente como *aprobado sin
  ejercitar* y los criterios afectados **no** se marcan como verificados por humano.
  Precedente y motivo: 03-06, 03.1-04 y 03.1-06.
- **No se enciende `documents_enabled` en ningún tenant.** Es decisión de producto,
  fuera de esta fase. Registrar su valor antes y después para probar que no se tocó.
- **Nada de `db push` en producción.** Esta fase no cambia el esquema; si alguna
  herramienta pidiera `--accept-data-loss`, se aborta y se avisa.
- **Sólo se despliega a `ibiza`.** No se toca el tag `localhost/prol-web:prol` ni la
  unit `prol-web`. Las dos instancias quedarán en SHA distinto a propósito, y eso se
  escribe en el registro; no es una anomalía que haya que "arreglar" desplegando PROL
  por iniciativa propia.
- **Si la migración v1.2 no ha terminado, no se despliega.** Sin la instancia de Ibiza
  en pie no hay dónde desplegar esto, y la secuencia manual vieja ya no existe.
- Si el usuario elige diferir, **no se despliega**, se documenta la decisión y la fase
  cierra igual. Diferir no es un fallo.
</constraints>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 1: mirar el PDF (criterios 1, 2 y 4)</name>
  <action>
Pausa para verificación humana. El ejecutor no escribe código en esta tarea: levanta
el servidor de desarrollo, resuelve los tres identificadores reales de la base
(`MULTIPAGE_ID`, `MANUAL_ID`, `DOCUMENT_ID`), le pega al usuario las URL ya
construidas —nada de plantillas con marcadores sin resolver— y espera. La respuesta
se transcribe literal al SUMMARY, sea cual sea; si llega en blanco, se registra como
«aprobado sin ejercitar» y los criterios 1, 2 y 4 se quedan con evidencia de servidor
únicamente.
  </action>
  <what-built>
El PDF del documento nativo, completo: banda de identidad en todas las páginas
(logo o inicial, código, nombre, versión), pie «código · vN — Página X de Y», bloque
de identidad completo en la primera página, sello diagonal para BORRADOR y OBSOLETO,
tablas con rejilla y filas que no se parten, y la vista previa del editor convertida
en el propio PDF.

Ya está verificado por servidor, sin intervención humana: número de páginas, presencia
de banda y pie página por página, detección de la rejilla por geometría, integridad de
las 45 filas de la tabla larga, y los hashes que demuestran que cambiar el logo cambia
el PDF sin tocar `company_documents`. Lo que falta es exclusivamente el juicio de
alguien mirando la hoja.
  </what-built>
  <how-to-verify>
Con el servidor de desarrollo arriba (`pnpm --filter @prol/web dev`), como
`admin@prol.prosuite.pro` / `password123`:

1. Abre el documento multipágina de prueba y descarga su PDF:
   `http://localhost:3000/api/documents/template/<MULTIPAGE_ID>/pdf`
   (el ejecutor te pega la URL exacta ya resuelta).
   → **¿Cada página lleva arriba el código del documento y abajo «Página X de Y»?**
2. Ve a la tabla larga del mismo PDF, y pasa por el salto de página.
   → **¿Se ven los bordes de todas las celdas, y ninguna fila queda cortada a la
   mitad entre dos páginas?**
3. Entra al editor de la plantilla
   (`/tenant-admin/manuals/<MANUAL_ID>/documents/<DOCUMENT_ID>`) y pulsa
   «Vista previa (PDF)».
   → **¿Lo que ves dentro del editor es el mismo documento que descargaste?**
4. Entra como cliente (`carlos.mendoza@gmail.com` / `password123`), abre
   Documentos → el procedimiento de Acme, y pulsa «Descargar PDF».
   → **¿Sale con el nombre de Acme y sin ningún sello de borrador?**

Son cuatro preguntas con respuesta de una línea. **Si respondes sólo «aprobado» sin
contestarlas, se registrará exactamente como «aprobado sin ejercitar» y los criterios
1, 2 y 4 quedarán con evidencia de servidor únicamente** — que es la situación en la
que terminaron los tres últimos checkpoints de este proyecto.
  </how-to-verify>
  <resume-signal>
Responde las cuatro (sí/no y qué viste), o escribe «aprobado sin mirar» si prefieres
seguir sin ejercitarlo — se registrará así, sin penalización y sin adornos.
  </resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Tarea 2: desplegar ahora o diferir</name>
  <action>
Pausa para decisión del usuario. Antes de presentarla, el ejecutor hace tres lecturas
en vivo y actualiza el alcance de abajo con lo que salga; no lo presenta tal cual si
ha cambiado:

1. **¿Terminó la migración?** `systemctl is-active ibiza-web.service` y un 200 en
   `https://ibizaonline.ibizaconsultores.mx/api/health`. Si no está en pie, la única
   opción viable es `diferir` y hay que decirlo al presentar la decisión.
2. **¿Qué SHA corre Ibiza ahora?** `podman image inspect localhost/prol-web:ibiza`.
   Ese es `$BASE`, no `9bf55ee`.
3. **¿Qué va dentro?** `git log --oneline "$BASE"..HEAD` y
   `git diff --stat "$BASE"..HEAD -- packages/db/prisma/schema.prisma`. Si aparece
   trabajo de otra sesión por debajo de los planes de esta fase —la migración v1.2
   incluida, si aún no se había desplegado—, **enseñarlo antes de decidir**: fijar el
   SHA no lo excluye, y ocurrió en 03.1-06.
  </action>
  <decision>Si el puente HTML→PDF va a producción en este momento.</decision>
  <context>
**Alcance del despliegue.** Dos rutas nuevas de descarga (`/api/documents/company/…`
y `/api/documents/template/…`), el mapeador y el armazón del PDF, el botón de
descarga en el visor del cliente, la vista previa del editor convertida en PDF, y
una excepción de encuadre acotada a esas dos rutas
(`X-Frame-Options: SAMEORIGIN` + `frame-ancestors 'self'`; todo lo demás sigue en
`DENY`/`'none'`). Dos dependencias nuevas, ambas ya presentes en el lockfile de forma
transitiva: `htmlparser2` y `domhandler`.

**Sin cambios de esquema.** `schema.prisma` no se ha tocado en toda la fase: no hay
`db push`, no hay migración, no hay riesgo de datos. Lo único tocado en `packages/db`
es la fixture de desarrollo, que en producción no se ejecuta.

**Destino: sólo la instancia de Ibiza** (`scripts/deploy.sh ibiza`). PROL no se toca;
su tag `localhost/prol-web:prol` se queda donde está y las dos instancias quedarán en
SHA distinto a propósito.

**Riesgo.** Bajo y acotado: código nuevo en rutas nuevas. El riesgo con superficie
real es la excepción de encuadre en `middleware.ts`, que afecta a un archivo por el
que pasa todo el tráfico — está limitada por una expresión regular a las dos rutas de
PDF y a `'self'`, y se comprueba con `curl -I` inmediatamente después del despliegue,
contra una ruta de PDF y contra otra cualquiera. `'self'` no lleva dominio escrito, así
que la misma imagen sigue valiendo para las dos instancias.

**Riesgo añadido por la migración.** Si el SHA que corre Ibiza es anterior a `597f22f`,
este despliegue arrastra también el cambio de marca por entorno, y sin `APP_URL` y
`APP_DOMAIN` en `/etc/containers/env/ibiza-web.env` **el contenedor no arranca**. Se
comprueba antes (ver `<interfaces>`), y si faltan se aborta sin desplegar.

**Rollback.** Un comando, con el SHA anterior todavía tagueado:
`scripts/deploy.sh ibiza $BASE`. Mueve el tag `…:ibiza` y reinicia sólo esa unit. Sin
esquema que revertir.

**Verificación posterior** (automatizable, se ejecuta y se registra en la tarea 3):
`ibiza-web` activa y `healthy`, `/api/health` y `/sign-in` en 200 sobre
`ibizaonline.ibizaconsultores.mx`, las dos rutas de PDF respondiendo 401 sin sesión,
`X-Frame-Options` correcto en una ruta de PDF y `DENY` en otra, `documents_enabled`
idéntico antes y después en los tenants de esa base, imagen nueva y anterior tagueadas,
log de arranque limpio, y `prol-web` intacta con su tag sin mover.

**Lo que este despliegue no va a demostrar.** Si `manual_documents` está en 0 filas en
la base de Ibiza —como lo estaban `manual_assignments` y `evidences` al cerrar la fase
3.1— no habrá ningún documento cuyo PDF descargar allí. Eso se comprueba y se dice; no
se disimula. Tampoco demuestra nada sobre PROL, que no recibe este código.
  </context>
  <options>
    <option id="desplegar-ibiza">
      <name>Desplegar a la instancia de Ibiza</name>
      <pros>La descarga en PDF queda disponible para IBIZA en cuanto cree su primer documento, en el único sitio donde el módulo documental está encendido; el trabajo no se acumula sin desplegar; es la ocasión de saldar la deuda de descargar un PDF real en producción, pendiente desde 03-08.</pros>
      <cons>Toca `middleware.ts`, por donde pasa todo el tráfico de esa instancia; hay unos segundos de corte al reiniciar `ibiza-web`; deja las dos instancias en SHA distinto.</cons>
    </option>
    <option id="diferir">
      <name>Diferir</name>
      <pros>Producción se queda exactamente como está; el código espera en `main` sin coste. Es la única opción viable si la migración v1.2 aún no ha terminado.</pros>
      <cons>Se acumula con lo que venga en la fase 5; la deuda de la verificación humana en producción sigue creciendo.</cons>
    </option>
  </options>
  <resume-signal>Elige: `desplegar-ibiza` o `diferir`.</resume-signal>
</task>

<task type="auto">
  <name>Tarea 3: ejecutar lo decidido y dejar el registro honesto</name>
  <files>DEPLOY.md, .planning/REQUIREMENTS.md</files>
  <action>
**Si la tarea 2 salió `diferir`:** no se toca producción. Se añade a DEPLOY.md una
§7f corta con la constancia (fecha, alcance listo para desplegar, SHA, rollback
disponible) marcada como **NO APLICADO**, y se salta al punto 3 de esta tarea.

**Si salió `desplegar-ibiza`:**

1. **Antes de tocar nada.**
   - `git status` limpio (`scripts/deploy.sh` aborta si no lo está: `git archive`
     exporta lo commiteado y la imagen no sería el SHA que dice).
   - **La migración v1.2 terminó**: `ibiza-web` activa y `/api/health` en 200 sobre
     `ibizaonline.ibizaconsultores.mx`. Si no, se aborta y se registra como diferido.
   - **`APP_URL` y `APP_DOMAIN` presentes** en `/etc/containers/env/ibiza-web.env`
     (`grep -cE '^(APP_URL|APP_DOMAIN)=' … ` → 2). Sin ellas el contenedor no arranca.
   - Fijar `BASE` leyendo el tag `localhost/prol-web:ibiza` en vivo, y decirle al
     usuario qué commits van dentro (`git log --oneline "$BASE"..HEAD`): si aparece
     trabajo de otra sesión por debajo de los planes de esta fase, **avisarlo antes de
     desplegar**, como ocurrió en 03.1-06.
   - Registrar, contra la base de Ibiza, el `documents_enabled` de los tenants que
     tenga y el número de filas de `manual_documents`, `company_documents` y
     `manual_assignments`. Leer usuario y base del entorno del contenedor, no
     escribirlos a mano.
2. **Desplegar** con `scripts/deploy.sh ibiza`. El script pide confirmación al mostrar
   el SQL pendiente: como esta fase no cambia el esquema, **ahí no debe salir ninguna
   sentencia**; si sale algo, se aborta y se avisa. **Nunca `db push` a mano.** No se
   ejecuta `scripts/deploy.sh prol`.
3. **Verificar** y anotar cada resultado real (no el esperado), todo sobre
   `https://ibizaonline.ibizaconsultores.mx`:
   - `systemctl is-active ibiza-web.service` → `active`; `podman inspect ibiza-web`
     → `healthy`
   - `GET /api/health` → 200; `GET /sign-in` → 200
   - `GET /api/documents/template/<cualquier-id>/pdf` **sin sesión** → 401
   - `GET /api/documents/company/<cualquier-id>/pdf` **sin sesión** → 401
   - `curl -sI` sobre una de esas rutas → `X-Frame-Options: SAMEORIGIN` y
     `frame-ancestors 'self'`; sobre `/api/health` → `DENY` y `'none'`
   - `documents_enabled` antes y después: idéntico, tenant por tenant
   - `manual_documents` / `company_documents` / `manual_assignments`: número de filas
   - `podman images | grep prol-web`: la nueva y `$BASE` tagueadas.
     **Sobre PROL se registra el estado, no se comprueba un tag.** Al 2026-09-04 la
     instancia de PROL no existe todavía: no hay unit `prol-web.service`, no hay tag
     `localhost/prol-web:prol`, la unit vieja `prol-web-1` está detenida y
     `prol.prosuite.pro` devuelve 502 (levantarla es un paso pendiente de la migración,
     confirmado por el usuario). Anotar lo que se encuentre en el momento; si para
     entonces la instancia ya existe, comprobar que su tag **no** se movió
   - `podman logs --since 10m ibiza-web`: arranque limpio, ninguna credencial
   Si `manual_documents` está en 0, escribirlo tal cual: **no hay ningún PDF real que
   descargar en producción**, y el despliegue no demuestra el circuito completo allí.
4. **DEPLOY.md §7f**, con la estructura de §7e: estado, alcance (sin esquema), tabla
   de verificación con los resultados reales, "lo que este despliegue NO demuestra", y
   el comando de rollback (`scripts/deploy.sh ibiza $BASE`). Añadir una línea explícita
   sobre **la divergencia deliberada entre instancias**: qué SHA corre Ibiza, cuál
   corre PROL, y que PROL no lleva el puente HTML→PDF por decisión, no por olvido. Si
   la deuda de descargar un PDF/certificado en producción sigue sin saldarse, decirlo
   ahí otra vez, con el contador de veces.

**Y en los dos casos:**

5. **REQUIREMENTS.md**, con la evidencia declarada por partes, como se hizo con DOC-04
   en 03-07:
   - **PDF-01** — encabezado ISO y pie numerado: evidencia de servidor (arnés, página
     por página) + lo que el usuario confirmara (o no) en la tarea 1.
   - **PDF-02** — tablas con bordes y sin filas partidas: evidencia de servidor (45
     filas íntegras, rejilla detectada por geometría) + confirmación humana o su
     ausencia. Si la detección de rejilla de la tarea 3 de 04-02 no fue concluyente,
     el borde depende de la confirmación visual: decirlo.
   - **PDF-04** — logo en vivo: server-verified con los hashes de 04-03.
   Nada se marca `Complete` con evidencia que no exista. Si un criterio se quedó sin
   firma humana, se escribe qué falta exactamente y quién puede darlo.
  </action>
  <verify>
    <automated>
cd /Users/flx/Documents/Developer/PROL
# La fase no tocó el esquema (invariante del que depende todo el plan de despliegue)
git diff --stat "$BASE"..HEAD -- packages/db/prisma/schema.prisma | tee /tmp/schema-diff.txt
test ! -s /tmp/schema-diff.txt && echo "OK sin cambios de esquema" || echo "ERROR: hay cambios de esquema, replantear el despliegue"

# Sólo si se desplegó — verificación en la instancia de Ibiza
IB=https://ibizaonline.ibizaconsultores.mx
ssh panel-prosuite-2 "systemctl is-active ibiza-web.service; podman inspect ibiza-web --format '{{.State.Health.Status}}'"
curl -s -o /dev/null -w '%{http_code}\n' $IB/api/health
curl -s -o /dev/null -w '%{http_code}\n' $IB/sign-in
curl -s -o /dev/null -w '%{http_code}\n' $IB/api/documents/template/inexistente/pdf   # 401
curl -sI $IB/api/documents/template/inexistente/pdf | grep -i "x-frame-options"        # SAMEORIGIN
curl -sI $IB/api/health | grep -i "x-frame-options"                                    # DENY

# Estado de PROL: se registra tal cual. Al 2026-09-04 no hay unit ni tag `:prol`
# y prol.prosuite.pro da 502 — levantar esa instancia es un paso pendiente de la
# migración, no algo que arregle esta fase. Si ya existe, su tag no debe haberse movido.
ssh panel-prosuite-2 "podman images --format '{{.Repository}}:{{.Tag}}' | grep -E 'prol-web:(ibiza|prol)$' ; systemctl is-active prol-web.service 2>&1"
curl -s -o /dev/null -w 'prol.prosuite.pro %{http_code}\n' https://prol.prosuite.pro/api/health

# Estado de la base de Ibiza — usuario y base del entorno, la separación pudo renombrarlos
ssh panel-prosuite-2 "DBU=\$(podman exec ibiza-db printenv POSTGRES_USER); DBN=\$(podman exec ibiza-db printenv POSTGRES_DB);
  podman exec ibiza-db psql -U \$DBU -d \$DBN -tAc \"select slug, documents_enabled from tenants order by slug\";
  podman exec ibiza-db psql -U \$DBU -d \$DBN -tAc \"select 'manual_documents='||count(*) from manual_documents\""

# El registro quedó escrito
grep -n "7f" DEPLOY.md | head -3
grep -nE "PDF-01|PDF-02|PDF-04" .planning/REQUIREMENTS.md
    </automated>
  </verify>
  <done>
Lo que el usuario decidió es lo que ocurrió. Si se desplegó, la instancia de Ibiza
corre la imagen nueva y está `healthy`, las dos rutas de PDF responden 401 sin sesión,
la excepción de encuadre está sólo donde debe, `documents_enabled` no se movió,
`localhost/prol-web:prol` sigue en su SHA anterior, y DEPLOY.md §7f recoge la tabla de
verificación con resultados reales, la divergencia deliberada entre instancias y una
sección explícita de lo que el despliegue no demuestra. Si se difirió, DEPLOY.md §7f
lo dice y las dos instancias siguen donde estaban.
En los dos casos, PDF-01, PDF-02 y PDF-04 están en REQUIREMENTS.md con la evidencia
que realmente existe, y la respuesta del checkpoint de la tarea 1 está registrada
literal.
  </done>
</task>

</tasks>

<verification>
- La respuesta humana de la tarea 1 aparece transcrita literalmente en el SUMMARY, sea
  cual sea.
- La decisión de la tarea 2 aparece con la opción elegida, no parafraseada.
- Si hubo despliegue: la tabla de verificación de DEPLOY.md §7f lleva resultados
  medidos, no esperados, y nombra lo que no demuestra.
- `.planning/REQUIREMENTS.md` no marca `Complete` nada que no tenga evidencia.
</verification>

<success_criteria>
La fase 4 cierra con el puente HTML→PDF funcionando y con un registro en el que se
puede confiar: cada criterio con la evidencia que realmente tiene, la verificación
humana ejercida o declarada como no ejercida, y producción en el estado que el usuario
eligió conscientemente.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/04-puente-html-pdf/04-04-SUMMARY.md`.
</output>
</content>
