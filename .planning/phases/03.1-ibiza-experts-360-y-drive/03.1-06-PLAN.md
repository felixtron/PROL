---
phase: 03.1-ibiza-experts-360-y-drive
plan: 06
type: execute
wave: 6
depends_on: ["03.1-05"]
files_modified:
  - DEPLOY.md
# Tiene dos checkpoints: no se toca el VPS sin aprobación explícita, y la
# confirmación humana sobre producción no se da por hecha.
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03, DRV-01, DRV-02, DRV-03, DRV-04]
parallel_safe: false  # Ola 6 con un solo plan, y además toca producción: nada
                      # concurrente mientras corre.

must_haves:
  truths:
    - "El usuario aprueba explícitamente, con alcance, riesgo, rollback y verificación delante, antes de que nada toque el VPS."
    - "Antes de decidir, el usuario sabe QUÉ hay exactamente en el commit que se va a desplegar — incluido si arrastra trabajo de la otra sesión."
    - "Existe un dump de la base de producción tomado antes del cambio de esquema."
    - "El preview del esquema se lee antes de aplicar nada y contiene sólo dos `ADD COLUMN` nullable."
    - "El esquema se aplica ANTES de mover el tag `latest`."
    - "`documents_enabled` queda exactamente como estaba: `ibiza-online` en true, los otros dos en false. Este despliegue no enciende ni apaga nada."
    - "IBIZA, que sí tiene el módulo encendido, ve el menú agrupado con su rótulo — y eso lo confirma una persona, no un `curl`."
    - "El rollback de un comando está disponible y escrito, con la imagen anterior todavía tagueada."
  artifacts:
    - path: "DEPLOY.md"
      provides: "Sección 7e con el estado real del despliegue de la fase 3.1"
      contains: "7e"
  key_links:
    - from: "db push contra la base de producción"
      to: "podman tag localhost/prol-web:$SHA latest"
      via: "el tag se mueve DESPUÉS del push, nunca antes"
      pattern: "podman tag"
    - from: "tenants.documents_menu_label"
      to: "el rótulo del menú en el panel de IBIZA"
      via: "columna nueva + resolveDocumentsMenuLabel; sin valor, sale el neutro"
      pattern: "documents_menu_label"
---

<objective>
Poner en producción la fase 3.1: el menú agrupado con rótulo por tenant, el enlace de
Drive por proyecto y el requisito que se cumple sin archivo, con su cambio de esquema
—dos columnas nullable— y sin encender el módulo en ningún tenant que hoy lo tenga
apagado.

Purpose: la fase existe por un hallazgo de producción (0 evidencias, 0 proyectos, 0
manuales, 0 actividades) y sólo sirve para algo allí. Además, IBIZA tiene el módulo
**encendido** desde antes de la fase 3: es el único tenant que va a ver este cambio, y
es exactamente el destinatario de la fase.
Output: producción corriendo la imagen de esta fase con las dos columnas aplicadas, el
reparto de `documents_enabled` sin tocar, `DEPLOY.md` §7e contando la verdad, y —por
fin— la confirmación humana sobre producción que quedó pendiente desde el plan 03-08.

**Este plan toca el VPS y cambia el esquema de la base de producción. No empieza hasta
que el usuario lo apruebe.**
</objective>

<execution_context>
@/Users/flx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-CONTEXT.md
@.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-01-SUMMARY.md
@.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-02-SUMMARY.md
@.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-03-SUMMARY.md
@.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-04-SUMMARY.md
@.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-05-SUMMARY.md
@.planning/phases/03-procedimientos-nativos/03-08-SUMMARY.md
@DEPLOY.md

<interfaces>
<!-- Estado de producción al planificar (2026-09-02). Reconfirmarlo en vivo: es una
     fotografía, no una garantía. -->

**Producción hoy:**
- Host: `panel-prosuite-2` = `propodvps2` = `195.26.255.71` (mismo host, dos alias
  confirmados en el plan 02-04 y documentados en `DEPLOY.md`).
- Imagen corriendo: **`04135ca`**. Siguen tagueadas `55c020d` y `64f7476` — rollback
  de dos niveles.
- Rollback de un comando:
  `podman tag localhost/prol-web:04135ca localhost/prol-web:latest && systemctl restart prol-web-1.service`
- `documents_enabled`: **`ibiza-online=true`**, `academia-digital=false`,
  `mecanica-g3=false`. Releído de la base real el 2026-09-02. **No se toca.**
- `company_documents` en 0 filas en los tres tenants; y el hallazgo que originó la
  fase: 0 evidencias, 0 proyectos, 0 manuales, 0 actividades.
- R2 activo para el tier confidencial, con sus cuatro variables en
  `/etc/containers/env/prol-web-1.env`. Esta fase no lo toca.
- Respaldo por cron a las 03:00 UTC.

**El cambio de esquema de esta fase es el más pequeño del milestone:**
```sql
ALTER TABLE "tenants" ADD COLUMN "documents_menu_label" TEXT;
ALTER TABLE "manual_assignments" ADD COLUMN "drive_url" TEXT;
```
Dos columnas nullable, sin `DEFAULT`, sin índice, sin FK, sin enums nuevos, sin
`SET NOT NULL`. **Nada más debe aparecer en el preview.**

**La ruta canónica de despliegue** (la que NO toca `/opt/prol`, cuyo
`docker-compose.prod.yml` diverge del repo):
```bash
git archive --format=tar.gz HEAD | ssh <host> "mkdir -p /opt/prol-deploy-$SHA && tar -xz -C /opt/prol-deploy-$SHA"
ssh <host> "podman build -t localhost/prol-web:$SHA /opt/prol-deploy-$SHA"
```
**No usar la sección "Re-deploy" de `DEPLOY.md`**, que hace `git pull` en el host:
rompería con el compose divergente. Está anotado como concern en `STATE.md`.

**Trampa operativa conocida** (`DEPLOY.md` §7c): `grep` no es seguro para **mover**
credenciales por SSH en esta máquina — un hook local (`rtk`) reescribe la invocación
incluso dentro de una tubería. Usar `awk` o una variable de shell capturada. `grep`
sigue siendo seguro para **contar** después.

**Lo que ha cambiado respecto al despliegue de la fase 3, y hay que mirar:**
1. **Otra sesión interactiva (`prol-1d`) trabaja en el mismo repositorio**, con
   ~18 archivos de DC-3 sin commitear al planificar esta fase. Si a la hora de
   desplegar ya los ha commiteado, `git archive HEAD` **los llevaría a producción**, y
   el `db push` aplicaría también sus cambios de esquema. Eso no es necesariamente
   malo, pero **no puede pasar sin que el usuario lo sepa y lo decida**: es la
   diferencia entre desplegar una fase y desplegar dos.
2. **IBIZA tiene el módulo encendido.** A diferencia de la fase 3, que se desplegó a
   oscuras, aquí hay un tenant que **sí va a ver el cambio**: su menú deja de tener
   cuatro entradas sueltas y pasa a tener un desplegable. Es lo que la fase quiere, y
   es también el único riesgo de percepción del despliegue.
3. **La confirmación visual humana del plan 03-08 sigue pendiente.** Nunca se
   registró como aprobada, a propósito. El checkpoint final de este plan es la ocasión
   de cerrarla junto con la de esta fase, y hay que decirlo en el SUMMARY.
</interfaces>
</context>

<constraints>
- **Nada toca el VPS —ni un comando de lectura— antes de que el usuario apruebe.** Es
  regla del proyecto: pausar antes de producción, con alcance, riesgo, rollback y
  verificación por delante.
- **`documents_enabled` no se toca en ningún tenant.** Ni para encender Academia
  Digital MX o Mecanica G3, ni para apagar IBIZA. Cualquier cambio ahí es decisión de
  producto y no de este plan.
- **El `db push` se aplica ANTES de mover el tag `latest`.** Un contenedor nuevo contra
  columnas que no existen es la única forma realista de romper esto.
- **Si `db push` pidiera `--accept-data-loss`, se para.** Dos `ADD COLUMN` nullable no
  pueden pedirlo; que lo pida significa que el diff no es el que se acaba de leer.
- **La imagen anterior (`04135ca`) sigue tagueada.** Sin ella no hay rollback de un
  comando.
- **Ninguna credencial en el diff, en el log ni en el SUMMARY.**
- **No se ejecuta `pnpm db:seed` contra producción.** Nunca, y aquí menos.
- **No se usa la sección "Re-deploy" de `DEPLOY.md`** (hace `git pull` en un host cuyo
  compose diverge del repo).
- Stagear sólo `DEPLOY.md` y los ficheros de `.planning/` por ruta explícita. **Nunca
  `git add -A`.**
</constraints>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Tarea 1: aprobación del despliegue</name>
  <decision>
¿Se despliega ahora la fase 3.1 a producción —con sus dos columnas nuevas— o se
difiere?
  </decision>
  <context>
Antes de tocar el VPS, esto es lo que hay sobre la mesa.

**Alcance.** El despliegue lleva tres cosas:
1. **El menú agrupado con rótulo por tenant**, en los paneles de administrador,
   consultor y cliente, escritorio y móvil, más el vocabulario nuevo (Manuales
   Maestros / Proyectos).
2. **El enlace de Drive por proyecto**: una columna, una acción que sólo puede usar el
   administrador, validación de host contra una lista cerrada al escribir y al leer, y
   el acceso a la carpeta desde el proyecto del cliente y desde la ficha de revisión.
3. **El requisito que se cumple sin subir archivo a PROL**, conservando entero el
   circuito de cumplimiento (actividad, periodicidad, aprobación, bitácora, agenda y
   cron). Con ello se retira la ruta de subida de evidencias, que ya no llama nadie.

**Cambio de esquema: el más pequeño del milestone.** Dos `ADD COLUMN` nullable
(`tenants.documents_menu_label`, `manual_assignments.drive_url`). Sin enums nuevos,
sin índices, sin FK, sin `SET NOT NULL`, sin `DROP` de nada.

**Riesgo.**
- *Del esquema: muy bajo.* Dos columnas nullable sobre tablas que en producción están
  prácticamente vacías en lo que a este módulo respecta (0 proyectos, 0 manuales).
  Mitigación de siempre: dump previo, leer el preview antes de aplicar, mover `latest`
  sólo después.
- *De percepción: aquí sí hay algo, y es nuevo respecto a la fase 3.* **IBIZA tiene el
  módulo encendido** (`ibiza-online = true`, desde antes de la fase 3). Es el único
  tenant que va a notar el despliegue: su sidebar deja de tener cuatro entradas
  sueltas y pasa a tener un desplegable, y "Manuales" pasa a llamarse "Manuales
  Maestros". Es exactamente lo que la fase quiere, pero conviene decirlo antes de que
  ocurra y no después. Con 0 manuales creados, no hay contenido que se rompa.
- *De alcance del commit — el riesgo que hay que mirar de verdad.* Otra sesión
  (`prol-1d`) trabaja en este mismo repositorio con cambios de DC-3. **Antes de
  presentar este checkpoint hay que decirle al usuario, con nombres y números, qué
  commits van dentro** de lo que se desplegaría (`git log --oneline 04135ca..HEAD`) y
  si alguno es de DC-3. Si los hay, desplegar HEAD sería desplegar dos trabajos a la
  vez, y eso lo decide el usuario, no el plan.
- *De comportamiento:* `submitEvidence` deja de aceptar un archivo y
  `/api/upload/evidence` desaparece. En producción no hay ni una evidencia, así que
  no hay nada que dejar de funcionar; las evidencias con archivo se seguirían leyendo
  igual si algún día las hubiera.

**Rollback.** Un comando, ya escrito:
```
podman tag localhost/prol-web:04135ca localhost/prol-web:latest && systemctl restart prol-web-1.service
```
Las dos columnas nuevas se quedan en la base sin molestar: el código anterior no las
lee. Segundo nivel, sólo para una catástrofe del push: restaurar el dump previo.

**Verificación posterior.** Contenedor activo, `/api/health` y `/sign-in` en 200, las
dos columnas presentes y nullable, `documents_enabled` con **el mismo reparto que
antes** (`ibiza-online=true`, los otros dos false), y una confirmación humana de que
IBIZA ve su menú agrupado y el resto de la plataforma sigue igual.

**Lo que este despliegue NO va a demostrar, y conviene decirlo antes que después:**
que alguien pegue un enlace de Drive real en producción y lo abra. En producción no
hay ni un proyecto (`manual_assignments` en 0 filas), así que no hay dónde pegarlo
hasta que IBIZA cree su primer manual y lo active para una empresa. Eso quedó
demostrado en local, en pantalla y con tu confirmación (plan 03.1-04). Lo que aquí se
verifica es más modesto y hay que llamarlo por su nombre: **desplegado, sano, el menú
agrupado visible en IBIZA, y el reparto de banderas sin tocar.**

**Y una deuda que se puede saldar de paso:** la confirmación visual humana del
despliegue de la fase 3 (plan 03-08) sigue pendiente desde el 2026-09-02 — se declaró
así en vez de darla por buena. El checkpoint final de este plan la cierra junto con la
de esta fase.
  </context>
  <options>
    <option id="desplegar-ahora">
      <name>Desplegar ahora</name>
      <pros>La fase existe por un hallazgo de producción y sólo sirve allí: el momento más barato para sacar el expediente de PROL es mientras no hay ni una evidencia dentro, y ese momento se encarece cada día. IBIZA, el destinatario real, tiene el módulo encendido y estrenaría su menú. El cambio de esquema es el más pequeño del milestone y el rollback es un comando.</pros>
      <cons>Una ventana de corte por el reinicio. Y IBIZA ve el cambio de navegación sin previo aviso, si nadie se lo cuenta antes.</cons>
    </option>
    <option id="desplegar-fijando-sha">
      <name>Desplegar fijando el SHA del último plan de esta fase</name>
      <pros>Si `04135ca..HEAD` trae commits de DC-3 de la otra sesión, esto despliega **sólo** la fase 3.1 y deja el trabajo ajeno para su propio despliegue, con su propia revisión. Es lo que se hizo en el plan 03-08.</pros>
      <cons>Sólo tiene sentido si de verdad hay commits ajenos por medio; si no, es idéntico a desplegar HEAD con un paso más.</cons>
    </option>
    <option id="diferir">
      <name>Diferir</name>
      <pros>Un solo despliegue para la 3.1 y la 4, con una sola ventana de corte. Y da tiempo a avisar a IBIZA del cambio de menú antes de que lo vea.</pros>
      <cons>El desfase repo ↔ producción vuelve a crecer, justo lo que la fase 2 cerró y la 3 mantuvo. La ventaja de mover el expediente "mientras está vacío" caduca sola: cada día que IBIZA use el módulo encarece la migración que esta fase evita. Y la fase 4 va a volver sobre estas mismas tablas, así que el despliegue acumulado tendría más superficie de fallo y menos capacidad de atribuir un problema a un cambio concreto.</cons>
    </option>
  </options>
  <action>
**Antes de presentar nada al usuario**, y sin tocar el VPS (todo esto es local):

```bash
git log --oneline 04135ca..HEAD
git diff --stat 04135ca..HEAD -- packages/db/prisma/schema.prisma
git status --short
```

Con eso, **decirle al usuario en el propio mensaje del checkpoint**:
- cuántos commits van dentro y cuáles **no** son de esta fase;
- si `schema.prisma` en HEAD trae algo más que las dos columnas de esta fase;
- si el árbol sigue sucio por la otra sesión (que no afecta a `git archive`, pero sí
  indica que hay trabajo vivo en paralelo).

Si aparecen commits ajenos, **decirlo antes que ninguna otra cosa** y recomendar
`desplegar-fijando-sha`. Si no aparece ninguno, decir también eso, y entonces
`desplegar-fijando-sha` y `desplegar-ahora` son la misma cosa: decírselo en vez de
ofrecer una opción vacía.

Presentar el bloque de contexto y las opciones **completos**, y **esperar la
respuesta**. No ejecutar ningún comando contra el VPS —**ni de lectura**— antes de
tenerla.

Según la respuesta:
- `desplegar-ahora` → tarea 2 con `SHA=$(git rev-parse --short HEAD)`.
- `desplegar-fijando-sha` → tarea 2 con el SHA del último commit de esta fase,
  anotándolo explícitamente en el SUMMARY.
- `diferir` → saltar a la tarea 3, hacer **sólo** su parte documental: escribir la
  §7e de `DEPLOY.md` como **no aplicada**, con la fecha, el motivo y la condición para
  retomarlo, y cerrar la fase declarando que sus siete requisitos están verificados en
  local y **no** en producción.
  </action>
  <resume-signal>Responde: desplegar-ahora, desplegar-fijando-sha o diferir</resume-signal>
</task>

<task type="auto">
  <name>Tarea 2: respaldar, aplicar las dos columnas, desplegar y verificar</name>
  <files>(sin cambios en el repositorio; operación sobre el VPS)</files>
  <action>
**Sólo si la tarea 1 resolvió desplegar.**

**1. Puertas locales.**
```bash
pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep "problems"   # "✖ 81 problems (0 errors, 81 warnings)"
pnpm exec turbo run build
SHA=<el que decidió la tarea 1>
git log --oneline -1 $SHA
```
El árbol **no** tiene que estar limpio (la otra sesión tiene trabajo vivo), pero
`git archive` sólo empaqueta lo commiteado: confirmarlo mirando que el tar no trae
los archivos sucios de `prol-1d`.

**2. Estado del host, antes de tocar nada.** Guardar todo para el SUMMARY:
```bash
ssh <host> 'podman images --format "{{.Repository}}:{{.Tag}}" | grep prol-web | head'
ssh <host> 'systemctl is-active prol-web-1.service'
ssh <host> "podman exec prol-db-1 psql -U prol -d prol -Atc \"select slug||'|'||documents_enabled from tenants order by slug;\""
ssh <host> "podman exec prol-db-1 psql -U prol -d prol -Atc \"select count(*) from manual_assignments;\""
ssh <host> "podman exec prol-db-1 psql -U prol -d prol -Atc \"select count(*) from evidences;\""
```
El reparto de `documents_enabled` **de antes** es el que hay que poder comparar
después, no el que dice esta planificación. Y los dos conteos son los que permiten
afirmar —no suponer— que las dos columnas nuevas caen sobre tablas vacías.

**3. Dump. No se sigue sin él.**
```bash
ssh <host> 'podman exec prol-db-1 pg_dump -U prol -d prol > /opt/prol/backup_$(date -u +%Y%m%d_%H%M)_pre_fase31.sql && ls -lh /opt/prol/backup_*_pre_fase31.sql'
```
Comprobar que el archivo tiene tamaño razonable (no cero, no unos pocos bytes).

**4. Construir la imagen** por la ruta canónica:
```bash
git archive --format=tar.gz $SHA | ssh <host> "mkdir -p /opt/prol-deploy-$SHA && tar -xz -C /opt/prol-deploy-$SHA"
ssh <host> "podman build -t localhost/prol-web:$SHA /opt/prol-deploy-$SHA"
```
**Todavía no mover `latest` ni reiniciar.**

**5. Preview del esquema, y leerlo.** Con el `prisma` que trae la imagen nueva, según
la receta de `DEPLOY.md`. Volcar el SQL a un archivo y **leerlo entero**. Debe
contener, y sólo:
```sql
ALTER TABLE "tenants" ADD COLUMN "documents_menu_label" TEXT;
ALTER TABLE "manual_assignments" ADD COLUMN "drive_url" TEXT;
```
Si aparece **cualquier otra cosa** —tablas de DC-3, `DROP`, `SET NOT NULL`, enums,
índices—, **parar y reportar al usuario** antes de aplicar nada: significaría que el
SHA elegido trae más que esta fase, que es justo lo que la tarea 1 intentaba evitar.
Copiar el SQL al SUMMARY: es el registro de qué se le hizo exactamente a la base de
producción.

**6. Aplicar el esquema.**
```bash
# mismo podman run del preview, con:  db push --schema prisma/schema.prisma --skip-generate
```
Si pidiera `--accept-data-loss`: **parar**. Dos `ADD COLUMN` nullable no pueden
pedirlo.

Comprobar inmediatamente:
```bash
ssh <host> "podman exec prol-db-1 psql -U prol -d prol -Atc \"select table_name||'.'||column_name||'|'||data_type||'|nullable='||is_nullable from information_schema.columns where (table_name='tenants' and column_name='documents_menu_label') or (table_name='manual_assignments' and column_name='drive_url');\""
ssh <host> "podman exec prol-db-1 psql -U prol -d prol -Atc \"select count(*) from tenants where documents_menu_label is not null;\""   # 0
```

**7. Mover `latest` y reiniciar. Sólo ahora.**
```bash
ssh <host> "podman tag localhost/prol-web:$SHA localhost/prol-web:latest && systemctl daemon-reload && systemctl restart prol-web-1.service"
```

**8. Verificar el arranque.**
```bash
ssh <host> 'systemctl is-active prol-web-1.service'
ssh <host> 'journalctl -u prol-web-1.service -n 120 --no-pager'
curl -s -o /dev/null -w '%{http_code}\n' https://prol.prosuite.pro/api/health   # 200
curl -s -o /dev/null -w '%{http_code}\n' https://prol.prosuite.pro/sign-in      # 200
```
En el log: ninguna traza de Prisma por columna desconocida y ninguna aparición de
"Configuración de R2 incompleta" (esta fase no toca R2, pero un env corrompido se
vería aquí).

**9. El reparto de banderas, sin tocar.** Es una condición del plan:
```bash
ssh <host> "podman exec prol-db-1 psql -U prol -d prol -Atc \"select slug||'|'||documents_enabled from tenants order by slug;\""
# Esperado, idéntico al paso 2: ibiza-online|t, academia-digital|f, mecanica-g3|f
```

**10. Si algo falla:**
- El contenedor no arranca → mirar el log, retaggear `04135ca` como `latest` y
  reiniciar. Las dos columnas se quedan; el código anterior no las lee.
- El `db push` falla a medias → leer el error, comprobar el estado real de las dos
  columnas y decidir entre completar a mano las dos sentencias (están delante, son dos
  líneas) o restaurar el dump. **No** mover `latest` mientras tanto: producción sigue
  con `04135ca`, que funciona con el esquema anterior.
- Arranca pero algo va mal → retaggear `04135ca`.

En cualquier caso, **no dejar producción a medias sin decirlo**.
  </action>
  <verify>
    <automated>
ssh <host> 'systemctl is-active prol-web-1.service'                              # active
curl -s -o /dev/null -w '%{http_code}\n' https://prol.prosuite.pro/api/health    # 200
curl -s -o /dev/null -w '%{http_code}\n' https://prol.prosuite.pro/sign-in       # 200

# Las dos columnas, nullable y sin default
ssh <host> "podman exec prol-db-1 psql -U prol -d prol -Atc \"select table_name||'.'||column_name||'|nullable='||is_nullable||'|default='||coalesce(column_default,'<none>') from information_schema.columns where (table_name='tenants' and column_name='documents_menu_label') or (table_name='manual_assignments' and column_name='drive_url');\""

# El enum NO ganó valores (la enmienda (a) del CONTEXT también vale en producción)
ssh <host> "podman exec prol-db-1 psql -U prol -d prol -Atc \"select string_agg(enumlabel,',' order by enumsortorder) from pg_enum e join pg_type t on t.oid=e.enumtypid where t.typname='EvidenceRequirementKind';\""
# Esperado: FILE,RISK_MATRIX,EVALUATION_LINK

# Banderas sin tocar
ssh <host> "podman exec prol-db-1 psql -U prol -d prol -Atc \"select slug||'|'||documents_enabled from tenants order by slug;\""
# Esperado: academia-digital|f, ibiza-online|t, mecanica-g3|f

# La ruta de subida de evidencias ya no existe en el servidor desplegado
curl -s -o /dev/null -w '%{http_code}\n' -X POST https://prol.prosuite.pro/api/upload/evidence   # 404
# (Si el plan 03.1-05 la conservó por tener un llamante, aquí se espera 401/403, no 404.)

# La imagen anterior sigue tagueada: sin ella no hay rollback
ssh <host> "podman images --format '{{.Repository}}:{{.Tag}}' | grep prol-web"
# Esperado: el SHA nuevo, 04135ca y 55c020d

# El dump existe
ssh <host> 'ls -lh /opt/prol/backup_*_pre_fase31.sql'

# Ninguna credencial se filtró
git diff | grep -iE '(SECRET|PASSWORD|ACCESS_KEY)=[^"]' && echo "ERROR: credencial en el diff" || echo "OK"
    </automated>
  </verify>
  <done>
Existe un dump `pre_fase31` de tamaño razonable, tomado antes de tocar el esquema. El
SQL del preview se leyó y contenía sólo los dos `ADD COLUMN … TEXT`. El `db push` se
aplicó sin pedir `--accept-data-loss` y **después** de eso se movió `latest` y se
reinició el servicio. El contenedor está activo, `/api/health` y `/sign-in` responden
200, el log no tiene errores de Prisma, las dos columnas existen nullable y sin
default, el enum `EvidenceRequirementKind` sigue con sus tres valores, y
`documents_enabled` tiene exactamente el mismo reparto que antes del despliegue. La
imagen `04135ca` sigue tagueada y el rollback de un comando está disponible.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 3: producción, vista por una persona — y la deuda de la fase 3, saldada</name>
  <what-built>
La fase 3.1 en producción: el menú agrupado con rótulo por tenant, el enlace de Drive
por proyecto y el requisito que se cumple sin archivo. Dos columnas nuevas en la base,
el reparto de `documents_enabled` sin tocar, y el rollback de un comando disponible.

Ya verificado por máquina, sin intervención humana: servicio activo, `/api/health` y
`/sign-in` en 200, las dos columnas presentes y nullable, el enum sin valores nuevos,
las banderas idénticas a antes, y la imagen anterior todavía tagueada.

Falta lo único que una máquina no puede firmar: **que la plataforma se vea bien**. Y
con ello se salda, de paso, una deuda que lleva abierta desde el 2026-09-02: la
confirmación visual humana del despliegue de la fase 3 (plan 03-08), que se declaró
pendiente en vez de darse por buena.
  </what-built>
  <how-to-verify>
En `https://prol.prosuite.pro`, con tu cuenta real.

**A. Lo de siempre sigue igual** (esto es la deuda de la fase 3).
1. Entra al panel. Debe verse normal: cursos, alumnos, lo de cada día.
2. Descarga algo que ya funcionara antes del despliegue de la fase 3: un certificado
   o un PDF de resultados de evaluación. Debe descargarse.

**B. IBIZA estrena su menú** (es el único tenant con el módulo encendido).
3. Entra como administrador de IBIZA. En el sidebar, donde antes había cuatro
   entradas sueltas (Manuales, Proyectos, Evidencias, Agenda), ahora debe haber **un
   solo desplegable**.
4. Ábrelo: dentro deben estar **Manuales Maestros, Proyectos, Evidencias y Agenda**.
5. El rótulo del desplegable dirá **«Gestión documental»**, porque nadie le ha puesto
   uno propio todavía. Si quieres, ve a Configuración → «Menú de gestión documental»
   y escribe **«Ibiza Experts 360»**: el sidebar debe cambiar al recargar. Eso es
   opcional y lo decides tú — es tu marca en tu producción.

**C. Los demás tenants siguen sin ver nada.**
6. Si tienes acceso a Academia Digital MX o Mecanica G3, comprueba que su sidebar no
   tiene ningún menú de gestión documental. Deben seguir exactamente igual que ayer.

Si algo se ve mal, dilo con el panel y el paso: el rollback es un comando y está
listo.
  </how-to-verify>
  <action>
**1. Antes de pedir nada**, capturar el estado de producción para poder contrastar
después la respuesta del usuario: el reparto de `documents_enabled`, el valor de
`documents_menu_label` de `ibiza-online` (debe ser `NULL`), y los códigos de respuesta
de `/api/health` y `/sign-in`.

Presentar el recorrido y **esperar la respuesta del usuario**. No navegar por él, y no
entrar a su producción con su sesión.

**2. Cómo se registra el resultado.** Si el usuario aprueba **sin contestar las tres
preguntas** del `resume-signal`, **no** se registra como verificado: se anota
literalmente "aprobado sin ejercitar" en el SUMMARY, y el estado de producción queda
descrito como "automatizable confirmado, confirmación humana pendiente" — igual que
hizo el plan 03-08, que fue honesto al respecto y por eso esta deuda es rastreable
hoy. **Nunca se redacta una confirmación que no ocurrió.**

Si contesta, releer `documents_menu_label` de `ibiza-online`: si dice haber puesto
«Ibiza Experts 360», la columna tiene que decir lo mismo. Ése es el contraste que
convierte su respuesta en evidencia.

**3. `DEPLOY.md` §7e — «Ibiza Experts 360 y gestión documental en Drive (fase 3.1)»**,
nueva sección después de la §7d, contando lo que pasó de verdad:
- SHA desplegado y desde qué imagen se venía; los tags disponibles para rollback y el
  comando literal.
- Las dos columnas aplicadas, con el SQL exacto del preview.
- El reparto de `documents_enabled` antes y después (idéntico), y la frase explícita
  de que **este despliegue no encendió ni apagó el módulo en ningún tenant**.
- Que IBIZA, con el módulo encendido desde antes, es el único tenant que ve el cambio
  de navegación, y que su rótulo se configura desde `/tenant-admin/settings` sin tocar
  código.
- Qué pasó con `/api/upload/evidence` (retirada, o conservada con su llamante).
- La ruta del dump `pre_fase31`.
- El resultado del checkpoint, tal como haya sido.

Si la tarea 1 resolvió `diferir`, escribir la §7e igualmente, **como no aplicada**,
con la fecha, el motivo y la condición para retomarlo, y saltarse todo lo demás de
esta tarea.

**4. Cerrar la fase.** Marcar en `REQUIREMENTS.md` sólo los requisitos cuya evidencia
exista de verdad, y con el tipo de evidencia declarado por separado (aprobación humana
vs. servidor-verificado), como hizo la fase 3. Si el checkpoint de este plan o el del
plan 03.1-02 no se ejerció, los requisitos de navegación **no** se marcan, y se dice
por qué.

**5. Commit**, stageando por ruta explícita —**nunca `git add -A`**, que la otra
sesión sigue con trabajo vivo en este árbol:
```bash
git add DEPLOY.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md \
        .planning/phases/03.1-ibiza-experts-360-y-drive/03.1-06-SUMMARY.md
git commit -m "docs(03.1): despliegue a producción y cierre de la fase"
```
  </action>
  <resume-signal>
Responde "aprobado" **y estas tres respuestas**, que es lo que hace la aprobación
comprobable en vez de creída:

1. ¿Descargaste algo (certificado o PDF de resultados) y funcionó? (sí / no)
2. En el panel de IBIZA, ¿cuántas entradas de gestión documental ves en el sidebar:
   un desplegable, o cuatro sueltas?
3. ¿Qué rótulo exacto lleva ese desplegable?

Si algo se ve mal, dilo con el panel y el paso donde ocurre.
  </resume-signal>
</task>

</tasks>

<verification>
```bash
ssh <host> 'systemctl is-active prol-web-1.service'                              # active
curl -s -o /dev/null -w '%{http_code}\n' https://prol.prosuite.pro/api/health    # 200
```

Comprobaciones específicas del plan:
- Las dos columnas existen en producción, nullable y sin `DEFAULT`.
- `EvidenceRequirementKind` sigue con exactamente tres valores en producción.
- `documents_enabled` tiene el mismo reparto antes y después
  (`ibiza-online=true`, los otros dos `false`).
- `podman images | grep prol-web` incluye `04135ca`: el rollback existe.
- Existe `/opt/prol/backup_*_pre_fase31.sql` con tamaño razonable.
- `git show --stat HEAD` no incluye ningún archivo de `apps/` ni de `packages/`: este
  plan sólo escribe documentación.
- `git status --short` sigue mostrando el trabajo de `prol-1d` sin commitear.
- Ninguna credencial en el diff ni en el SUMMARY.
</verification>

<success_criteria>
1. Nada tocó el VPS antes de una aprobación explícita del usuario, con alcance,
   riesgo, rollback y verificación delante — y sabiendo qué commits iban dentro.
2. Existe un dump previo y el preview del esquema se leyó antes de aplicarlo; contenía
   sólo los dos `ADD COLUMN` nullable.
3. El esquema se aplicó antes de mover `latest`, y el servicio arrancó sano.
4. `documents_enabled` quedó exactamente como estaba en los tres tenants.
5. El rollback de un comando está disponible, con `04135ca` todavía tagueada.
6. Un humano confirmó que la plataforma se ve bien y que IBIZA tiene su menú agrupado
   —o quedó registrado, con esas palabras, que no lo hizo.
7. `DEPLOY.md` §7e cuenta lo que pasó, incluido lo que no se pudo demostrar.
8. `REQUIREMENTS.md` marca sólo lo que tiene evidencia, con el tipo de evidencia
   declarado.
</success_criteria>

<output>
Al terminar, crear
`.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-06-SUMMARY.md`.

Registrar en el SUMMARY:
- **La decisión del usuario, literal**, y el SHA que se desplegó (y si se fijó para
  excluir trabajo de la otra sesión, cuál era ese trabajo).
- **El SQL exacto del preview** aplicado a la base de producción.
- **El reparto de `documents_enabled` antes y después**, leído de la base las dos
  veces. No copiarlo de este plan: releerlo.
- **Las tres respuestas del usuario al checkpoint, literales**, junto al valor de
  `documents_menu_label` releído después. Si no contestó, decirlo con esas palabras y
  dejar el estado de producción descrito como "confirmación humana pendiente".
- **Si la deuda del plan 03-08 quedó saldada o no**: era el motivo de incluir el paso
  A del recorrido.
- El comando de rollback, escrito entero, y las imágenes que quedaron tagueadas.
- Lo que este despliegue **no** demostró: nadie pegó un enlace de Drive real en
  producción, porque no hay ni un proyecto donde pegarlo. Decirlo con esas palabras.
</output>
