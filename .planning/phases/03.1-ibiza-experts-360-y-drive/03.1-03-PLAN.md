---
phase: 03.1-ibiza-experts-360-y-drive
plan: 03
type: execute
wave: 3
depends_on: ["03.1-01"]
files_modified:
  - apps/web/lib/actions/manual.ts
  - apps/web/lib/queries/manual.ts
  - apps/web/components/project-drive-link.tsx
  - apps/web/components/company-project-panel.tsx
autonomous: true
requirements: [DRV-01, DRV-03]
parallel_safe: false  # Ola 3 con un solo plan. `branching_strategy` es "none" y otra
                      # sesión interactiva (`prol-1d`) tiene ~18 archivos sin commitear
                      # en este mismo árbol; en la ola 1 de la fase 1 dos ejecutores
                      # concurrentes se absorbieron archivos entre el `add` y el `commit`.

must_haves:
  truths:
    - "Un administrador pega el enlace de Drive de un proyecto y la columna `manual_assignments.drive_url` lo guarda."
    - "Un consultor (PROFESSOR) ve el panel del proyecto pero no puede escribir el enlace: la acción lo rechaza y la columna no se mueve."
    - "Un administrador de otro tenant y una petición sin sesión también son rechazados."
    - "Una URL que no sea `https:` sobre un host de Drive se rechaza con un mensaje que dice qué se esperaba, y la columna no cambia."
    - "Una fila con un enlace no confiable escrito directamente en la base no se pinta como `<a href>`: la pantalla lo dice y no lo enlaza."
    - "Un proyecto sin enlace lo dice, en vez de fallar o de dejar un hueco."
  artifacts:
    - path: "apps/web/lib/actions/manual.ts"
      provides: "setProjectDriveUrl con guarda de administrador más estrecha que la del panel"
      contains: "setProjectDriveUrl"
    - path: "apps/web/lib/queries/manual.ts"
      provides: "getAssignmentPanel devuelve driveUrl revalidado, canEditDriveUrl y driveUrlIsInvalid"
      contains: "safeDriveUrl"
    - path: "apps/web/components/project-drive-link.tsx"
      provides: "Bloque de la carpeta de Drive del proyecto: enlace, mensaje de ausencia y edición sólo para administrador"
      contains: "rel=\"noopener noreferrer\""
      min_lines: 60
  key_links:
    - from: "apps/web/lib/actions/manual.ts"
      to: "apps/web/lib/drive-url.ts"
      via: "isValidDriveUrl en escritura, antes del update"
      pattern: "isValidDriveUrl"
    - from: "apps/web/lib/queries/manual.ts"
      to: "apps/web/lib/drive-url.ts"
      via: "safeDriveUrl en lectura, antes de exponer el enlace a la vista"
      pattern: "safeDriveUrl"
    - from: "apps/web/components/company-project-panel.tsx"
      to: "apps/web/components/project-drive-link.tsx"
      via: "sección propia del panel, con canEdit resuelto en el servidor"
      pattern: "ProjectDriveLink"
---

<objective>
El proyecto —`ManualAssignment`, lo que la interfaz llama Proyecto— guarda el enlace
a su carpeta de Google Drive. Sólo el administrador puede escribirlo, sólo se aceptan
URLs de Drive, y el enlace se vuelve a validar cada vez que se lee.

Purpose: es el criterio 4 de la fase y la mitad de escritura del criterio 6. También
es la pieza de la que cuelga el criterio 5 (plan 03.1-04): sin enlace guardado no hay
nada a lo que llevar al usuario. Y es la primera URL externa pegada por un usuario que
este repo valida de verdad — `AdvisorySession.meetingUrl` no valida nada, y este plan
existe en parte para no heredar esa omisión.
Output: una server action estrecha, la consulta del panel devolviendo el enlace ya
revalidado, y el bloque de interfaz donde se pega.
</objective>

<execution_context>
@/Users/flx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-CONTEXT.md
@.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-RESEARCH.md
@.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

@apps/web/lib/actions/manual.ts
@apps/web/lib/queries/manual.ts
@apps/web/lib/manual-access.ts
@apps/web/lib/actions/evidence.ts
@apps/web/components/company-project-panel.tsx
@apps/web/components/activity-due-date.tsx
@apps/web/lib/drive-url.ts

<interfaces>
<!-- Lectura directa del código, 2026-09-02. -->

**Del plan 03.1-01** (firmas exactas en su SUMMARY):
```typescript
// apps/web/lib/drive-url.ts
export const DRIVE_URL_ERROR: string;
export function isValidDriveUrl(value: string | null | undefined): boolean;
export function safeDriveUrl(value: string | null | undefined): string | null;
```
La columna `ManualAssignment.driveUrl` (`@map("drive_url")`) existe y está en `NULL`
en las tres activaciones locales.

**Las tres guardas que hay, y por qué ninguna sirve tal cual**
(`apps/web/lib/manual-access.ts`):

| Guarda | Quién pasa | Sirve para |
|---|---|---|
| `requireManualAdmin()` (línea 45) | ADMIN, SUPER_ADMIN | **Esta acción.** No resuelve el alcance de tenant: hay que comprobarlo a mano. |
| `requireManualReviewer()` (línea 58) | ADMIN, SUPER_ADMIN, **PROFESSOR** | Revisión de evidencias |
| `requireAssignmentManageAccess(id)` (línea 196) | lo mismo que reviewer, + carga la fila y el tenant | El panel entero del proyecto |
| `isManualAdmin(user)` (línea 70) | predicado sin efectos | Decidir si se pinta el formulario |

`requireAssignmentManageAccess` es la puerta del **panel**, y deja pasar a PROFESSOR
a propósito (el consultor necesita ver a su cliente). DRV-01 dice "editable por el
administrador", así que reutilizarla para escribir el enlace **violaría el requisito**.

**El patrón exacto a imitar** — `resolveEvidenceDeletion`
(`apps/web/lib/actions/evidence.ts:451-476`), que tiene el mismo problema y ya lo
resolvió así:
```typescript
export async function resolveEvidenceDeletion(input: {...}): Promise<EvidenceActionResult> {
  const user = await requireManualAdmin();          // más estrecho que el guard del panel
  const evidence = await db.evidence.findUnique({ where: { id: input.evidenceId }, select: { /*…*/ assignment: { select: { tenantId: true } } } });
  if (!evidence) return { success: false, error: "Evidencia no encontrada" };
  if (user.role !== "SUPER_ADMIN" && user.tenantId !== evidence.assignment.tenantId) {
    return { success: false, error: "No autorizado: tenant no coincide" };
  }
  // …
}
```

**`getAssignmentPanel`** (`lib/queries/manual.ts:241-334`) hoy hace
`const { assignment } = await requireAssignmentManageAccess(assignmentId);` —
**descarta el `user`**, que ahora hace falta para `isManualAdmin`. Su `select` de
`db.manualAssignment.findUnique` pide `id, status, notes, activatedAt, company,
consultant, manual`; le falta `driveUrl`. Devuelve
`{ assignment: row, activities, companyDocuments, progress }`.

**`CompanyProjectPanel`** (`components/company-project-panel.tsx`, 259 líneas) tipa su
prop así, de modo que **todo lo que se añada al retorno de la query llega solo**:
```typescript
data: NonNullable<Awaited<ReturnType<typeof import("@/lib/queries/manual").getAssignmentPanel>>>
```
Lo renderizan `app/tenant-admin/projects/[assignmentId]/page.tsx` y
`app/professor/projects/[assignmentId]/page.tsx`, con `backHref`,
`evidenceBasePath` y `canEditDueDates` distintos. **Ninguna de las dos páginas se
toca en este plan**: el permiso viaja dentro de `data`, resuelto en el servidor.

**Patrón de edición en línea a imitar** — `components/activity-due-date.tsx`
(componente cliente, `useTransition`, `router.refresh()` al terminar, error en
`<span className="text-xs text-rose-600">`).

**Antipatrón del repo, a NO copiar** (`app/dashboard/advisory/page.tsx:131-135`):
```tsx
{s.meetingUrl && (
  <a href={s.meetingUrl} target="_blank" rel="noopener noreferrer">
```
La forma del `<a>` **sí** se copia (es el único precedente de enlace externo y trae
`rel="noopener noreferrer"` bien puesto). Lo que no se copia es que ese `href` sale
directo de la base sin validar nada.

**Convenciones:** retorno discriminable `{ success: true as const, … }` /
`{ success: false as const, error }`; textos en español minúscula sin punto final;
`revalidatePath` de las rutas afectadas; nada de correo ni despacho en `"use server"`.
</interfaces>
</context>

<constraints>
- **La guarda de escritura es más estrecha que la del panel.** `requireManualAdmin()`
  + comprobación de tenant a mano. Nada de `requireAssignmentManageAccess` para
  escribir: dejaría escribir a PROFESSOR y DRV-01 dice administrador.
- **Se valida en escritura Y en lectura.** No basta con la acción: una fila puede
  haberse editado a mano en la base o venir de antes. `safeDriveUrl` se llama en la
  consulta, antes de que el enlace llegue a la vista.
- **La lista de hosts no se duplica.** Se importa de `lib/drive-url.ts`. Si aparece un
  `"drive.google.com"` en cualquier otro archivo que no sea texto de usuario, está mal.
- **NO se cablean `consultantId`, `status` ni `notes`** de `updateManualAssignment`
  (enmienda (e) del CONTEXT). Están ahí, tientan, y no son de esta fase.
  `updateManualAssignment` no se toca ni se borra.
- **Nada de API de Drive**: no se comprueba que la carpeta exista, ni que sea
  accesible, ni se listan archivos. El alcance es pegar un enlace.
- **Un enlace por proyecto.** Nada de enlace por sección ni por requisito (diferido y
  nombrado en el CONTEXT para que nadie lo improvise).
- **Nada de rutas de redirect nuevas.** Validar en cada lectura es una garantía más
  fuerte que revalidar sólo en el clic, y no cuesta más.
- **Nunca `pnpm db:seed`.** Y las tres `manual_assignments` locales, las dos
  `companies` y las dos `evidences` con `formSnapshot` no se borran.
- Stagear sólo los cuatro archivos de este plan por ruta explícita. **Nunca
  `git add -A`.**
- La puerta de lint sale con **exit 1** estando sana:
  `✖ 81 problems (0 errors, 81 warnings)`.
</constraints>

<tasks>

<task type="auto">
  <name>Tarea 1: la acción estrecha y la lectura revalidada</name>
  <files>apps/web/lib/actions/manual.ts, apps/web/lib/queries/manual.ts</files>
  <action>
**1. `setProjectDriveUrl`** en `lib/actions/manual.ts`, junto a `updateManualAssignment`
(que **no se toca**):

```typescript
/**
 * Enlace a la carpeta de Google Drive de un proyecto.
 *
 * La guarda es `requireManualAdmin()` y no `requireAssignmentManageAccess()` a
 * propósito: esa segunda comparte puerta con `requireManualReviewer()` y deja pasar
 * a PROFESSOR, que sí debe VER el panel del proyecto pero no escribir este campo
 * (DRV-01: "editable por el administrador"). Mismo patrón que
 * `resolveEvidenceDeletion` en `evidence.ts`, que tuvo el mismo problema.
 *
 * La URL se valida contra la lista cerrada de hosts de `lib/drive-url.ts`. No es
 * cosmética: es un enlace que un administrador pega y un cliente sigue después, o
 * sea un redirector abierto si no se valida. Se valida también al leer
 * (`getAssignmentPanel`), porque una fila puede no haber pasado por aquí.
 */
export async function setProjectDriveUrl(input: {
  assignmentId: string;
  driveUrl: string | null;
}): Promise<
  | { success: true; driveUrl: string | null }
  | { success: false; error: string }
> {
  const user = await requireManualAdmin();

  const assignment = await db.manualAssignment.findUnique({
    where: { id: input.assignmentId },
    select: { id: true, tenantId: true },
  });
  if (!assignment) return { success: false, error: "Proyecto no encontrado" };
  if (user.role !== "SUPER_ADMIN" && user.tenantId !== assignment.tenantId) {
    return { success: false, error: "No autorizado: tenant no coincide" };
  }

  const raw = (input.driveUrl ?? "").trim();
  // Vaciar el campo es una operación legítima (la carpeta se movió, el proyecto se
  // reorganizó): se guarda `null`, no la cadena vacía, para que la columna tenga
  // dos estados y no tres.
  const next = raw === "" ? null : raw;
  if (next !== null && !isValidDriveUrl(next)) {
    return { success: false, error: DRIVE_URL_ERROR };
  }

  await db.manualAssignment.update({
    where: { id: assignment.id },
    data: { driveUrl: next },
  });

  revalidatePath(`/tenant-admin/projects/${assignment.id}`);
  revalidatePath(`/professor/projects/${assignment.id}`);
  revalidatePath(`/dashboard/manuals/${assignment.id}`);
  return { success: true, driveUrl: next };
}
```

Importar `isValidDriveUrl` y `DRIVE_URL_ERROR` de `@/lib/drive-url`. **Ningún
`revalidatePath` de `/admin`**: el panel de super-admin no tiene nada del módulo.

**2. `getAssignmentPanel`** en `lib/queries/manual.ts` — tres cambios pequeños:
- Quedarse con el `user`: `const { user, assignment } = await requireAssignmentManageAccess(assignmentId);`
- Añadir `driveUrl: true` al `select` de `db.manualAssignment.findUnique`.
- En el retorno:
  ```typescript
  return row
    ? {
        assignment: {
          ...row,
          // Revalidado en CADA lectura, no sólo al escribir: esta fila puede haber
          // sido editada a mano en la base, o ser anterior a la validación de
          // escritura. Si no pasa, la vista dirá "sin enlace" en vez de pintar un
          // `<a href>` con una URL que nadie ha comprobado.
          driveUrl: safeDriveUrl(row.driveUrl),
        },
        // Distingue "nunca se puso" de "se puso algo que ya no es confiable": la
        // pantalla dice cosas distintas, y confundirlas escondería una fila mala.
        driveUrlIsInvalid: row.driveUrl !== null && safeDriveUrl(row.driveUrl) === null,
        canEditDriveUrl: isManualAdmin(user),
        activities: /* … igual que hoy … */,
        companyDocuments: /* … igual que hoy … */,
        progress,
      }
    : null;
  ```
- Importar `safeDriveUrl` de `@/lib/drive-url` e `isManualAdmin` de
  `@/lib/manual-access` (este último ya se importa en el archivo si hace falta;
  confirmarlo antes de añadir el import).

El permiso se resuelve **en el servidor y viaja dentro de `data`**: así las dos
páginas de proyecto (`tenant-admin` y `professor`) no se tocan y no pueden divergir
en quién puede editar.

**Nada más de la consulta cambia.** No se toca `getManualOverview` ni
`getSectionForCompany`: son del plan 03.1-04.
  </action>
  <verify>
    <automated>
# La acción existe con la guarda estrecha, no con la del panel
grep -n "export async function setProjectDriveUrl" apps/web/lib/actions/manual.ts
sed -n "/export async function setProjectDriveUrl/,/^}/p" apps/web/lib/actions/manual.ts | grep -n "requireManualAdmin"
sed -n "/export async function setProjectDriveUrl/,/^}/p" apps/web/lib/actions/manual.ts | grep -n "requireAssignmentManageAccess" && echo "ERROR: guarda demasiado ancha, deja escribir a PROFESSOR" || echo "OK: guarda estrecha"
sed -n "/export async function setProjectDriveUrl/,/^}/p" apps/web/lib/actions/manual.ts | grep -n "tenantId !== assignment.tenantId\|user.tenantId !== assignment.tenantId"

# Valida en escritura, con el helper compartido
sed -n "/export async function setProjectDriveUrl/,/^}/p" apps/web/lib/actions/manual.ts | grep -n "isValidDriveUrl"
grep -n "drive.google.com" apps/web/lib/actions/manual.ts apps/web/lib/queries/manual.ts && echo "ERROR: lista de hosts duplicada" || echo "OK: la lista vive sólo en drive-url.ts"

# La enmienda (e): updateManualAssignment sigue sin cablearse
git diff -- apps/web/lib/actions/manual.ts | grep -n "^[-+].*updateManualAssignment" && echo "REVISAR: se tocó updateManualAssignment" || echo "OK: intacta"

# Valida en lectura
grep -n "safeDriveUrl" apps/web/lib/queries/manual.ts
grep -n "driveUrlIsInvalid\|canEditDriveUrl" apps/web/lib/queries/manual.ts
sed -n "/export const getAssignmentPanel/,/^});/p" apps/web/lib/queries/manual.ts | grep -n "isManualAdmin"

# El plan 03.1-04 todavía no empezó: estas dos consultas no se tocan aquí
git diff -- apps/web/lib/queries/manual.ts | grep -n "getManualOverview\|getSectionForCompany" && echo "REVISAR: fuera de alcance" || echo "OK"

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep "problems"
    </automated>
  </verify>
  <done>
`setProjectDriveUrl` existe con `requireManualAdmin()` más la comprobación de tenant a
mano —el patrón de `resolveEvidenceDeletion`—, valida con `isValidDriveUrl` antes de
escribir, guarda `null` cuando el campo se vacía y revalida las tres rutas que pintan
el proyecto. `getAssignmentPanel` trae `driveUrl` ya pasado por `safeDriveUrl`, más
`driveUrlIsInvalid` y `canEditDriveUrl`. La lista de hosts no aparece fuera de
`lib/drive-url.ts`. `updateManualAssignment` sigue intacta y sin cablear.
`check-types` limpio y `lint` en 81 advertencias.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: el bloque de la carpeta de Drive en el panel del proyecto</name>
  <files>apps/web/components/project-drive-link.tsx, apps/web/components/company-project-panel.tsx</files>
  <action>
**1. `components/project-drive-link.tsx`**, componente cliente nuevo, con la forma de
`activity-due-date.tsx` (`useTransition`, `router.refresh()`, error en rojo):

```typescript
export function ProjectDriveLink(props: {
  assignmentId: string;
  driveUrl: string | null;   // ya revalidado por el servidor
  invalid: boolean;          // había algo guardado y no es confiable
  canEdit: boolean;
}): JSX.Element
```

Tres estados de lectura, y sólo uno de ellos pinta un `<a>`:

- **Con enlace confiable** → el enlace, con la forma del único precedente del repo:
  ```tsx
  <a href={driveUrl} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 rounded-lg bg-primary-600 px-4 py-2 text-sm font-medium text-white transition-colors hover:bg-primary-700">
    <FolderOpen className="h-4 w-4" />
    Abrir la carpeta de Drive
  </a>
  ```
- **Sin enlace** (`driveUrl === null && !invalid`) → texto, no un hueco:
  > Este proyecto todavía no tiene carpeta de Drive.

  Y, si `canEdit`, una línea más: "Pega el enlace de la carpeta compartida con el
  cliente." Si no puede editar: "Pídele a un administrador que la configure."
- **Con enlace no confiable** (`invalid`) → aviso ámbar, y **ningún `<a href>`**:
  > El enlace guardado no es de Google Drive, así que no se abre desde aquí.

  Es la mitad de lectura de DRV-03 y la razón por la que la consulta distingue este
  caso del anterior.

Si `canEdit`, debajo: un `<input type="url">` con el valor actual —**el crudo, no el
saneado**: si la fila tiene basura, el administrador tiene que poder verla para
corregirla, así que el componente recibe además `rawDriveUrl` o, más simple, se pinta
vacío y con `placeholder="https://drive.google.com/drive/folders/…"`— , un botón
Guardar y, cuando ya hay algo guardado, un botón "Quitar el enlace" que llama a la
acción con `null`. `await setProjectDriveUrl({...})`; si `!result.success`, pintar
`result.error` (que para una URL mala es literalmente `DRIVE_URL_ERROR`, así que el
usuario lee qué se esperaba, no "error"). Si `result.success`, `router.refresh()`.

Sin `<img>`: sólo iconos de `lucide-react`, que son SVG y no cuentan para
`@next/next/no-img-element`.

**2. `components/company-project-panel.tsx`** — una sección nueva, **entre el bloque
de "Avance de implantación" y el de "Documentos de la empresa"**, con la misma forma
de tarjeta (`rounded-xl border border-border bg-surface p-5`) y encabezado con icono
que ya usan las otras:

```tsx
<section className="rounded-xl border border-border bg-surface p-5">
  <h2 className="flex items-center gap-2 font-heading text-base font-semibold text-text-primary">
    <FolderOpen className="h-4 w-4 text-text-tertiary" />
    Carpeta de Drive del proyecto
  </h2>
  <p className="mt-1 text-sm text-text-secondary">
    El expediente de archivos de este proyecto vive en Google Drive. PROL lleva el
    control de qué requisito toca, cuándo vence y quién lo aprobó.
  </p>
  <div className="mt-3">
    <ProjectDriveLink
      assignmentId={assignment.id}
      driveUrl={assignment.driveUrl}
      invalid={data.driveUrlIsInvalid}
      canEdit={data.canEditDriveUrl}
    />
  </div>
</section>
```

Va **arriba, no al final**: es el punto de entrada al expediente y el consultor lo va
a usar más que la lista de plantillas.

Importar `FolderOpen` de `lucide-react` (el archivo ya importa `Download` y
`FileText` de ahí).

**Nada más del panel cambia**: ni las plantillas, ni las actividades, ni
`ActivityDueDate`, ni `CompanyDocumentUpload`. Y **las dos páginas de proyecto no se
tocan**: `canEditDriveUrl` viaja dentro de `data`.
  </action>
  <verify>
    <automated>
# El componente existe y enlaza como el único precedente del repo
grep -n "target=\"_blank\"" apps/web/components/project-drive-link.tsx
grep -n "rel=\"noopener noreferrer\"" apps/web/components/project-drive-link.tsx

# El <a> sólo se pinta cuando hay enlace confiable: nunca con `invalid`
sed -n "/invalid/,/}/p" apps/web/components/project-drive-link.tsx | grep -n "<a href" && echo "REVISAR: enlaza una URL no confiable" || echo "OK"

# El formulario sólo existe para quien puede editar
grep -n "canEdit" apps/web/components/project-drive-link.tsx

# El panel lo monta y no toca nada más
grep -n "ProjectDriveLink\|driveUrlIsInvalid\|canEditDriveUrl" apps/web/components/company-project-panel.tsx
git diff --stat -- "apps/web/app/tenant-admin/projects/[assignmentId]/page.tsx" "apps/web/app/professor/projects/[assignmentId]/page.tsx"
# Esperado: sin cambios en ninguna de las dos

# Ningún <img> nuevo
grep -n "<img" apps/web/components/project-drive-link.tsx && echo "REVISAR: necesita eslint-disable con motivo" || echo "OK"

# La lista de hosts sigue sin duplicarse
grep -n "drive.google.com" apps/web/components/project-drive-link.tsx
# Sólo aceptable como `placeholder` de ayuda al usuario, nunca como comparación

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep "problems"
pnpm exec turbo run build
    </automated>
  </verify>
  <done>
`ProjectDriveLink` pinta tres estados distintos —enlace confiable, sin enlace, enlace
no confiable— y sólo el primero produce un `<a href>`, con `target="_blank"
rel="noopener noreferrer"`. El formulario de edición aparece únicamente cuando
`canEdit`, y el error que muestra es el mensaje de `DRIVE_URL_ERROR`, que dice qué se
esperaba. `CompanyProjectPanel` monta la sección entre el avance y las plantillas, sin
tocar nada más, y las dos páginas de proyecto siguen sin cambios. `check-types`
limpio, `lint` en 81 advertencias, `build` verde.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: quién puede escribir el enlace, demostrado con cuatro sesiones reales</name>
  <files>(sin cambios de código; script y filas desechables que NO se commitean)</files>
  <action>
DRV-01 dice "editable por el administrador". Eso no se firma leyendo el guard: se
firma invocando la acción real desde cada rol y mirando la columna después.

**Receta** (la de la fase 3, `03-06-SUMMARY.md` §"Cierre de la brecha", que funcionó):
login por `POST /api/auth/sign-in/email` → cookie `better-auth.session_token`;
`POST http://localhost:3000/tenant-admin/projects/<assignmentId>` con
`Next-Action: <id>`, `Content-Type: text/plain;charset=UTF-8`,
`Origin: http://localhost:3000` y cuerpo = array JSON de argumentos. El id de acción
se lee del **manifest por ruta**
(`.next/dev/server/app/tenant-admin/projects/[assignmentId]/page/server-reference-manifest.json`);
el global de `.next/server/` da 404 con Turbopack.

**Cuentas locales** (contraseña `password123`, confirmadas en la base):

| Rol | Cuenta | Qué debe pasar |
|---|---|---|
| ADMIN del tenant | `admin@prol.prosuite.pro` | **escribe** |
| PROFESSOR del mismo tenant | `maria.garcia@academiadigitalmx.com` | **rechazado** (ve el panel, no escribe) |
| Cliente (STUDENT) | `carlos.mendoza@gmail.com` | **rechazado** |
| Sin sesión | — | **rechazado** |

**Proyecto de pruebas:** la activación de Acme Corp del "Manual de Gestión de Calidad
ISO 9001" (`select id from manual_assignments a join companies c on c.id=a.company_id
join manuals m on m.id=a.manual_id where c.slug='acme-corp' and m.title like 'Manual de
Gestión%'`). Empieza y termina con `drive_url` en `NULL`.

**La quinta sesión, el otro tenant.** La base local sólo tiene un tenant
(`academia-digital`), así que el caso "administrador de otra empresa" no existe hoy y
no se puede fingir. Crear con un script desechable un tenant de usar y tirar
(`slug='tmp-drv-check'`, `documentsEnabled: true` — hace falta que esté encendido para
que el rechazo sea inequívocamente el de tenant y no el del flag) y un ADMIN suyo, con
`hashPassword` de `packages/db/prisma/seed-password.ts` para que entre con
`password123`. Invocar la acción sobre el proyecto de Acme y esperar
`"No autorizado: tenant no coincide"`. **Borrar después el tenant por su id** (cascada
sólo sobre sus propias filas: no tiene empresas ni manuales) y comprobar que
`select count(*) from tenants` vuelve a 1. Si el borrado fallara, **decirlo en el
SUMMARY** en vez de dejarlo escondido.

**Secuencia completa, con lectura de la columna después de cada paso:**

| # | Quién | Qué envía | Esperado en la respuesta | Esperado en `drive_url` |
|---|---|---|---|---|
| 1 | ADMIN | `https://drive.google.com/drive/folders/1AbCdEf` | `{"success":true,…}` | esa URL |
| 2 | ADMIN | `https://evil.com/carpeta` | `success:false` con el texto de `DRIVE_URL_ERROR` | **sin cambiar** (paso 1) |
| 3 | ADMIN | `http://drive.google.com/drive/folders/1AbCdEf` | `success:false` (sin TLS) | **sin cambiar** |
| 4 | ADMIN | `https://drive.google.com.evil.com/x` | `success:false` | **sin cambiar** |
| 5 | PROFESSOR | una URL válida | error de autorización, no `success:true` | **sin cambiar** |
| 6 | STUDENT | una URL válida | error de autorización | **sin cambiar** |
| 7 | sin cookie | una URL válida | error de autenticación | **sin cambiar** |
| 8 | ADMIN de otro tenant | una URL válida | `"No autorizado: tenant no coincide"` | **sin cambiar** |
| 9 | ADMIN | `""` | `{"success":true,"driveUrl":null}` | `NULL` |

**Validación en lectura (la otra mitad de DRV-03), que no pasa por la acción.**
Con la columna en `NULL`, escribir directamente en la base una URL hostil —saltándose
la validación de escritura, que es exactamente el escenario que la revalidación de
lectura existe para cubrir:
```sql
update manual_assignments set drive_url='https://evil.com/carpeta' where id='<assignmentId>';
```
Después, `GET /tenant-admin/projects/<assignmentId>` con la cookie del ADMIN, y exigir
del HTML servido:
- **no** contiene `href="https://evil.com/carpeta"`,
- **sí** contiene el aviso de enlace no confiable.

Repetir el mismo `GET` como PROFESSOR: mismo resultado (la revalidación es de la
consulta, no de la pantalla). Devolver la columna a `NULL` al terminar.

**Limpieza y estado final para el plan 03.1-04:** `drive_url` de las tres
activaciones en `NULL`, un solo tenant, dos companies, dos evidences con
`form_snapshot`, tres filas de `company_documents`. El script desechable vive fuera
del repo (`/tmp`) y no aparece en `git status`.
  </action>
  <verify>
    <automated>
# La traza de los nueve pasos la imprime el script/la sesión; estas son las lecturas de control

# Estado final de la columna
docker exec prol-db psql -U prol -d prol -Atc "select coalesce(drive_url,'<NULL>') from manual_assignments;"
# Esperado: tres líneas <NULL>

# El tenant desechable se fue
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from tenants;"                 # 1
docker exec prol-db psql -U prol -d prol -Atc "select slug from tenants;"                     # academia-digital
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from users where email like '%tmp-drv%';"  # 0

# La base de las fases 1 y 3 sigue entera
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from companies;"                                  # 2
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from evidences where form_snapshot is not null;"   # 2
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from company_documents;"                          # 3
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from manual_assignments;"                         # 3

# Validación en lectura: con una fila hostil escrita a mano, el HTML no la enlaza
# (se ejecuta con la fila puesta y antes de restaurarla)
# curl -s -b "$ADMIN_COOKIE" "http://localhost:3000/tenant-admin/projects/$ASSIGNMENT" | grep -c 'href="https://evil.com/carpeta"'   # 0
# curl -s -b "$ADMIN_COOKIE" "http://localhost:3000/tenant-admin/projects/$ASSIGNMENT" | grep -c "no es de Google Drive"             # >= 1

# Nada de basura en el árbol
git status --porcelain | grep -E "_tmp-|drv-check" && echo "ERROR: queda un script desechable" || echo "OK: limpio"

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep "problems"
pnpm exec turbo run build
    </automated>
  </verify>
  <done>
Los nueve pasos se ejercitaron invocando `setProjectDriveUrl` de verdad por HTTP —no
un script que imite su forma— con cuatro sesiones reales más un ADMIN de un tenant
desechable, leyendo `drive_url` de la base después de cada uno: sólo el administrador
del tenant dueño escribió, y las tres URLs hostiles y los cuatro actores no
autorizados dejaron la columna sin mover. Vaciar el campo lo dejó en `NULL`, no en
cadena vacía. Con una URL hostil escrita directamente en la base, el HTML servido del
panel no la enlaza y muestra el aviso de enlace no confiable — la validación de
lectura hace su trabajo sin depender de la de escritura. El tenant desechable se
borró y la base quedó como estaba: un tenant, tres activaciones con `drive_url` en
`NULL`, dos companies, dos evidences con `form_snapshot`, tres `company_documents`.
`check-types` limpio, `lint` en 81 advertencias, `build` verde.
  </done>
</task>

</tasks>

<verification>
```bash
pnpm exec turbo run check-types                    # limpio
pnpm exec turbo run lint 2>&1 | grep "problems"    # "✖ 81 problems (0 errors, 81 warnings)"
pnpm exec turbo run build                          # verde
```

Comprobaciones específicas del plan:
- `git show --stat HEAD` lista exactamente los cuatro archivos del plan.
- `git diff --stat -- "apps/web/app/tenant-admin/projects/[assignmentId]/page.tsx"
  "apps/web/app/professor/projects/[assignmentId]/page.tsx"` vacío.
- `grep -rn "drive.google.com" apps/web --include-dirs lib components app | grep -v .next`
  sólo devuelve `lib/drive-url.ts` y, como mucho, un `placeholder` de ayuda.
- `git status --short` sigue mostrando los archivos de `prol-1d` sin commitear.
</verification>

<success_criteria>
1. Un administrador pega el enlace de Drive de un proyecto y queda guardado.
   (Criterio 4 / DRV-01.)
2. Un consultor, un cliente, una petición sin sesión y un administrador de otro
   tenant son rechazados, y la columna no se mueve en ninguno de los cuatro casos.
3. Una URL que no sea de Google Drive se rechaza con un mensaje que dice qué se
   esperaba. (Mitad de escritura del criterio 6 / DRV-03.)
4. Una fila con un enlace no confiable escrita directamente en la base no se pinta
   como enlace: la pantalla lo dice. (La otra mitad, la que no depende de la
   validación de escritura.)
5. Un proyecto sin enlace lo dice en vez de fallar o dejar un hueco.
6. `updateManualAssignment` sigue sin cablearse (enmienda (e)), y las dos páginas de
   proyecto no cambiaron.
7. `check-types` limpio, `lint` en 0 errores y 81 advertencias, `build` verde.
</success_criteria>

<output>
Al terminar, crear
`.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-03-SUMMARY.md`.

Registrar en el SUMMARY:
- **La firma exacta de `setProjectDriveUrl`** y los tres campos nuevos del retorno de
  `getAssignmentPanel` (`driveUrl` saneado, `driveUrlIsInvalid`, `canEditDriveUrl`):
  el plan 03.1-04 lee de ahí.
- **La tabla de los nueve pasos** con la respuesta real y el valor real de
  `drive_url` después de cada uno. Es la evidencia de DRV-01 y de la mitad de
  escritura de DRV-03.
- **Los dos fragmentos del HTML servido** con la fila hostil puesta: el que demuestra
  que no hay `href` a `evil.com` y el del aviso. Es la evidencia de la validación en
  lectura.
- Que el tenant desechable se creó y se borró, con el conteo antes y después. Si algo
  quedó, decirlo.
- Si algún rechazo llegó con un mensaje distinto del esperado (por ejemplo, el de
  `documentsEnabled` en vez del de tenant), decirlo tal cual en vez de redactarlo
  como si hubiera salido el esperado.
</output>
