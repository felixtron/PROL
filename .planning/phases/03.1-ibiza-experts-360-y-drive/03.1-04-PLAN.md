---
phase: 03.1-ibiza-experts-360-y-drive
plan: 04
type: execute
wave: 4
depends_on: ["03.1-03"]
files_modified:
  - apps/web/lib/queries/manual.ts
  - apps/web/lib/queries/evidence.ts
  - apps/web/components/drive-folder-link.tsx
  - apps/web/components/project-drive-link.tsx
  - apps/web/components/evidence-detail.tsx
  - apps/web/app/dashboard/manuals/[assignmentId]/page.tsx
autonomous: false
requirements: [DRV-02, DRV-03]
parallel_safe: false  # Ola 4 con un solo plan. `branching_strategy` es "none" y otra
                      # sesión interactiva (`prol-1d`) tiene ~18 archivos sin commitear
                      # en este mismo árbol; en la ola 1 de la fase 1 dos ejecutores
                      # concurrentes se absorbieron archivos entre el `add` y el `commit`.

must_haves:
  truths:
    - "Desde el proyecto, el cliente llega a la carpeta de Drive de su empresa en un clic."
    - "El revisor, mirando una evidencia, llega a la misma carpeta sin salir a buscarla."
    - "El enlace que sirve el servidor es exactamente el que está guardado en la columna, no otro."
    - "Un proyecto sin enlace lo dice en las tres pantallas, y ninguna falla ni deja un hueco."
    - "Un enlace no confiable escrito a mano en la base no se convierte en `<a href>` en ninguna de las tres pantallas."
    - "Un usuario de otra empresa y una petición sin sesión no llegan a ver el enlace de un proyecto ajeno."
  artifacts:
    - path: "apps/web/components/drive-folder-link.tsx"
      provides: "El bloque de sólo lectura de la carpeta de Drive, compartido por las tres pantallas"
      contains: "rel=\"noopener noreferrer\""
      min_lines: 40
    - path: "apps/web/app/dashboard/manuals/[assignmentId]/page.tsx"
      provides: "El punto de entrada al expediente desde el proyecto del cliente"
      contains: "DriveFolderLink"
    - path: "apps/web/components/evidence-detail.tsx"
      provides: "Acceso a la carpeta desde la ficha de revisión"
      contains: "DriveFolderLink"
  key_links:
    - from: "apps/web/lib/queries/manual.ts"
      to: "apps/web/lib/drive-url.ts"
      via: "safeDriveUrl en getManualOverview, antes de exponer el enlace al cliente"
      pattern: "safeDriveUrl"
    - from: "apps/web/lib/queries/evidence.ts"
      to: "manual_assignments.drive_url"
      via: "select de assignment.driveUrl en getEvidenceDetail, revalidado con safeDriveUrl"
      pattern: "driveUrl"
    - from: "apps/web/components/project-drive-link.tsx"
      to: "apps/web/components/drive-folder-link.tsx"
      via: "composición: la mitad de lectura deja de estar duplicada"
      pattern: "DriveFolderLink"
---

<objective>
El enlace guardado deja de ser un dato y pasa a ser un camino: desde el proyecto del
cliente y desde la ficha de revisión de una evidencia se llega a la carpeta de Drive
en un clic, y donde no hay enlace se dice, en vez de fallar.

Purpose: es el criterio 5 de la fase y la mitad de lectura del criterio 6. Es también
el momento en que la decisión de la fase se vuelve visible: PROL ya no guarda el
expediente, lo señala — y sigue sabiendo qué requisito toca y quién lo aprobó, que es
lo que una carpeta compartida no sabe hacer.
Output: un bloque de sólo lectura compartido por las tres pantallas, dos consultas que
lo alimentan revalidando el enlace, y la comprobación de que nadie ve la carpeta de
otra empresa.
</objective>

<execution_context>
@/Users/flx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-CONTEXT.md
@.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-RESEARCH.md
@.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-01-SUMMARY.md
@.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-03-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

@apps/web/lib/queries/manual.ts
@apps/web/lib/queries/evidence.ts
@apps/web/lib/manual-access.ts
@apps/web/components/project-drive-link.tsx
@apps/web/components/evidence-detail.tsx
@apps/web/app/dashboard/manuals/[assignmentId]/page.tsx
@apps/web/lib/drive-url.ts

<interfaces>
<!-- Lectura directa del código, 2026-09-02, más lo que dejó el plan 03.1-03. -->

**Del plan 03.1-01:** `safeDriveUrl(value) → string | null` en `lib/drive-url.ts`.

**Del plan 03.1-03** (firmas exactas en su SUMMARY): `getAssignmentPanel` ya devuelve
`assignment.driveUrl` (saneado), `driveUrlIsInvalid` y `canEditDriveUrl`, y
`components/project-drive-link.tsx` ya pinta los tres estados de lectura **más** el
formulario de edición. Este plan **extrae** la mitad de lectura para no escribirla
tres veces.

**`getManualOverview`** (`lib/queries/manual.ts:389-467`) autoriza con
`requireAssignmentMemberAccess(assignmentId)` y devuelve
`{ assignment, isLeader, isStaff, manual, progress, checkedBySection, upcoming }`.
El `assignment` que devuelve **no** viene de un `findUnique` propio: es el que
construye la guarda (`manual-access.ts:324-332`), con exactamente
`{ id, manualId, companyId, tenantId, status }` — **no trae `driveUrl`, y la guarda
tampoco lo selecciona**. Hay dos formas de resolverlo; elegir la primera:
1. Añadir `driveUrl: true` al `select` de `requireAssignmentMemberAccess`
   (`manual-access.ts:303-313`) y a su objeto de retorno. Un solo sitio, y sirve
   también a `getSectionForCompany`, que lo va a necesitar en el plan 03.1-05.
2. Un `findUnique` extra en `getManualOverview`. Una consulta más por carga de
   página para un campo que la guarda ya podría traer.

**`getEvidenceDetail`** (`lib/queries/evidence.ts:95-178`) ya selecciona
`assignment: { select: { id: true, company: {...}, manual: {...} } }`. Añadir
`driveUrl: true` ahí es una línea.

**`EvidenceDetail`** (`components/evidence-detail.tsx`) tipa su prop como
`NonNullable<Awaited<ReturnType<typeof getEvidenceDetail>>>`, así que lo que se añada
a la consulta llega solo. Su línea 129 ya es null-safe con el archivo:
```tsx
{evidence.fileName ? (
  <a href={`/files/evidence/${evidence.id}`} …>…</a>
) : null}
```
**No hay que cambiar esa rama** — sigue sirviendo a las evidencias con archivo que
existan; lo que se añade es un bloque nuevo al lado.

**`app/dashboard/manuals/[assignmentId]/page.tsx`** (228 líneas) es Server Component;
destructura `const { manual, progress, checkedBySection, upcoming } = data;` — hay que
añadir `assignment` a esa desestructuración.

**Autorización que ya existe y no hay que reinventar**
(`manual-access.ts:299-336`, `requireAssignmentMemberAccess`): comprueba
`assertDocumentsEnabled`, y luego, para el personal, `assertTenantScope`; para todo el
que no sea revisor, exige `user.companyId === assignment.companyId`. Es la puerta que
impide que Constructora Delta vea la carpeta de Acme, y ya está puesta.

**Convenciones:** todo `lib/queries/*` con `cache()` de React; textos de usuario en
español minúscula sin punto final.
</interfaces>
</context>

<constraints>
- **Se revalida en cada lectura.** Ninguna de las tres pantallas recibe
  `assignment.driveUrl` crudo: pasa antes por `safeDriveUrl` en la consulta. Es la
  mitad de DRV-03 que no depende de que la escritura haya sido correcta.
- **Nada de rutas de redirect nuevas.** El `<a>` apunta directo a Drive. Una ruta
  `/api/drive-redirect` sólo revalidaría en el clic; validar en cada carga es más
  fuerte y ya está hecho.
- **El bloque de lectura vive en un solo archivo.** Si `drive.google.com` o la lógica
  de los tres estados aparece duplicada en dos componentes, está mal.
- **`/dashboard/company/evidence` (panel del líder) queda fuera a propósito**, y esto
  no es un olvido: esa pantalla lista las evidencias de **todas** las activaciones de
  la empresa, que pueden ser tres con tres carpetas distintas. Un solo botón "Abrir la
  carpeta" ahí sería mentira. Anotarlo en el SUMMARY para que nadie lo "complete".
- **El `EvidenceBlock` de la página de sección queda para el plan 03.1-05**, que
  reescribe esa rama entera para DRV-04. Meterlo aquí obligaría a tocar el mismo
  archivo dos veces en dos olas.
- **Nada de API de Drive**: no se comprueba que la carpeta exista ni que el usuario
  tenga permiso. Si Drive dice "necesitas acceso", eso lo resuelve IBIZA en Drive.
- **Nada de enlace por sección o por requisito**: el enlace es del proyecto (diferido
  y nombrado en el CONTEXT).
- **Nunca `pnpm db:seed`.**
- Stagear sólo los seis archivos de este plan por ruta explícita. **Nunca
  `git add -A`.**
- La puerta de lint sale con **exit 1** estando sana:
  `✖ 81 problems (0 errors, 81 warnings)`.
</constraints>

<tasks>

<task type="auto">
  <name>Tarea 1: el bloque compartido y las dos consultas que lo alimentan</name>
  <files>apps/web/components/drive-folder-link.tsx, apps/web/components/project-drive-link.tsx, apps/web/lib/queries/manual.ts, apps/web/lib/queries/evidence.ts</files>
  <action>
**1. `components/drive-folder-link.tsx`** — el bloque de **sólo lectura**, sin
`"use client"` (no tiene estado; los componentes cliente pueden importarlo igual):

```typescript
export function DriveFolderLink(props: {
  driveUrl: string | null;   // YA pasado por safeDriveUrl en el servidor
  invalid?: boolean;         // había algo guardado y no es confiable
  emptyHint?: string;        // qué decirle a este usuario concreto cuando no hay enlace
  size?: "default" | "compact";
}): JSX.Element
```

Tres estados, y **sólo el primero produce un `<a href>`**:

- **Enlace confiable**:
  ```tsx
  <a href={driveUrl} target="_blank" rel="noopener noreferrer" className="…bg-primary-600…">
    <FolderOpen className="h-4 w-4" />
    Abrir la carpeta de Drive
  </a>
  ```
  Debajo, en `text-xs text-text-tertiary`: "Se abre en una pestaña nueva, en Google
  Drive." Que el usuario sepa que sale de PROL antes de salir.
- **Sin enlace** (`driveUrl === null && !invalid`): "Este proyecto todavía no tiene
  carpeta de Drive." más `emptyHint`, que cada pantalla escribe a su manera (al
  cliente: "pídesela a tu consultor"; al revisor: "configúrala en el panel del
  proyecto"). **Nunca un hueco**, que es literalmente lo que pide DRV-03.
- **Enlace no confiable** (`invalid`): aviso ámbar "El enlace guardado no es de Google
  Drive, así que no se abre desde aquí" y **ningún `href`**.

`size: "compact"` sólo cambia paddings y tamaño de fuente, para que quepa en la ficha
de revisión sin dominarla.

**2. `components/project-drive-link.tsx`** — sustituir su mitad de lectura por
`<DriveFolderLink …/>` y quedarse **sólo** con el formulario de edición y su estado.
Es refactor puro: el comportamiento no cambia y las comprobaciones del plan 03.1-03
deben seguir pasando.

**3. `lib/manual-access.ts` + `lib/queries/manual.ts`** — llevar `driveUrl` al cliente:
- En `requireAssignmentMemberAccess`, añadir `driveUrl: true` al `select` y
  `driveUrl: assignment.driveUrl` al objeto que devuelve. Un solo sitio; el plan
  03.1-05 lo aprovecha para la página de sección.
- En `getManualOverview`, devolver el `assignment` con el enlace ya revalidado:
  ```typescript
  assignment: { ...assignment, driveUrl: safeDriveUrl(assignment.driveUrl) },
  driveUrlIsInvalid: assignment.driveUrl !== null && safeDriveUrl(assignment.driveUrl) === null,
  ```
  Mismos dos campos y mismos nombres que en `getAssignmentPanel` (plan 03.1-03): que
  las dos consultas hablen igual es lo que permite que las pantallas compartan
  componente.

> `manual-access.ts` no está en `files_modified` como archivo principal porque el
> cambio son dos líneas dentro de una guarda existente; **sí hay que stagearlo**.
> Si al ejecutar se prefiere la opción 2 (un `findUnique` extra en
> `getManualOverview`, sin tocar la guarda), es aceptable — pero entonces hay que
> decirlo en el SUMMARY y el plan 03.1-05 tendrá que resolverlo por su cuenta para
> `getSectionForCompany`.

**4. `lib/queries/evidence.ts`** — en `getEvidenceDetail`, añadir `driveUrl: true` al
`select` de `assignment`, y devolverlo saneado:
```typescript
return {
  user,
  evidence: {
    ...row,
    assignment: { ...row.assignment, driveUrl: safeDriveUrl(row.assignment.driveUrl) },
  },
  driveUrlIsInvalid: row.assignment.driveUrl !== null && safeDriveUrl(row.assignment.driveUrl) === null,
  versions,
};
```

**No se toca `listCompanyEvidence`** (panel del líder): ver la razón en
`<constraints>`.
  </action>
  <verify>
    <automated>
# El bloque de lectura existe y sólo enlaza cuando debe
grep -n "export function DriveFolderLink" apps/web/components/drive-folder-link.tsx
grep -n "target=\"_blank\"\|rel=\"noopener noreferrer\"" apps/web/components/drive-folder-link.tsx

# La mitad de lectura ya no está duplicada
grep -n "DriveFolderLink" apps/web/components/project-drive-link.tsx
grep -c "Abrir la carpeta de Drive" apps/web/components/project-drive-link.tsx   # 0: ahora lo dice el componente compartido
grep -rn "Abrir la carpeta de Drive" apps/web/components apps/web/app 2>/dev/null | grep -v "\.next" | grep -v "drive-folder-link.tsx" && echo "ERROR: texto duplicado" || echo "OK: un solo sitio"

# Las dos consultas revalidan
grep -n "safeDriveUrl" apps/web/lib/queries/manual.ts apps/web/lib/queries/evidence.ts
sed -n "/export const getManualOverview/,/^});/p" apps/web/lib/queries/manual.ts | grep -n "driveUrlIsInvalid"
sed -n "/export const getEvidenceDetail/,/^});/p" apps/web/lib/queries/evidence.ts | grep -n "driveUrl"

# Ningún componente recibe el valor crudo
grep -rn "row.driveUrl\|assignment.driveUrl" apps/web/components apps/web/app 2>/dev/null | grep -v "\.next"
# Esperado: sólo lecturas de campos YA saneados por la consulta, ninguna consulta directa a la base

# El panel del líder queda fuera, a propósito
git diff -- apps/web/lib/queries/evidence.ts | grep -n "listCompanyEvidence" && echo "REVISAR: fuera de alcance" || echo "OK"

# Ningún <img> nuevo
grep -n "<img" apps/web/components/drive-folder-link.tsx && echo "REVISAR" || echo "OK"

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep "problems"
    </automated>
  </verify>
  <done>
`DriveFolderLink` existe con sus tres estados y sólo el confiable produce un `<a
href>` con `target="_blank" rel="noopener noreferrer"`; el texto del enlace vive en
ese único archivo. `ProjectDriveLink` se quedó con el formulario y compone el bloque
compartido, sin cambiar de comportamiento. `getManualOverview` y `getEvidenceDetail`
devuelven el enlace pasado por `safeDriveUrl` y su bandera `driveUrlIsInvalid`, con los
mismos nombres que ya usa `getAssignmentPanel`. `listCompanyEvidence` no se tocó.
`check-types` limpio y `lint` en 81 advertencias.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: las dos pantallas nuevas, y que nadie vea la carpeta de otra empresa</name>
  <files>apps/web/app/dashboard/manuals/[assignmentId]/page.tsx, apps/web/components/evidence-detail.tsx</files>
  <action>
**1. El proyecto del cliente** (`app/dashboard/manuals/[assignmentId]/page.tsx`).
Añadir `assignment` a la desestructuración de `data` y, **inmediatamente después del
bloque de "Avance de implantación"** (que es lo primero que mira el cliente al
entrar), una sección:

```tsx
<section className="rounded-xl border border-border bg-surface p-5">
  <h2 className="flex items-center gap-2 font-heading text-base font-semibold text-text-primary">
    <FolderOpen className="h-4 w-4 text-text-tertiary" />
    Evidencias del proyecto
  </h2>
  <p className="mt-1 text-sm text-text-secondary">
    Los archivos de tu expediente viven en la carpeta de Google Drive que compartió tu
    consultor. Aquí dentro sigues viendo qué toca, cuándo vence y cómo va la revisión.
  </p>
  <div className="mt-3">
    <DriveFolderLink
      driveUrl={assignment.driveUrl}
      invalid={data.driveUrlIsInvalid}
      emptyHint="Pídesela a tu consultor: es donde va a subir y a revisar tus archivos."
    />
  </div>
</section>
```

El encabezado dice **"Evidencias del proyecto"** a propósito: el criterio 5 está
redactado como "abrir Evidencias desde ese proyecto", y esta es esa puerta. La
segunda frase es la que evita la lectura equivocada de que PROL ya no pinta nada.

Importar `FolderOpen` de `lucide-react` (el archivo ya importa de ahí) y
`DriveFolderLink` de `@/components/drive-folder-link`.

**2. La ficha de revisión** (`components/evidence-detail.tsx`). Un bloque `compact`
en la columna de datos de la evidencia, **junto a** la rama de descarga que ya
existe y sin tocarla:

```tsx
<div className="mt-3">
  <DriveFolderLink
    driveUrl={data.evidence.assignment.driveUrl}
    invalid={data.driveUrlIsInvalid}
    emptyHint="Configúrala en el panel del proyecto."
    size="compact"
  />
</div>
```

Por qué aquí: desde el plan 03.1-05 habrá evidencias sin archivo, y el revisor que
las apruebe necesita llegar al artefacto que está aprobando **sin salir a buscar la
carpeta en otra pantalla**. Sin este bloque, DRV-04 produciría un circuito que
funciona pero que nadie puede usar de verdad.

**3. Nadie ve la carpeta de otra empresa.** La guarda ya existe
(`requireAssignmentMemberAccess`), pero después de meterle un campo nuevo hay que
volver a comprobarlo, con las tres sesiones de siempre y sobre la activación de Acme
—con un enlace puesto—:

| Sesión | `GET /dashboard/manuals/<assignmentId de Acme>` | Debe |
|---|---|---|
| `carlos.mendoza@gmail.com` (Acme) | | **200** y el `href` de Drive en el HTML |
| `lucia.delgado@constructoradelta.test` (Delta) | | **no** 200 con contenido, y **cero** apariciones del enlace |
| sin cookie | | redirección a login, cero apariciones del enlace |

Y lo mismo sobre la ficha de revisión: `maria.garcia@academiadigitalmx.com`
(PROFESSOR del tenant) **sí** ve el bloque; `carlos.mendoza@gmail.com` no llega a
`/tenant-admin/evidence/<id>` ni a `/professor/evidence/<id>`.

Para tener algo que ver, poner el enlace con la acción real del plan 03.1-03
(`setProjectDriveUrl` por HTTP como ADMIN), no con un `UPDATE`. Y comprobar que **el
`href` servido es carácter por carácter el valor de la columna** — no una versión
normalizada, recortada o re-escapada por el camino.
  </action>
  <verify>
    <automated>
# Las dos pantallas montan el bloque compartido
grep -n "DriveFolderLink" "apps/web/app/dashboard/manuals/[assignmentId]/page.tsx" apps/web/components/evidence-detail.tsx

# La rama de descarga de la ficha de revisión no se tocó
git diff -- apps/web/components/evidence-detail.tsx | grep -n "^-.*files/evidence" && echo "ERROR: se rompió la descarga existente" || echo "OK"

# --- El href servido == la columna, carácter por carácter ---
DB_URL=$(docker exec prol-db psql -U prol -d prol -Atc "select drive_url from manual_assignments where id='$ASSIGNMENT';")
curl -s -b "$CLIENT_COOKIE" "http://localhost:3000/dashboard/manuals/$ASSIGNMENT" | grep -c "href=\"$DB_URL\""   # >= 1

# --- Tres sesiones sobre el proyecto de Acme ---
curl -s -o /dev/null -w "acme %{http_code}\n"  -b "$ACME_COOKIE"  "http://localhost:3000/dashboard/manuals/$ASSIGNMENT"
curl -s -o /dev/null -w "delta %{http_code}\n" -b "$DELTA_COOKIE" "http://localhost:3000/dashboard/manuals/$ASSIGNMENT"
curl -s -o /dev/null -w "anon %{http_code}\n"                     "http://localhost:3000/dashboard/manuals/$ASSIGNMENT"
# Y, lo que de verdad importa, que el enlace no se filtre:
curl -s -b "$DELTA_COOKIE" "http://localhost:3000/dashboard/manuals/$ASSIGNMENT" | grep -c "drive.google.com"   # 0
curl -s                    "http://localhost:3000/dashboard/manuals/$ASSIGNMENT" | grep -c "drive.google.com"   # 0

# --- La ficha de revisión ---
curl -s -b "$PROFESSOR_COOKIE" "http://localhost:3000/professor/evidence/$EVIDENCE_ID" | grep -c "drive.google.com"  # >= 1
curl -s -b "$ACME_COOKIE"      "http://localhost:3000/professor/evidence/$EVIDENCE_ID" | grep -c "drive.google.com"  # 0

# --- Sin enlace, lo dice; con enlace hostil, no lo enlaza ---
# (poner la columna en NULL, recargar; luego escribir a mano 'https://evil.com/x', recargar)
# grep -c "todavía no tiene carpeta de Drive"   # >= 1 con NULL
# grep -c 'href="https://evil.com/x"'           # 0 con la fila hostil
# grep -c "no es de Google Drive"               # >= 1 con la fila hostil

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep "problems"
pnpm exec turbo run build
    </automated>
  </verify>
  <done>
El proyecto del cliente tiene una sección "Evidencias del proyecto" con el botón a la
carpeta, y la ficha de revisión tiene el mismo bloque en versión compacta, sin haber
tocado la rama de descarga que ya existía. El `href` servido coincide carácter por
carácter con `manual_assignments.drive_url`. Con las tres sesiones: la empresa dueña
ve el enlace, la otra empresa y la petición sin sesión no lo ven ni una vez; el
consultor ve el bloque en la ficha de revisión y el cliente no llega a esa ruta. Con
la columna en `NULL` las dos pantallas lo dicen; con una URL hostil escrita a mano,
ninguna la enlaza y las dos muestran el aviso. `check-types` limpio, `lint` en 81
advertencias, `build` verde.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 3: el clic que sale de PROL y aterriza en Drive</name>
  <what-built>
El camino completo: un administrador pega la carpeta de Drive del proyecto, y desde
ese proyecto —tanto el cliente como el consultor— llegan a ella en un clic. Donde no
hay carpeta, la pantalla lo dice; donde hay algo que no es Drive, no se enlaza.

Ya verificado por máquina: el `href` que sirve el servidor es carácter por carácter el
valor de la columna; la empresa que no es dueña y la petición sin sesión no lo ven ni
una vez; con la columna en `NULL` sale el mensaje y con una URL hostil sale el aviso
sin enlace. Lo que ninguna de esas comprobaciones puede firmar es lo único que importa
de verdad aquí: **que el clic aterrice donde tiene que aterrizar.**
  </what-built>
  <how-to-verify>
Necesito primero una carpeta de Drive **de verdad**, tuya: crea o elige una en tu
Google Drive, dale a Compartir → Copiar enlace, y pégamela. Sin eso el clic aterriza
en un 404 de Google y la prueba no valdría nada.

Con `pnpm dev` en `http://localhost:3000` y la contraseña local `password123`:

**A. Pegar el enlace (administrador).** Entra como `admin@prol.prosuite.pro` →
menú del módulo → **Proyectos** → el proyecto de **Acme Corp** (Manual de Gestión de
Calidad ISO 9001). Verás una tarjeta **«Carpeta de Drive del proyecto»**. Pega ahí tu
enlace y guarda.
1. Debe quedarse guardado y aparecer el botón **«Abrir la carpeta de Drive»**.
2. Pulsa el botón: **debe abrirse tu carpeta real, en una pestaña nueva.**

**B. Una URL que no es de Drive.** En el mismo campo, escribe
`https://ejemplo.com/carpeta` y guarda: debe **rechazarlo** con un mensaje que diga
que espera un enlace de Google Drive. Vuelve a poner el tuyo.

**C. El cliente llega a la misma carpeta.** Entra como `carlos.mendoza@gmail.com` →
**Proyectos** → el manual de Acme. Justo debajo del avance debe haber
**«Evidencias del proyecto»** con el mismo botón. Púlsalo: **misma carpeta.**

**D. El revisor también.** Entra como el consultor
(`maria.garcia@academiadigitalmx.com`) → menú del módulo → **Evidencias** → abre
cualquier evidencia. Debe haber un acceso a la misma carpeta desde la ficha, sin tener
que ir a buscarla a otra pantalla.

**E. Un proyecto sin carpeta.** Entra al otro proyecto de Acme ("Manual de prueba
OPS-04"), que no tiene enlace: debe **decir que no tiene carpeta de Drive**, no
quedarse en blanco ni dar error.
  </how-to-verify>
  <action>
Antes de pedir nada:

1. **Pedir el enlace real de Drive del usuario y esperar a tenerlo.** No inventar uno
   ni usar un id falso: el paso A2 y el C existen justamente para comprobar que el
   clic aterriza, y con una URL inventada aterrizaría en un error de Google y el
   checkpoint no demostraría nada.
2. Confirmar que el segundo proyecto de Acme ("Manual de prueba OPS-04") tiene
   `drive_url` en `NULL`, para que el paso E tenga algo que enseñar.
3. Arrancar el servidor y confirmar que las tres sesiones entran de verdad.
4. Guardar el valor de `drive_url` y el fragmento de HTML con el `href` servido en las
   tres pantallas **antes** de que el usuario pulse nada.

Presentar el recorrido y **esperar la respuesta del usuario**. No navegar por él.

**Cómo se registra el resultado.** Si el usuario aprueba **sin contestar las tres
preguntas** del `resume-signal`, **no** se registra como verificado: se anota
literalmente "aprobado sin ejercitar" en el SUMMARY y DRV-02 queda **sin marcar** en
`REQUIREMENTS.md`. Precedente directo: en el plan 03-06 un checkpoint se dio por
aprobado sin haberse ejercitado y hizo falta una sesión entera (03-06b) para cerrar la
brecha.

Si contesta, contrastar su respuesta con lo capturado: el `drive_url` de la base debe
coincidir con el enlace que dice haber abierto, y con el `href` servido en las tres
pantallas. Guardar los tres valores en el SUMMARY.

**Al terminar, borrar el enlace real del usuario de la base**
(`setProjectDriveUrl({ assignmentId, driveUrl: null })` o un `UPDATE … SET drive_url =
NULL`) y decirlo en el SUMMARY: es una carpeta suya y no tiene por qué quedarse en una
base de desarrollo. El plan 03.1-05 pone la suya con una URL de forma válida y destino
ficticio, que para lo que necesita basta.
  </action>
  <resume-signal>
Responde "aprobado" **y estas tres respuestas**, que es lo que hace la aprobación
comprobable en vez de creída:

1. ¿El botón te llevó a **tu** carpeta de Drive? (sí / no, y desde qué panel lo
   probaste)
2. ¿Qué mensaje exacto te salió al pegar `https://ejemplo.com/carpeta`?
3. ¿Qué dice el proyecto que no tiene carpeta ("Manual de prueba OPS-04")?

Si describes un problema en vez de aprobar, dilo con el panel y el paso donde ocurre.
  </resume-signal>
</task>

</tasks>

<verification>
```bash
pnpm exec turbo run check-types                    # limpio
pnpm exec turbo run lint 2>&1 | grep "problems"    # "✖ 81 problems (0 errors, 81 warnings)"
pnpm exec turbo run build                          # verde
```

Comprobaciones específicas del plan:
- `git show --stat HEAD` lista los archivos del plan (los seis, más
  `apps/web/lib/manual-access.ts` si se tomó la opción 1 de la tarea 1).
- `grep -rn "Abrir la carpeta de Drive" apps/web | grep -v .next` devuelve un solo
  archivo: `components/drive-folder-link.tsx`.
- `docker exec prol-db psql -U prol -d prol -Atc "select coalesce(drive_url,'<NULL>')
  from manual_assignments;"` → tres `<NULL>` al cerrar el plan (el enlace real del
  usuario se borra).
- `git status --short` sigue mostrando los archivos de `prol-1d` sin commitear.
</verification>

<success_criteria>
1. Abrir Evidencias desde un proyecto lleva a la carpeta de Drive configurada, desde
   el panel del cliente y desde la ficha de revisión. (Criterio 5 / DRV-02.)
2. El `href` servido coincide carácter por carácter con la columna.
3. Un proyecto sin enlace lo dice en las tres pantallas, y una fila con un enlace no
   confiable no se enlaza en ninguna. (Criterio 6 / DRV-03, mitad de lectura.)
4. Un usuario de otra empresa y una petición sin sesión no ven el enlace ni una vez.
5. La mitad de lectura del bloque vive en un solo archivo y las tres pantallas la
   comparten.
6. Un humano pulsó el botón y aterrizó en una carpeta real de Drive —o quedó
   registrado que no lo hizo.
7. `check-types` limpio, `lint` en 0 errores y 81 advertencias, `build` verde.
</success_criteria>

<output>
Al terminar, crear
`.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-04-SUMMARY.md`.

Registrar en el SUMMARY:
- **La firma de `DriveFolderLink`** y los campos que ahora devuelven
  `getManualOverview` y `getEvidenceDetail`: el plan 03.1-05 monta el mismo bloque en
  la página de sección.
- **Si `driveUrl` se llevó al cliente por la guarda (`requireAssignmentMemberAccess`)
  o por un `findUnique` extra**, y por qué. El plan 03.1-05 depende de esa decisión.
- **La tabla de las tres sesiones** con el código de respuesta y el número de
  apariciones del enlace en cada una. Es la evidencia de que la carpeta no se filtra.
- **Las tres respuestas del usuario al checkpoint, literales**, junto al `drive_url`
  de la base y al `href` servido, para que se puedan contrastar. Si no contestó,
  decirlo con esas palabras y dejar DRV-02 sin marcar.
- Que el enlace real del usuario se borró de la base al terminar.
- Que `/dashboard/company/evidence` quedó fuera **a propósito** (varias activaciones
  por empresa ⇒ varias carpetas ⇒ un solo botón sería mentira), para que nadie lo
  "complete" sin pensarlo.
</output>
