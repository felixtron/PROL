---
phase: 03.1-ibiza-experts-360-y-drive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - apps/web/lib/drive-url.ts
  - apps/web/lib/tenant-labels.ts
  - apps/web/lib/actions/tenant.ts
  - apps/web/app/tenant-admin/settings/page.tsx
  - apps/web/app/tenant-admin/settings/documents-menu-label-form.tsx
autonomous: true
requirements: [NAV-01, DRV-01, DRV-03]
parallel_safe: false  # Ola 1 con un solo plan, como todas las de esta fase.
                      # `branching_strategy` es "none": en la ola 1 de la fase 1 dos
                      # ejecutores concurrentes sobre este mismo working tree se
                      # absorbieron archivos entre el `add` y el `commit`. Las fases 2 y
                      # 3 lo evitaron serializando y funcionó en doce planes seguidos.
                      # Además este plan toca `schema.prisma`, que AHORA MISMO tiene
                      # cambios sin commitear de otra sesión interactiva (`prol-1d`).

must_haves:
  truths:
    - "La base local tiene las columnas `tenants.documents_menu_label` y `manual_assignments.drive_url`, ambas nullable, sin haber perdido ninguna fila de las fases 1 y 3."
    - "El enum `EvidenceRequirementKind` lleva escrito, encima, que `FILE` significa «artefacto externo, en la carpeta de Drive del proyecto» y que ya no exige subir un archivo a PROL."
    - "Una URL que no sea `https:` sobre un host de la lista cerrada de Drive se rechaza, incluidas las que sólo lo parecen (`drive.google.com.evil.com`, `https://drive.google.com@evil.com`)."
    - "Un tenant sin rótulo propio resuelve al neutro «Gestión documental» sin que nadie haya hecho backfill en la base."
    - "El administrador de un tenant guarda su propio rótulo desde `/tenant-admin/settings` y la columna cambia de verdad."
    - "El trabajo sin commitear de la otra sesión sigue en el árbol de trabajo después del commit de este plan."
  artifacts:
    - path: "apps/web/lib/drive-url.ts"
      provides: "isValidDriveUrl / safeDriveUrl sobre una lista cerrada de hosts, más el mensaje de error de DRV-03"
      contains: "ALLOWED_DRIVE_HOSTS"
      min_lines: 25
    - path: "apps/web/lib/tenant-labels.ts"
      provides: "DEFAULT_DOCUMENTS_MENU_LABEL y resolveDocumentsMenuLabel, con los límites de longitud"
      contains: "DEFAULT_DOCUMENTS_MENU_LABEL"
    - path: "apps/web/app/tenant-admin/settings/documents-menu-label-form.tsx"
      provides: "Formulario del rótulo del menú, visible sólo si el tenant tiene el módulo encendido"
      contains: "updateDocumentsMenuLabel"
    - path: "packages/db/prisma/schema.prisma"
      provides: "documentsMenuLabel en Tenant, driveUrl en ManualAssignment y el comentario del enum"
      contains: "documents_menu_label"
  key_links:
    - from: "apps/web/lib/actions/tenant.ts"
      to: "tenants.documents_menu_label"
      via: "updateDocumentsMenuLabel tras requireTenantAdmin()"
      pattern: "documentsMenuLabel"
    - from: "apps/web/app/tenant-admin/settings/page.tsx"
      to: "DocumentsMenuLabelForm"
      via: "render condicionado a tenant.documentsEnabled"
      pattern: "documentsEnabled"
    - from: "apps/web/lib/drive-url.ts"
      to: "URL nativo + Set cerrado de hosts"
      via: "new URL(value).hostname contra ALLOWED_DRIVE_HOSTS, exigiendo protocol === https:"
      pattern: "new URL"
---

<objective>
Los cimientos de la fase: las dos columnas aditivas que todo lo demás lee, el
validador de enlaces de Drive, la constante del rótulo por tenant, y la pantalla
donde ese rótulo se edita.

Purpose: los cinco planes siguientes leen estas dos columnas y llaman a estos dos
helpers. Poner el validador aquí —y no dentro de la acción que escribe el enlace—
es lo que permite que el plan 03.1-03 lo llame **en escritura** y los planes 03.1-04
y 03.1-05 lo llamen **en lectura** sin duplicar la lista cerrada, que es la
propiedad de seguridad de la fase.
Output: dos columnas en la base local, dos módulos puros, una server action y una
sección nueva en `/tenant-admin/settings`.
</objective>

<execution_context>
@/Users/flx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-CONTEXT.md
@.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

@packages/db/prisma/schema.prisma
@apps/web/lib/actions/tenant.ts
@apps/web/app/tenant-admin/settings/page.tsx
@apps/web/app/tenant-admin/settings/branding-form.tsx

<interfaces>
<!-- Contratos exactos leídos del código real el 2026-09-02. El ejecutor no
     necesita explorar el repo para nada de lo de abajo. -->

**Dónde van exactamente las dos columnas** (líneas del archivo *hoy*; reconfírmalas,
el archivo se mueve):

- `schema.prisma:496-507` — bloque de feature flags de `model Tenant`, terminado por
  `documentsEnabled Boolean @default(false) @map("documents_enabled")`.
- `schema.prisma:2643-2682` — `model ManualAssignment`, con `notes String? @db.Text`
  en la 2667 y `createdAt` en la 2669.
- `schema.prisma:402-410` — el comentario `///` del enum `EvidenceRequirementKind`
  y el enum, hoy con `FILE | RISK_MATRIX | EVALUATION_LINK`.

**El texto actual del comentario del enum, que hay que reescribir:**
```prisma
/// Cómo se satisface un requisito de evidencia.
///   FILE            — se sube un archivo (Word, Excel, PDF, imagen, audio, video).
///   RISK_MATRIX     — se llena la matriz de riesgos y oportunidades en la plataforma.
///   EVALUATION_LINK — se enlaza una entrega del módulo de Evaluaciones (p. ej. el Test DAFO).
```

**El patrón exacto de la acción a imitar** (`apps/web/lib/actions/tenant.ts`, archivo
completo hoy: 69 líneas, un solo export):
```typescript
"use server";
import { revalidatePath } from "next/cache";
import { db } from "@prol/db";
import { requireTenantAdmin } from "@/lib/auth";

export async function updateTenantBranding(data: {...}) {
  const admin = await requireTenantAdmin();
  if (!admin.tenantId) throw new Error("SUPER_ADMIN debe seleccionar un tenant");
  // ...validación campo por campo, lanzando Error con texto en español...
  await db.tenant.update({ where: { id: admin.tenantId }, data: {...} });
  revalidatePath("/dashboard", "layout");
  revalidatePath("/professor", "layout");
  revalidatePath("/admin", "layout");
  revalidatePath("/tenant-admin", "layout");
  revalidatePath("/", "layout");
  return { success: true };
}
```
`updateTenantBranding` **lanza** en vez de devolver `{ success: false }`; el
formulario de branding ya sabe capturarlo. Seguir esa misma forma, no inventar otra.

**El select del tenant en la página de settings hoy** (`settings/page.tsx:117-125`)
pide `id, name, logo, primaryColor, accentColor`. Le faltan los dos campos nuevos.

**Formas reales de URL de carpeta de Drive** (de la investigación):
`https://drive.google.com/drive/folders/<ID>`, `…?usp=sharing`,
`https://drive.google.com/drive/u/<n>/folders/<ID>`,
`https://drive.google.com/open?id=<ID>`, y `docs.google.com/...` para un índice en
Google Docs. **Todas comparten host y esquema y ninguna comparte una forma de path
única** — de ahí que la validación sea por host, no por regex.

**Convenciones que aplican:** identificadores en inglés, textos de usuario en
español minúscula sin punto final; archivos `kebab-case.ts` con exports
`camelCase`; nada de despacho ni correo en un módulo `"use server"`.
</interfaces>
</context>

<constraints>
- **`packages/db/prisma/schema.prisma` es un archivo compartido con otra sesión.**
  Al investigar esta fase, la sesión `prol-1d` tenía ~18 archivos sin commitear en
  este árbol, ese archivo incluido. **Al cerrar la planificación ya los había
  commiteado** (`aaaf8d5 feat(dc3): cerrar las siete brechas del modulo DC-3`), así
  que lo más probable es que el árbol esté limpio al ejecutar y baste con el caso A
  de la tarea 3. **Pero eso hay que comprobarlo, no suponerlo**: la sesión sigue viva
  y puede haber vuelto sobre el archivo. Sus cambios rondan las líneas 1458-1680, que
  **no** se solapan con las regiones de este plan. Reglas duras: **releer `git diff --
  packages/db/prisma/schema.prisma` inmediatamente antes de editar**; usar el tool
  `Edit` (diff puntual), **nunca `Write`** sobre ese archivo; y **stagear sólo los
  archivos de este plan por ruta explícita** — nada de `git add -A`, nada de
  `git commit -a`. La tarea 3 tiene el procedimiento exacto para no arrastrar el
  trabajo ajeno al commit.
- **Sólo cambios aditivos.** Prisma no tiene carpeta de migraciones: se aplica con
  `db push`. Ninguna columna se borra, ninguna se hace obligatoria, ningún valor de
  enum se añade.
- **`EvidenceRequirementKind` no gana ningún valor.** Se reutiliza `FILE`
  (enmienda (a) del CONTEXT), y por eso el comentario del enum **no es opcional**:
  un valor llamado `FILE` que ya no exige archivo es una trampa sin él.
- **La lista de hosts es cerrada y vive en un solo sitio.** Nada de regex sobre el
  path. Nada de `startsWith("https://drive.google.com")` — `https://drive.google.com.evil.com`
  lo satisface.
- **No se enciende `documents_enabled` en ningún tenant** (hoy: `ibiza-online=true`,
  `academia-digital=false`, `mecanica-g3=false`). Ni en local ni en producción.
- **Nunca `pnpm db:seed`**: `tenant.deleteMany()` cascadea y destruiría los datos
  reales de la fase 3 (Acme `v2 VIGENTE`/`v1 OBSOLETO`, Delta `v1 VIGENTE`) y las dos
  `Evidence` con `formSnapshot` de la fase 1.
- **Nada de UI de navegación** (plan 03.1-02), **nada de la acción del enlace de
  Drive** (plan 03.1-03), **nada de tocar `evidence.ts`** (plan 03.1-05).
- La puerta de lint sale con **exit 1** estando sana:
  `✖ 81 problems (0 errors, 81 warnings)`. Eso es la línea base, no una regresión.
</constraints>

<tasks>

<task type="auto">
  <name>Tarea 1: las dos columnas aditivas y el comentario que salva al enum</name>
  <files>packages/db/prisma/schema.prisma</files>
  <action>
**Antes de tocar nada**, releer el estado real del archivo compartido:

```bash
git status --short -- packages/db/prisma/schema.prisma
git diff --stat -- packages/db/prisma/schema.prisma
```

Si el diff toca las regiones de este plan (el bloque de flags de `Tenant`, el enum
`EvidenceRequirementKind`, o `model ManualAssignment`), **parar y reportarlo** en vez
de resolverlo por cuenta propia: significaría que la otra sesión entró en la misma
zona y la fotografía de la investigación caducó.

Tres ediciones puntuales con el tool `Edit`. **Nada de `Write` sobre este archivo.**

**1. El comentario del enum** (sustituye la línea `///   FILE  — se sube un
archivo…` conservando las otras dos):

```prisma
/// Cómo se satisface un requisito de evidencia.
///   FILE            — artefacto externo: vive en la carpeta de Google Drive del
///                     proyecto (`ManualAssignment.driveUrl`), NO dentro de PROL.
///                     Desde la fase 3.1 este valor **no exige subir ningún
///                     archivo** a la plataforma: el requisito se marca como
///                     cumplido y PROL conserva sólo el circuito (qué toca, cuándo
///                     vence, quién lo aprobó, la bitácora). Se conservó el nombre
///                     `FILE` a propósito, para no añadir un valor a un enum de
///                     Postgres cuando la rama de interfaz había que reescribirla
///                     igual. Si algún día vuelve a significar "archivo dentro de
///                     PROL", este comentario es lo primero que hay que corregir.
///   RISK_MATRIX     — se llena la matriz de riesgos y oportunidades en la plataforma.
///   EVALUATION_LINK — se enlaza una entrega del módulo de Evaluaciones (p. ej. el Test DAFO).
```

**2. `Tenant.documentsMenuLabel`**, inmediatamente después de `documentsEnabled`,
dentro del mismo bloque de feature flags:

```prisma
  /// Rótulo del menú del módulo de gestión documental, propio de cada tenant
  /// (IBIZA usa «Ibiza Experts 360»). Nullable a propósito: el neutro por defecto
  /// vive en `apps/web/lib/tenant-labels.ts`, no en la base, para que cambiar ese
  /// texto no exija un backfill.
  documentsMenuLabel String? @map("documents_menu_label")
```

**3. `ManualAssignment.driveUrl`**, inmediatamente después de `notes String? @db.Text`:

```prisma
  /// Carpeta de Google Drive del proyecto: desde la fase 3.1 el expediente de
  /// archivos vive ahí y no en PROL. Se guarda el enlace y nada más — no hay API
  /// de Drive, ni OAuth, ni listado de archivos. El host se valida contra una
  /// lista cerrada (`apps/web/lib/drive-url.ts`) al escribirlo Y al exponerlo:
  /// es una URL que un usuario pega y otro sigue, es decir un redirector abierto
  /// si no se valida en los dos extremos.
  driveUrl String? @map("drive_url")
```

**Aplicar a la base local.** `db push` sincroniza el archivo entero, incluidos los
cambios de DC-3 de la otra sesión — por eso se corre **sin** `--accept-data-loss`:
si algo del árbol compartido implicara una operación destructiva, el comando debe
abortar en vez de ejecutarla en silencio.

```bash
pnpm --filter @prol/db db:push
```

Si `db push` pide `--accept-data-loss` o anuncia un `DROP`: **parar, no forzar,
reportar qué cambio lo provoca.** Es casi seguro trabajo ajeno, y no es de este
plan decidir sobre él.

Después del push, comprobar que la base de la fase anterior sigue entera. Si algún
conteo bajó, parar.
  </action>
  <verify>
    <automated>
# Las dos columnas existen, nullable, en las tablas correctas
docker exec prol-db psql -U prol -d prol -Atc "select column_name||'|'||data_type||'|nullable='||is_nullable from information_schema.columns where table_name='tenants' and column_name='documents_menu_label';"
# Esperado: documents_menu_label|text|nullable=YES
docker exec prol-db psql -U prol -d prol -Atc "select column_name||'|'||data_type||'|nullable='||is_nullable from information_schema.columns where table_name='manual_assignments' and column_name='drive_url';"
# Esperado: drive_url|text|nullable=YES

# El enum NO ganó ningún valor
docker exec prol-db psql -U prol -d prol -Atc "select string_agg(enumlabel,',' order by enumsortorder) from pg_enum e join pg_type t on t.oid=e.enumtypid where t.typname='EvidenceRequirementKind';"
# Esperado exacto: FILE,RISK_MATRIX,EVALUATION_LINK

# El comentario del enum quedó escrito (enmienda (a) del CONTEXT, no negociable)
grep -n "artefacto externo" packages/db/prisma/schema.prisma
grep -c "no exige subir ningún" packages/db/prisma/schema.prisma   # 1

# La base de las fases 1 y 3 sigue entera
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from companies;"                       # 2
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from evidences where form_snapshot is not null;"  # 2
docker exec prol-db psql -U prol -d prol -Atc "select company_id||'|v'||version||'|'||status from company_documents order by company_id, version;"
# Esperado: 3 filas — Acme v1 OBSOLETO, Acme v2 VIGENTE, Delta v1 VIGENTE

# El módulo sigue apagado donde estaba apagado
docker exec prol-db psql -U prol -d prol -Atc "select slug||'='||documents_enabled from tenants order by slug;"

# Ningún valor por defecto se coló en la base (el neutro vive en código)
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from tenants where documents_menu_label is not null;"   # 0

pnpm exec turbo run check-types
    </automated>
  </verify>
  <done>
`tenants.documents_menu_label` y `manual_assignments.drive_url` existen en la base
local, ambas `text` nullable y sin valor por defecto. `EvidenceRequirementKind`
sigue teniendo exactamente tres valores. El comentario del enum dice por escrito que
`FILE` significa artefacto externo en Drive y que ya no exige archivo. Los conteos de
las fases 1 y 3 (2 companies, 2 evidences con `form_snapshot`, 3 filas de
`company_documents` con Acme v1/v2 y Delta v1) no se movieron. `documents_enabled`
sigue como estaba en los tres tenants. `check-types` limpio.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: el validador de Drive y el rótulo por defecto, dos módulos puros</name>
  <files>apps/web/lib/drive-url.ts, apps/web/lib/tenant-labels.ts</files>
  <action>
Dos archivos nuevos, los dos **puros**: sin `"use server"`, sin `node:*`, sin
importar `@prol/db`. Los van a importar tanto Server Components como componentes
cliente.

**1. `apps/web/lib/drive-url.ts`**

```typescript
/**
 * Validación del enlace a la carpeta de Google Drive de un proyecto.
 *
 * Un administrador pega esta URL y un cliente la sigue después: sin una lista
 * cerrada de hosts, el campo es un redirector abierto. **La lista cerrada es la
 * propiedad de seguridad**, igual que en `sanitize-manual-html.ts`; una regex
 * sobre el path no lo sería, porque las formas de URL de Drive cambian
 * (`/drive/folders/`, `/drive/u/2/folders/`, `/open?id=`) y el host no.
 *
 * Precedente que NO hay que copiar: `AdvisorySession.meetingUrl` se escribe y se
 * renderiza sin validar nada (`lib/actions/advisory.ts`). Está fuera del alcance
 * de esta fase arreglarlo, pero este módulo existe para no repetirlo.
 */
const ALLOWED_DRIVE_HOSTS = new Set(["drive.google.com", "docs.google.com"]);

/** Mensaje único de DRV-03: dice qué se esperaba, no sólo que falló. */
export const DRIVE_URL_ERROR =
  "El enlace debe ser de Google Drive (https://drive.google.com/… o https://docs.google.com/…)";

export function isValidDriveUrl(value: string | null | undefined): boolean {
  if (typeof value !== "string" || value.trim() === "") return false;
  let url: URL;
  try {
    url = new URL(value.trim());
  } catch {
    return false;
  }
  // `URL` normaliza el host a punycode y minúsculas; el `toLowerCase` es
  // redundante a propósito, para que la comparación no dependa de esa garantía.
  return (
    url.protocol === "https:" &&
    ALLOWED_DRIVE_HOSTS.has(url.hostname.toLowerCase())
  );
}

/**
 * El enlace, sólo si sigue siendo confiable. Se usa en **cada lectura** que vaya a
 * exponerlo, no sólo al guardarlo: una fila puede ser anterior a la validación de
 * escritura, o haberse editado a mano en la base. Cuando devuelve `null`, la
 * pantalla debe decir "sin enlace configurado" — nunca pintar un `<a href>`.
 */
export function safeDriveUrl(value: string | null | undefined): string | null {
  return isValidDriveUrl(value) ? (value as string).trim() : null;
}
```

**2. `apps/web/lib/tenant-labels.ts`**

```typescript
/**
 * Rótulo del menú del módulo de gestión documental. Lo define cada tenant
 * («Ibiza Experts 360» es marca de una consultora concreta y PROL es
 * multi-tenant), y el neutro vive aquí y no como `@default` en la base: cambiar
 * este texto no debe exigir un backfill.
 */
export const DEFAULT_DOCUMENTS_MENU_LABEL = "Gestión documental";

/** El sidebar mide 256 px: a partir de ~40 caracteres el rótulo se parte en varias líneas. */
export const DOCUMENTS_MENU_LABEL_MIN = 2;
export const DOCUMENTS_MENU_LABEL_MAX = 40;

export function resolveDocumentsMenuLabel(
  label: string | null | undefined,
): string {
  const trimmed = (label ?? "").trim();
  return trimmed.length > 0 ? trimmed : DEFAULT_DOCUMENTS_MENU_LABEL;
}
```

**Ejercitar `isValidDriveUrl` de verdad, no de memoria.** Script desechable
`/tmp/drive-url-check.mjs` (fuera del repo: no debe aparecer en `git status`) que
importe el módulo real por ruta relativa y recorra la tabla de abajo, imprimiendo
`OK`/`FALLO` por caso y saliendo con código 1 si alguno falla.

| Entrada | Esperado | Por qué está en la tabla |
|---|---|---|
| `https://drive.google.com/drive/folders/1AbC` | acepta | forma canónica |
| `https://drive.google.com/drive/folders/1AbC?usp=sharing` | acepta | la que produce "Compartir" |
| `https://drive.google.com/drive/u/2/folders/1AbC` | acepta | varias cuentas de Google |
| `https://drive.google.com/open?id=1AbC` | acepta | formato antiguo, sigue vivo |
| `https://docs.google.com/document/d/1AbC/edit` | acepta | índice en Google Docs (host del allowlist) |
| `https://DRIVE.GOOGLE.COM/drive/folders/1AbC` | acepta | mayúsculas |
| `http://drive.google.com/drive/folders/1AbC` | rechaza | sin TLS |
| `https://drive.google.com.evil.com/x` | rechaza | **el caso que un `startsWith` dejaría pasar** |
| `https://evil.com/drive.google.com` | rechaza | el host real es otro |
| `https://drive.google.com@evil.com/x` | rechaza | userinfo: el host real es `evil.com` |
| `https://xn--drve-lta.google.com/x` | rechaza | homógrafo en punycode |
| `javascript:alert(1)` | rechaza | esquema no https |
| `data:text/html,<script>` | rechaza | esquema no https |
| `ftp://drive.google.com/x` | rechaza | esquema no https |
| `//drive.google.com/x` | rechaza | sin esquema: `new URL` lanza |
| `""`, `"   "`, `null`, `undefined` | rechaza | vacíos y ausentes |

Borrar el script al terminar. **No se commitea.**
  </action>
  <verify>
    <automated>
# Los 16 casos, contra el módulo real (no una copia)
node /tmp/drive-url-check.mjs   # sale 0 sólo si los 16 pasan

# La lista es cerrada y está en un solo sitio
grep -c "ALLOWED_DRIVE_HOSTS" apps/web/lib/drive-url.ts    # 2 (definición + uso)
grep -rn "drive.google.com" apps/web/lib apps/web/app apps/web/components 2>/dev/null | grep -v "\.next" | grep -v "lib/drive-url.ts"
# Esperado: sólo apariciones en texto de usuario (el mensaje de error), ninguna comparación de host fuera del módulo

# Nada de regex sobre el path ni startsWith sobre el host
grep -nE "startsWith\(\"https://drive|/drive/folders/|new RegExp" apps/web/lib/drive-url.ts && echo "ERROR: validación por forma de path" || echo "OK: validación por host"

# Módulos puros: nada de servidor dentro
grep -nE '"use server"|@prol/db|node:' apps/web/lib/drive-url.ts apps/web/lib/tenant-labels.ts && echo "ERROR: no es puro" || echo "OK"

# El neutro no está duplicado por ahí
grep -rn "Gestión documental" apps/web 2>/dev/null | grep -v "\.next" | grep -v "lib/tenant-labels.ts"
# Esperado en este plan: ninguna otra definición (los layouts la importarán en el plan 03.1-02)

rm -f /tmp/drive-url-check.mjs
git status --porcelain | grep -E "drive-url-check|_tmp-" && echo "ERROR: queda basura" || echo "OK: limpio"

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep "problems"
    </automated>
  </verify>
  <done>
`lib/drive-url.ts` valida por host contra un `Set` cerrado exigiendo `https:`, y los
16 casos de la tabla —incluidos `drive.google.com.evil.com`, el userinfo
`@evil.com` y el homógrafo en punycode— dan el resultado esperado ejecutando el
módulo real. `safeDriveUrl` existe para revalidar en lectura. `lib/tenant-labels.ts`
expone el neutro, los dos límites de longitud y `resolveDocumentsMenuLabel`. Ninguno
de los dos módulos importa `@prol/db` ni lleva `"use server"`. `check-types` limpio,
`lint` en 81 advertencias, sin basura en `git status`.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: el tenant edita su propio rótulo, y el commit no se lleva trabajo ajeno</name>
  <files>apps/web/lib/actions/tenant.ts, apps/web/app/tenant-admin/settings/page.tsx, apps/web/app/tenant-admin/settings/documents-menu-label-form.tsx</files>
  <action>
**1. La acción**, al final de `lib/actions/tenant.ts`, junto a `updateTenantBranding`
y con su misma forma (lanza `Error` con texto en español; no devuelve
`{ success: false }`):

```typescript
/**
 * Rótulo del menú del módulo de gestión documental de este tenant.
 *
 * Vive aquí y no en el panel de super-admin (decisión (c) del CONTEXT): es marca
 * del tenant, igual que el nombre y el logo, y el ADMIN de IBIZA ya existe en
 * producción — no necesita que nadie se lo configure.
 */
export async function updateDocumentsMenuLabel(label: string | null) {
  const admin = await requireTenantAdmin();
  if (!admin.tenantId) {
    throw new Error("SUPER_ADMIN debe seleccionar un tenant");
  }

  const trimmed = (label ?? "").trim();
  // Vaciarlo es una operación válida: devuelve el menú al neutro de
  // `tenant-labels.ts`. Por eso se guarda `null` y no la cadena vacía — así la
  // columna sólo tiene dos estados y `resolveDocumentsMenuLabel` no tiene que
  // desempatar entre "" y null.
  const next = trimmed.length > 0 ? trimmed : null;
  if (
    next !== null &&
    (next.length < DOCUMENTS_MENU_LABEL_MIN || next.length > DOCUMENTS_MENU_LABEL_MAX)
  ) {
    throw new Error(
      `El rótulo del menú debe tener ${DOCUMENTS_MENU_LABEL_MIN}-${DOCUMENTS_MENU_LABEL_MAX} caracteres`,
    );
  }

  await db.tenant.update({
    where: { id: admin.tenantId },
    data: { documentsMenuLabel: next },
  });

  // El rótulo se pinta en el sidebar de los tres paneles.
  revalidatePath("/dashboard", "layout");
  revalidatePath("/professor", "layout");
  revalidatePath("/tenant-admin", "layout");
  return { success: true };
}
```

Importar `DOCUMENTS_MENU_LABEL_MIN` / `DOCUMENTS_MENU_LABEL_MAX` de
`@/lib/tenant-labels` — no re-teclear los números.

**Nada de `revalidatePath("/admin", "layout")`**: el panel de super-admin no tiene
ningún item del módulo (confirmado leyendo `app/admin/layout.tsx` completo) y no hay
nada que revalidar ahí.

**2. El formulario**, archivo nuevo
`app/tenant-admin/settings/documents-menu-label-form.tsx`, componente cliente
calcado en estructura de `branding-form.tsx` pero mucho más corto: un `<input
type="text">`, un botón Guardar, estado `isPending` con `useTransition`, error en
rojo si la acción lanza, y `router.refresh()` al terminar. Texto de ayuda debajo:

> Es el nombre del menú que agrupa Manuales Maestros, Proyectos, Evidencias y
> Agenda. Si lo dejas vacío se usa «Gestión documental».

Importar `DEFAULT_DOCUMENTS_MENU_LABEL` para ese texto en vez de repetirlo a mano.
Props: `initialLabel: string | null`.

**Sin `<img>`**: no hay logo que pintar aquí, así que este plan no toca el conteo de
`@next/next/no-img-element` (36 de los 81 avisos de la línea base).

**3. La página.** En `settings/page.tsx`, añadir `documentsEnabled: true` y
`documentsMenuLabel: true` al `select` del tenant, y una sección nueva **debajo de
"Marca de la academia"**, renderizada sólo si `tenant?.documentsEnabled`:

```tsx
{tenant?.documentsEnabled ? (
  <section>
    <h2 className="mb-3 font-heading text-sm font-semibold uppercase tracking-wider text-text-tertiary">
      Menú de gestión documental
    </h2>
    <DocumentsMenuLabelForm initialLabel={tenant.documentsMenuLabel} />
  </section>
) : null}
```

Condicionarla al flag es deliberado: Academia Digital MX y Mecanica G3 tienen el
módulo apagado y un campo para renombrar un menú que no ven sólo genera preguntas.

**4. Ejercitar la acción de verdad, por HTTP.** No se firma "el administrador guarda
su rótulo" sin que la columna se haya movido. Receta de la fase 3 (`03-06-SUMMARY.md`
§"Cierre de la brecha"), que funcionó y hay que reutilizar tal cual:

- `POST /api/auth/sign-in/email` con `admin@prol.prosuite.pro` / `password123`
  (ADMIN, Academia Digital MX, `documentsEnabled=true`) → cookie
  `better-auth.session_token`.
- `POST http://localhost:3000/tenant-admin/settings` con cabeceras
  `Next-Action: <id>`, `Content-Type: text/plain;charset=UTF-8`,
  `Origin: http://localhost:3000` y la cookie; cuerpo = array JSON de argumentos.
- **El id de acción se lee del manifest POR RUTA**, no del global:
  `.next/dev/server/app/tenant-admin/settings/page/server-reference-manifest.json`.
  El global `.next/server/server-reference-manifest.json` devuelve 404 "Server action
  not found" contra un `next dev` con Turbopack — hallazgo de la fase 3, no repetir el
  error de buscarlo ahí.

Tres invocaciones y una lectura de base después de cada una:
1. `"Ibiza Experts 360"` → la columna guarda ese texto.
2. `""` → la columna vuelve a `NULL` (no a la cadena vacía).
3. Un texto de 41 caracteres → la acción lanza y **la columna no cambia**.

Dejar la columna en `NULL` al terminar: el plan 03.1-02 arranca desde el neutro y
demuestra el cambio de rótulo delante del usuario.

**5. El commit — el paso delicado de este plan.**

`git add <ruta>` stagea el **archivo entero**, así que sobre `schema.prisma`
arrastraría también los cambios de DC-3 de `prol-1d`. Primero mirar si eso sigue
siendo cierto:

```bash
git diff -- packages/db/prisma/schema.prisma
```

- **Caso A — el diff contiene sólo las tres ediciones de la tarea 1** (la otra sesión
  ya commiteó lo suyo): `git add packages/db/prisma/schema.prisma` y listo.

- **Caso B — el diff contiene además hunks ajenos** (DC-3, `Dc3CourseEdition`,
  `Dc3Certificate`, relaciones de `Company`…): stagear un blob construido, sin tocar
  el árbol de trabajo — así lo ajeno sigue sin commitear y donde estaba:

  ```bash
  git show HEAD:packages/db/prisma/schema.prisma > /tmp/schema-mine.prisma
  # Aplicar sobre /tmp/schema-mine.prisma las MISMAS tres ediciones de la tarea 1,
  # con el tool Edit (no con sed).
  BLOB=$(git hash-object -w /tmp/schema-mine.prisma)
  git update-index --cacheinfo 100644,$BLOB,packages/db/prisma/schema.prisma
  git diff --cached -- packages/db/prisma/schema.prisma   # revisar: sólo lo nuestro
  ```

  `git commit` (sin `-a`) commitea el índice y deja el árbol intacto. Después del
  commit, `git status` debe volver a mostrar `schema.prisma` como modificado: eso es
  exactamente lo correcto — es el trabajo de `prol-1d`, que sigue suyo y sin commitear.

Stagear el resto por ruta explícita, **nunca `git add -A`**:

```bash
git add apps/web/lib/drive-url.ts apps/web/lib/tenant-labels.ts \
        apps/web/lib/actions/tenant.ts \
        apps/web/app/tenant-admin/settings/page.tsx \
        apps/web/app/tenant-admin/settings/documents-menu-label-form.tsx
git commit -m "feat(documental): columnas de Drive y rótulo por tenant, con validación de host"
```
  </action>
  <verify>
    <automated>
# La acción existe, con su guard, y no toca los campos que la enmienda (e) prohíbe
grep -n "export async function updateDocumentsMenuLabel" apps/web/lib/actions/tenant.ts
grep -n "requireTenantAdmin" apps/web/lib/actions/tenant.ts
grep -nE "consultantId|status:|notes:" apps/web/lib/actions/tenant.ts && echo "REVISAR" || echo "OK"

# Los límites no están re-tecleados
grep -n "DOCUMENTS_MENU_LABEL_MIN\|DOCUMENTS_MENU_LABEL_MAX" apps/web/lib/actions/tenant.ts
grep -nE "length < 2|length > 40" apps/web/lib/actions/tenant.ts && echo "ERROR: números a mano" || echo "OK"

# La sección se condiciona al flag y el select trae los dos campos
grep -n "documentsEnabled" apps/web/app/tenant-admin/settings/page.tsx
grep -n "documentsMenuLabel" apps/web/app/tenant-admin/settings/page.tsx
grep -n "DocumentsMenuLabelForm" apps/web/app/tenant-admin/settings/page.tsx

# Ningún <img> nuevo (la línea base tiene 36 no-img-element y no debe moverse)
grep -n "<img" apps/web/app/tenant-admin/settings/documents-menu-label-form.tsx && echo "REVISAR: necesita eslint-disable con motivo" || echo "OK"

# --- Ejercicio real por HTTP (los tres pasos, con lectura de base entre ellos) ---
# 1) Rótulo propio
docker exec prol-db psql -U prol -d prol -Atc "select slug||'|'||coalesce(documents_menu_label,'<NULL>') from tenants where slug='academia-digital';"
# Esperado tras el paso 1: academia-digital|Ibiza Experts 360
# 2) Vaciar
# Esperado tras el paso 2: academia-digital|<NULL>   (NULL, no cadena vacía)
# 3) 41 caracteres -> la acción lanza y la columna NO cambia
# Esperado tras el paso 3: academia-digital|<NULL>

# Estado final para el plan 03.1-02
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from tenants where documents_menu_label is not null;"   # 0

# --- El commit no se llevó trabajo ajeno ---
git show HEAD:packages/db/prisma/schema.prisma | grep -c "documents_menu_label"   # 1
git show HEAD:packages/db/prisma/schema.prisma | grep -c "drive_url"              # 1
git show HEAD:packages/db/prisma/schema.prisma | grep -ci "Dc3CourseEdition"      # el mismo valor que antes del plan
git show --stat HEAD                                                             # exactamente los 6 archivos de este plan
git status --short                                                               # el trabajo de prol-1d sigue sin commitear

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep "problems"
pnpm exec turbo run build
    </automated>
  </verify>
  <done>
`updateDocumentsMenuLabel` existe en `lib/actions/tenant.ts` tras `requireTenantAdmin()`,
importa los dos límites de `tenant-labels.ts` en vez de repetir los números, guarda
`null` cuando el texto queda vacío y revalida los tres layouts que pintan el rótulo.
`/tenant-admin/settings` muestra la sección sólo si el tenant tiene el módulo
encendido. Los tres pasos se ejercitaron por HTTP contra la acción real (login real +
`Next-Action` leído del manifest por ruta), con la columna leída de la base después de
cada uno: guardó el texto, volvió a `NULL` al vaciarlo, y rechazó los 41 caracteres sin
mover la columna. La columna queda en `NULL` para el plan siguiente. El commit contiene
exactamente los seis archivos de este plan y el trabajo de `prol-1d` sigue sin
commitear en el árbol. `check-types` limpio, `lint` en 81 advertencias, `build` verde.
  </done>
</task>

</tasks>

<verification>
```bash
pnpm exec turbo run check-types                    # limpio
pnpm exec turbo run lint 2>&1 | grep "problems"    # "✖ 81 problems (0 errors, 81 warnings)"
pnpm exec turbo run build                          # verde
```

Comprobaciones específicas del plan:
- `git show --stat HEAD` lista exactamente seis archivos y ninguno de DC-3.
- `git status --short` sigue mostrando los archivos de `prol-1d` sin commitear,
  `packages/db/prisma/schema.prisma` incluido si estábamos en el caso B.
- `git status --porcelain` no menciona ningún script desechable.
- El enum en la base tiene exactamente `FILE,RISK_MATRIX,EVALUATION_LINK`.
- `select count(*) from tenants where documents_enabled` devuelve el mismo reparto
  que antes del plan (`ibiza-online=true`, los otros dos `false`).
</verification>

<success_criteria>
1. La base local tiene las dos columnas nullable y ninguna fila de las fases 1 y 3
   se perdió.
2. El enum no ganó valores y lleva escrito encima qué significa ahora `FILE`.
   (Enmienda (a) del CONTEXT, condición no negociable.)
3. `isValidDriveUrl` acepta las seis formas reales de URL de Drive y rechaza los diez
   casos hostiles, incluido el que un `startsWith` dejaría pasar — demostrado
   ejecutando el módulo real.
4. `safeDriveUrl` existe para que los planes de lectura revaliden sin duplicar la
   lista.
5. El administrador de un tenant guarda, vacía y ve rechazado un rótulo demasiado
   largo, ejercitando la server action real por HTTP y leyendo la columna después de
   cada paso.
6. El commit contiene sólo los archivos de este plan; el trabajo sin commitear de la
   otra sesión sigue intacto en el árbol.
7. `check-types` limpio, `lint` en 0 errores y 81 advertencias, `build` verde.
</success_criteria>

<output>
Al terminar, crear
`.planning/phases/03.1-ibiza-experts-360-y-drive/03.1-01-SUMMARY.md`.

Registrar en el SUMMARY:
- **Las firmas exactas** de `isValidDriveUrl`, `safeDriveUrl`,
  `resolveDocumentsMenuLabel` y `updateDocumentsMenuLabel`: los cinco planes
  siguientes las llaman.
- **La tabla de los 16 casos** del validador con el resultado observado. Es la
  evidencia de DRV-03 en su mitad de escritura.
- **Cuál de los dos casos de commit se dio (A o B)** y, si fue el B, el hash del blob
  construido y el `git status` posterior. Si alguien duda después de si este plan
  pisó el trabajo de `prol-1d`, esa es la traza.
- Si `db push` avisó de algo relacionado con los modelos de DC-3, decirlo aunque no
  se haya actuado: es información para la otra sesión.
- Los tres resultados reales del ejercicio HTTP (texto guardado, vaciado a `NULL`,
  rechazo por longitud), con el valor leído de la base en cada uno.
</output>
</content>
