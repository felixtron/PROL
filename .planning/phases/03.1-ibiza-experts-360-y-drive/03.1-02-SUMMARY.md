---
phase: 03.1-ibiza-experts-360-y-drive
plan: 02
subsystem: ui
tags: [nextjs, react, sidebar-navigation, accessibility, tenant-branding]

# Dependency graph
requires:
  - phase: 03.1-01
    provides: "tenants.documentsMenuLabel, resolveDocumentsMenuLabel, DEFAULT_DOCUMENTS_MENU_LABEL"
provides:
  - "SidebarNavLink | SidebarNavGroup union con isNavGroup y flattenNavHrefs (components/nav-icons.ts)"
  - "NavGroup / NavLinkItem / groupContainsHref compartidos (components/nav-group.tsx)"
  - "Los tres paneles (administrador, consultor, cliente) agrupan las cuatro/tres/tres entradas del módulo bajo un menú desplegable con el rótulo del tenant"
  - "Vocabulario corregido: Manuales Maestros (plantillas) vs. Proyectos (implementaciones), en menú, h1 y breadcrumbs"
affects: [03.1-03, 03.1-04, 03.1-05, 03.1-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Grupo de sidebar sin store ni useEffect: apertura derivada de pathname con el patrón 'ajustar estado durante el render' (comparar lastContains vs contains dentro del render, no en un efecto)"
    - "Hijos del grupo omitidos del DOM al cerrar (no display:none), para que el estado sea verificable por HTTP"
    - "flattenNavHrefs aplana grupos antes de resolveActiveHref, que sigue sin saber que existen grupos"

key-files:
  created:
    - apps/web/components/nav-group.tsx
  modified:
    - apps/web/components/nav-icons.ts
    - apps/web/components/sidebar-shell.tsx
    - apps/web/app/tenant-admin/layout.tsx
    - apps/web/app/professor/layout.tsx
    - apps/web/app/dashboard/layout.tsx
    - apps/web/app/dashboard/sidebar-nav.tsx
    - apps/web/app/dashboard/mobile-nav.tsx
    - apps/web/app/tenant-admin/manuals/page.tsx
    - "apps/web/app/tenant-admin/manuals/[id]/page.tsx"
    - apps/web/app/dashboard/manuals/page.tsx
    - "apps/web/app/dashboard/manuals/[assignmentId]/page.tsx"

key-decisions:
  - "El checkpoint se registra como EJERCITADO, no aprobado en blanco: el usuario contestó las tres preguntas del resume-signal via Q&A estructurada del orquestador, no en texto libre — se documenta así, sin adornar."
  - "El panel de consultor (professor) no fue visto por el usuario en el checkpoint. En vez de inferirlo del código compartido, esta continuación lo cerró con evidencia de la misma clase que las otras dos: login real + GET autenticado por HTTP, mostrando el grupo con sus tres hijos correctos."
  - "NAV-01/02/03 se marcan Complete con evidencia mixta declarada por partes (aprobación humana + servidor-verificado por HTTP), siguiendo el precedente de DOC-04 en REQUIREMENTS.md — no se afirma nada que el usuario no haya dicho."
  - "tenants.documents_menu_label se tocó temporalmente (NULL y de vuelta) sólo para capturar el par de fragmentos HTML que exige el criterio 2, y se restauró exactamente a Ibiza Experts 360 al terminar — mismo valor que tenía al empezar esta continuación."

patterns-established:
  - "Cuando un checkpoint deja un panel sin ejercitar por el humano, cerrar la brecha con HTTP real (login + GET autenticado) en vez de inferir del código o de repetir el checkpoint — precedente ya usado en 03-06b."

requirements-completed: [NAV-01, NAV-02, NAV-03]

duration: "tareas 1-2 ~7min (sesión previa) + tarea 3 no cronometrable (incluye espera de aprobación humana) + ~20min de continuación (login triple, captura de evidencia HTTP, cierre)"
completed: 2026-09-02
---

# Phase 03.1 Plan 02: El menú agrupado por tenant, en los tres paneles Summary

**Los cuatro/tres/tres items del módulo dejaron de ser entradas sueltas y ahora viven bajo un menú desplegable único (`NavGroup`, patrón de acordeón de `knowledge-base.tsx`) con el rótulo que cada tenant define, en administrador, consultor y cliente — con el vocabulario corregido (Manuales Maestros vs. Proyectos) y el checkpoint humano cerrado con las tres respuestas que exigía el plan, más HTTP real cubriendo el panel que el usuario no llegó a ver.**

## Performance

- **Duration:** tareas 1-2 ejecutadas en sesión previa (~7min entre sus dos commits); tarea 3 (checkpoint) no cronometrable por incluir espera de aprobación humana; esta continuación (verificación de commits, cierre del checkpoint, evidencia HTTP del panel de consultor, REQUIREMENTS/SUMMARY/STATE) ~20min.
- **Started:** 2026-09-02T22:51:27Z (commit de la tarea 1, sesión previa)
- **Completed:** 2026-09-02T23:35:00Z (aprox.)
- **Tasks:** 3/3
- **Files modified:** 12 (tareas 1-2) + 0 archivos de código en esta continuación (sólo documentación y una columna de datos tocada y restaurada)

## Accomplishments
- `SidebarNavItem` es ahora la unión `SidebarNavLink | SidebarNavGroup`; `nav-group.tsx` pinta el acordeón colapsable compartido por los tres renderizadores de navegación del repo.
- Los tres paneles (administrador: 4 hijos, consultor: 3 hijos, cliente: 3 hijos) agrupan el módulo bajo un único ítem con el rótulo del tenant, demostrado por HTTP en los tres — no sólo en el que el usuario vio.
- El vocabulario quedó corregido: "Manuales Maestros" en el panel de administrador (menú, h1, breadcrumb, línea explicativa), "Proyectos" en el panel de cliente (menú, h1, breadcrumb).
- El checkpoint de la tarea 3 se registró honestamente como ejercitado sobre dos de los tres paneles por el usuario, y el tercero (consultor) se cerró con evidencia HTTP real en vez de darse por bueno por inferencia de código compartido.

## Task Commits

Each task was committed atomically:

1. **Tarea 1: el contrato del grupo y el componente que lo pinta** - `a18a2a8` (feat)
2. **Tarea 2: los tres paneles agrupados, vocabulario corregido, rótulo por tenant** - `b385efe` (feat)
3. **Tarea 3: el menú, visto por una persona en los tres paneles (checkpoint)** - sin commit de código; cerrada con esta documentación (SUMMARY + REQUIREMENTS + STATE + ROADMAP)

**Plan metadata:** (pendiente — commit de este SUMMARY.md/STATE.md/ROADMAP.md/REQUIREMENTS.md)

## Files Created/Modified
- `apps/web/components/nav-icons.ts` - unión `SidebarNavLink | SidebarNavGroup`, `isNavGroup`, `flattenNavHrefs`
- `apps/web/components/nav-group.tsx` - `NavGroup`, `NavLinkItem`, `groupContainsHref` (nuevo)
- `apps/web/components/sidebar-shell.tsx` - aplana antes de `resolveActiveHref`, delega el render en `NavGroup`/`NavLinkItem`
- `apps/web/app/tenant-admin/layout.tsx` - grupo "documents" con 4 hijos, rótulo de `resolveDocumentsMenuLabel(tenant?.documentsMenuLabel)`
- `apps/web/app/professor/layout.tsx` - mismo grupo con 3 hijos (sin Manuales Maestros)
- `apps/web/app/dashboard/layout.tsx` - mismo grupo con Proyectos/Documentos/Agenda
- `apps/web/app/dashboard/sidebar-nav.tsx` - `flattenNavHrefs`/`isNavGroup` para escritorio del cliente
- `apps/web/app/dashboard/mobile-nav.tsx` - grupo como encabezado de sección en la hoja "Más", sin acordeón
- `apps/web/app/tenant-admin/manuals/page.tsx` - h1 "Manuales Maestros" + línea explicativa
- `apps/web/app/tenant-admin/manuals/[id]/page.tsx` - breadcrumb "← Manuales Maestros"
- `apps/web/app/dashboard/manuals/page.tsx` - h1 "Proyectos" + descripción corregida
- `apps/web/app/dashboard/manuals/[assignmentId]/page.tsx` - breadcrumb "← Proyectos"

## Las firmas exactas (contrato compartido, para no reinventarlo)

```typescript
// apps/web/components/nav-icons.ts
export interface SidebarNavLink {
  href: string;
  label: string;
  icon: SidebarIcon;
}
export interface SidebarNavGroup {
  id: string;
  label: string;
  icon: SidebarIcon;
  children: SidebarNavLink[];
}
export type SidebarNavItem = SidebarNavLink | SidebarNavGroup;
export function isNavGroup(item: SidebarNavItem): item is SidebarNavGroup;
export function flattenNavHrefs(items: SidebarNavItem[]): string[];

// apps/web/components/nav-group.tsx
export function groupContainsHref(group: SidebarNavGroup, activeHref: string | null): boolean;
export function NavLinkItem(props: { item: SidebarNavLink; active: boolean; nested?: boolean }): JSX.Element;
export function NavGroup(props: { group: SidebarNavGroup; activeHref: string | null }): JSX.Element;
```

## Evidencia del criterio 2 (rótulo por tenant, sin recompilar)

Mismo `GET /tenant-admin/projects`, cookie real de `admin@prol.prosuite.pro`, sin ningún cambio de código entre las dos capturas — sólo la columna en la base:

| Estado de la columna | Fragmento servido |
|---|---|
| `NULL` | `...<span class="flex-1 truncate text-left">Gestión documental</span>...` |
| `'Ibiza Experts 360'` | `...<span class="flex-1 truncate text-left">Ibiza Experts 360</span>...` |

La columna quedó **restaurada a `Ibiza Experts 360`** al terminar (mismo valor que tenía al empezar esta continuación, tal como pedía el resume_instructions).

## Las tres respuestas del usuario al checkpoint, literales

El usuario respondió **"aprobado"**, confirmando explícitamente, en respuesta a la pregunta directa "¿Recorriste el menú en pantalla (rótulo, plegar/desplegar, entrada del cliente)?", la opción:

> "Sí, todo correcto — Vi «Ibiza Experts 360» en el sidebar, el menú se pliega y despliega, y el panel del cliente dice «Proyectos»."

Esto es la confirmación del propio usuario a las tres respuestas que pedía el `resume-signal`:
1. **Rótulo leído en el sidebar del administrador:** «Ibiza Experts 360».
2. **¿Se plegó y desplegó al pulsarlo?:** sí.
3. **Qué dice la entrada del panel del cliente:** «Proyectos».

**Registro honesto del alcance de esta aprobación:** el checkpoint se ejercitó vía una pregunta estructurada del orquestador (con la opción de confirmación ya redactada), no tecleando cada respuesta en texto libre. No se afirma que el usuario haya reportado nada más allá de estos tres puntos — en particular, **no se afirma** que haya visto el panel de consultor, el comportamiento en móvil (paso D), ni que haya ejercitado el cambio de rótulo desde Configuración (paso E). Esos tres puntos quedan explícitamente **no reportados por separado** por el usuario.

## Cómo se cerró lo que el usuario no vio (panel de consultor)

En vez de asumir que el panel de consultor funciona porque comparte código con el de administrador (razonamiento válido pero no observado), esta continuación lo verificó con el mismo tipo de evidencia que los otros dos paneles — HTTP real, no inferencia:

```
POST /api/auth/sign-in/email  (maria.garcia@academiadigitalmx.com, PROFESSOR de academia-digital) -> 200
GET  /professor/evidence  (con la cookie de sesión) -> 200
  grep "Ibiza Experts 360"        -> 1 (el rótulo del tenant, no un neutro)
  grep 'aria-expanded="true"'     -> 1 (el grupo abierto porque la ruta activa cae dentro)
  hijos presentes: Proyectos, Evidencias, Agenda
  "Manuales Maestros" ausente (correcto: el consultor no tiene esa entrada)
```

Los mismos tres `GET` autenticados (administrador, consultor, cliente) en esta continuación:

| Panel | Ruta | HTTP | Rótulo del grupo | `aria-expanded="true"` | Hijos |
|---|---|---|---|---|---|
| Administrador | `/tenant-admin/projects` | 200 | Ibiza Experts 360 | sí | Manuales Maestros, Proyectos, Evidencias, Agenda |
| Consultor | `/professor/evidence` | 200 | Ibiza Experts 360 | sí | Proyectos, Evidencias, Agenda |
| Cliente | `/dashboard/manuals` | 200 | Ibiza Experts 360 | sí | Proyectos, Documentos, Agenda |

## Valor de `tenants.documents_menu_label` para el plan siguiente

`academia-digital` queda en **`'Ibiza Experts 360'`** — el mismo valor que tenía al empezar esta continuación, confirmado con una lectura final tras la ida-y-vuelta a `NULL` usada sólo para capturar el fragmento del criterio 2. El plan 03.1-03 arranca desde ese valor.

## Decisions Made
- Ver `key-decisions` en el frontmatter.
- Ningún cambio de vocabulario objetado por el usuario: no hubo objeción sobre el plural "Proyectos" en el panel del cliente (la enmienda (b) del CONTEXT), porque el usuario no llegó a comentar sobre ese punto específico más allá de confirmar que decía "Proyectos" y no "Manuales".

## Deviations from Plan

None de las Reglas 1-4 sobre el código. La única desviación de proceso fue de esta continuación: en vez de sólo registrar "aprobado sin ejercitar" para el panel de consultor (que el plan habría exigido si el usuario no hubiera contestado ninguna de las tres preguntas), el usuario **sí contestó las tres preguntas del resume-signal** — así que el checkpoint cuenta como ejercitado según el propio criterio del plan — y esta continuación fue más allá, cerrando con evidencia HTTP real la única parte que el usuario no reportó (el panel de consultor), en vez de dejarlo como hueco o asumirlo del código compartido.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
El plan 03.1-03 (Drive) puede apoyarse en el menú agrupado y en `documentsMenuLabel = 'Ibiza Experts 360'` para `academia-digital`. NAV-01, NAV-02 y NAV-03 quedan `Complete` en `REQUIREMENTS.md`, con la evidencia mixta (aprobación humana + servidor-verificado por HTTP) declarada por partes, siguiendo el mismo formato que DOC-04.

---
*Phase: 03.1-ibiza-experts-360-y-drive*
*Completed: 2026-09-02*

## Self-Check: PASSED

Commits `a18a2a8` y `b385efe` existen en el historial de git. Los doce archivos del plan (`nav-group.tsx`, `nav-icons.ts`, y los diez restantes listados en `Files Created/Modified`) existen en disco. `check-types`, `lint` (81 advertencias) y `build` habían quedado verdes al cierre de la tarea 2 (ya commiteada). Esta continuación no tocó código: sólo documentación (`REQUIREMENTS.md`, este `SUMMARY.md`) y una ida-y-vuelta reversible sobre `tenants.documents_menu_label`, confirmada restaurada a `Ibiza Experts 360` con una lectura final de la base.
