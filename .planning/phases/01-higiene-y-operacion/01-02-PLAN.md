---
phase: 01-higiene-y-operacion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/actions/manual.ts
  - apps/web/app/api/evaluations/results/[assignmentId]/pdf/route.tsx
autonomous: true
requirements: [OPS-03]

must_haves:
  truths:
    - "Dos subidas simultáneas del mismo documento para la misma empresa producen versiones consecutivas y ninguna violación de unicidad."
    - "Sin el lock, el mismo escenario es reproducible como error P2002 — la prueba puede fallar, así que prueba algo."
    - "El chequeo de pertenencia documento↔manual sigue ocurriendo fuera de la transacción."
    - "La ruta PDF de resultados de evaluación usa el único helper de data-URL del repo y sigue degradando a null en vez de lanzar."
  artifacts:
    - path: "apps/web/lib/actions/manual.ts"
      provides: "uploadCompanyDocument con lock de fila sobre manual_documents"
      contains: "FOR UPDATE"
    - path: "apps/web/app/api/evaluations/results/[assignmentId]/pdf/route.tsx"
      provides: "Generación del PDF de resultados sin helper duplicado"
      contains: "loadUploadAsDataUrl"
  key_links:
    - from: "apps/web/lib/actions/manual.ts (uploadCompanyDocument)"
      to: "tabla manual_documents"
      via: "tx.$queryRaw SELECT 1 ... FOR UPDATE dentro de db.$transaction"
      pattern: "SELECT 1 FROM manual_documents WHERE id = .* FOR UPDATE"
    - from: "apps/web/app/api/evaluations/results/[assignmentId]/pdf/route.tsx"
      to: "apps/web/lib/certificate-assets.ts"
      via: "import { loadUploadAsDataUrl }"
      pattern: "from \"@/lib/certificate-assets\""
---

<objective>
Serializar el incremento de versión de `CompanyDocument` con el mismo patrón de
bloqueo de fila que ya usa el repo en otros cinco sitios, y eliminar la segunda
copia del helper que carga un upload como data-URL antes de que la fase 4 cree
una tercera.

Purpose: hoy dos consultores que suban a la vez la versión personalizada del
mismo documento para la misma empresa leen ambos `last.version = N` y ambos
intentan crear `N+1`, violando `@@unique([documentId, companyId, version])`. Uno
de los dos ve un error crudo de Prisma y su archivo se pierde.
Output: `uploadCompanyDocument` transaccional y con lock; la ruta PDF de
evaluaciones importando el helper canónico.
</objective>

<execution_context>
@/Users/flx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-higiene-y-operacion/01-CONTEXT.md
@.planning/phases/01-higiene-y-operacion/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

@apps/web/lib/actions/manual.ts
@apps/web/lib/actions/evidence.ts
@apps/web/lib/certificate-assets.ts

<interfaces>
<!-- Contratos exactos, transcritos del código. No hace falta explorar. -->

**Patrón de la casa** (`apps/web/lib/actions/evidence.ts` líneas 146-176, dentro de
`submitEvidence`). Nótese que bloquea la tabla PADRE que siempre existe
(`compliance_activities`), no la tabla cuya versión se calcula:
```typescript
const evidence = await db.$transaction(async (tx) => {
  // Serializa a los miembros de la misma empresa que entreguen a la vez.
  await tx.$queryRaw`SELECT 1 FROM compliance_activities WHERE id = ${activity.id} FOR UPDATE`;
  const last = await tx.evidence.findFirst({
    where: { activityId: activity.id },
    orderBy: { version: "desc" },
    select: { version: true },
  });
  return tx.evidence.create({ data: { /* ... */ version: (last?.version ?? 0) + 1 }, select: { id: true } });
});
```

**Restricción que hoy se puede violar** (`packages/db/prisma/schema.prisma`, modelo
`CompanyDocument`, línea 2681):
```
@@unique([documentId, companyId, version])
```

**Nombres reales de tabla** (verificados por `@@map` en el schema):
| Modelo Prisma | Tabla Postgres | Línea |
|---|---|---|
| `ManualDocument` | `manual_documents` | 2519 |
| `CompanyDocument` | `company_documents` | 2684 |

**Helper canónico de data-URL** (`apps/web/lib/certificate-assets.ts` líneas 11-37,
ya usado por 3 rutas PDF; NO requiere cambios):
```typescript
export async function loadUploadAsDataUrl(
  url: string | null | undefined
): Promise<string | null>
```
Convención: **degrada devolviendo null, nunca lanza.** No cambiarla.
</interfaces>
</context>

<constraints>
- **Bloquear `manual_documents`, NO `company_documents`.** Es la decisión clave de
  la fase. El par (documento, empresa) puede tener **cero filas** la primera vez que
  una empresa personaliza un documento, y un `FOR UPDATE` sobre un resultado vacío
  no bloquea nada: dos primeras-subidas concurrentes seguirían compitiendo sin
  protección. `manual_documents` siempre tiene fila (se creó en
  `createManualDocument`) y es el punto natural de serialización.
  Contrapartida aceptada: sobre-serializa levemente entre dos empresas distintas
  que suban a la vez el mismo documento. Es estrictamente mejor que la carrera actual.
- **No añadir exports nuevos a `apps/web/lib/actions/manual.ts`.** Es un módulo
  `"use server"`: todo export async queda expuesto como RPC público. Si hiciera
  falta un helper compartido, iría en un módulo sin `"use server"` — pero en este
  plan **no hace falta**: el fix es puntual dentro de la función.
- El script de concurrencia es **desechable y NO se commitea**.
- No tocar `apps/web/lib/actions/risk.ts` ni `apps/web/components/evidence-detail.tsx`:
  son del plan 01-04.
</constraints>

<tasks>

<task type="auto">
  <name>Tarea 1: lock de fila en uploadCompanyDocument</name>
  <files>apps/web/lib/actions/manual.ts</files>
  <action>
En `apps/web/lib/actions/manual.ts`, función `uploadCompanyDocument` (líneas
859-906). Sustituir el bloque de las líneas 880-900 —el `findFirst` suelto más el
`create` suelto, hoy dos transacciones implícitas independientes— por una sola
transacción interactiva con bloqueo de fila:

```typescript
  await db.$transaction(async (tx) => {
    // Serializa la siguiente versión de este documento para cualquier empresa.
    // Se bloquea `manual_documents` y no `company_documents` a propósito: el par
    // (documento, empresa) puede no tener ninguna fila todavía, y un FOR UPDATE
    // sobre cero filas no bloquea nada.
    await tx.$queryRaw`SELECT 1 FROM manual_documents WHERE id = ${input.documentId} FOR UPDATE`;

    const last = await tx.companyDocument.findFirst({
      where: { documentId: input.documentId, companyId: assignment.companyId },
      orderBy: { version: "desc" },
      select: { version: true },
    });

    // Append-only: la versión anterior se conserva y la vigente es la mayor.
    await tx.companyDocument.create({
      data: {
        documentId: input.documentId,
        companyId: assignment.companyId,
        version: (last?.version ?? 0) + 1,
        codeOverride: optionalText(input.codeOverride, 60),
        fileKey: input.file.fileKey,
        fileName: input.file.fileName,
        fileSize: input.file.fileSize,
        mimeType: input.file.mimeType,
        notes: optionalText(input.notes, 500),
        uploadedById: user.id,
      },
      select: { id: true },
    });
  });
```

**Qué NO cambia:**
- El chequeo de pertenencia de las líneas 869-878 (`db.manualDocument.findUnique`
  + comparación `doc.manualId !== assignment.manualId`) se queda **fuera** de la
  transacción, tal cual está. Es una validación de pertenencia, no parte de la carrera.
- Los tres `revalidatePath` y el `return { success: true as const }` finales
  (líneas 902-905) se quedan fuera de la transacción, tal cual están.
- La firma de la función y su valor de retorno no cambian.

Riesgo de deadlock: verificado en la investigación que ningún otro flujo bloquea
`company_documents` antes que `manual_documents` dentro de la misma transacción,
así que no hay contención de orden inverso.
  </action>
  <verify>
    <automated>
# El lock está sobre la tabla correcta, dentro de una transacción
grep -n 'FOR UPDATE' apps/web/lib/actions/manual.ts
grep -n 'db.\$transaction' apps/web/lib/actions/manual.ts

# No debe quedar ningún db.companyDocument.create fuera de la transacción
grep -n 'db\.companyDocument\.create' apps/web/lib/actions/manual.ts || echo "OK: ya no hay create suelto"

# No se añadieron exports nuevos al módulo "use server"
git diff apps/web/lib/actions/manual.ts | grep '^+export' || echo "OK: sin exports nuevos"

pnpm exec turbo run check-types
    </automated>
  </verify>
  <done>
`manual.ts` contiene exactamente un `SELECT 1 FROM manual_documents WHERE id = ... FOR UPDATE`
dentro de un `db.$transaction`, seguido de `tx.companyDocument.findFirst` y
`tx.companyDocument.create`. No queda ningún `db.companyDocument.create` fuera de
transacción. `check-types` limpio. Sin exports nuevos.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: demostrar la concurrencia con un script desechable (control + fix)</name>
  <files>packages/db/prisma/_tmp-concurrency.ts (temporal, NO se commitea)</files>
  <action>
No hay suite de pruebas en el repo, así que el criterio 3 de la fase se demuestra
con un script desechable contra la base local (`localhost:5435`, contenedor
`prol-db`, ya sembrada). El script debe tener **dos fases**, porque una prueba que
no puede fallar no prueba nada.

Crear `packages/db/prisma/_tmp-concurrency.ts`:

1. **Fixture.** Reutilizar filas del seed y crear lo mínimo:
   ```typescript
   const company = await db.company.findFirstOrThrow();       // Acme Corp del seed
   const author  = await db.user.findFirstOrThrow();
   const manual  = await db.manual.create({
     data: { tenantId: company.tenantId, createdById: author.id, title: "TMP concurrencia" },
   });
   const doc = await db.manualDocument.create({
     data: { manualId: manual.id, code: "TMP-01", name: "Documento de prueba" },
   });
   ```

2. **Fase A — control, reproduce el bug (sin lock).** Dos escrituras que replican
   el código ANTERIOR (leer el máximo, luego insertar, sin transacción ni lock),
   sincronizadas con una barrera para que ambas lean antes de que ninguna escriba:
   ```typescript
   async function sinLock(barrera: Promise<void>, liberar: () => void) {
     const last = await db.companyDocument.findFirst({
       where: { documentId: doc.id, companyId: company.id },
       orderBy: { version: "desc" }, select: { version: true },
     });
     liberar();
     await barrera;                       // ambas han leído ya
     return db.companyDocument.create({
       data: {
         documentId: doc.id, companyId: company.id,
         version: (last?.version ?? 0) + 1,
         fileKey: "tmp/a.pdf", fileName: "a.pdf", fileSize: 1, mimeType: "application/pdf",
       },
     });
   }
   ```
   Lanzar las dos con `Promise.allSettled` y **exigir** que al menos una rechace
   con el código Prisma `P2002` (violación de unicidad). Si ninguna falla, el
   experimento no vale y hay que abortar con mensaje explícito.

3. **Fase B — con lock, el código nuevo.** Copiar **literalmente** el cuerpo de la
   transacción que la tarea 1 dejó en `uploadCompanyDocument` (`FOR UPDATE` sobre
   `manual_documents`, luego `findFirst`, luego `create`). Aquí **no se usa
   barrera**: la barrera provocaría un interbloqueo artificial porque la segunda
   transacción no puede llegar a leer hasta que la primera haga commit. Lanzar las
   dos con `Promise.all` y exigir:
   - ninguna rechaza,
   - las dos filas creadas tienen versiones **consecutivas** (`v` y `v+1`).

4. **Limpieza.** En un `finally`, borrar el `Manual` creado (el `onDelete: Cascade`
   de `ManualDocument` y `CompanyDocument` se lleva el resto) y `db.$disconnect()`.
   La base local debe quedar como estaba.

5. **Ejecutar y borrar el script:**
   ```bash
   pnpm --filter @prol/db exec tsx prisma/_tmp-concurrency.ts
   rm packages/db/prisma/_tmp-concurrency.ts
   ```

Copiar la salida de las dos fases al SUMMARY: es la única evidencia del criterio 3.
  </action>
  <verify>
    <automated>
pnpm --filter @prol/db exec tsx prisma/_tmp-concurrency.ts
# Salida esperada:
#   FASE A (sin lock): al menos una escritura falló con P2002  ← el bug es real
#   FASE B (con lock): versiones 1 y 2, cero errores           ← el fix funciona

rm -f packages/db/prisma/_tmp-concurrency.ts
git status --porcelain | grep '_tmp-concurrency' && echo "ERROR: el script sigue ahí" || echo "OK: script desechable eliminado"
    </automated>
  </verify>
  <done>
La fase A produce al menos un `P2002` (el bug es reproducible). La fase B produce
dos filas con versiones consecutivas y cero errores. El script ya no existe y
`git status --porcelain` no lo menciona. La base local queda sin el manual temporal.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: eliminar el helper de data-URL duplicado en la ruta PDF de evaluaciones</name>
  <files>apps/web/app/api/evaluations/results/[assignmentId]/pdf/route.tsx</files>
  <action>
En `apps/web/app/api/evaluations/results/[assignmentId]/pdf/route.tsx` (1265 líneas):

1. Borrar el bloque de comentario y la función `loadAsDataUrl` completa
   (líneas 19-52: el JSDoc de 20-24 más la función de 25-52).
2. Borrar los tres imports que quedan huérfanos —verificado por grep que sólo se
   usaban dentro de esa función—:
   - línea 2: `import { readFile } from "node:fs/promises";`
   - línea 3: `import { join } from "node:path";`
   - línea 18: `import { resolveUploadDir } from "@/lib/upload-paths";`
   Ojo: la línea 34 usa `rest.join("/")`, que es el método de Array y **no** el
   import de `node:path`. Al borrar la función desaparece también ese uso.
3. Añadir, en el grupo de imports locales (junto a
   `import { getEvaluationResults } from "@/lib/queries/evaluation";`, línea 17):
   ```typescript
   import { loadUploadAsDataUrl } from "@/lib/certificate-assets";
   ```
4. En la línea 1223, cambiar la llamada:
   ```typescript
   const logoDataUrl = await loadUploadAsDataUrl(tenant.logo);
   ```

**Por qué es equivalente.** La única diferencia entre las dos funciones es la línea
28 del helper local, `if (url.startsWith("data:")) return url;`. Es código muerto
en este call site: `apps/web/lib/actions/tenant.ts` (líneas 31-37) rechaza al
guardar cualquier logo que no empiece por `/uploads/`, así que `tenant.logo` nunca
puede ser una `data:` URL en runtime. El cambio de comportamiento es cero.

No cambiar `apps/web/lib/certificate-assets.ts`: es el origen único y ya funciona
para las otras tres rutas PDF (`dc3`, `certificates/preview`, `certificates/[id]`).
  </action>
  <verify>
    <automated>
# El helper duplicado ya no existe y el canónico se importa
grep -n 'loadAsDataUrl' 'apps/web/app/api/evaluations/results/[assignmentId]/pdf/route.tsx'
grep -n 'certificate-assets' 'apps/web/app/api/evaluations/results/[assignmentId]/pdf/route.tsx'

# Imports huérfanos eliminados
grep -nE 'node:fs/promises|node:path|upload-paths' 'apps/web/app/api/evaluations/results/[assignmentId]/pdf/route.tsx' || echo "OK: sin imports huerfanos"

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep 'problems'
    </automated>
  </verify>
  <done>
El archivo no contiene ninguna definición de `loadAsDataUrl`; la única referencia
a un helper de data-URL es `loadUploadAsDataUrl` importado de
`@/lib/certificate-assets` y usado en el antiguo call site. Los imports de
`node:fs/promises`, `node:path` y `@/lib/upload-paths` ya no están.
`check-types` limpio y `lint` sigue en `✖ 81 problems (0 errors, 81 warnings)`.
  </done>
</task>

</tasks>

<verification>
Puertas transversales del milestone (obligatorias antes de cerrar el plan):

```bash
pnpm exec turbo run check-types          # limpio
pnpm exec turbo run lint 2>&1 | grep 'problems'   # "✖ 81 problems (0 errors, 81 warnings)"
pnpm exec turbo run build                # verde
```

**Atención con `lint`:** `apps/web` corre eslint con `--max-warnings 0`, así que en
la línea base el comando **sale con exit 1** mostrando exactamente
`✖ 81 problems (0 errors, 81 warnings)`. Eso es lo esperado. Este plan **elimina
código** (el helper duplicado), así que si el número baja de 81, comprobar que la
advertencia que desapareció venía de la función borrada y anotarlo en el SUMMARY
como nueva línea base; nunca puede subir ni aparecer un error.

Comprobaciones específicas del plan:
- La fase A del script de concurrencia falla con `P2002` (la prueba puede fallar).
- La fase B produce versiones consecutivas sin errores.
- `git status --porcelain` no menciona `_tmp-concurrency`.
- El diff toca exactamente dos archivos.
</verification>

<success_criteria>
1. Dos subidas simultáneas del mismo documento para la misma empresa producen
   versiones consecutivas, no un error de unicidad. (Criterio 3 de la fase,
   demostrado por las fases A y B del script.)
2. El lock está sobre `manual_documents`, dentro de `db.$transaction`, y el chequeo
   de pertenencia sigue fuera de ella.
3. `apps/web/lib/actions/manual.ts` no exporta nada nuevo (módulo `"use server"`).
4. Existe una sola implementación de "cargar un upload como data-URL" en el repo,
   en `apps/web/lib/certificate-assets.ts`, y la ruta PDF de evaluaciones la usa.
5. `check-types` limpio, `lint` en 0 errores y ≤81 advertencias, `build` verde.
6. El script de concurrencia no está en el repositorio.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/01-higiene-y-operacion/01-02-SUMMARY.md`.

Registrar en el SUMMARY:
- La salida literal de las fases A y B del script de concurrencia (única evidencia
  del criterio 3, ya que el script no se conserva).
- El contenido del script, o su idea en 10 líneas, por si la fase 3 necesita
  repetir el experimento sobre el versionado de documentos nativos.
- Si el número de advertencias de lint bajó de 81, la nueva línea base y por qué.
</output>
