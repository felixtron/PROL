---
phase: 01-higiene-y-operacion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/backup.sh
  - scripts/README.md
  - docker-compose.prod.yml
  - DEPLOY.md
autonomous: true
requirements: [OPS-01, OPS-02]

must_haves:
  truths:
    - "Una corrida de scripts/backup.sh produce un tarball con el contenido del volumen privado, además del de uploads y del dump de la base."
    - "El tarball privado se poda con la misma antigüedad que el de uploads (RETAIN_UPLOADS_WEEKS)."
    - "El tarball privado se empuja al remoto off-site por rclone igual que los otros dos artefactos."
    - "docker-compose.prod.yml declara un volumen que resuelve al nombre físico prol_prol_private que producción ya creó a mano."
    - "El servicio web monta ese volumen en /app/private-uploads y recibe PRIVATE_UPLOAD_DIR apuntando ahí."
    - "scripts/README.md y DEPLOY.md §7b describen el artefacto nuevo y la regla de sincronía compose ↔ quadlet."
  artifacts:
    - path: "scripts/backup.sh"
      provides: "Respaldo diario de db + uploads + privado"
      contains: "PRIVATE_VOLUME"
    - path: "docker-compose.prod.yml"
      provides: "Declaración y montaje del volumen privado"
      contains: "prol_private"
    - path: "scripts/README.md"
      provides: "Tabla de artefactos y receta de restauración del volumen privado"
      contains: "private_"
    - path: "DEPLOY.md"
      provides: "§7b coherente: compose y quadlet se mantienen sincronizados"
      contains: "docker-compose.prod.yml"
  key_links:
    - from: "docker-compose.prod.yml (clave de volumen prol_private)"
      to: "volumen físico prol_prol_private del host"
      via: "prefijo de proyecto que antepone Docker Compose"
      pattern: "name: prol_prol_private"
    - from: "docker-compose.prod.yml (environment del servicio web)"
      to: "resolvePrivateUploadDir() en apps/web/lib/upload-paths.ts"
      via: "PRIVATE_UPLOAD_DIR=/app/private-uploads"
      pattern: "PRIVATE_UPLOAD_DIR"
    - from: "scripts/backup.sh (PRIVATE_VOLUME)"
      to: "volumen físico prol_prol_private"
      via: "docker run -v \"$PRIVATE_VOLUME\":/data:ro"
      pattern: "PRIVATE_VOLUME.*prol_prol_private"
---

<objective>
Cerrar el agujero de respaldo del volumen de evidencias confidenciales y dejar
`docker-compose.prod.yml` coherente con lo que el quadlet de producción ya monta
a mano.

Purpose: hoy el dato más sensible de la plataforma —las evidencias de
cumplimiento de cada cliente— no tiene ninguna copia. Es el riesgo de mayor
severidad del milestone, y la fase 2 (migración a R2) no puede empezar sin un
punto de restauración.
Output: `scripts/backup.sh` produciendo un tercer artefacto, `docker-compose.prod.yml`
declarando el volumen privado, y la documentación de operación al día.
</objective>

<execution_context>
@/Users/flx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-higiene-y-operacion/01-CONTEXT.md
@.planning/phases/01-higiene-y-operacion/01-RESEARCH.md

@scripts/backup.sh
@scripts/README.md
@docker-compose.prod.yml
@apps/web/lib/upload-paths.ts

<interfaces>
<!-- Contratos que este plan debe respetar. No hace falta explorar el codebase. -->

Nombre físico del volumen en producción (DEPLOY.md líneas 217-220, ya creado a mano):
```
podman volume create prol_prol_private
# En /etc/containers/systemd/prol-web-1.container:
#   Volume=prol_prol_private:/app/private-uploads:Z
```

Variable que la aplicación lee (apps/web/lib/upload-paths.ts líneas 61-76):
```typescript
export function resolvePrivateUploadDir(subdir: string): string {
  const base = process.env.PRIVATE_UPLOAD_DIR;
  if (base) return join(base, subdir);
  // ...si falta, cae a <cwd>/private-uploads (NO persistente) y avisa por log
  return join(process.cwd(), "private-uploads", subdir);
}
```

Convención de nombres de Docker Compose (verificada ejecutando
`docker compose -f docker-compose.prod.yml -p prol config`, salida actual):
```yaml
volumes:
  prol_db_data:
    name: prol_prol_db_data
  prol_uploads:
    name: prol_prol_uploads
```
Compose antepone el nombre del proyecto (`prol`) a cada clave de volumen no
`external`. Por eso la clave nueva es `prol_private` — que resuelve a
`prol_prol_private` — y NO `prol_prol_private`, que resolvería a
`prol_prol_prol_private` y no coincidiría con nada real.
</interfaces>
</context>

<constraints>
- **NO tocar las líneas 44 ni 55 de `scripts/backup.sh`** (los `docker exec` / `docker run`
  existentes). Migrar el script de `docker` a `podman` está **diferido** en
  `01-CONTEXT.md`: depende de un diagnóstico por SSH que no ejecuta esta fase.
  El bloque nuevo del volumen privado se escribe con la **misma** invocación
  `docker` que ya usa el bloque de uploads. Si el diff toca esas líneas, se salió
  del alcance.
- **Ningún paso de este plan se ejecuta contra el VPS de producción.** Sólo se
  editan archivos del repositorio y se hacen pruebas locales.
- Texto de cara al usuario y comentarios nuevos en español; identificadores y
  nombres de variable de shell en inglés, como el resto del archivo.
</constraints>

<tasks>

<task type="auto">
  <name>Tarea 1: segundo tarball del volumen privado en backup.sh</name>
  <files>scripts/backup.sh</files>
  <action>
Editar `scripts/backup.sh` (83 líneas hoy) con cinco cambios quirúrgicos. Todos
replican exactamente el estilo del bloque de uploads que ya existe.

1. **Variable** — después de la línea 27 (`UPLOADS_VOLUME="${UPLOADS_VOLUME:-prol_prol_uploads}"`):
   ```bash
   PRIVATE_VOLUME="${PRIVATE_VOLUME:-prol_prol_private}"
   ```
   El default `prol_prol_private` es el nombre físico que `DEPLOY.md` línea 218 ya
   crea en el host.

2. **Directorio de salida** — extender la línea 37 existente (no añadir un `mkdir`
   aparte; `set -euo pipefail` de la línea 22 ya aborta si falla):
   ```bash
   mkdir -p "$BACKUP_DIR/db" "$BACKUP_DIR/uploads" "$BACKUP_DIR/private"
   ```

3. **Bloque nuevo** — insertar después de la línea 61 (fin del bloque de uploads) y
   **renumerar los banners siguientes**: el nuevo pasa a ser `3.`, el off-site
   actual pasa de `3.` a `4.` y la retención de `4.` a `5.`.
   ```bash
   # ── 3. Private volume tarball (evidencias y plantillas confidenciales) ──────
   private_file="$BACKUP_DIR/private/private_${date_ymd}.tar.gz"
   log "Private tarball → $private_file"
   docker run --rm \
     -v "$PRIVATE_VOLUME":/data:ro \
     -v "$BACKUP_DIR/private":/out \
     alpine:3.20 \
     sh -c "cd /data && tar czf /out/$(basename "$private_file").tmp . && mv /out/$(basename "$private_file").tmp /out/$(basename "$private_file")"
   private_size="$(du -h "$private_file" | cut -f1)"
   log "Private tarball OK ($private_size)"
   ```

4. **Off-site** — dentro del `if` de rclone (líneas 64-72 actuales), añadir una
   tercera línea con la misma forma que las dos existentes:
   ```bash
   rclone copy "$private_file" "$RCLONE_REMOTE/private/" --transfers=2 || log "WARN rclone private failed"
   ```
   Decisión cerrada del usuario: el volumen privado SÍ se replica off-site. Dejarlo
   fuera haría que el dato más sensible fuera el único sin copia remota.

5. **Retención** — añadir tras el bloque de poda de uploads (líneas 78-80), con
   **la misma** política `RETAIN_UPLOADS_WEEKS`:
   ```bash
   log "Pruning private tarballs beyond last $RETAIN_UPLOADS_WEEKS weeks"
   find "$BACKUP_DIR/private" -name 'private_*.tar.gz' -type f \
     -mtime +"$((RETAIN_UPLOADS_WEEKS * 7))" -delete
   ```

6. **Cabecera** — actualizar el comentario de las líneas 5-11 para que liste el
   tercer artefacto (`/opt/prol/backups/private/private_YYYYMMDD.tar.gz`) y su
   retención (misma que uploads).
  </action>
  <verify>
    <automated>
# Sintaxis
bash -n scripts/backup.sh

# El diff NO puede tocar las invocaciones docker preexistentes (db línea 43,
# uploads línea 54). Ojo: el bloque privado nuevo empieza con `docker run --rm \`,
# texto IDÉNTICO al de uploads, así que filtrar por contenido no distingue una
# línea añadida de una modificada. La señal fiable es la ELIMINACIÓN: cualquier
# modificación de una línea preexistente produce su `-` en el diff.
# Sin `|| true`: esto DEBE poder fallar.
if git diff -U0 -- scripts/backup.sh | grep -qE '^-[[:space:]]*docker (exec|run)'; then
  echo "FALLO: se modificó o eliminó una invocación docker preexistente"
  git diff -U0 -- scripts/backup.sh | grep -E '^-[[:space:]]*docker (exec|run)'
  exit 1
fi

# Invariante de recuento: exactamente 1 docker exec (db) y 2 docker run
# (uploads + privado). Cualquier otra cifra significa que sobra o falta un bloque.
n_exec=$(grep -cE '^[[:space:]]*docker exec' scripts/backup.sh)
n_run=$(grep -cE '^[[:space:]]*docker run' scripts/backup.sh)
if [ "$n_exec" -ne 1 ] || [ "$n_run" -ne 2 ]; then
  echo "FALLO: se esperaban 1 docker exec y 2 docker run; hay $n_exec y $n_run"
  exit 1
fi

# Humo real: dos volúmenes falsos + la base local, corrida completa del script
SB=$(mktemp -d)
docker volume create prol_test_uploads >/dev/null
docker volume create prol_test_private >/dev/null
docker run --rm -v prol_test_uploads:/d alpine:3.20 sh -c 'echo up > /d/u.txt'
docker run --rm -v prol_test_private:/d alpine:3.20 sh -c 'mkdir -p /d/evidence && echo secreto > /d/evidence/e.txt'
BACKUP_DIR="$SB" DB_CONTAINER=prol-db DB_USER=prol DB_NAME=prol \
  UPLOADS_VOLUME=prol_test_uploads PRIVATE_VOLUME=prol_test_private \
  bash scripts/backup.sh
ls "$SB"/db "$SB"/uploads "$SB"/private
tar tzf "$SB"/private/private_*.tar.gz | grep 'evidence/e.txt'
docker volume rm prol_test_uploads prol_test_private
rm -rf "$SB"
    </automated>
  </verify>
  <done>
`bash -n` limpio. La corrida de humo termina en exit 0 y deja los tres
directorios (`db/`, `uploads/`, `private/`) con un archivo cada uno; el
`tar tzf` del tarball privado lista `./evidence/e.txt`. El diff no modifica
las líneas 44 ni 55 originales.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: declarar, montar y apuntar el volumen privado en el compose de producción</name>
  <files>docker-compose.prod.yml</files>
  <action>
Tres cambios en `docker-compose.prod.yml` (107 líneas hoy):

1. **Montaje** — en el bloque `volumes:` del servicio `web` (líneas 64-66), añadir
   una segunda entrada bajo la existente:
   ```yaml
       volumes:
         # Persist uploaded files (PDFs, thumbnails, avatars) across rebuilds
         - prol_uploads:/app/apps/web/public/uploads
         # Evidencias y plantillas por empresa: FUERA de public/, sólo salen por /files/*
         - prol_private:/app/private-uploads
   ```

2. **Variable de entorno** — hoy `PRIVATE_UPLOAD_DIR` no aparece en ninguna parte
   del archivo. Añadirla al bloque `environment:` del servicio `web`, como grupo
   propio con su comentario, justo después del grupo `# Database` (línea 34):
   ```yaml
         # Archivos confidenciales (evidencias y plantillas por empresa)
         PRIVATE_UPLOAD_DIR: ${PRIVATE_UPLOAD_DIR:-/app/private-uploads}
   ```
   Sin ella, `resolvePrivateUploadDir()` cae a `<cwd>/private-uploads`, que en el
   contenedor NO es el punto de montaje: el volumen quedaría declarado pero sin
   usar y los archivos se seguirían perdiendo al recrear el contenedor.

3. **Declaración** — en el bloque `volumes:` de nivel superior (líneas 98-100):
   ```yaml
   volumes:
     prol_db_data:
       prol_uploads:
       prol_private:
   ```
   (respetando la indentación existente de dos espacios: `  prol_private:`)

**Clave correcta: `prol_private`, NO `prol_prol_private`.** Compose antepone el
nombre del proyecto, así que `prol_private` resuelve a `prol_prol_private`, que es
el volumen que el host ya tiene. Escribir `prol_prol_private` produciría
`prol_prol_prol_private`, que no existe en ninguna parte.
  </action>
  <verify>
    <automated>
# El compose sigue siendo válido
docker compose -f docker-compose.prod.yml -p prol config --quiet

# El nombre resuelto debe ser exactamente prol_prol_private
docker compose -f docker-compose.prod.yml -p prol config 2>/dev/null | sed -n '/^volumes:/,/^networks:/p'

# El montaje y la variable llegan al servicio web
docker compose -f docker-compose.prod.yml -p prol config 2>/dev/null | grep -E 'PRIVATE_UPLOAD_DIR|/app/private-uploads'
    </automated>
  </verify>
  <done>
`docker compose config` sale sin error. El bloque `volumes:` resuelto muestra
`prol_private:` con `name: prol_prol_private`. El servicio `web` muestra
`PRIVATE_UPLOAD_DIR: /app/private-uploads` y un montaje cuyo `target` es
`/app/private-uploads`.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: documentar el artefacto nuevo y la regla de sincronía compose ↔ quadlet</name>
  <files>scripts/README.md, DEPLOY.md</files>
  <action>
1. **`scripts/README.md`** (63 líneas hoy):
   - Añadir una tercera fila a la tabla de artefactos (líneas 10-13):
     `| Private volume tarball | /opt/prol/backups/private/private_<date>.tar.gz | every run |`
   - Actualizar la línea de retención (línea 15) para que mencione que los tarballs
     privados usan la misma retención que los de uploads.
   - Añadir, tras la receta de restauración del volumen de uploads (líneas 52-62),
     la receta equivalente para el privado, con el mismo estilo:
     ```bash
     docker compose stop web
     docker run --rm \
       -v prol_prol_private:/data \
       -v /opt/prol/backups/private:/in:ro \
       alpine:3.20 \
       sh -c 'cd /data && tar xzf /in/private_20260424.tar.gz'
     docker compose up -d web
     ```

2. **`DEPLOY.md` §7b "Gestion documental y evidencias"** (a partir de la línea 207):
   Dentro del apartado **Volumen privado**, tras el bloque de comandos de podman,
   añadir un párrafo corto que deje escrito:
   - Que `docker-compose.prod.yml` declara el mismo volumen con la clave
     `prol_private`, que Compose prefija a `prol_prol_private`, el mismo nombre que
     crea el `podman volume create` de arriba.
   - Que **el compose y el quadlet se mantienen sincronizados**: producción corre
     con quadlets, pero el compose es la receta para reconstruir desde cero, y si
     divergen el siguiente que reconstruya pierde el volumen.
   - Que `backup.sh` respalda ese volumen a diario (`private_<date>.tar.gz`).

   No reescribir el resto de §7b. No cambiar la ortografía existente del archivo
   (DEPLOY.md está escrito sin acentos; mantener ese estilo en el texto nuevo).
  </action>
  <verify>
    <automated>
grep -n 'private_' scripts/README.md
grep -n 'prol_prol_private' scripts/README.md DEPLOY.md
grep -n 'docker-compose.prod.yml' DEPLOY.md | head
    </automated>
  </verify>
  <done>
`scripts/README.md` lista el tarball privado en su tabla, su retención y su
receta de restauración. `DEPLOY.md` §7b menciona la clave `prol_private` del
compose, el prefijo que la convierte en `prol_prol_private`, y la regla explícita
de mantener compose y quadlet sincronizados.
  </done>
</task>

</tasks>

<verification>
Puertas transversales del milestone (obligatorias antes de cerrar el plan). Este
plan no toca código TypeScript, así que las tres deben salir **idénticas a la
línea base**:

```bash
pnpm exec turbo run check-types          # limpio
pnpm exec turbo run lint 2>&1 | grep 'problems'   # "✖ 81 problems (0 errors, 81 warnings)"
pnpm exec turbo run build                # verde
```

**Atención con `lint`:** `apps/web` corre eslint con `--max-warnings 0`, así que
en la línea base el comando **sale con exit 1** mostrando exactamente
`✖ 81 problems (0 errors, 81 warnings)`. Eso es lo esperado, no un fallo del
plan. Lo que no se admite es un número distinto de 81 ni un solo error.

Comprobaciones específicas del plan:
- `bash -n scripts/backup.sh` limpio.
- La corrida de humo de la tarea 1 produce los tres artefactos.
- `docker compose -f docker-compose.prod.yml -p prol config` resuelve
  `prol_private` → `prol_prol_private`.
- `git diff scripts/backup.sh` no toca las invocaciones `docker exec`/`docker run`
  preexistentes (las de db y uploads).
</verification>

<success_criteria>
1. Una corrida de `scripts/backup.sh` con `PRIVATE_VOLUME` apuntando a un volumen
   con contenido produce `private_<fecha>.tar.gz` con ese contenido dentro.
   (Criterio 1 de la fase.)
2. `docker compose config` sobre `docker-compose.prod.yml` declara un volumen que
   resuelve a `prol_prol_private` y lo monta en `/app/private-uploads` en el
   servicio `web`, con `PRIVATE_UPLOAD_DIR` apuntando ahí. (Criterio 2 de la fase.)
3. El tarball privado se poda con `RETAIN_UPLOADS_WEEKS` y se empuja por rclone,
   exactamente igual que el de uploads.
4. `scripts/README.md` y `DEPLOY.md` §7b describen el artefacto y la sincronía
   compose ↔ quadlet.
5. `check-types` limpio, `lint` en 81/0, `build` verde.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/01-higiene-y-operacion/01-01-SUMMARY.md`.

Registrar en el SUMMARY:
- El comando exacto de la corrida de humo, para reusarlo en futuras fases.
- Que la migración `docker` → `podman` de `backup.sh` sigue **diferida** y por qué
  (bloqueo declarado en STATE.md: hace falta el diagnóstico por SSH de si el cron
  corre siquiera; arreglar el script no sirve si nunca se ejecuta).
</output>
