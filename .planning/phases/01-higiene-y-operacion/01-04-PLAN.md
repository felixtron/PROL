---
phase: 01-higiene-y-operacion
plan: 04
type: execute
wave: 2
depends_on: ["01-03"]
files_modified:
  - packages/shared/src/evidence-snapshot.ts
  - packages/shared/src/index.ts
  - apps/web/components/evidence-detail.tsx
  - apps/web/lib/actions/risk.ts
autonomous: false
requirements: [OPS-04]

must_haves:
  truths:
    - "Una evidencia de matriz de riesgos anterior al cambio se sigue viendo con su tabla completa, sin tocar la base de datos."
    - "Una evidencia entregada después del cambio lleva snapshotVersion y kind, y también se ve."
    - "Un formSnapshot que no encaja en ninguna rama degrada a null y la ficha se pinta sin la tabla, en vez de romper la pantalla."
    - "Un objeto con snapshotVersion distinto de 1 NO cae en la rama legacy: se rechaza."
    - "No queda ningún cast `as` sobre formSnapshot en el codebase."
  artifacts:
    - path: "packages/shared/src/evidence-snapshot.ts"
      provides: "Unión de Zod v1 + legacy y lector degradante parseEvidenceSnapshot"
      exports: ["evidenceSnapshotSchema", "parseEvidenceSnapshot", "EVIDENCE_SNAPSHOT_VERSION"]
      min_lines: 50
    - path: "packages/shared/src/index.ts"
      provides: "Barrel que reexporta el schema nuevo"
      contains: "./evidence-snapshot"
    - path: "apps/web/components/evidence-detail.tsx"
      provides: "Lectura validada del snapshot, sin cast"
      contains: "parseEvidenceSnapshot"
    - path: "apps/web/lib/actions/risk.ts"
      provides: "Escritura del snapshot con discriminador"
      contains: "snapshotVersion"
  key_links:
    - from: "apps/web/components/evidence-detail.tsx"
      to: "packages/shared/src/evidence-snapshot.ts"
      via: "import { parseEvidenceSnapshot } from \"@prol/shared\""
      pattern: "parseEvidenceSnapshot\\(evidence\\.formSnapshot\\)"
    - from: "apps/web/lib/actions/risk.ts (submitRiskMatrix)"
      to: "rama v1 del schema"
      via: "snapshotVersion + kind en el objeto que guarda"
      pattern: "snapshotVersion"
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/evidence-snapshot.ts"
      via: "export * from"
      pattern: "export \\* from \"./evidence-snapshot\""
---

<objective>
Sustituir el cast `as` sin validación en runtime sobre `Evidence.formSnapshot` por
una unión de Zod versionada con rama legacy, y hacer que las escrituras nuevas
lleven el discriminador.

Purpose: hoy `evidence-detail.tsx` afirma por `as` que el JSON tiene una forma
concreta. Un cast de TypeScript no comprueba nada en runtime: si mañana el
escritor cambia de forma —y la fase 5, Registros nativos, va a escribir en esa
misma columna con una forma completamente distinta— la ficha de revisión renderiza
datos incorrectos en silencio. Un revisor aprobaría algo que no es lo que está viendo.
Output: `packages/shared/src/evidence-snapshot.ts`, el barrel actualizado, el lector
validando y el escritor discriminando.
</objective>

<execution_context>
@/Users/flx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-higiene-y-operacion/01-CONTEXT.md
@.planning/phases/01-higiene-y-operacion/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

<!-- Este SUMMARY trae el evidenceId legacy, su JSON literal y las URLs de
     re-verificación. Es la razón por la que este plan va en la ola 2. -->
@.planning/phases/01-higiene-y-operacion/01-03-SUMMARY.md

@packages/shared/src/lesson-blocks.ts
@packages/shared/src/index.ts
@apps/web/components/evidence-detail.tsx
@apps/web/lib/actions/risk.ts

<interfaces>
<!-- Contratos exactos. No hace falta explorar el codebase. -->

**Patrón de referencia** (`packages/shared/src/lesson-blocks.ts` + su lector en
`apps/web/lib/actions/lesson-blocks.ts` líneas 34-37):
```typescript
function readContent(content: unknown): MultiLessonContent {
  const parsed = multiLessonContentSchema.safeParse(content);
  return parsed.success ? parsed.data : { blocks: [] };
}
```

**Barrel actual** (`packages/shared/src/index.ts`, archivo completo — 6 líneas):
```typescript
// @prol/shared - Types, Zod schemas, constants
export * from "./constants";
export * from "./types";
export * from "./schemas";
export * from "./video";
export * from "./lesson-blocks";
```

**El cast a eliminar** (`apps/web/components/evidence-detail.tsx` líneas 72-75):
```typescript
  const snapshot = evidence.formSnapshot as
    | { items?: RiskSnapshotItem[]; config?: unknown; periodLabel?: string | null }
    | null;
  const riskConfig = parseRiskConfig(snapshot?.config);
```
Con `interface RiskSnapshotItem` declarada localmente en las líneas 35-44 del mismo
archivo. Consumo: línea 166 (`{snapshot?.items?.length ? (`) y línea 181
(`{snapshot.items.map((item, i) => {`).

**El escritor** (`apps/web/lib/actions/risk.ts` líneas 225-245, único escritor del
codebase):
```typescript
await db.evidence.update({
  where: { id: result.evidenceId! },
  data: {
    formSnapshot: {
      title: full.title,
      periodLabel: full.periodLabel,
      config: full.config,
      submittedAt: submittedAt.toISOString(),
      items: full.items.map((i) => ({ type, description, probability, impact, score, level, actions, responsible })),
    },
  },
});
```

**Consumidor de `config`** (`apps/web/lib/compliance.ts` línea 189), acepta `unknown`
y sanea campo a campo — no hay que validar `config` en el schema:
```typescript
export function parseRiskConfig(raw: unknown): RiskMatrixConfig
```

**Sólo `RISK_MATRIX` escribe `formSnapshot`.** `EvidenceRequirementKind` es
`FILE | RISK_MATRIX | EVALUATION_LINK`; las otras dos dejan la columna en `null`.
La unión sólo necesita una rama real más la legacy.
</interfaces>
</context>

<constraints>
- **Prisma no cambia.** `formSnapshot` ya es `Json?`. No hay migración de datos ni
  `db push`: las filas actuales se leen tal como están, por la rama legacy.
- **Nada de `z.discriminatedUnion`.** Zod exige que la clave discriminadora exista
  y sea literal en TODAS las ramas; la rama legacy no la tiene. Hay que usar
  `z.union` con la rama v1 primero.
- **Al leer se usa `safeParse` con degradación limpia, nunca `parse`.** Una fila
  corrupta debe dejar la ficha sin tabla, no reventar la pantalla del revisor.
- **No borrar ni migrar la evidencia legacy** que creó el plan 01-03: es el sujeto
  de la prueba.
</constraints>

<tasks>

<task type="auto">
  <name>Tarea 1: schema de snapshot de evidencia con rama legacy</name>
  <files>packages/shared/src/evidence-snapshot.ts, packages/shared/src/index.ts</files>
  <action>
Crear `packages/shared/src/evidence-snapshot.ts` siguiendo el estilo de
`lesson-blocks.ts` (comentario de cabecera explicando el porqué, luego schemas,
luego tipos inferidos).

Esta forma exacta **ya está verificada contra Zod 3.24 ejecutando los cuatro casos**
—legacy pasa, v1 pasa, `snapshotVersion: 2` se rechaza en ambas ramas, `null` se
rechaza—, así que no hay que reinventarla:

```typescript
import { z } from "zod";

/** Versión que llevan los snapshots escritos a partir de la fase 1. */
export const EVIDENCE_SNAPSHOT_VERSION = 1 as const;

export const riskSnapshotItemSchema = z.object({
  type: z.enum(["RISK", "OPPORTUNITY"]),
  description: z.string(),
  probability: z.number(),
  impact: z.number(),
  score: z.number(),
  level: z.string().nullable(),
  actions: z.string().nullable(),
  responsible: z.string().nullable(),
});

/** Campos comunes a las dos formas. `config` se deja sin validar a propósito:
 *  `parseRiskConfig()` ya lo sanea campo a campo y degrada al default. */
const riskSnapshotBody = {
  title: z.string().optional(),
  periodLabel: z.string().nullable().optional(),
  config: z.unknown().optional(),
  submittedAt: z.string().optional(),
  items: z.array(riskSnapshotItemSchema).default([]),
};

/** Forma emitida desde que existe el discriminador. */
export const riskSnapshotV1Schema = z.object({
  snapshotVersion: z.literal(EVIDENCE_SNAPSHOT_VERSION),
  kind: z.literal("RISK_MATRIX"),
  ...riskSnapshotBody,
});

/**
 * Forma emitida antes de la fase 1: sin discriminador. Se reconoce por ausencia.
 *
 * `snapshotVersion: z.undefined()` no es decorativo: rechaza explícitamente
 * cualquier objeto que traiga la clave con otro valor, para que una v2 futura mal
 * formada NO caiga aquí en silencio y se renderice como si fuera una matriz de
 * riesgos. Ese render silenciosamente incorrecto es justo el bug que este archivo
 * viene a cerrar.
 */
export const riskSnapshotLegacySchema = z.object({
  snapshotVersion: z.undefined(),
  ...riskSnapshotBody,
});

/** `z.union` y no `z.discriminatedUnion`: la rama legacy no tiene la clave. */
export const evidenceSnapshotSchema = z.union([
  riskSnapshotV1Schema,
  riskSnapshotLegacySchema,
]);

export type RiskSnapshotItem = z.infer<typeof riskSnapshotItemSchema>;
export type RiskSnapshotV1 = z.infer<typeof riskSnapshotV1Schema>;
export type EvidenceSnapshot = z.infer<typeof evidenceSnapshotSchema>;

/**
 * Lee un `Evidence.formSnapshot` degradando a `null`, igual que `readContent()`
 * en `lib/actions/lesson-blocks.ts`. Nunca lanza: una fila corrupta debe dejar la
 * ficha sin tabla, no romper la pantalla del revisor.
 */
export function parseEvidenceSnapshot(value: unknown): EvidenceSnapshot | null {
  const parsed = evidenceSnapshotSchema.safeParse(value);
  return parsed.success ? parsed.data : null;
}
```

Añadir la línea correspondiente al barrel `packages/shared/src/index.ts`:
```typescript
export * from "./evidence-snapshot";
```
  </action>
  <verify>
    <automated>
grep -n 'evidence-snapshot' packages/shared/src/index.ts
grep -nE 'z\.union|discriminatedUnion' packages/shared/src/evidence-snapshot.ts
# No debe aparecer discriminatedUnion.

pnpm exec turbo run check-types
    </automated>
  </verify>
  <done>
`packages/shared/src/evidence-snapshot.ts` existe y exporta
`evidenceSnapshotSchema`, `parseEvidenceSnapshot` y `EVIDENCE_SNAPSHOT_VERSION`.
Usa `z.union`, nunca `z.discriminatedUnion`. El barrel lo reexporta.
`check-types` limpio.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: leer con safeParse y escribir con discriminador</name>
  <files>apps/web/components/evidence-detail.tsx, apps/web/lib/actions/risk.ts</files>
  <action>
**A) Lectura — `apps/web/components/evidence-detail.tsx`:**

1. Sustituir las líneas 72-75:
   ```typescript
     const snapshot = parseEvidenceSnapshot(evidence.formSnapshot);
     const riskConfig = parseRiskConfig(snapshot?.config);
   ```
2. Añadir el import en el **grupo 3** (imports de workspace `@prol/*`), que hoy no
   existe en este archivo: va después de los imports de `next`/externos y **antes**
   del grupo `@/lib/*`, sin línea en blanco entre grupos (convención del repo):
   ```typescript
   import { parseEvidenceSnapshot } from "@prol/shared";
   ```
3. **Borrar la `interface RiskSnapshotItem` local (líneas 35-44).** Sólo la usaba el
   cast; si se deja, eslint la marca como no usada y el conteo de advertencias se
   mueve de la línea base de 81.
4. **No tocar** las líneas 166 (`{snapshot?.items?.length ? (`) ni 181
   (`{snapshot.items.map((item, i) => {`) ni el cuerpo de la tabla: con el schema,
   `items` ya es `RiskSnapshotItem[]` y todos los campos que consume la tabla
   (`type`, `description`, `probability`, `impact`, `score`, `level`, `actions`,
   `responsible`) están tipados. Si `check-types` se quejara del `?.` innecesario
   en la 166, simplificarlo a `snapshot?.items.length`.

**B) Escritura — `apps/web/lib/actions/risk.ts`, líneas 225-245:**

Añadir el discriminador como las **dos primeras** claves del objeto `formSnapshot`,
sin tocar nada más del bloque:
```typescript
      formSnapshot: {
        snapshotVersion: EVIDENCE_SNAPSHOT_VERSION,
        kind: "RISK_MATRIX",
        title: full.title,
        // ...el resto del objeto tal cual está
      },
```
con el import correspondiente en el grupo de workspace:
```typescript
import { EVIDENCE_SNAPSHOT_VERSION } from "@prol/shared";
```

**Por qué también el escritor:** sin esto la rama v1 sería código muerto para
siempre y el "versionado" de OPS-04 sería teórico. Con esto, cada fila nueva se
autoidentifica y la fase 5 (Registros nativos), que va a escribir en esta misma
columna con una forma distinta, puede añadir su rama sin ambigüedad.

**No usar `.parse()` al escribir aquí.** El `update` ocurre justo después de que
`submitEvidence` ya haya creado la evidencia: si el schema lanzara, quedaría una
evidencia entregada sin snapshot. La garantía de forma en la escritura la da
`check-types`, no una excepción en runtime.
  </action>
  <verify>
    <automated>
# El cast desapareció y el lector valida
grep -n 'parseEvidenceSnapshot' apps/web/components/evidence-detail.tsx
grep -n 'formSnapshot as' apps/web/components/evidence-detail.tsx || echo "OK: sin cast"
grep -n 'interface RiskSnapshotItem' apps/web/components/evidence-detail.tsx || echo "OK: interface local eliminada"

# El escritor discrimina
grep -n 'snapshotVersion' apps/web/lib/actions/risk.ts

# No queda ningún cast sobre formSnapshot en todo el codebase
rg -n 'formSnapshot as' apps/web packages --glob '!**/.next/**' || echo "OK: cero casts sobre formSnapshot"

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep 'problems'
pnpm exec turbo run build
    </automated>
  </verify>
  <done>
`evidence-detail.tsx` llama a `parseEvidenceSnapshot(evidence.formSnapshot)`, no
contiene ningún `as` sobre `formSnapshot` ni la interface local. `risk.ts` escribe
`snapshotVersion` y `kind`. `check-types` limpio, `lint` en `0 errors` y 81
advertencias, `build` verde.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 3: verificar las dos ramas contra datos reales</name>
  <what-built>
El snapshot de evidencia ahora se lee con una unión de Zod versionada:
- **rama legacy** para las filas escritas antes de este cambio (sin discriminador),
- **rama v1** para las que escribe `risk.ts` a partir de ahora,
- degradación a `null` —ficha sin tabla, sin pantalla rota— para cualquier otra cosa.

La evidencia legacy que se creó en el plan 01-03 sigue en la base local, intacta:
no se ha tocado ni una fila. El identificador y las URLs están en
`.planning/phases/01-higiene-y-operacion/01-03-SUMMARY.md`.

Antes de este checkpoint, ejecutar `pnpm dev` (http://localhost:3000).
  </what-built>
  <how-to-verify>
**Parte 1 — la evidencia legacy se sigue viendo (criterio 4 de la fase):**
1. Iniciar sesión como `maria.garcia@academiadigitalmx.com` (PROFESSOR).
2. Abrir `/professor/evidence/<evidenceId>` — el id está en el SUMMARY del plan 01-03.
3. Comprobar que la tabla del snapshot se ve **igual que antes del cambio**: las dos
   filas, sus descripciones, probabilidad, consecuencia, la puntuación con su
   etiqueta de nivel coloreada, acciones y responsable. Compararlo con la
   descripción "antes" guardada en el SUMMARY del plan 01-03.
   → Si la tabla desapareció, la rama legacy no está reconociendo la fila. Es el
     fallo que este plan existe para evitar; reportarlo en vez de aprobar.

**Parte 2 — una entrega nueva lleva discriminador y también se ve:**
4. Como `maria.garcia`, en esa misma ficha pulsar **"Devolver para corrección"**.
5. Cerrar sesión, entrar como `carlos.mendoza@gmail.com`, volver a la sección del
   manual y pulsar **"Corregir la matriz"**. Cambiar algún valor y pulsar
   **"Enviar como evidencia"**.
6. Volver como `maria.garcia` a `/professor/evidence` y abrir la evidencia nueva
   (versión 2). Comprobar que **también** se ve la tabla completa.

**Parte 3 — confirmar que la fila nueva sí lleva el discriminador:**
7. Este comando lo puede correr el ejecutor y pegar la salida:
   ```bash
   docker exec prol-db psql -U prol -d prol -tAc \
     "select version, (form_snapshot::jsonb ? 'snapshotVersion') as tiene_discriminador
      from evidences where form_snapshot is not null order by version;"
   ```
   Salida esperada: `1|f` (la legacy) y `2|t` (la nueva). Las dos se ven en pantalla.

Responder con: si las dos tablas se ven y qué imprimió la consulta.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe qué se ve mal</resume-signal>
  <files>ninguno (verificación sobre datos ya existentes)</files>
  <action>
Levantar `pnpm dev`, releer del SUMMARY del plan 01-03 el `evidenceId` legacy y la
descripción "antes" de la tabla, e imprimir las dos URLs de re-verificación. Pausar
y pedir a la persona que ejecute los pasos de `<how-to-verify>`.

Claude puede correr la parte 3 (la consulta SQL) por su cuenta y pegar la salida;
las partes 1 y 2 exigen una sesión autenticada en el navegador.

**No modificar ninguna fila de la base de datos.** Si la tabla legacy no se ve, el
fallo es de la rama legacy del schema y hay que arreglar el schema, nunca el dato.
  </action>
  <verify>
    <automated>
docker exec prol-db psql -U prol -d prol -tAc \
  "select version, (form_snapshot::jsonb ? 'snapshotVersion') as tiene_discriminador
   from evidences where form_snapshot is not null order by version;"
# Esperado: 1|f (legacy) y 2|t (nueva)
    </automated>
  </verify>
  <done>
La persona confirma que las dos evidencias muestran su tabla completa, y la consulta
devuelve `1|f` y `2|t`: la fila legacy se sigue leyendo por la rama sin
discriminador y la nueva por la rama v1.
  </done>
</task>

</tasks>

<verification>
Puertas transversales del milestone (obligatorias antes de cerrar el plan):

```bash
pnpm exec turbo run check-types          # limpio
pnpm exec turbo run lint 2>&1 | grep 'problems'   # "✖ 81 problems (0 errors, 81 warnings)"
pnpm exec turbo run build                # verde
```

**Atención con `lint`:** `apps/web` corre eslint con `--max-warnings 0`, así que el
comando **sale con exit 1** mostrando exactamente `✖ 81 problems (0 errors, 81 warnings)`.
Eso es la línea base, no un fallo. Borrar la `interface RiskSnapshotItem` local es
justamente lo que evita que ese número suba a 82.

Comprobaciones específicas del plan:
- `rg 'formSnapshot as' apps/web packages` no devuelve nada.
- `packages/shared/src/evidence-snapshot.ts` no usa `z.discriminatedUnion`.
- La consulta de la parte 3 del checkpoint devuelve `1|f` y `2|t`.
</verification>

<success_criteria>
1. La evidencia de matriz de riesgos creada antes del cambio se sigue viendo, con
   su tabla completa, sin haber tocado la base de datos. (Criterio 4 de la fase.)
2. Una evidencia entregada después del cambio lleva `snapshotVersion` y `kind`, y
   también se ve.
3. Un `snapshotVersion` distinto de 1 se rechaza en las dos ramas: no cae en legacy
   por descarte.
4. No queda ningún cast `as` sobre `formSnapshot` en el codebase.
5. La lectura degrada a `null` sin lanzar; la escritura no valida en runtime.
6. `check-types` limpio, `lint` en 81/0, `build` verde.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/01-higiene-y-operacion/01-04-SUMMARY.md`.

Registrar en el SUMMARY, porque la fase 5 (Registros nativos) escribe en esta misma
columna y va a heredar el diseño:
- Cómo añadir una rama nueva a `evidenceSnapshotSchema` (bump de
  `EVIDENCE_SNAPSHOT_VERSION` vs. rama nueva por `kind`) y por qué la rama legacy
  debe seguir siendo la última del `z.union`.
- Que `parseRiskConfig()` sigue siendo el saneador de `config` y que el schema lo
  deja deliberadamente como `z.unknown()`.
- La salida de la consulta `1|f` / `2|t` como evidencia del criterio 4.
</output>
