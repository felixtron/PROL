---
phase: 01-higiene-y-operacion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [OPS-04]

must_haves:
  truths:
    - "Existe en la base local al menos una fila de evidencias con form_snapshot no nulo, escrita por el código de producción actual (risk.ts), antes de que nadie toque el tipado."
    - "Ese JSON no tiene ningún discriminador: ni snapshotVersion ni kind."
    - "El SUMMARY conserva el JSON literal y las URLs para volver a ver esa evidencia después del cambio."
  artifacts:
    - path: ".planning/phases/01-higiene-y-operacion/01-03-SUMMARY.md"
      provides: "JSON legacy literal, ids del fixture y URLs de re-verificación"
      contains: "form_snapshot"
  key_links:
    - from: "apps/web/lib/actions/risk.ts (submitRiskMatrix, líneas 225-245)"
      to: "columna evidences.form_snapshot"
      via: "db.evidence.update con el objeto sin discriminador"
      pattern: "formSnapshot"
    - from: "fila de evidencia creada"
      to: "apps/web/components/evidence-detail.tsx"
      via: "página /tenant-admin/evidence/{id} o /professor/evidence/{id}"
      pattern: "snapshot\\?\\.items"
---

<objective>
Fabricar el caso legacy real contra el que se va a verificar OPS-04: una evidencia
de matriz de riesgos entregada por la interfaz, con el `formSnapshot` que escribe
hoy el código de producción, **antes** de introducir el tipado.

Purpose: el criterio 4 de la fase dice que "las evidencias de matriz de riesgos ya
existentes se siguen viendo después de introducir el snapshot discriminado y
versionado". El seed local **no crea** ninguna evidencia de riesgo, así que sin
este plan no hay nada contra lo que verificar y la unión de Zod se validaría contra
datos que ella misma acaba de generar — que es exactamente el error que el criterio
intenta atrapar.
Output: una fila `evidences` con `form_snapshot` legacy en la base local, y su JSON
literal conservado en el SUMMARY.
</objective>

<execution_context>
@/Users/flx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-higiene-y-operacion/01-CONTEXT.md
@.planning/phases/01-higiene-y-operacion/01-RESEARCH.md

@apps/web/lib/actions/risk.ts
@apps/web/lib/compliance-dispatch.ts

<interfaces>
<!-- Todo lo necesario para armar el fixture, ya verificado contra el schema. -->

**Cadena de filas que el cliente necesita para ver un requisito de matriz de riesgos:**
```
Tenant.documentsEnabled = true            (si es false, manual-access.ts:39 corta el acceso)
  └─ Manual { tenantId, createdById, title, status: "PUBLISHED" }
       └─ ManualChapter { manualId, title, position }
            └─ ManualSection { chapterId, title, position, ... }
                 └─ EvidenceRequirement { sectionId, name, position, kind: "RISK_MATRIX" }
  └─ ManualAssignment { manualId, companyId, tenantId, activatedById, status: "ACTIVE" }
       └─ ComplianceActivity { assignmentId, requirementId, periodNumber: 1, reminderDaysBefore }
```
La última fila es la que `activateManualForCompany` crea llamando a
`createActivitiesForAssignment` (`apps/web/lib/compliance-dispatch.ts` líneas 80-101):
```typescript
await db.complianceActivity.createMany({
  data: requirements.map((r) => ({
    assignmentId, requirementId: r.id, periodNumber: 1,
    reminderDaysBefore: r.reminderDaysBefore,
  })),
  skipDuplicates: true,
});
```

**Usuarios del seed** (ver `CREDENTIALS.md`, todos con la misma contraseña de demo):
- `super@prol.prosuite.pro` — SUPER_ADMIN
- `maria.garcia@academiadigitalmx.com` — PROFESSOR (revisor del tenant)
- `carlos.mendoza@gmail.com` — STUDENT, **miembro de Acme Corp** (el seed mueve los
  dos estudiantes a `acmeCorp.id`, `packages/db/prisma/seed.ts` líneas 1038-1043).
  Es quien llena y entrega la matriz.

**Único escritor de `formSnapshot`** (`apps/web/lib/actions/risk.ts` líneas 225-245).
La forma que produce, **sin discriminador**, es la que hay que capturar:
```typescript
formSnapshot: {
  title, periodLabel, config, submittedAt,     // submittedAt: ISO string
  items: [{ type, description, probability, impact, score, level, actions, responsible }],
}
```

**Columna en Postgres:** `evidences.form_snapshot` (modelo `Evidence`, `@map("form_snapshot")`).

**Base local:** `localhost:5435`, contenedor `prol-db`, usuario y base `prol`.
</interfaces>
</context>

<constraints>
- **Este plan NO modifica ningún archivo del repositorio.** Sólo crea filas en la
  base local y un script desechable que se borra al terminar.
- **La evidencia la entrega una persona por la interfaz**, no un `INSERT` directo.
  El objetivo es que el JSON lo escriba el código real de `risk.ts`; un insert a
  mano fabricaría el "legacy" a imagen del esquema que aún no existe y no probaría nada.
- **Ejecutar antes que el plan 01-04.** Es una precondición de orden, no una
  preferencia: `risk.ts` no cambia en esta fase, pero el valor de la prueba está en
  que la fila exista antes de escribir el schema de Zod.
- No tocar `apps/web/lib/actions/risk.ts`.
</constraints>

<tasks>

<task type="auto">
  <name>Tarea 1: sembrar el fixture de manual con requisito de matriz de riesgos</name>
  <files>packages/db/prisma/_tmp-risk-fixture.ts (temporal, NO se commitea)</files>
  <action>
El seed no crea ningún manual. Escribir un script desechable
`packages/db/prisma/_tmp-risk-fixture.ts` que cree la cadena mínima descrita en
`<interfaces>`, replicando lo que harían las server actions (que no se pueden
invocar sin sesión):

1. `tenant`: tomar el del seed y hacer `update { documentsEnabled: true }`.
2. `company`: Acme Corp (`slug: "acme-corp"`).
3. `author`: el usuario ADMIN o SUPER_ADMIN del seed.
4. `Manual`: `{ tenantId, createdById: author.id, title: "Manual de prueba OPS-04", normaLabel: "ISO 9001:2015", status: "PUBLISHED" }`.
5. `ManualChapter` y `ManualSection` con `position: 1` (leer el schema para los
   campos obligatorios de `ManualSection`; usar el mínimo).
6. `EvidenceRequirement`: `{ sectionId, name: "Matriz de riesgos y oportunidades", position: 1, kind: "RISK_MATRIX", required: true }`. Dejar `toolConfig` en
   `null` para que `openRiskMatrix` use `DEFAULT_RISK_CONFIG`.
7. `ManualAssignment`: `{ manualId, companyId, tenantId, activatedById: author.id }`.
8. `ComplianceActivity`: `{ assignmentId, requirementId, periodNumber: 1, reminderDaysBefore: requirement.reminderDaysBefore }`.

El script debe ser **idempotente o auto-limpiante** (si ya existe un manual con ese
título, borrarlo antes de recrear) e imprimir al final, en formato copiable:
- `assignmentId`
- `sectionId`
- URL para el cliente: `http://localhost:3000/dashboard/manuals/<assignmentId>/sections/<sectionId>`

Ejecutar con:
```bash
pnpm --filter @prol/db exec tsx prisma/_tmp-risk-fixture.ts
```
Después de que la tarea 2 termine, borrar el script (`rm packages/db/prisma/_tmp-risk-fixture.ts`).

Levantar el servidor de desarrollo para la tarea 2:
```bash
pnpm dev     # http://localhost:3000
```
  </action>
  <verify>
    <automated>
docker exec prol-db psql -U prol -d prol -tAc "
  select ca.id, er.kind, ma.id as assignment_id
  from compliance_activities ca
  join evidence_requirements er on er.id = ca.requirement_id
  join manual_assignments ma on ma.id = ca.assignment_id
  where er.kind = 'RISK_MATRIX';"
docker exec prol-db psql -U prol -d prol -tAc "select documents_enabled from tenants;"
curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3000/api/health
    </automated>
  </verify>
  <done>
La consulta devuelve al menos una `compliance_activity` cuyo requisito es
`RISK_MATRIX`, ligada a una `manual_assignment` de Acme Corp. `documents_enabled`
del tenant es `t`. El servidor de desarrollo responde 200 en `/api/health`. Los
tres identificadores (assignmentId, sectionId, activityId) quedan anotados.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Tarea 2: entregar una matriz de riesgos por la interfaz</name>
  <what-built>
Un manual publicado y activado para Acme Corp con un requisito de tipo "Matriz de
riesgos y oportunidades", su actividad de cumplimiento abierta, y el servidor de
desarrollo corriendo en http://localhost:3000.

Lo que falta sólo lo puede hacer una persona: la matriz se llena y se entrega desde
la interfaz autenticada, y el objetivo del plan es precisamente que el
`formSnapshot` lo escriba el código real de `risk.ts` y no un insert fabricado.
  </what-built>
  <how-to-verify>
1. Abrir http://localhost:3000 en una ventana privada e iniciar sesión como
   `carlos.mendoza@gmail.com` (contraseña de demo en `CREDENTIALS.md`). Es miembro
   de Acme Corp.
2. Ir a la URL que imprimió la tarea 1:
   `http://localhost:3000/dashboard/manuals/<assignmentId>/sections/<sectionId>`
3. En el bloque de la evidencia, pulsar **"Abrir la matriz de riesgos"**.
4. Añadir **dos filas** con datos distintos entre sí, por ejemplo:
   - Riesgo — "Incumplimiento de requisitos legales" — probabilidad 3, consecuencia 4,
     acciones "Auditoría legal semestral", responsable "Jurídico"
   - Oportunidad — "Certificación ISO 9001" — probabilidad 2, consecuencia 5,
     acciones "Contratar consultoría", responsable "Dirección"
5. Pulsar **"Guardar borrador"** y comprobar que no da error.
6. Pulsar **"Enviar como evidencia"**.
7. Cerrar sesión e iniciar sesión como `maria.garcia@academiadigitalmx.com`
   (PROFESSOR). Ir a `/professor/evidence`, abrir la evidencia recién entregada y
   **confirmar que se ve la tabla del snapshot con las dos filas**, con su columna
   "Evaluación" mostrando la puntuación y la etiqueta de nivel.
8. **Capturar una captura de pantalla o describir literalmente lo que muestra esa
   tabla.** Es la referencia visual "antes" contra la que el plan 01-04 va a comparar.

Responder con: el id de la evidencia (está en la URL `/professor/evidence/<id>`) y
la confirmación de que la tabla se ve.
  </how-to-verify>
  <resume-signal>Escribe "listo &lt;evidenceId&gt;" o describe el problema encontrado</resume-signal>
  <files>ninguno (sólo filas en la base local)</files>
  <action>
Pausar y pedir a la persona que ejecute los pasos de `<how-to-verify>`. Claude no
puede hacerlo: la matriz se llena y se entrega desde la interfaz autenticada, y el
punto del plan es que el `formSnapshot` lo escriba el código real de `risk.ts`.

Antes de pausar, dejar `pnpm dev` corriendo y volver a imprimir la URL exacta que
generó la tarea 1. Al reanudar, anotar el `evidenceId` que devuelva la persona.
  </action>
  <verify>
    <automated>
docker exec prol-db psql -U prol -d prol -tAc \
  "select id, version, kind, status from evidences where form_snapshot is not null;"
    </automated>
  </verify>
  <done>
La consulta devuelve al menos una fila con `kind = RISK_MATRIX`, `status = PENDING`
y `form_snapshot` no nulo, y la persona confirma que la tabla del snapshot se ve en
`/professor/evidence/<id>`.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: capturar el JSON legacy y limpiar</name>
  <files>packages/db/prisma/_tmp-risk-fixture.ts (borrado)</files>
  <action>
1. Extraer el `form_snapshot` literal de la fila recién creada:
   ```bash
   docker exec prol-db psql -U prol -d prol -tAc \
     "select id, kind, status, jsonb_pretty(form_snapshot::jsonb)
      from evidences where form_snapshot is not null order by submitted_at desc limit 1;"
   ```
2. **Confirmar que NO tiene discriminador**: el JSON no debe contener las claves
   `snapshotVersion` ni `kind`. Si las tuviera, algo escribió esa fila fuera del
   camino de `risk.ts` y el fixture no sirve como caso legacy.
3. Guardar en el SUMMARY, literalmente:
   - el `evidenceId`,
   - el JSON completo con `jsonb_pretty`,
   - `assignmentId`, `sectionId`, `activityId`,
   - las dos URLs de re-verificación: `/professor/evidence/<evidenceId>` y
     `/tenant-admin/evidence/<evidenceId>`,
   - la descripción de la tabla tal como se ve hoy (de la tarea 2).
4. Borrar el script desechable:
   ```bash
   rm -f packages/db/prisma/_tmp-risk-fixture.ts
   ```
   **No borrar las filas de la base**: el plan 01-04 las necesita.
  </action>
  <verify>
    <automated>
docker exec prol-db psql -U prol -d prol -tAc \
  "select count(*) from evidences where form_snapshot is not null;"
docker exec prol-db psql -U prol -d prol -tAc \
  "select (form_snapshot::jsonb ? 'snapshotVersion') or (form_snapshot::jsonb ? 'kind')
   from evidences where form_snapshot is not null;"
# Debe imprimir 'f' (falso): la fila es legacy, sin discriminador.

rm -f packages/db/prisma/_tmp-risk-fixture.ts
git status --porcelain
    </automated>
  </verify>
  <done>
`count(*)` ≥ 1. La consulta del discriminador devuelve `f` para la fila creada.
`git status --porcelain` no lista ningún archivo de código nuevo (sólo el SUMMARY
de este plan). El SUMMARY contiene el JSON legacy completo y los identificadores.
  </done>
</task>

</tasks>

<verification>
Puertas transversales del milestone. Este plan **no modifica código**, así que las
tres deben salir exactamente en la línea base:

```bash
pnpm exec turbo run check-types          # limpio
pnpm exec turbo run lint 2>&1 | grep 'problems'   # "✖ 81 problems (0 errors, 81 warnings)"
pnpm exec turbo run build                # verde
```

**Atención con `lint`:** `apps/web` corre eslint con `--max-warnings 0`, así que el
comando sale con exit 1 mostrando `✖ 81 problems (0 errors, 81 warnings)`. Es lo
esperado en la línea base.

Comprobación específica: `git diff --stat` sobre el código debe estar vacío. El
único archivo nuevo commiteable es el SUMMARY.
</verification>

<success_criteria>
1. La base local tiene al menos una fila `evidences` con `form_snapshot` no nulo,
   escrita por `submitRiskMatrix` a través de la interfaz.
2. Ese JSON no contiene `snapshotVersion` ni `kind`: es el caso legacy real.
3. La evidencia se ve correctamente hoy en `/professor/evidence/<id>`, con su tabla
   de dos filas — referencia "antes" del criterio 4 de la fase.
4. El SUMMARY conserva el JSON literal, los identificadores y las URLs.
5. No queda ningún script desechable en el repositorio ni ningún cambio de código.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/01-higiene-y-operacion/01-03-SUMMARY.md`.

El SUMMARY es el entregable real de este plan: el plan 01-04 lo lee para saber qué
evidencia abrir y qué JSON debe seguir parseando. Debe incluir el `evidenceId`, el
`form_snapshot` con `jsonb_pretty`, `assignmentId`/`sectionId`/`activityId`, las URLs
de re-verificación y la descripción de la tabla tal como se ve antes del cambio.

Anotar también, para la fase 3 (Procedimientos nativos), que el seed sigue sin crear
manuales y que este fixture tuvo que hacerse a mano.
</output>
