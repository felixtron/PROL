---
phase: 03-procedimientos-nativos
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - apps/web/lib/documents/document-identity.ts
  - apps/web/lib/documents/resolve-identity.ts
  - apps/web/lib/actions/manual-document.ts
  - apps/web/lib/queries/manual-document.ts
  - apps/web/lib/actions/manual.ts
autonomous: true
requirements: [DOC-01, DOC-04, DOC-07]
parallel_safe: false  # Ola 4 con un solo plan. Define los contratos que consumen
                      # los planes 05, 06 y 07, y `branching_strategy` es "none":
                      # en la fase 1 dos ejecutores concurrentes sobre el mismo
                      # working tree se absorbieron archivos entre `add` y `commit`.

must_haves:
  truths:
    - "`DocumentIdentity` entrega la razón social, el logo, el código, el nombre, la versión, el estatus, la fecha y la norma ya resueltos: la plantilla no interpreta ningún fallback."
    - "La razón social se resuelve con la MISMA regla que ya usa el emisor DC-3: `dc3LegalName ?? name`."
    - "El logo se lee en vivo desde `Company.logo`, no se congela: cambiarlo cambia lo que se ve sin regenerar nada."
    - "Guardar el mismo cuerpo dos veces no sube `templateVersion`; guardar uno distinto sí."
    - "Guardar el primer cuerpo de un documento sin cuerpo lo deja en `templateVersion = 1`, no en 2."
    - "Escribir un cuerpo sobre un documento `FILE` lo convierte en `PROCEDIMIENTO`."
    - "El cuerpo pasa por `sanitizeManualHtml` antes de llegar a la base, también por este camino."
  artifacts:
    - path: "apps/web/lib/documents/document-identity.ts"
      provides: "Tipo DocumentIdentity y ensamblador puro con los fallbacks aplicados; importable desde cliente"
      min_lines: 80
    - path: "apps/web/lib/documents/resolve-identity.ts"
      provides: "Lectura de base que alimenta al ensamblador; sólo servidor, sin autorizar por su cuenta"
    - path: "apps/web/lib/actions/manual-document.ts"
      provides: "updateManualDocumentBody con saneado y política de templateVersion"
      contains: "sanitizeManualHtml"
    - path: "apps/web/lib/queries/manual-document.ts"
      provides: "getManualDocumentForEdit con el catálogo de empresas y su estado de emisión"
      contains: "cache("
  key_links:
    - from: "apps/web/lib/actions/manual-document.ts"
      to: "apps/web/lib/sanitize-manual-html.ts"
      via: "sanitizeManualHtml(input.contentHtml) antes del update"
      pattern: "sanitizeManualHtml"
    - from: "apps/web/lib/documents/resolve-identity.ts"
      to: "apps/web/lib/documents/document-identity.ts"
      via: "buildDocumentIdentity(rowsYaLeidas)"
      pattern: "buildDocumentIdentity"
    - from: "apps/web/lib/queries/manual-document.ts"
      to: "apps/web/lib/manual-access.ts"
      via: "requireManualAdmin / assertTenantScope antes de cualquier lectura"
      pattern: "requireManualAdmin"
---

<objective>
Poner los contratos del documento nativo: la identidad que cada empresa ve
resuelta de una vez, y la acción que escribe el cuerpo de la plantilla con su
política de versión.

Purpose: los tres planes de interfaz que vienen detrás (03-05, 03-06, 03-07)
consumen esto. Definirlo primero es la diferencia entre entregarle al ejecutor los
planos y decirle "constrúyeme una casa": nadie tendrá que salir a explorar el
código para averiguar qué campos existen ni cómo se resuelve un fallback.
Output: `DocumentIdentity` (puro + resolutor de servidor), la acción de cuerpo de
plantilla con la política de `templateVersion`, y la consulta que alimenta el
editor del consultor con el estado de emisión de cada empresa.
</objective>

<execution_context>
@/Users/flx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-procedimientos-nativos/03-CONTEXT.md
@.planning/phases/03-procedimientos-nativos/03-RESEARCH.md
@.planning/phases/03-procedimientos-nativos/03-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

@apps/web/lib/certificate-templates/index.tsx
@apps/web/lib/manual-access.ts
@apps/web/lib/actions/manual.ts
@apps/web/lib/queries/manual.ts
@apps/web/lib/sanitize-manual-html.ts

<interfaces>
<!-- Contratos exactos del código real. No hace falta explorar. -->

**El patrón que se copia** (`apps/web/lib/certificate-templates/index.tsx`): una
función pura `renderX(templateId, data: XRenderData)` donde la interfaz de datos
**no tiene opcionales evitables** — todo llega resuelto (fallbacks aplicados,
fechas ya formateadas) para que el componente de presentación no pregunte `??` en
ningún sitio. Sus dos consumidores (PDF real y vista previa) llenan la misma
interfaz desde fuentes distintas, y por eso lo que se ve en preview es idéntico a
lo emitido.

**La regla de razón social ya existe** — `apps/web/lib/dc3/readiness.ts:181`:
```typescript
const employerName = clean(company.dc3LegalName) ?? clean(company.name);
```
Reutilizar **esa misma regla**, no escribir una segunda idéntica: sería una segunda
fuente de verdad para el mismo dato.

**El logo se pinta directo, sin data-URL** —
`apps/web/components/tenant-brand.tsx:21-23` (`<img src={logo} …/>`).
`loadUploadAsDataUrl` de `lib/certificate-assets.ts` existe **sólo** para
`@react-pdf/renderer`, que no carga URLs remotas de forma fiable. Eso es de la
fase 4. Aquí no.

**Fallbacks de código y nombre**, patrón ya presente en
`components/company-project-panel.tsx:119` y en la página de sección:
`own?.codeOverride ?? doc.code`. El nombre es el campo nuevo y simétrico:
`nameOverride ?? name`.

**Campos disponibles tras el plan 03-01:**
```
Company:         name, dc3LegalName, logo, tenantId
Tenant:          name, logo
Manual:          title, normaLabel, tenantId
ManualDocument:  code, name, description, kind, contentHtml, templateVersion, manualId
CompanyDocument: version, status, kind, contentHtml, codeOverride, nameOverride,
                 sourceTemplateVersion, notes, createdAt, publishedAt,
                 uploadedById, publishedById, fileKey/fileName/fileSize/mimeType (nullable)
```

**Guardias** (`apps/web/lib/manual-access.ts`):
- `requireManualAdmin()` → ADMIN/SUPER_ADMIN + `documentsEnabled`.
- `requireManualManageAccess(manualId)` → lo anterior + carga el manual + comprueba
  el tenant. Devuelve `{ user, manual }`.
- `assertTenantScope(user, tenantId)` lanza si el recurso es de otro tenant.
- Filtro fail-closed: `tenantId: user.tenantId ?? "__none__"`.

**Helpers de validación de `lib/actions/manual.ts`** (líneas 29-37) —
`text(value, field, max)` y `optionalText(value, max)`. Reusar el mismo estilo en el
archivo nuevo; son cuatro líneas y duplicarlas es peor que importarlas, así que
**exportarlas desde `lib/actions/manual.ts` no es opción** (es `"use server"`: todo
export async ahí es RPC, y aunque éstas sean síncronas, es más limpio redefinirlas
localmente). Copiarlas al archivo nuevo con un comentario que diga de dónde salen.

**`createManualDocument` hoy** (`lib/actions/manual.ts:478-515`) recibe
`{ manualId, code, name, description?, sectionId?, file? }` y crea la fila sin
`kind`.

**Convenciones que aplican:**
- Server actions en `lib/actions/*`; capa de consulta RSC en `lib/queries/*` con
  `cache()` de React; route handlers **sólo** cuando hace falta un `Response`.
- **Nada de despacho, correo ni planificación** en ficheros `"use server"`: toda
  exportación async ahí queda expuesta como RPC público.
- Retorno discriminable: `{ success: true as const, … }` /
  `{ success: false as const, error: string }`.
- Identificadores en inglés, textos de usuario en español, minúscula, sin punto
  final.
</interfaces>
</context>

<constraints>
- **`DocumentIdentity` no tiene nulls evitables.** El único opcional permitido es
  el logo (`string | null`), porque una empresa puede no tener: la vista ramifica
  una vez y no cinco. Todo lo demás llega resuelto, incluida la fecha ya
  formateada y la etiqueta del estatus.
- **`document-identity.ts` es puro**: sin `db`, sin `node:*`, sin `server-only`.
  Tiene que poder importarse desde un componente de cliente, igual que
  `lib/document-files.ts` es deliberadamente puro por el mismo motivo.
- **`resolve-identity.ts` no autoriza.** Lo llama la capa de consulta, que ya
  autorizó. Decirlo en su docstring y **no exportarlo a las páginas**.
- **`templateVersion` sube sólo cuando el cuerpo cambia de verdad.** Es la
  enmienda (a) del CONTEXT: si subiera en cada guardado, la insignia de "hay una
  versión más reciente" se dispararía por cambios que no existen y dejaría de
  significar nada.
- **El cuerpo se sanea al escribir, siempre.** Este es el segundo camino nuevo de
  escritura de HTML de la fase (el primero es la importación) y el invariante no
  admite excepciones.
- **Nada de emisión, borrador ni publicación aquí.** Es el plan 03-05.
- **Nada de `formSchema`, `CompanyRecord`, bloques ni `EvidenceRequirementKind`
  nuevos.** Fase 5.
- La puerta de lint sale con **exit 1** estando sana: `✖ 81 problems (0 errors, 81
  warnings)`.
</constraints>

<tasks>

<task type="auto">
  <name>Tarea 1: DocumentIdentity — resuelta una vez, sin nulls que interpretar</name>
  <files>apps/web/lib/documents/document-identity.ts, apps/web/lib/documents/resolve-identity.ts</files>
  <action>
**1. `apps/web/lib/documents/document-identity.ts`** — puro. Docstring que explique
las dos mitades de la decisión: *el logo se lee en vivo, lo acreditativo se
congela*, que es la convención que ya sigue el DC-3 ("son marca, no dato
acreditativo") y la ventaja principal de esto sobre generar `.docx` con plantillas:
cambiar el logo de una empresa re-renderiza todos sus documentos sin regenerar nada.

```typescript
import type { CompanyDocumentStatus, ManualDocumentKind } from "@prol/db";

export const DOCUMENT_KIND_LABEL: Record<ManualDocumentKind, string> = {
  FILE: "Archivo",
  PROCEDIMIENTO: "Procedimiento",
  REGISTRO: "Registro",
};

export const DOCUMENT_STATUS_LABEL: Record<CompanyDocumentStatus, string> = {
  BORRADOR: "Borrador",
  VIGENTE: "Vigente",
  OBSOLETO: "Obsoleto",
};

/** Mismas familias de color que EVIDENCE_STATUS_CLASS en `lib/compliance.ts`. */
export const DOCUMENT_STATUS_CLASS: Record<CompanyDocumentStatus, string> = {
  BORRADOR: "bg-amber-100 text-amber-800",
  VIGENTE: "bg-emerald-100 text-emerald-700",
  OBSOLETO: "bg-slate-100 text-slate-700",
};

/** Lo que la vista recibe. Sin `??` que la plantilla tenga que resolver. */
export interface DocumentIdentity {
  /** `dc3LegalName ?? name`, misma regla que el emisor DC-3. */
  companyName: string;
  /** Se lee en vivo. Único opcional: hay empresas sin logo. */
  companyLogo: string | null;
  tenantName: string;
  code: string;          // codeOverride ?? code
  name: string;          // nameOverride ?? name
  /** "" cuando el manual no declara norma: la vista comprueba truthiness, no null. */
  normaLabel: string;
  kind: ManualDocumentKind;
  kindLabel: string;
  version: number;
  status: CompanyDocumentStatus;
  statusLabel: string;
  statusClass: string;
  /** Ya formateada en es-MX / America/Mexico_City. */
  issuedAt: string;
  sourceTemplateVersion: number | null;
  latestTemplateVersion: number;
  /** La plantilla avanzó respecto a lo que esta empresa adoptó (DOC-07). */
  isOutdated: boolean;
  /** Texto listo para pintar, o null si está al día. */
  outdatedLabel: string | null;
}

export interface DocumentIdentityInput { /* los campos crudos de las cuatro filas */ }

export function buildDocumentIdentity(input: DocumentIdentityInput): DocumentIdentity;
```

Detalles que importan dentro de `buildDocumentIdentity`:
- `clean(v)` local: `trim()` y `null` si queda vacío. La razón social es
  `clean(dc3LegalName) ?? clean(name) ?? name` — una cadena de espacios en
  `dc3LegalName` no puede ganarle al nombre real.
- `issuedAt` con un `Intl.DateTimeFormat("es-MX", { day: "numeric", month: "long",
  year: "numeric", timeZone: "America/Mexico_City" })` a nivel de módulo, igual que
  el `DATE` de `components/company-project-panel.tsx:12-17`. Crear el formateador
  una vez y no por llamada.
- `isOutdated = sourceTemplateVersion !== null && latestTemplateVersion > sourceTemplateVersion`.
  Con `sourceTemplateVersion` nulo (una versión antigua subida como archivo, de
  antes de esta fase) **no** se avisa de nada: no se sabe de qué plantilla salió, y
  un aviso inventado es peor que ninguno.
- `outdatedLabel`: `Hay una versión más reciente de la plantilla (v${latest})`.

**2. `apps/web/lib/documents/resolve-identity.ts`** — sólo servidor:

```typescript
import { db } from "@prol/db";
import { buildDocumentIdentity, type DocumentIdentity } from "./document-identity";

/**
 * Lee de la base lo que `buildDocumentIdentity` necesita y lo ensambla.
 *
 * **No autoriza.** Lo llama la capa de consulta (`lib/queries/manual-document.ts`),
 * que ya comprobó rol, tenant y pertenencia. Se separa del ensamblador puro para
 * que ese ensamblador siga siendo importable desde el cliente — misma razón por la
 * que `lib/document-files.ts` está separado de `lib/document-storage.ts`.
 */
export async function loadCompanyDocumentIdentity(
  companyDocumentId: string,
): Promise<{ identity: DocumentIdentity; contentHtml: string; companyId: string; documentId: string } | null>
```

Una sola consulta con los `include`/`select` anidados que hacen falta
(`companyDocument → document → manual`, y `companyDocument → company → tenant`),
más el `templateVersion` actual del `ManualDocument` para el cálculo de DOC-07.
Devolver `null` si la fila no existe: el 404 lo decide quien llama.

Exportar además un ensamblador para la **vista previa de la plantilla** (lo que el
consultor ve antes de emitir, plan 03-06):
```typescript
export async function loadTemplatePreviewIdentity(
  documentId: string,
  companyId: string,
): Promise<{ identity: DocumentIdentity; contentHtml: string } | null>
```
Rellena la misma interfaz desde la plantilla en vez de desde una emisión
(`version: 0`? no: usar `latestTemplateVersion` como `version` y
`status: "BORRADOR"` con `sourceTemplateVersion` igual al actual, de modo que
`isOutdated` sea `false`). Que los dos consumidores llenen **la misma interfaz
desde fuentes distintas** es justo lo que hace que la vista previa y lo emitido no
puedan divergir: es el mismo truco que usa `renderCertificate`.
  </action>
  <verify>
    <automated>
# El módulo puro no importa nada de servidor
grep -nE "from \"@prol/db\"|node:|next/|server-only" apps/web/lib/documents/document-identity.ts | grep -v "^.*import type" && echo "ERROR: el módulo puro dejó de serlo" || echo "OK: puro (sólo import type de @prol/db)"

# La regla de razón social es la del DC-3, no una nueva
grep -n 'dc3LegalName' apps/web/lib/documents/document-identity.ts
grep -n 'dc3LegalName' apps/web/lib/dc3/readiness.ts    # la original sigue ahí

# El logo NO se convierte a data-URL: eso es de la fase 4
grep -n 'loadUploadAsDataUrl\|dataUrl\|base64' apps/web/lib/documents/*.ts && echo "ERROR: patrón de PDF adelantado" || echo "OK: logo en vivo"

# El resolutor dice explícitamente que no autoriza
grep -n 'No autoriza\|no autoriza' apps/web/lib/documents/resolve-identity.ts

# Las dos rutas de ensamblado existen
grep -n 'loadCompanyDocumentIdentity\|loadTemplatePreviewIdentity' apps/web/lib/documents/resolve-identity.ts

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep 'problems'
    </automated>
  </verify>
  <done>
`document-identity.ts` exporta `DocumentIdentity`, las tres tablas de etiquetas y
`buildDocumentIdentity`, sin importar nada de servidor (sólo `import type` de
`@prol/db`), con la razón social resuelta por la misma regla que el DC-3, el logo
en vivo y `isOutdated` calculado sólo cuando se sabe de qué plantilla salió la
versión. `resolve-identity.ts` lee la base y llena esa misma interfaz por dos
caminos —una emisión existente y una vista previa de plantilla—, declara en su
docstring que no autoriza, y no convierte el logo a data-URL. `check-types` limpio
y `lint` en 81 advertencias.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: escribir el cuerpo de la plantilla, con su política de versión</name>
  <files>apps/web/lib/actions/manual-document.ts, apps/web/lib/queries/manual-document.ts, apps/web/lib/actions/manual.ts</files>
  <action>
**1. `apps/web/lib/actions/manual-document.ts`** — archivo nuevo, `"use server"`.
Abre con un comentario que diga por qué es un archivo aparte y no más líneas en
`lib/actions/manual.ts` (que ya tiene 1000+): el ciclo de vida del documento nativo
es un tema propio, y el archivo va a crecer con el plan 03-05.

```typescript
export async function updateManualDocumentBody(input: {
  documentId: string;
  contentHtml: string;
}): Promise<
  | { success: true; templateVersion: number; changed: boolean; kind: ManualDocumentKind }
  | { success: false; error: string }
>
```

Cuerpo, en este orden:

a) `const user = await requireManualAdmin();` — escribir el procedimiento es
   autoría, no revisión.

b) Cargar el documento con su manual:
```typescript
   const doc = await db.manualDocument.findUnique({
     where: { id: input.documentId },
     select: { id: true, manualId: true, kind: true, contentHtml: true,
               templateVersion: true, manual: { select: { tenantId: true } } },
   });
   if (!doc) throw new Error("Documento no encontrado");
   assertTenantScope(user, doc.manual.tenantId);
```

c) **Sanear antes de comparar y antes de escribir.** Comparar el saneado con el
   guardado, no el crudo con el guardado: si no, dos guardados idénticos que
   difieren sólo en marcado que el sanitizador tira contarían como cambio y
   subirían la versión de plantilla por nada.
```typescript
   const safeHtml = sanitizeManualHtml(input.contentHtml);
   const previous = doc.contentHtml ?? "";
   if (safeHtml === previous) {
     return { success: true as const, changed: false,
              templateVersion: doc.templateVersion, kind: doc.kind };
   }
```

d) **La política de `templateVersion`**, con su razón escrita al lado:
```typescript
   // Sube sólo cuando el cuerpo cambia de verdad (enmienda (a) del CONTEXT), y
   // NO en el primer cuerpo: un documento recién creado está en v1, y que su
   // primera redacción lo dejara en v2 haría que "plantilla v1" no significara
   // nada. A partir de ahí, cada cambio real es una versión de plantilla.
   const firstBody = previous.trim().length === 0;
   const templateVersion = firstBody ? doc.templateVersion : doc.templateVersion + 1;
```

e) **La conversión de arquetipo**, que el CONTEXT anticipa ("un documento puede
   convertirse de `FILE` a `PROCEDIMIENTO` tras importar el Word"):
```typescript
   // Escribir un cuerpo es lo que convierte un documento de archivo en uno
   // nativo. No hace falta una acción aparte para decir "ahora es un
   // procedimiento": el hecho de que tenga cuerpo ya lo dice. `REGISTRO` no se
   // asigna en esta fase — es de la fase 5.
   const kind = doc.kind === "FILE" && safeHtml.trim() ? "PROCEDIMIENTO" : doc.kind;
```

f) `update` con `contentHtml: safeHtml`, `templateVersion`, `kind`; luego
   `revalidatePath` de la página del manual y de la del documento; devolver
   `{ success: true, changed: true, templateVersion, kind }`.

Nota sobre el borrado del cuerpo: si `safeHtml` queda vacío y antes había cuerpo,
se guarda vacío y **sí** cuenta como cambio (sube la versión), pero el `kind` **no**
vuelve a `FILE`: las emisiones ya hechas siguen siendo procedimientos y degradar el
arquetipo dejaría filas huérfanas de semántica. Dejarlo escrito.

**2. `createManualDocument` gana `kind`** (`lib/actions/manual.ts`):
- Añadir `kind?: ManualDocumentKind` a la entrada, por defecto `"FILE"`.
- Pasarlo al `create`.
- Aceptar sólo `"FILE"` y `"PROCEDIMIENTO"`; si llega `"REGISTRO"`, devolver
  `{ success: false, error: "Los registros llegan en una entrega posterior" }`.
  Es la única barrera que impide que la UI de la fase 5 se cuele por accidente en
  ésta.
- **Nada más de este archivo se toca.** En particular, `deleteManualDocument` y el
  `findFirst` de versión de `uploadCompanyDocument` siguen intactos (los intocables
  del plan 03-02).

**3. `apps/web/lib/queries/manual-document.ts`** — archivo nuevo, con `cache()` de
React como el resto de `lib/queries/*`:

```typescript
export const getManualDocumentForEdit = cache(async (documentId: string) => { … });
```
Autoriza con `requireManualAdmin()` + `assertTenantScope`, y devuelve todo lo que
el editor del consultor (plan 03-06) necesita **sin que tenga que consultar nada
más**:
- el documento: `id, code, name, description, kind, contentHtml, templateVersion,
  baseFileName`
- su manual: `id, title, normaLabel, tenantId`
- las secciones a las que está enlazado: `code, title`
- **las empresas con el manual activo y su estado de emisión**, que es lo que
  convierte la página en útil:
  ```
  { assignmentId, companyId, companyName, companyLogo,
    current: { id, version, status, sourceTemplateVersion, updatedAt } | null,
    draft:   { id, version } | null,
    isOutdated: boolean }
  ```
  `current` es la fila `VIGENTE` (si la hay) y `draft` la `BORRADOR` (si la hay);
  se resuelven con **una** consulta a `companyDocument` filtrada por `documentId` y
  `status: { in: ["VIGENTE", "BORRADOR"] }`, agrupando en memoria por `companyId`.
  Nada de una consulta por empresa.
  `isOutdated` se calcula con la misma expresión que
  `buildDocumentIdentity` —importarla de ahí, no reescribirla— para que el aviso
  del consultor y el del cliente no puedan divergir.

Documentar en el docstring que esta consulta es de **gestión** (staff) y que la
del cliente vive aparte, en el plan 03-05.
  </action>
  <verify>
    <automated>
# El saneado está en el camino de escritura
grep -n 'sanitizeManualHtml' apps/web/lib/actions/manual-document.ts

# La comparación es entre saneados, no entre crudo y guardado
grep -n 'safeHtml === previous\|safeHtml !== previous' apps/web/lib/actions/manual-document.ts

# La política de versión y la conversión de arquetipo están, con su razón
grep -n 'firstBody\|templateVersion' apps/web/lib/actions/manual-document.ts
grep -n 'PROCEDIMIENTO' apps/web/lib/actions/manual-document.ts

# No hay despacho ni correo en un "use server"
head -1 apps/web/lib/actions/manual-document.ts    # "use server";
grep -nE 'notify|sendEmail|dispatch|schedule' apps/web/lib/actions/manual-document.ts && echo "ERROR: despacho en un use server" || echo "OK"

# La consulta usa cache() y autoriza
grep -n 'cache(' apps/web/lib/queries/manual-document.ts
grep -n 'requireManualAdmin\|assertTenantScope' apps/web/lib/queries/manual-document.ts

# Una sola consulta para el estado de emisión de todas las empresas
grep -c 'companyDocument.findMany' apps/web/lib/queries/manual-document.ts   # 1

# `kind` entró en createManualDocument y REGISTRO está cerrado
grep -n 'kind' apps/web/lib/actions/manual.ts | head
grep -n 'REGISTRO' apps/web/lib/actions/manual.ts

# Los intocables del plan 03-02 siguen intactos
git diff apps/web/lib/actions/manual.ts | grep -E '^[-+].*(personalized|orderBy: \{ version)' && echo "ERROR: se tocó un intocable" || echo "OK"

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep 'problems'
    </automated>
  </verify>
  <done>
`updateManualDocumentBody` autoriza con `requireManualAdmin`, comprueba el tenant,
sanea el cuerpo antes de compararlo y antes de escribirlo, no sube
`templateVersion` cuando el cuerpo saneado no cambia, deja el primer cuerpo en
v1, sube una versión en cada cambio real, y convierte el documento de `FILE` a
`PROCEDIMIENTO` la primera vez que recibe cuerpo. `createManualDocument` acepta
`kind` y rechaza `REGISTRO` con un mensaje explícito. `getManualDocumentForEdit`
usa `cache()`, autoriza, y devuelve el documento, su manual, sus secciones y el
estado de emisión de cada empresa con **una** consulta a `companyDocument`,
calculando `isOutdated` con la expresión importada de `document-identity.ts`.
Ningún despacho en un `"use server"`. `check-types` limpio y `lint` en 81
advertencias.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: la política de versión, demostrada contra la base</name>
  <files>(sin cambios de código; script desechable que NO se commitea)</files>
  <action>
`updateManualDocumentBody` es una server action y no se invoca por `curl`, así que
—igual que hizo el plan 01-02 con la carrera de versión— se demuestra la **misma
lógica sobre la base real** con un script desechable que replica su cuerpo, y se
deja anotado que la ejecución por la interfaz llega en el plan 03-06.

Script `packages/db/prisma/_tmp-template-version.ts`, sobre el documento
`P-RFC-4.1-01` de la fixture. Cinco pasos, comprobando `template_version`,
`content_html` y `kind` después de cada uno:

| # | Qué se guarda | `templateVersion` esperada | `kind` esperado |
|---|---|---|---|
| 0 | (estado inicial de la fixture) | 1 | PROCEDIMIENTO |
| 1 | el **mismo** cuerpo que ya tiene | **1** (sin cambio) | PROCEDIMIENTO |
| 2 | el mismo cuerpo con `<script>alert(1)</script>` añadido | **1** — el sanitizador lo borra y el resultado es idéntico al guardado | PROCEDIMIENTO |
| 3 | un cuerpo distinto de verdad | **2** | PROCEDIMIENTO |
| 4 | otro cuerpo distinto | **3** | PROCEDIMIENTO |

El paso 2 es el más revelador de los cinco: demuestra que la comparación se hace
**entre saneados**. Si estuviera hecha entre el crudo entrante y lo guardado,
pegar algo que el sanitizador tira subiría la versión de plantilla, y la insignia de
DOC-07 avisaría a las dos empresas de un cambio que no existe.

Y dos pasos más sobre un documento **nuevo** creado al vuelo con `kind: "FILE"` y
sin cuerpo (borrarlo al terminar):

| # | Qué se guarda | `templateVersion` | `kind` |
|---|---|---|---|
| 5 | su **primer** cuerpo | **1** (no 2) | pasa a **PROCEDIMIENTO** |
| 6 | un cuerpo distinto | **2** | PROCEDIMIENTO |

Comprobar además, después del paso 3, que **ninguna** fila de `company_documents`
cambió: editar la plantilla no toca lo emitido. Como en este momento no hay
emisiones, dejarlo como comprobación de conteo (`0`) y anotar que la demostración
de verdad del criterio 2 es del plan 03-07, cuando ya existan dos emisiones.

Restaurar al terminar el cuerpo y el `templateVersion` originales de
`P-RFC-4.1-01` (`content_html` de la fixture, `template_version = 1`) para que el
plan 03-05 arranque desde un estado conocido, y borrar el documento auxiliar y el
script.
  </action>
  <verify>
    <automated>
# La traza de los seis pasos (la imprime el script)
pnpm --filter @prol/db exec tsx prisma/_tmp-template-version.ts
# Esperado: 1, 1, 1, 2, 3 en el documento de la fixture; 1 y 2 en el auxiliar,
# con kind FILE -> PROCEDIMIENTO en el paso 5

# Estado restaurado para el plan 03-05
docker exec prol-db psql -U prol -d prol -Atc "select code||'|tv='||template_version||'|kind='||kind||'|tabla='||(content_html like '%<table%') from manual_documents;"
# Esperado: una sola fila, P-RFC-4.1-01 | tv=1 | kind=PROCEDIMIENTO | tabla=t

# Editar la plantilla no tocó ninguna emisión
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from company_documents;"   # 0

# Las evidencias de la fase 1 siguen ahí
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from evidences where form_snapshot is not null;"   # 2

rm -f packages/db/prisma/_tmp-template-version.ts
git status --porcelain | grep '_tmp-' && echo "ERROR: queda un script desechable" || echo "OK: limpio"

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep 'problems'
pnpm exec turbo run build
    </automated>
  </verify>
  <done>
Guardar el mismo cuerpo dos veces deja `templateVersion` quieta; guardarlo con
marcado que el sanitizador descarta **también** la deja quieta, lo que prueba que la
comparación es entre saneados; dos cambios reales la llevan a 2 y a 3. El primer
cuerpo de un documento `FILE` sin contenido lo deja en `templateVersion = 1` y lo
convierte en `PROCEDIMIENTO`. Ninguna fila de `company_documents` cambió. El
documento de la fixture quedó restaurado a `tv = 1` con su cuerpo original, el
documento auxiliar y el script desechable ya no existen, y las dos evidencias de
`form_snapshot` siguen intactas. `check-types` limpio, `lint` en 81 advertencias,
`build` verde.
  </done>
</task>

</tasks>

<verification>
```bash
pnpm exec turbo run check-types                    # limpio
pnpm exec turbo run lint 2>&1 | grep 'problems'    # "✖ 81 problems (0 errors, 81 warnings)"
pnpm exec turbo run build                          # verde
```

Comprobaciones específicas del plan:
- `git diff --name-only` devuelve exactamente
  `apps/web/lib/documents/document-identity.ts`,
  `apps/web/lib/documents/resolve-identity.ts`,
  `apps/web/lib/actions/manual-document.ts`,
  `apps/web/lib/queries/manual-document.ts` y `apps/web/lib/actions/manual.ts`.
- Ningún archivo de `apps/web/app/` en el diff: en este plan no hay UI.
- `apps/web/lib/sanitize-manual-html.ts` no aparece en el diff.
- `git status --porcelain` no menciona ningún `_tmp-`.
</verification>

<success_criteria>
1. `DocumentIdentity` entrega razón social, logo, código, nombre, versión,
   estatus, fecha y norma ya resueltos, con un único opcional (el logo) y la misma
   regla de razón social que el emisor DC-3. (DOC-04.)
2. El módulo puro no importa nada de servidor y puede usarse desde un componente
   de cliente.
3. La misma interfaz se llena por dos caminos —emisión existente y vista previa de
   plantilla—, que es lo que impide que la vista previa y lo emitido diverjan.
4. `isOutdated` y su etiqueta salen de una sola expresión, compartida entre la
   vista del consultor y la del cliente. (DOC-07.)
5. `updateManualDocumentBody` sanea antes de la base, no sube `templateVersion`
   cuando el cuerpo saneado no cambia, deja el primer cuerpo en v1 y convierte
   `FILE` en `PROCEDIMIENTO`. (DOC-01.)
6. `getManualDocumentForEdit` autoriza, usa `cache()` y resuelve el estado de
   emisión de todas las empresas con una sola consulta.
7. `check-types` limpio, `lint` en 0 errores y 81 advertencias, `build` verde.
</success_criteria>

<output>
Al terminar, crear
`.planning/phases/03-procedimientos-nativos/03-04-SUMMARY.md`.

Registrar en el SUMMARY:
- **La firma exacta de `DocumentIdentity` y de las cuatro funciones nuevas**,
  copiada tal cual. Los planes 03-05, 03-06 y 03-07 la consumen y no deberían
  tener que abrir el código para conocerla.
- La forma exacta del objeto que devuelve `getManualDocumentForEdit`, incluida la
  lista de empresas con `current`/`draft`/`isOutdated`.
- La tabla de los seis pasos de la política de `templateVersion` con los valores
  observados, incluido el paso del `<script>` descartado.
- Si `createManualDocument` obligó a tocar algún llamador (la UI del catálogo es
  del plan 03-06), decirlo.
</output>
</content>
