---
phase: 03-procedimientos-nativos
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/web/lib/queries/manual.ts
  - apps/web/lib/actions/manual.ts
  - apps/web/components/company-project-panel.tsx
autonomous: true
requirements: [OPS-05]
parallel_safe: false  # Ola 2 con un solo plan. Toca los dos módulos centrales del
                      # módulo documental (queries/manual.ts y actions/manual.ts)
                      # y `branching_strategy` es "none": en la fase 1 dos
                      # ejecutores concurrentes se pisaron el índice de git.

must_haves:
  truths:
    - "La página de sección del cliente muestra el documento `VIGENTE`, aunque exista un `BORRADOR` con versión más alta."
    - "El panel del consultor muestra el documento `VIGENTE`, aunque exista un `BORRADOR` con versión más alta."
    - "Subir un archivo nuevo para el mismo documento y empresa degrada la versión anterior a `OBSOLETO` dentro de la misma transacción."
    - "En ningún momento existe más de una fila `VIGENTE` por (documento, empresa), ni siquiera tras dos subidas seguidas."
    - "Los otros tres puntos de llamada de `CompanyDocument` siguen contando o buscando TODAS las versiones: el contador de borrado, el cálculo de la versión máxima y el `_count` del catálogo."
    - "El panel del consultor no muestra un separador huérfano ni el botón de subir archivo para un documento nativo."
  artifacts:
    - path: "apps/web/lib/queries/manual.ts"
      provides: "getAssignmentPanel y getSectionForCompany filtrando por status VIGENTE, con kind/status/nameOverride en el select"
      contains: "status: \"VIGENTE\""
    - path: "apps/web/lib/actions/manual.ts"
      provides: "uploadCompanyDocument degradando la VIGENTE anterior dentro de su transacción con lock"
      contains: "OBSOLETO"
    - path: "apps/web/components/company-project-panel.tsx"
      provides: "Fila de documento consciente del kind: sin nombre de archivo ni botón de subida para los nativos"
  key_links:
    - from: "getSectionForCompany"
      to: "company_documents con status VIGENTE"
      via: "where: { companyId, status: \"VIGENTE\", document: { sections: { some: { sectionId } } } }"
      pattern: "status: \"VIGENTE\""
    - from: "uploadCompanyDocument"
      to: "degradación de la versión anterior"
      via: "tx.companyDocument.updateMany dentro del mismo $transaction que el FOR UPDATE"
      pattern: "updateMany"
---

<objective>
Cortar la deriva semántica que introduce `BORRADOR`: hoy "vigente" se calcula como
`max(version)` en dos consultas, y en cuanto exista un borrador sin publicar con
versión más alta, esas dos pantallas —una del cliente, otra del consultor—
empezarían a enseñarlo como si fuera el documento oficial.

Purpose: es el criterio 5 de la fase y el bug silencioso más probable de todo el
milestone. La investigación enumeró **los seis** puntos de llamada de
`CompanyDocument` y dictaminó cada uno: sólo dos cambian. Los otros tres —el
contador de `deleteManualDocument`, el cálculo de versión máxima de
`uploadCompanyDocument` y el `_count` del catálogo— **tienen que quedarse como
están**; filtrarlos sería introducir bugs nuevos, no arreglar el que hay.
Output: "vigente" pasa a ser un estatus consultado, no una versión deducida, y el
invariante "como mucho una `VIGENTE` por (documento, empresa)" se sostiene también
para los documentos que se siguen subiendo como archivo.
</objective>

<execution_context>
@/Users/flx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-procedimientos-nativos/03-CONTEXT.md
@.planning/phases/03-procedimientos-nativos/03-RESEARCH.md
@.planning/phases/03-procedimientos-nativos/03-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

@apps/web/lib/queries/manual.ts
@apps/web/lib/actions/manual.ts
@apps/web/components/company-project-panel.tsx
@apps/web/app/dashboard/manuals/[assignmentId]/sections/[sectionId]/page.tsx

<interfaces>
<!-- Dictamen completo de la investigación sobre los SEIS puntos de llamada de
     `CompanyDocument`. Es la salida más valiosa del research: no re-derivarla. -->

| # | Archivo:línea | Función | Semántica | Acción |
|---|---|---|---|---|
| 1 | `lib/queries/manual.ts:287` (dedup en 313-315) | `getAssignmentPanel` | **VIGENTE** | `where` gana `status: "VIGENTE"` |
| 2 | `lib/queries/manual.ts:556` (dedup en 613-615) | `getSectionForCompany` | **VIGENTE** | `where` gana `status: "VIGENTE"` |
| 3 | `app/files/company-document/[id]/route.ts:22` | `GET` descarga | cualquier versión | **NO TOCAR** — ya resuelto en 03-01 |
| 4 | `lib/actions/manual.ts:573-575` | `deleteManualDocument` | cualquier versión, **a propósito** | **NO TOCAR** |
| 5 | `lib/actions/manual.ts:887-908` | `uploadCompanyDocument` | cualquier versión, **a propósito** | el cálculo de versión **no cambia**; gana una responsabilidad nueva |
| 6 | `lib/queries/manual.ts:119` (`_count.companyDocuments`) | catálogo admin | cualquier versión, **a propósito** | **NO TOCAR** |

**Por qué los tres intocables lo son** (esto es lo que impide que un ejecutor
aplique el filtro "de más"):
- **#4** cuenta TODAS las versiones para bloquear el borrado de un
  `ManualDocument` que ya tiene historial en alguna empresa. Filtrar por `VIGENTE`
  permitiría borrar un documento que sólo tiene versiones `OBSOLETO`/`BORRADOR`,
  destruyendo el historial de un cliente.
- **#5** necesita el **máximo real** para no reciclar números de versión. Si sólo
  mirara las `VIGENTE`, tras una publicación volvería a emitir un número ya usado y
  reventaría `@@unique([documentId, companyId, version])`.
- **#6** es un contador informativo ("N versiones de empresa"), no "cuántas
  empresas tienen la vigente".

**El `where` de #2, hoy** (`lib/queries/manual.ts:556`):
```typescript
db.companyDocument.findMany({
  where: {
    companyId: assignment.companyId,
    document: { sections: { some: { sectionId } } },
  },
  orderBy: [{ documentId: "asc" }, { version: "desc" }],
  select: { id: true, documentId: true, version: true, codeOverride: true, fileName: true, fileSize: true },
});
```

**El `where` de #1, hoy** (`lib/queries/manual.ts:287`):
```typescript
db.companyDocument.findMany({
  where: { companyId: assignment.companyId },
  orderBy: [{ documentId: "asc" }, { version: "desc" }],
  select: {
    id: true, documentId: true, version: true, codeOverride: true,
    fileName: true, fileSize: true, createdAt: true,
    uploadedBy: { select: { name: true } },
  },
});
```

**La transacción de #5, hoy** (`lib/actions/manual.ts:880-909`, añadida en la
fase 1 con el commit `160bc5a`) — se reutiliza, no se reinventa:
```typescript
await db.$transaction(async (tx) => {
  // Se bloquea `manual_documents` y no `company_documents` a propósito: el par
  // (documento, empresa) puede no tener ninguna fila todavía, y un FOR UPDATE
  // sobre cero filas no bloquea nada.
  await tx.$queryRaw`SELECT 1 FROM manual_documents WHERE id = ${input.documentId} FOR UPDATE`;

  const last = await tx.companyDocument.findFirst({
    where: { documentId: input.documentId, companyId: assignment.companyId },
    orderBy: { version: "desc" },
    select: { version: true },
  });

  await tx.companyDocument.create({ data: { /* ..., version: (last?.version ?? 0) + 1, ... */ } });
});
```

**Los dos consumidores de esas consultas:**
- `app/dashboard/manuals/[assignmentId]/sections/[sectionId]/page.tsx` construye
  `companyDocByDocumentId` a partir de `companyDocuments` y enlaza a
  `/files/company-document/${own.id}` (líneas ~97-140).
- `components/company-project-panel.tsx` construye `docsByDocumentId` (líneas
  48-50), pinta `Versión {own.version} · {own.fileName} · {autor} · {fecha}`
  (líneas 121-133) y monta `<CompanyDocumentUpload>` sin condición (línea 145).

**Fixture de la base local** (ids exactos en `03-01-SUMMARY.md`): manual publicado
con sección `4.1`, documento `P-RFC-4.1-01` (`kind = PROCEDIMIENTO`) enlazado a la
sección, y dos activaciones (Acme Corp y Constructora Delta).
`company_documents` quedó **vacía** al cerrar el plan 03-01.

**Sesiones** (contraseña `password123`): `carlos.mendoza@gmail.com` (Acme),
`lucia.delgado@constructoradelta.test` (Constructora Delta),
`admin@prol.prosuite.pro` (ADMIN), `maria.garcia@academiadigitalmx.com` (PROFESSOR).
</interfaces>
</context>

<constraints>
- **Sólo dos consultas cambian de `where`.** Si el diff toca el `count` de
  `deleteManualDocument`, el `findFirst` de versión máxima de
  `uploadCompanyDocument` o el `_count` de `getManualForEdit`, el plan está mal
  ejecutado: son los tres intocables de la tabla de arriba.
- **El dedup manual por `documentId` se queda** en las dos consultas. Con el
  invariante sano no debería hacer falta —a lo sumo hay una `VIGENTE` por par—,
  pero quitarlo convertiría una violación del invariante en filas duplicadas en
  pantalla en vez de en un no-evento. Es red de seguridad barata.
- **`uploadCompanyDocument` conserva su `FOR UPDATE` sobre `manual_documents`.** No
  se cambia la tabla bloqueada, no se envuelve en otra transacción, no se le añade
  un segundo lock. La degradación va **dentro** de la transacción que ya existe.
- **Nada de acciones nuevas, nada de UI nueva, nada de identidad de documento.**
  Eso es de los planes 03-04 en adelante.
- La puerta de lint sale con **exit 1** estando sana: `✖ 81 problems (0 errors, 81
  warnings)`.
</constraints>

<tasks>

<task type="auto">
  <name>Tarea 1: "vigente" es un estatus en las dos consultas que lo significan</name>
  <files>apps/web/lib/queries/manual.ts, apps/web/components/company-project-panel.tsx</files>
  <action>
**1. `getAssignmentPanel`** (`lib/queries/manual.ts:287`). Añadir el filtro y los
tres campos que el renderizado consciente del `kind` necesita:

```typescript
    db.companyDocument.findMany({
      // "Vigente" es un estatus, no la versión más alta. Desde que existe
      // BORRADOR las dos cosas dejaron de ser sinónimas, y este panel enlaza a
      // la descarga: enseñar aquí un borrador sin publicar sería enseñárselo al
      // consultor como si fuera lo que su cliente tiene adoptado.
      where: { companyId: assignment.companyId, status: "VIGENTE" },
      orderBy: [{ documentId: "asc" }, { version: "desc" }],
      select: {
        id: true,
        documentId: true,
        version: true,
        codeOverride: true,
        nameOverride: true,
        kind: true,
        status: true,
        fileName: true,
        fileSize: true,
        createdAt: true,
        uploadedBy: { select: { name: true } },
      },
    }),
```

El comentario de la línea 312 ("Sólo la versión vigente (la mayor) de cada
documento") ya no dice la verdad: reescribirlo a "Red de seguridad: con el
invariante sano hay como mucho una VIGENTE por documento; si alguna vez hubiera
dos, se pinta una y no dos filas contradictorias."

**2. `getSectionForCompany`** (`lib/queries/manual.ts:556`). El mismo cambio, con
el `where` que ya trae el join por sección:

```typescript
      db.companyDocument.findMany({
        where: {
          companyId: assignment.companyId,
          status: "VIGENTE",
          document: { sections: { some: { sectionId } } },
        },
        orderBy: [{ documentId: "asc" }, { version: "desc" }],
        select: {
          id: true,
          documentId: true,
          version: true,
          codeOverride: true,
          nameOverride: true,
          kind: true,
          status: true,
          fileName: true,
          fileSize: true,
        },
      }),
```

Mismo tratamiento para el comentario del dedup de la línea ~613.

**3. `company-project-panel.tsx`, fila de documento consciente del `kind`.**
Con `fileName` ahora nullable, la línea `Versión {own.version} · {own.fileName} ·
{autor} · {fecha}` deja un separador huérfano en los documentos nativos ("Versión 2
·  · Ana · 2 sep"). Y el botón de subir archivo no tiene sentido semántico sobre un
procedimiento redactado en la plataforma.

- Sustituir el segmento del nombre de archivo por uno condicional: si
  `own.kind === "FILE"` se pinta `own.fileName`; si no, se omite el segmento
  entero (separador incluido). Construir la línea juntando los trozos con `" · "`
  a partir de un array filtrado, en vez de encadenar ternarios dentro del JSX.
- Mostrar el nombre del documento con `own?.nameOverride ?? doc.name` (el
  `codeOverride` ya se resolvía así en la línea 119; el nombre es el campo nuevo y
  simétrico).
- Envolver `<CompanyDocumentUpload …/>` en `{(!own || own.kind === "FILE") ? … : null}`:
  un documento sin versión de empresa todavía puede recibir un archivo (es el
  flujo de hoy), pero uno cuya versión vigente es nativa, no.
- Para los `kind !== "FILE"`, en lugar del enlace de descarga, pintar una etiqueta
  de texto con el tipo (`Procedimiento`) y la versión. El enlace al visor del
  cliente lo añade el plan 03-07; aquí no se inventa una ruta que todavía no
  existe.
- **No** ocultar los documentos nativos de la lista: la enmienda (c) del CONTEXT
  es explícita — el consultor perdería la vista de qué tiene emitida cada empresa
  justo cuando esta fase existe para dársela.

**No tocar `getManualForEdit`** (`lib/queries/manual.ts:119`): su
`_count: { select: { companyDocuments: true } }` cuenta todas las versiones a
propósito.
  </action>
  <verify>
    <automated>
# Exactamente dos where con el filtro nuevo, y en las dos funciones correctas
grep -n 'status: "VIGENTE"' apps/web/lib/queries/manual.ts
# Esperado: 2 coincidencias (getAssignmentPanel y getSectionForCompany)

# Los intocables siguen intactos
git diff apps/web/lib/actions/manual.ts | grep -n 'personalized' && echo "ERROR: se tocó deleteManualDocument" || echo "OK: #4 intacto"
grep -n '_count: { select: { sections: true, companyDocuments: true } }' apps/web/lib/queries/manual.ts   # sigue sin filtro
git diff apps/web/lib/queries/manual.ts | grep -E '^\+.*_count' && echo "REVISAR: no se debe tocar el _count" || echo "OK: #6 intacto"

# Los campos nuevos entraron en los dos selects
grep -c 'nameOverride: true' apps/web/lib/queries/manual.ts   # 2
grep -c 'kind: true'         apps/web/lib/queries/manual.ts   # >= 2

# El panel ya no pinta fileName sin condición ni monta la subida sin condición
grep -n 'nameOverride' apps/web/components/company-project-panel.tsx
grep -n 'CompanyDocumentUpload' apps/web/components/company-project-panel.tsx

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep 'problems'
    </automated>
  </verify>
  <done>
`getAssignmentPanel` y `getSectionForCompany` filtran por `status: "VIGENTE"` y sus
selects traen `kind`, `status` y `nameOverride`; los comentarios del dedup dicen la
verdad nueva. `deleteManualDocument`, el cálculo de versión de
`uploadCompanyDocument` y el `_count` del catálogo no aparecen en el diff. El panel
del consultor pinta el nombre de archivo sólo para `kind === "FILE"`, resuelve el
nombre con `nameOverride ?? name` y no ofrece subir archivo sobre una versión
nativa, sin dejar de listar los documentos nativos. `check-types` limpio y `lint`
en 81 advertencias.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: subir un archivo degrada la versión anterior</name>
  <files>apps/web/lib/actions/manual.ts</files>
  <action>
El hueco que el CONTEXT no vio y la investigación sí (y que ya está recogido como
enmienda del 2026-09-02): `uploadCompanyDocument` crea filas con el `status` por
defecto —`VIGENTE`— sin tocar la anterior. Tras dos subidas del mismo documento
para la misma empresa habría **dos filas `VIGENTE` simultáneas**, y el filtro que
acaba de entrar en la tarea 1 devolvería dos documentos donde debería devolver uno.
No es opcional: es parte del criterio 5.

Dentro del `db.$transaction` que ya existe (líneas 880-909), **después** del
`FOR UPDATE` y **antes** del `create`:

```typescript
    // Como mucho una VIGENTE por (documento, empresa). Va dentro de la misma
    // transacción y bajo el mismo lock que el cálculo de versión: si se hiciera
    // fuera, dos subidas concurrentes podrían degradar la fila que la otra acaba
    // de crear. Aplica también —sobre todo— a los documentos que se siguen
    // subiendo como archivo: sin esto, el arreglo de OPS-05 se sostiene sólo
    // para los nativos.
    await tx.companyDocument.updateMany({
      where: {
        documentId: input.documentId,
        companyId: assignment.companyId,
        status: "VIGENTE",
      },
      data: { status: "OBSOLETO" },
    });
```

Y en el `create`, hacer explícito lo que hasta ahora era implícito:
```typescript
        kind: "FILE",
        status: "VIGENTE",
```
`kind: "FILE"` porque esta acción sube un archivo, siempre: es el arquetipo de la
fila que crea, y dejarlo al `@default` haría que dependiera de un valor de esquema
que un día podría cambiar.

**Lo que NO se toca en esta función:**
- El `findFirst({ orderBy: { version: "desc" } })` sigue mirando **todas** las
  versiones. Necesita el máximo real; filtrarlo por `VIGENTE` reciclaría números y
  reventaría `@@unique([documentId, companyId, version])`.
- El `FOR UPDATE` sigue sobre `manual_documents`. El par (documento, empresa) puede
  no tener ninguna fila todavía y un `FOR UPDATE` sobre cero filas no bloquea nada.
- El comentario "Append-only: la versión anterior se conserva y la vigente es la
  mayor" ya no es cierto en su segunda mitad. Reescribirlo: append-only sigue
  siendo cierto (no se borra nada), pero la vigente ahora es la que tiene el
  estatus, no la mayor.
  </action>
  <verify>
    <automated>
# La degradación está dentro de la transacción, entre el FOR UPDATE y el create
sed -n '/uploadCompanyDocument/,/^}/p' apps/web/lib/actions/manual.ts | grep -n 'FOR UPDATE\|updateMany\|OBSOLETO\|companyDocument.create\|orderBy: { version: "desc" }'
# Orden esperado: FOR UPDATE → updateMany/OBSOLETO → findFirst(version desc) o al revés, pero SIEMPRE antes del create

# El cálculo de versión sigue sin filtro de estatus
sed -n '/uploadCompanyDocument/,/^}/p' apps/web/lib/actions/manual.ts | grep -A3 'orderBy: { version: "desc" }' | grep 'status' && echo "ERROR: el maximo de version se filtro por estatus" || echo "OK: #5 conserva el maximo real"

# El lock sigue sobre manual_documents
grep -n 'FROM manual_documents WHERE id' apps/web/lib/actions/manual.ts

# El diff toca un solo archivo y no roza los otros dos intocables
git diff --name-only    # apps/web/lib/actions/manual.ts
git diff apps/web/lib/actions/manual.ts | grep -E '^\+.*deleteManualDocument|^-.*personalized' && echo "ERROR" || echo "OK"

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep 'problems'
    </automated>
  </verify>
  <done>
`uploadCompanyDocument` degrada a `OBSOLETO` la fila `VIGENTE` anterior con un
`updateMany` dentro de la transacción existente, bajo el mismo `FOR UPDATE` sobre
`manual_documents`, y crea la nueva con `kind: "FILE"` y `status: "VIGENTE"`
explícitos. El cálculo de la versión máxima sigue mirando todas las versiones. El
comentario de append-only dice la verdad nueva.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: criterio 5 demostrado — el borrador con versión más alta no se ve</name>
  <files>(sin cambios de código; scripts desechables que NO se commitean)</files>
  <action>
El repo no tiene suite de pruebas: el criterio se demuestra contra el servidor
real, con sesiones reales, igual que hizo la fase 2.

**1. Servidor y sesiones.**
```bash
pnpm --filter @prol/web dev > /tmp/prol-dev-03-02.log 2>&1 &
sleep 25
```
Cookies para `carlos.mendoza@gmail.com` (Acme), `admin@prol.prosuite.pro` (ADMIN) y
`lucia.delgado@constructoradelta.test`.

**2. Fabricar el escenario que rompe la vieja lógica.** Script desechable
`packages/db/prisma/_tmp-ops05.ts`, sobre el documento `P-RFC-4.1-01` y **Acme
Corp** (ids en `03-01-SUMMARY.md`):
- **v1, `status: "VIGENTE"`, `kind: "PROCEDIMIENTO"`**, `contentHtml` con el texto
  reconocible `MARCA-VIGENTE-V1`, sin archivo.
- **v2, `status: "BORRADOR"`, `kind: "PROCEDIMIENTO"`**, `contentHtml` con
  `MARCA-BORRADOR-V2`, sin archivo.

Con la lógica anterior (`orderBy version desc` + primera fila) las dos pantallas
habrían elegido la v2. Ése es exactamente el bug.

**3. La página de sección del cliente elige la v1.**
```bash
curl -s -b /tmp/cookies-acme.txt \
  "http://localhost:3000/dashboard/manuals/$ASSIGNMENT_ACME/sections/$SECTION_ID" \
  -o /tmp/seccion-cliente.html -w 'status=%{http_code}\n'
```
Exigir: 200, que el HTML contenga `Versión 1` y **no** `Versión 2`, y que el
enlace de descarga —si lo hubiera— apunte al id de la v1. Si sale 200 pero no
aparece ninguna de las dos, revisar antes de dar nada por bueno: puede que el
documento no esté enlazado a esa sección y la prueba no esté probando nada.

**4. El panel del consultor elige la v1.**
```bash
curl -s -b /tmp/cookies-admin.txt \
  "http://localhost:3000/tenant-admin/projects/$ASSIGNMENT_ACME" \
  -o /tmp/panel-consultor.html -w 'status=%{http_code}\n'
```
Mismas exigencias. Si la ruta del panel del consultor difiere, resolverla mirando
qué página monta `<CompanyProjectPanel>` y anotarlo en el SUMMARY.

**5. Control negativo — sin el arreglo esto fallaría.** Poner temporalmente la v2
en `VIGENTE` y la v1 en `OBSOLETO` con una sentencia SQL, repetir los dos `curl` y
exigir que ahora salga `Versión 2` y no `Versión 1`. Devolver los estatus a como
estaban. Sin este paso, "sale la v1" también sería compatible con "la consulta
está rota y no devuelve nada": lo que se está demostrando es que **sigue el
estatus**, no que ignore la versión más alta por casualidad.

**6. La degradación de `uploadCompanyDocument`, con dos subidas reales.** El
camino completo pasa por una server action, que no se invoca por `curl`; lo que sí
se puede demostrar de punta a punta es el efecto en la base reproduciendo la
**misma forma de transacción** que la acción ejecuta, como hizo el plan 01-02 con
la carrera de versión. Script desechable `packages/db/prisma/_tmp-upload-twice.ts`
que, para el documento y la empresa del fixture, ejecuta **dos veces** seguidas:

```typescript
await prisma.$transaction(async (tx) => {
  await tx.$queryRaw`SELECT 1 FROM manual_documents WHERE id = ${documentId} FOR UPDATE`;
  await tx.companyDocument.updateMany({
    where: { documentId, companyId, status: "VIGENTE" },
    data: { status: "OBSOLETO" },
  });
  const last = await tx.companyDocument.findFirst({
    where: { documentId, companyId },
    orderBy: { version: "desc" },
    select: { version: true },
  });
  await tx.companyDocument.create({
    data: { documentId, companyId, version: (last?.version ?? 0) + 1, kind: "FILE",
            status: "VIGENTE", fileKey: "templates/fake.pdf", fileName: "fake.pdf",
            fileSize: 1234, mimeType: "application/pdf", uploadedById: adminId },
  });
});
```

Exigir después: versiones consecutivas sin hueco, **una sola** `VIGENTE` (la
última) y el resto `OBSOLETO`. Anotar en el SUMMARY que la comprobación se hizo
sobre la forma de la transacción y no invocando la acción, para que nadie lo lea
como más de lo que es.

**7. Invariante global** sobre toda la tabla, no sólo sobre el fixture:
```sql
select count(*) from (
  select document_id, company_id from company_documents
  where status='VIGENTE' group by 1,2 having count(*)>1
) x;
```
Debe dar 0.

**8. Limpieza.** Borrar todas las filas `CompanyDocument` fabricadas y los dos
scripts. `company_documents` vuelve a quedar vacía para el plan 03-03. **No**
borrar nada del bucket ni las dos evidencias de `form_snapshot`.
  </action>
  <verify>
    <automated>
# Cliente: se ve la VIGENTE (v1), no el BORRADOR (v2)
grep -c 'Versión 1' /tmp/seccion-cliente.html    # >= 1
grep -c 'Versión 2' /tmp/seccion-cliente.html    # 0

# Consultor: lo mismo
grep -c 'Versión 1' /tmp/panel-consultor.html    # >= 1
grep -c 'Versión 2' /tmp/panel-consultor.html    # 0

# Control negativo: al mover el estatus, la pantalla sigue al estatus
# (tras el UPDATE temporal del paso 5)
grep -c 'Versión 2' /tmp/seccion-cliente-invertida.html    # >= 1
grep -c 'Versión 1' /tmp/seccion-cliente-invertida.html    # 0

# Dos "subidas": versiones consecutivas y una sola vigente
docker exec prol-db psql -U prol -d prol -Atc "select version||'|'||status from company_documents order by version;"
# Esperado: la última VIGENTE, todas las anteriores OBSOLETO, sin huecos

# Invariante global
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from (select document_id, company_id from company_documents where status='VIGENTE' group by 1,2 having count(*)>1) x;"   # 0

# Limpieza
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from company_documents;"   # 0
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from evidences where form_snapshot is not null;"   # 2
rm -f packages/db/prisma/_tmp-ops05.ts packages/db/prisma/_tmp-upload-twice.ts
git status --porcelain | grep '_tmp-' && echo "ERROR: quedan scripts desechables" || echo "OK: limpio"
pkill -f 'next dev' || true

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep 'problems'
pnpm exec turbo run build
    </automated>
  </verify>
  <done>
Con una v1 `VIGENTE` y una v2 `BORRADOR` en la base, tanto la página de sección del
cliente como el panel del consultor muestran la v1 y no la v2; invirtiendo los
estatus, las dos pantallas cambian a la v2, lo que descarta que la consulta
simplemente no devuelva nada. Dos "subidas" consecutivas con la forma de
transacción de `uploadCompanyDocument` dejan versiones consecutivas y una sola
`VIGENTE`. La consulta de invariante devuelve 0 sobre toda la tabla.
`company_documents` vuelve a estar vacía, los scripts desechables no existen, y las
dos evidencias de `form_snapshot` siguen intactas. `check-types` limpio, `lint` en
81 advertencias, `build` verde.
  </done>
</task>

</tasks>

<verification>
```bash
pnpm exec turbo run check-types                    # limpio
pnpm exec turbo run lint 2>&1 | grep 'problems'    # "✖ 81 problems (0 errors, 81 warnings)"
pnpm exec turbo run build                          # verde
```

Comprobaciones específicas del plan:
- `git diff --name-only` devuelve exactamente `apps/web/lib/queries/manual.ts`,
  `apps/web/lib/actions/manual.ts` y
  `apps/web/components/company-project-panel.tsx`.
- `grep -c 'status: "VIGENTE"' apps/web/lib/queries/manual.ts` devuelve **2**.
- El diff **no** contiene cambios en `deleteManualDocument`, en el
  `findFirst({ orderBy: { version: "desc" } })` de `uploadCompanyDocument` ni en el
  `_count` de `getManualForEdit`.
- `git status --porcelain` no menciona ningún `_tmp-`.
</verification>

<success_criteria>
1. La página de sección del cliente sigue mostrando el documento vigente, no el
   borrador de versión más alta. (Criterio 5 de la fase.)
2. El panel del consultor hace lo mismo.
3. Invertir los estatus invierte lo que se ve: las pantallas siguen al estatus, no
   a la versión.
4. Subir un archivo nuevo degrada la versión anterior a `OBSOLETO` dentro de la
   transacción con lock, y las versiones siguen siendo consecutivas.
5. Cero pares (documento, empresa) con más de una fila `VIGENTE` en toda la base.
6. Los tres puntos de llamada que la investigación marcó como intocables no
   aparecen modificados en el diff.
7. `check-types` limpio, `lint` en 0 errores y 81 advertencias, `build` verde.
</success_criteria>

<output>
Al terminar, crear
`.planning/phases/03-procedimientos-nativos/03-02-SUMMARY.md`.

Registrar en el SUMMARY:
- La tabla de los seis puntos de llamada con lo que se hizo en cada uno, incluidos
  los tres que se dejaron a propósito como estaban y por qué. Es la documentación
  que impide que un plan posterior "termine el trabajo" aplicando el filtro donde
  no debe.
- Los fragmentos de HTML (o los conteos) que demuestran el criterio 5 en las dos
  pantallas, y el resultado del control negativo.
- La ruta real del panel del consultor que se usó para el `curl`, si difiere de
  `/tenant-admin/projects/[assignmentId]`.
- La advertencia explícita de que la degradación se verificó reproduciendo la
  forma de la transacción y no invocando la server action: el plan 03-06 la
  ejercita de verdad por la interfaz.
</output>
</content>
