---
phase: 03-procedimientos-nativos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/db/prisma/seed.ts
  - packages/db/prisma/seed-password.ts
  - packages/db/prisma/seed-documents.ts
  - apps/web/app/files/company-document/[id]/route.ts
autonomous: true
requirements: [DOC-01, DOC-03, DOC-06]
parallel_safe: false  # Ola 1 con un solo plan a propósito. Toca el esquema, del
                      # que cuelga todo lo demás, y `branching_strategy` es
                      # "none": en la fase 1 dos ejecutores concurrentes sobre el
                      # mismo working tree se absorbieron archivos entre el `add`
                      # y el `commit`. La fase 2 serializó y funcionó.

must_haves:
  truths:
    - "`ManualDocument` puede guardar un cuerpo HTML y un número de versión de plantilla; `CompanyDocument` puede guardar un cuerpo congelado, un estatus y la versión de plantilla de la que salió."
    - "`CompanyDocument.fileKey/fileName/fileSize/mimeType` admiten nulo, y ningún lector se rompe: `/files/company-document/[id]` responde 404 —no 500— cuando la fila no tiene archivo."
    - "La base local tiene dos empresas con logo y razón social distintos, un manual publicado con una sección, un documento `PROCEDIMIENTO` con cuerpo, y una activación por empresa."
    - "El mismo juego de datos se reconstruye con `pnpm --filter @prol/db db:seed` en una base vacía: la fixture está en el seed, no sólo en la base de esta máquina."
    - "Las dos evidencias de `form_snapshot` que dejó la fase 1 (`1|f` y `2|t`) siguen en la base local."
    - "Como mucho una fila `VIGENTE` por (documento, empresa) en toda la base, también para las filas que existían antes del cambio."
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "Enums ManualDocumentKind y CompanyDocumentStatus, columnas nativas y las cuatro columnas de archivo relajadas a nullable"
      contains: "CompanyDocumentStatus"
    - path: "packages/db/prisma/seed-documents.ts"
      provides: "Fixture idempotente del módulo documental: segunda empresa, manual, sección, documento PROCEDIMIENTO y dos activaciones"
      min_lines: 120
    - path: "packages/db/prisma/seed-password.ts"
      provides: "hashPassword con la configuración de scrypt de Better Auth, en un solo sitio"
    - path: "apps/web/app/files/company-document/[id]/route.ts"
      provides: "Guarda de fileKey nulo antes de readPrivateFile"
      contains: "if (!doc.fileKey)"
  key_links:
    - from: "packages/db/prisma/seed.ts"
      to: "packages/db/prisma/seed-documents.ts"
      via: "import { seedDocumentFixture }"
      pattern: "seedDocumentFixture"
    - from: "apps/web/app/files/company-document/[id]/route.ts"
      to: "respuesta 404 en vez de readPrivateFile(null)"
      via: "guarda copiada de app/files/evidence/[id]/route.ts:39"
      pattern: "!doc.fileKey"
---

<objective>
Meter en el esquema todo lo que el documento nativo necesita —dos enums, nueve
columnas nuevas y cuatro relajadas a nullable—, dejar el árbol compilando, y
fabricar la fixture reproducible sin la cual ningún criterio de esta fase se
puede verificar.

Purpose: es el cimiento de las siete ollas siguientes. Además cierra un pendiente
arrastrado desde la fase 1 (falta una segunda empresa en el seed) que el criterio
1 convierte en bloqueante: sin dos empresas no hay forma de enseñar que la misma
plantilla se ve distinta en cada una.
Output: esquema aplicado con `db push`, `/files/company-document/[id]` a prueba de
filas sin archivo, y una base local con dos empresas, un manual, un documento
`PROCEDIMIENTO` y dos activaciones — todo también en el seed.
</objective>

<execution_context>
@/Users/flx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-procedimientos-nativos/03-CONTEXT.md
@.planning/phases/03-procedimientos-nativos/03-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

@packages/db/prisma/schema.prisma
@packages/db/prisma/seed.ts
@apps/web/app/files/company-document/[id]/route.ts
@apps/web/app/files/evidence/[id]/route.ts

<interfaces>
<!-- Todo transcrito del código y de la base local reales. No hace falta explorar. -->

**Estado de la base local, consultado el 2026-09-02** (`localhost:5435`,
contenedor `prol-db`, rol `prol`, base `prol`):

| Qué | Valor |
|---|---|
| Tenant | `cmtj13ozx0000p1bgg0a14a0f` · `academia-digital` · `documents_enabled = true` |
| Empresas | **una**: Acme Corp `cmtj13pae007ip1bgkz47qn7h`, sin logo, sin `dc3_legal_name` |
| Manual | `cmtj66abg0001t30l7tpt2in0` "Manual de prueba OPS-04" (PUBLISHED) |
| Sección | `cmtj66abo0005t30l4dkcn9ix` · código `6.1` |
| Activación | `cmtj66ac20009t30l32b3yipo` (Acme sobre ese manual) |
| `manual_documents` | **0 filas** |
| `company_documents` | **0 filas** |
| Evidencias con `form_snapshot` | dos (`1\|f` y `2\|t`) — **banco de regresión de la fase 1, no borrar** |

Consulta psql que se usa en todo el plan (sin exponer la contraseña, que está en
`.env`):
```bash
docker exec prol-db psql -U prol -d prol -Atc "<consulta>"
```

**Usuarios del seed** (contraseña `password123` para todos):

| Correo | Rol | Empresa |
|---|---|---|
| `carlos.mendoza@gmail.com` | STUDENT | Acme Corp |
| `ana.rodriguez@outlook.com` | STUDENT | Acme Corp |
| `maria.garcia@academiadigitalmx.com` | PROFESSOR | — (revisora del tenant) |
| `admin@prol.prosuite.pro` | **ADMIN** | — |
| `super@prol.prosuite.pro` | SUPER_ADMIN | — |

**El seed BORRA la base entera antes de sembrar** (`prisma.tenant.deleteMany()`
en cascada, `seed.ts` líneas ~20-45). Por eso este plan **no ejecuta
`db:seed` contra la base local**: se llevaría por delante las dos evidencias de
`form_snapshot` de la fase 1. La fixture se escribe en el seed *para que sea
reproducible* y se aplica a la base viva con un runner desechable que llama a la
**misma** función. Una sola fuente de verdad, cero destrucción.

**`hashPassword` vive hoy dentro de `seed.ts`** (líneas 13-22) y clava la
configuración de scrypt de Better Auth (`N: 16384, r: 16, p: 1, maxmem: 128 *
16384 * 16 * 2`). Si el hash no coincide, el login por API falla y las
verificaciones de sesión de los planes siguientes no prueban nada. Por eso se
extrae a su propio módulo en vez de copiarse.

**Cuenta de credenciales de Better Auth** (molde exacto de `seed.ts`):
```typescript
await prisma.account.create({
  data: {
    id: `account_${user.id}`,
    userId: user.id,
    accountId: user.id,
    providerId: "credential",
    password: hashedPw,
  },
});
```

**La guarda que hay que copiar, ya en producción** (`apps/web/app/files/evidence/[id]/route.ts:39`):
```typescript
if (!evidence?.fileKey || evidence.deletedAt) {
  return new NextResponse("No encontrado", { status: 404 });
}
```

**El único lector que rompe la compilación** (`app/files/company-document/[id]/route.ts:41`):
`readPrivateFile(doc.fileKey)` — la firma es `readPrivateFile(fileKey: string)`, y
`strict: true` está confirmado en `packages/typescript-config/base.json:16`.
`components/company-project-panel.tsx:123` renderiza `{own.fileName}` en JSX: con
`null` **no** rompe la compilación (React lo pinta como nada), sólo deja un
separador huérfano — se arregla en el plan 03-02, no aquí.

**Ruta de subida que sirve para fabricar un `fileKey` real** (para la tarea 3):
`POST /api/upload/document-template` exige `requireManualReviewer()` (ADMIN,
SUPER_ADMIN o PROFESSOR) y devuelve `{ fileKey, fileName, fileSize, mimeType }`
con `fileKey = templates/<uuid>.<ext>`.

**Sesión por API** (Better Auth; el plugin de captcha sólo se activa con
`TURNSTILE_SECRET_KEY`, que no está en el `.env` local):
```bash
curl -s -c /tmp/cookies-X.txt -X POST http://localhost:3000/api/auth/sign-in/email \
  -H 'Content-Type: application/json' \
  -d '{"email":"...","password":"password123"}'
```
</interfaces>
</context>

<constraints>
- **No hay directorio de migraciones.** El esquema viaja con `prisma db push`.
  Todos los cambios son aditivos salvo relajar `NOT NULL` en cuatro columnas de
  `company_documents`, que `db push` aplica con `ALTER COLUMN ... DROP NOT NULL`,
  sin pérdida ni backfill.
- **`formSchema` NO se añade.** Es de los registros (fase 5). Tampoco
  `CompanyRecord`, ni bloques, ni valores nuevos en `EvidenceRequirementKind`.
- **No ejecutar `pnpm --filter @prol/db db:seed` contra la base local.** Borra
  todo, incluidas las dos evidencias de `form_snapshot` de la fase 1. La fixture
  se aplica con el runner desechable de la tarea 2.
- **Ninguna operación de borrado en el bucket de R2.** Está compartido con datos
  de producción de otro producto.
- **Nada de PDF, nada de URL firmada, nada de UI.** Fases 4 y 6, y planes
  posteriores de ésta.
- La puerta de lint sale con **exit 1 estando sana**: `✖ 81 problems (0 errors,
  81 warnings)`. No es un fallo y no hay que "arreglar" esas 81; lo que no puede
  pasar es que suba el número o aparezca un error.
</constraints>

<tasks>

<task type="auto">
  <name>Tarea 1: esquema del documento nativo y guarda de `fileKey` nulo</name>
  <files>packages/db/prisma/schema.prisma, apps/web/app/files/company-document/[id]/route.ts</files>
  <action>
**1. Dos enums nuevos.** Colocarlos junto al resto de enums del módulo documental
(cerca de `ManualItemKind` / `ManualStatus`), con su comentario:

```prisma
/// Arquetipo del documento. Ternario y no `FILE|NATIVE` + `formSchema != null`
/// porque con dos valores cada lector tendría que preguntar después cuál de los
/// dos arquetipos nativos es: un estado ternario codificado como booleano más
/// campo opcional. La UX ramifica en tres y el precedente del repo es enum
/// explícito (`ManualItemKind`, `EvaluationKind`).
enum ManualDocumentKind {
  FILE
  PROCEDIMIENTO
  REGISTRO
}

/// Ciclo de vida de la versión de una empresa. Antes de esta fase "vigente" y
/// "versión más alta" eran sinónimos exactos; con `BORRADOR` en juego dejan de
/// serlo, y por eso el estatus pasa a ser explícito (OPS-05).
enum CompanyDocumentStatus {
  BORRADOR
  VIGENTE
  OBSOLETO
}
```

**2. `ManualDocument` gana tres columnas** (`schema.prisma`, modelo
`ManualDocument`, después del bloque `baseFileKey`/`baseFileName`/…):

```prisma
  kind ManualDocumentKind @default(FILE)

  /// Cuerpo del procedimiento en HTML **ya saneado** por `sanitizeManualHtml`.
  /// Nulo mientras el documento sea `FILE`. Sanear al escribir y no al leer es
  /// el invariante que hace seguro el único `dangerouslySetInnerHTML` del
  /// proyecto (`components/manual-content.tsx`).
  contentHtml String? @db.Text @map("content_html")

  /// Sube SÓLO cuando `contentHtml` cambia de verdad, no en cada guardado. Es el
  /// número que el cliente ve ("basada en plantilla v3") y el que dispara la
  /// insignia de DOC-07: si subiera con cada pulsación de guardar, el aviso de
  /// "hay una versión más reciente" se dispararía por cambios que no existen.
  templateVersion Int @default(1) @map("template_version")
```

**3. `CompanyDocument` gana seis columnas y relaja cuatro.** Sustituir el bloque
`fileKey`/`fileName`/`fileSize`/`mimeType` y añadir el resto:

```prisma
  /// Denormalizado desde `ManualDocument`, igual que `Evidence.kind` ya lo está.
  /// Un documento puede convertirse de `FILE` a `PROCEDIMIENTO` tras importar el
  /// Word, y la v1 que se subió con `fileKey` no debe intentar renderizarse como
  /// nativa: cada fila recuerda qué era cuando se emitió.
  kind ManualDocumentKind @default(FILE)

  /// Snapshot del cuerpo en el momento de emitir. Editar la plantilla después no
  /// toca esto: es lo que hace que DOC-03 sea cierto.
  contentHtml String? @db.Text @map("content_html")

  /// Nombre propio de la empresa cuando difiere del base. Simétrico al
  /// `codeOverride` que ya existía.
  nameOverride String? @map("name_override")

  status CompanyDocumentStatus @default(VIGENTE)

  /// `ManualDocument.templateVersion` del que salió este snapshot. Comparado con
  /// el actual de la plantilla produce la insignia de DOC-07.
  sourceTemplateVersion Int? @map("source_template_version")

  publishedAt   DateTime? @map("published_at")
  publishedById String?   @map("published_by")
  publishedBy   User?     @relation("CompanyDocumentPublisher", fields: [publishedById], references: [id], onDelete: SetNull)

  /// Nulos cuando la versión es nativa: el cuerpo vive en `contentHtml` y no hay
  /// archivo que descargar. Mismo diseño que `Evidence`, donde estos cuatro
  /// campos ya son nulos desde el principio para las capturas hechas en la
  /// plataforma (`schema.prisma`, modelo `Evidence`).
  fileKey  String? @map("file_key")
  fileName String? @map("file_name")
  fileSize Int?    @map("file_size")
  mimeType String? @map("mime_type")
```

**4. Contra-relación en `User`.** Junto a `companyDocumentsUploaded` (línea ~624):

```prisma
  companyDocumentsPublished   CompanyDocument[]            @relation("CompanyDocumentPublisher")
```

Sin esto `prisma validate` falla: toda relación con nombre necesita sus dos
extremos.

**5. No se añade ningún índice.** Los `@@index([companyId])` y
`@@index([documentId])` que ya existen cubren las consultas de esta fase, y
`db push` con índices nuevos es superficie extra sin beneficio medido. Si algún
día hiciera falta, es aditivo y barato.

**6. Aplicar y generar:**
```bash
pnpm --filter @prol/db exec prisma validate
pnpm --filter @prol/db exec prisma db push
pnpm --filter @prol/db exec prisma generate
```
Los dos enums son **nuevos**, así que Postgres los crea con `CREATE TYPE` normal;
el caveat de `ALTER TYPE ... ADD VALUE` fuera de transacción sólo aplica al añadir
valores a un enum existente. Si `db push` pidiera `--accept-data-loss`, **parar y
reportar**: ninguno de estos cambios pierde datos, así que la petición
significaría que el diff no es el que este plan describe.

**7. Backfill del invariante, idempotente.** `status` entra con
`@default(VIGENTE)`, así que toda fila preexistente queda `VIGENTE` — y si alguna
empresa tuviera dos versiones del mismo documento quedarían **dos vigentes**, que
es exactamente lo que rompe los arreglos del plan 03-02. La base local tiene cero
filas, así que hoy es un no-op; se ejecuta igual porque un no-op comprobado vale
más que uno supuesto, y porque el plan 03-08 repite esta misma sentencia contra
producción:

```sql
UPDATE company_documents c
SET status = 'OBSOLETO'
WHERE c.status = 'VIGENTE'
  AND EXISTS (
    SELECT 1 FROM company_documents c2
    WHERE c2.document_id = c.document_id
      AND c2.company_id  = c.company_id
      AND c2.version     > c.version
  );
```

**8. Guarda de `fileKey` nulo** en `apps/web/app/files/company-document/[id]/route.ts`.
Justo después del `if (!doc) return ...` y **antes** del `readPrivateFile`
(línea 41), copiando el patrón que `app/files/evidence/[id]/route.ts:39` ya usa en
producción:

```typescript
    // Una versión nativa no tiene archivo: su cuerpo vive en `contentHtml` y se
    // lee por la vista del documento, no por aquí. 404 y no 500 — pedir el
    // archivo de algo que no lo tiene es un "no existe", no un fallo del
    // servidor. Mismo guard que /files/evidence/[id], donde `fileKey` es nullable
    // desde el diseño original.
    if (!doc.fileKey) return new NextResponse("No encontrado", { status: 404 });
```

`doc.fileName` y `doc.mimeType` también son nulos ahora y se pasan a
`privateFileResponse({ fileName: string; mimeType: string })`. Después de la
guarda, TypeScript **no** los estrecha (son campos independientes), así que dar el
fallback explícito en la llamada:

```typescript
    return privateFileResponse(buffer, {
      fileName: doc.fileName ?? "documento",
      mimeType: doc.mimeType ?? "application/octet-stream",
    });
```

**Nada más en esta ruta.** El `findUnique({ where: { id } })` **no** gana filtro de
`status`: descargar una versión histórica por su propio id es correcto y sigue
siéndolo (call site #3 de la tabla de OPS-05 del research).
  </action>
  <verify>
    <automated>
# El esquema declara lo que debe y nada más
pnpm --filter @prol/db exec prisma validate

# Enums creados en Postgres con sus tres valores cada uno
docker exec prol-db psql -U prol -d prol -Atc "select t.typname||' = '||string_agg(e.enumlabel, ',' order by e.enumsortorder) from pg_type t join pg_enum e on e.enumtypid = t.oid where t.typname in ('ManualDocumentKind','CompanyDocumentStatus') group by t.typname;"
# Esperado: CompanyDocumentStatus = BORRADOR,VIGENTE,OBSOLETO
#           ManualDocumentKind = FILE,PROCEDIMIENTO,REGISTRO

# Columnas nuevas y nulabilidad de las cuatro de archivo
docker exec prol-db psql -U prol -d prol -Atc "select table_name||'.'||column_name||' nullable='||is_nullable from information_schema.columns where table_name in ('manual_documents','company_documents') and column_name in ('kind','content_html','template_version','name_override','status','source_template_version','published_at','published_by','file_key','file_name','file_size','mime_type') order by table_name, column_name;"
# Esperado: las cuatro de archivo con nullable=YES; kind/status/template_version con nullable=NO

# El invariante se sostiene tras el backfill (cero pares con más de una VIGENTE)
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from (select document_id, company_id from company_documents where status='VIGENTE' group by 1,2 having count(*)>1) x;"
# Esperado: 0

# La guarda está antes de readPrivateFile
grep -n 'doc.fileKey' 'apps/web/app/files/company-document/[id]/route.ts'

# El where de la ruta NO ganó filtro de estatus (call site #3: no se toca)
grep -n 'status' 'apps/web/app/files/company-document/[id]/route.ts' && echo "REVISAR: la ruta no debe filtrar por estatus" || echo "OK: sin filtro de estatus"

# Las evidencias de la fase 1 intactas
docker exec prol-db psql -U prol -d prol -Atc "select version||'|'||(form_snapshot::jsonb ? 'snapshotVersion') from evidences where form_snapshot is not null order by version;"
# Esperado: 1|f y 2|t

# Puertas transversales
pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep 'problems'   # "✖ 81 problems (0 errors, 81 warnings)"
    </automated>
  </verify>
  <done>
`prisma validate` limpio y `db push` aplicado sin pedir `--accept-data-loss`. Los
dos enums existen en Postgres con sus tres valores. `manual_documents` tiene
`kind`, `content_html` y `template_version`; `company_documents` tiene `kind`,
`content_html`, `name_override`, `status`, `source_template_version`,
`published_at` y `published_by`, y sus cuatro columnas de archivo admiten nulo. El
backfill dejó cero pares (documento, empresa) con más de una fila `VIGENTE`.
`/files/company-document/[id]` devuelve 404 —no 500— ante una fila sin `fileKey`, y
su `where` sigue siendo `findUnique({ where: { id } })` sin filtro de estatus. Las
dos evidencias de `form_snapshot` siguen ahí. `check-types` limpio y `lint` en
`✖ 81 problems (0 errors, 81 warnings)`.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: fixture reproducible del módulo documental</name>
  <files>packages/db/prisma/seed-password.ts, packages/db/prisma/seed-documents.ts, packages/db/prisma/seed.ts</files>
  <action>
Los criterios 1-6 de esta fase necesitan **dos empresas con marca distinta**, un
manual publicado con una sección, un documento `PROCEDIMIENTO` con cuerpo, dos
activaciones y un usuario en cada empresa. Hoy no existe nada de eso: el seed sólo
crea Acme Corp, y el manual de la base local se fabricó a mano en la fase 1.

**1. `packages/db/prisma/seed-password.ts`** — extraer `hashPassword` de
`seed.ts` (líneas 13-22) tal cual, sin cambiar ni un parámetro de scrypt, y
sustituir en `seed.ts` la definición local por el import. Motivo en el docstring:
la configuración tiene que coincidir con la de Better Auth o el login por API
falla, y dos copias divergen.

**2. `packages/db/prisma/seed-documents.ts`** — la fixture, **idempotente**, en
una función exportada:

```typescript
import type { PrismaClient } from "@prisma/client";

export interface DocumentFixtureIds {
  tenantId: string;
  manualId: string;
  sectionId: string;
  documentId: string;
  companies: { id: string; name: string; slug: string; assignmentId: string }[];
  leaderUserId: string;
}

/**
 * Juego mínimo del módulo documental para poder verificar la fase 3.
 *
 * Idempotente a propósito: se llama desde el seed (base recién limpiada) y
 * también desde un runner contra una base viva que NO se puede borrar — la fase 1
 * dejó dos evidencias con `form_snapshot` que son el banco de regresión de
 * OPS-04. Todo va por `upsert` o por búsqueda previa; ninguna sentencia borra.
 */
export async function seedDocumentFixture(
  prisma: PrismaClient,
  opts: { tenantId: string; authorUserId: string; hashPassword: (p: string) => Promise<string> },
): Promise<DocumentFixtureIds>
```

Qué crea, en orden:

a) `tenant.update({ where: { id: tenantId }, data: { documentsEnabled: true } })`.
   Sin esto `assertDocumentsEnabled` tumba todas las rutas del módulo para
   cualquiera que no sea SUPER_ADMIN. En la base local ya está en `true`; en una
   base recién sembrada, no.

b) **Dos empresas con marca distinta.** `upsert` por `tenantId_slug`:

   | slug | name | dc3LegalName | logo |
   |---|---|---|---|
   | `acme-corp` | Acme Corp | `Acme Corporation, S.A. de C.V.` | SVG verde con "AC" |
   | `constructora-delta` | Constructora Delta | `Constructora Delta, S.A. de C.V.` | SVG azul con "CD" |

   El logo va como **data-URI de un SVG** en línea (`data:image/svg+xml;base64,…`),
   no como archivo en `public/`: `Company.logo` se pinta directo como `src` de un
   `<img>` (patrón de `components/tenant-brand.tsx:21-23`), el navegador lo
   resuelve sin red, y así la fixture no ensucia el bundle con activos de demo.
   Generar cada SVG con un `<rect>` de color y un `<text>` con las dos iniciales,
   codificarlo con `Buffer.from(svg).toString("base64")`.

   La razón social y el logo distintos son **el criterio 1**: es lo único que hace
   observable que la misma plantilla se ve diferente en cada empresa.

c) **Una usuaria en la segunda empresa**, para poder abrir una sesión de "otra
   empresa" en las verificaciones de autorización y para ver el documento como
   cliente: `lucia.delgado@constructoradelta.test`, nombre "Lucía Delgado", rol
   `STUDENT`, `tenantId`, `companyId` de Constructora Delta, `emailVerified: true`,
   `onboardingCompleted: true`, contraseña `password123` por `hashPassword`, con su
   fila `account` (`providerId: "credential"`, molde exacto de `seed.ts`).
   Ponerla además como `leaderId` de su empresa. `upsert` por `email`.

d) **Manual** "Manual de Gestión de Calidad ISO 9001", `normaLabel`
   `"ISO 9001:2015"`, `status: "PUBLISHED"`, `createdById: authorUserId`.
   `Manual` no tiene clave única natural: buscar primero por
   `findFirst({ where: { tenantId, title } })` y crear sólo si falta.

e) **Capítulo** "4. Contexto de la organización" (`position: 0`) y **sección**
   `code: "4.1"`, título "Comprensión de la organización y de su contexto",
   `position: 0`. Misma técnica de idempotencia (buscar por padre + título).

f) **`ManualDocument`** `upsert` por `manualId_code`:
   - `code: "P-RFC-4.1-01"`, `name: "Procedimiento de control de documentos"`
   - `kind: "PROCEDIMIENTO"`, `templateVersion: 1`
   - `description`: una línea de para qué sirve
   - `contentHtml`: cuerpo **ya en el dialecto que sobrevive al sanitizador** —
     `<h2>`, `<p>`, `<ul><li>` y una `<table>` con `<thead>`/`<tbody>` y una celda
     con `colspan`. Sin `<h1>`, sin `style=`, sin `<script>`. Que tenga tabla
     importa: es lo que los planes 03-06 y 03-07 enseñan en pantalla.

   Ojo con el `upsert`: en `update` **no** tocar `contentHtml` ni
   `templateVersion` si la fila ya existe, para no pisar lo que el consultor haya
   escrito al ejecutar el runner una segunda vez.

g) **Enlace documento↔sección**: `manualSectionDocument.upsert` por
   `sectionId_documentId`, `position: 0`.

h) **Dos activaciones**: `manualAssignment.upsert` por `manualId_companyId`,
   `tenantId`, `activatedById: authorUserId`, `status: "ACTIVE"`.

i) Devolver `DocumentFixtureIds` y hacer un `console.log` con los ids: los planes
   siguientes los necesitan y van al SUMMARY.

**No crea ningún `CompanyDocument`.** Emitir es lo que demuestra el criterio 1, y
lo hace el plan 03-06 por la interfaz; sembrar el resultado adelantaría la
respuesta al examen.

**3. Enganchar en `seed.ts`:** importar `seedDocumentFixture` y llamarlo al final
del bloque de la empresa demo (después de `acmeCorp`, antes del resumen), pasando
`tenant.id`, `admin.id` y `hashPassword`. Añadir su línea al resumen final.

**4. Aplicar a la base local con un runner desechable** —
`packages/db/prisma/_tmp-apply-fixture.ts`, que instancia `PrismaClient`, llama a
`seedDocumentFixture` con el tenant `cmtj13ozx0000p1bgg0a14a0f` y el id de
`admin@prol.prosuite.pro`, imprime los ids y sale:

```bash
pnpm --filter @prol/db exec tsx prisma/_tmp-apply-fixture.ts
```

Ejecutarlo **dos veces** y comprobar que la segunda no crea nada nuevo (mismos
ids, mismos conteos): la idempotencia no es un adorno, es lo que permite volver a
aplicarla si un plan posterior ensucia la base. Borrar el runner al terminar.

**NO ejecutar `pnpm --filter @prol/db db:seed`.** Borraría las dos evidencias de
`form_snapshot` de la fase 1.
  </action>
  <verify>
    <automated>
# hashPassword vive en un solo sitio
grep -n 'scryptAsync' packages/db/prisma/seed-password.ts
grep -c 'function hashPassword' packages/db/prisma/seed.ts   # 0: ya no lo define, lo importa
grep -n 'seed-password' packages/db/prisma/seed.ts packages/db/prisma/seed-documents.ts

# El seed llama a la fixture
grep -n 'seedDocumentFixture' packages/db/prisma/seed.ts

# Dos empresas, las dos con logo y razón social
docker exec prol-db psql -U prol -d prol -Atc "select slug||' | '||coalesce(dc3_legal_name,'SIN RAZON SOCIAL')||' | logo='||(logo is not null) from companies order by slug;"
# Esperado: dos filas, ambas con razón social y logo=t

# Manual publicado, sección, documento PROCEDIMIENTO con cuerpo y tabla
docker exec prol-db psql -U prol -d prol -Atc "select title||' | '||status||' | '||coalesce(norma_label,'-') from manuals order by title;"
docker exec prol-db psql -U prol -d prol -Atc "select code||' | '||kind||' | tv='||template_version||' | tabla='||(content_html like '%<table%')||' | h1='||(content_html like '%<h1%') from manual_documents;"
# Esperado: P-RFC-4.1-01 | PROCEDIMIENTO | tv=1 | tabla=t | h1=f

# El documento está enlazado a la sección
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from manual_section_documents;"   # >= 1

# Dos activaciones, una por empresa
docker exec prol-db psql -U prol -d prol -Atc "select c.slug||' -> '||a.status from manual_assignments a join companies c on c.id=a.company_id join manuals m on m.id=a.manual_id where m.title like 'Manual de Gestión%' order by c.slug;"
# Esperado: dos filas ACTIVE

# La usuaria de la segunda empresa puede iniciar sesión de verdad
curl -s -c /tmp/cookies-delta.txt -o /dev/null -w 'login=%{http_code}\n' \
  -X POST http://localhost:3000/api/auth/sign-in/email \
  -H 'Content-Type: application/json' \
  -d '{"email":"lucia.delgado@constructoradelta.test","password":"password123"}'
# Esperado: 200. Si no, el hash de scrypt no coincide con Better Auth y hay que
# arreglarlo AQUÍ: los planes 03-02, 03-06 y 03-07 dependen de esta sesión.

# Idempotencia: segunda pasada sin filas nuevas
docker exec prol-db psql -U prol -d prol -Atc "select (select count(*) from companies)||'/'||(select count(*) from manuals)||'/'||(select count(*) from manual_documents)||'/'||(select count(*) from manual_assignments);"
# Anotar, volver a correr el runner, repetir la consulta: mismos números

# Las evidencias de la fase 1 siguen ahí (el seed NO se ejecutó)
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from evidences where form_snapshot is not null;"   # 2

rm -f packages/db/prisma/_tmp-apply-fixture.ts
git status --porcelain | grep '_tmp-' && echo "ERROR: queda un script desechable" || echo "OK: limpio"

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep 'problems'
    </automated>
  </verify>
  <done>
`hashPassword` vive sólo en `seed-password.ts` y lo importan tanto `seed.ts` como
`seed-documents.ts`. `seedDocumentFixture` está enganchada al seed y aplicada a la
base local sin haber ejecutado `db:seed`: hay dos empresas con logo y razón social
distintos, un manual publicado con `normaLabel`, un capítulo, una sección `4.1`, un
`ManualDocument` `P-RFC-4.1-01` de `kind = PROCEDIMIENTO` con cuerpo que contiene
una `<table>` y ningún `<h1>`, el enlace documento↔sección, dos activaciones y una
usuaria con sesión funcionando en Constructora Delta. Ejecutar el runner dos veces
no crea filas nuevas. Las dos evidencias de `form_snapshot` siguen en la base. El
runner desechable ya no existe.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: /files/company-document con las tres sesiones y una fila sin archivo</name>
  <files>(sin cambios de código; fixtures y scripts desechables que NO se commitean)</files>
  <action>
La ruta de descarga es el único lector de `fileKey` que la nulabilidad podía
romper en runtime. Hay que demostrar cuatro cosas sobre ella, no una: que la fila
sin archivo devuelve 404, y que la autorización **sigue igual** que antes del
cambio de esquema. El camino feliz solo no basta.

**1. Servidor y sesiones.**
```bash
pnpm --filter @prol/web dev > /tmp/prol-dev-03-01.log 2>&1 &
sleep 25
```
Tres cookies:
- `/tmp/cookies-acme.txt` → `carlos.mendoza@gmail.com` (miembro de Acme Corp)
- `/tmp/cookies-delta.txt` → `lucia.delgado@constructoradelta.test` (otra empresa)
- `/tmp/cookies-admin.txt` → `admin@prol.prosuite.pro` (ADMIN del tenant)

**2. Fabricar un `fileKey` real** subiendo un PDF de prueba por la ruta que ya
existe (con la sesión de admin, que pasa `requireManualReviewer`):
```bash
curl -s -b /tmp/cookies-admin.txt -F "file=@/tmp/plantilla.pdf;type=application/pdf" \
  http://localhost:3000/api/upload/document-template
```
Guardar el `fileKey`, `fileName`, `fileSize` y `mimeType` que devuelve. El archivo
debe superar los 10 bytes (por debajo lo rechaza `storePrivateFile`).

**3. Dos filas `CompanyDocument` desechables**, con un script en `packages/db`
(`prisma/_tmp-companydoc-fixture.ts`), sobre el documento `P-RFC-4.1-01` y la
empresa **Acme Corp**:
- **Fila A, `kind: "FILE"`**: `version: 1`, `status: "VIGENTE"`, con los cuatro
  campos de archivo del paso 2.
- **Fila B, `kind: "PROCEDIMIENTO"`**: `version: 2`, `status: "VIGENTE"`,
  `contentHtml` con dos párrafos, y `fileKey`/`fileName`/`fileSize`/`mimeType`
  **en `null`** — la fila que antes de este plan habría reventado la ruta.

  Ojo: con las dos en `VIGENTE` el invariante queda temporalmente roto a
  propósito, porque aquí sólo se prueba la ruta de descarga (que busca por id). Al
  terminar la tarea las dos filas se borran, así que el plan 03-02 empieza con la
  base limpia. Comprobarlo explícitamente al final.

El script imprime los dos ids.

**4. La matriz completa**, sobre la **fila A** (la que sí tiene archivo):

| Sesión | Esperado | Por qué |
|---|---|---|
| ninguna | **401** | `requireUser()` lanza `UnauthenticatedError` y el `catch` de la ruta lo traduce (arreglo `5e2352d`, en el repo desde la fase 2) |
| Constructora Delta | **403** | `user.companyId !== doc.companyId` |
| Acme Corp | **200** | control positivo: sin él, 401 y 403 serían compatibles con "la ruta está rota para todos" |
| ADMIN del tenant | **200** | el otro brazo del `if`: `isManualReviewer` + mismo tenant |

Y sobre la **fila B** (nativa, sin archivo), con la sesión de Acme Corp: **404**,
no 500 y no 200. Comprobar además que el cuerpo es `No encontrado` y que en
`/tmp/prol-dev-03-01.log` **no** aparece ninguna traza de excepción de
`readPrivateFile` a esa hora: un 404 que por dentro fue un throw capturado no es
el arreglo que este plan quería.

Si el 200 de Acme falla porque el objeto no está en el bucket, revisar el
`fileKey` del paso 2 antes de dar nada por bueno — un 404 por archivo ausente no
demuestra la guarda.

**5. Limpieza.** Borrar las dos filas `CompanyDocument` y los dos scripts
desechables. **No borrar el objeto del bucket**: en este milestone no existe
ninguna operación de borrado en R2.
  </action>
  <verify>
    <automated>
# Fila A: las cuatro sesiones
echo "sin sesion:";     curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3000/files/company-document/$ROW_A
echo "otra empresa:";   curl -s -b /tmp/cookies-delta.txt -o /dev/null -w '%{http_code}\n' http://localhost:3000/files/company-document/$ROW_A
echo "empresa duena:";  curl -s -b /tmp/cookies-acme.txt  -o /dev/null -w '%{http_code}\n' http://localhost:3000/files/company-document/$ROW_A
echo "admin tenant:";   curl -s -b /tmp/cookies-admin.txt -o /dev/null -w '%{http_code}\n' http://localhost:3000/files/company-document/$ROW_A
# Esperado: 401, 403, 200, 200

# Fila B (nativa, sin archivo): 404 limpio, no 500
curl -s -b /tmp/cookies-acme.txt -o /tmp/cuerpo-b.txt -w 'nativa=%{http_code}\n' http://localhost:3000/files/company-document/$ROW_B
cat /tmp/cuerpo-b.txt    # "No encontrado"
grep -c 'readPrivateFile' /tmp/prol-dev-03-01.log    # 0: ningún throw capturado

# Base limpia para el plan 03-02
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from company_documents;"   # 0
docker exec prol-db psql -U prol -d prol -Atc "select count(*) from evidences where form_snapshot is not null;"   # 2

rm -f packages/db/prisma/_tmp-companydoc-fixture.ts
git status --porcelain | grep '_tmp-' && echo "ERROR: quedan scripts desechables" || echo "OK: limpio"
pkill -f 'next dev' || true

pnpm exec turbo run check-types
pnpm exec turbo run lint 2>&1 | grep 'problems'
pnpm exec turbo run build
    </automated>
  </verify>
  <done>
Sobre una `CompanyDocument` con archivo: sin sesión 401, otra empresa 403, empresa
dueña 200, ADMIN del tenant 200. Sobre una `CompanyDocument` nativa con
`fileKey = NULL`: 404 con cuerpo `No encontrado`, sin traza de excepción en el log
del servidor. Las filas y los scripts desechables ya no existen, `company_documents`
vuelve a estar vacía y las dos evidencias de `form_snapshot` siguen intactas.
`check-types` limpio, `lint` en `✖ 81 problems (0 errors, 81 warnings)`, `build`
verde.
  </done>
</task>

</tasks>

<verification>
Puertas transversales del milestone (obligatorias antes de cerrar el plan):

```bash
pnpm exec turbo run check-types                    # limpio
pnpm exec turbo run lint 2>&1 | grep 'problems'    # "✖ 81 problems (0 errors, 81 warnings)"
pnpm exec turbo run build                          # verde
```

`lint` sale con **exit 1** en la línea base sana. No es un fallo.

Comprobaciones específicas del plan:
- `git status --porcelain` no menciona ningún `_tmp-`.
- El diff toca exactamente: `packages/db/prisma/schema.prisma`,
  `packages/db/prisma/seed.ts`, `packages/db/prisma/seed-password.ts`,
  `packages/db/prisma/seed-documents.ts` y
  `apps/web/app/files/company-document/[id]/route.ts`.
- Ningún archivo de `apps/web/lib/queries/` ni `apps/web/lib/actions/` en el diff:
  eso es el plan 03-02.
- `grep -n 'formSchema' packages/db/prisma/schema.prisma` no devuelve nada nuevo.
</verification>

<success_criteria>
1. El esquema soporta el documento nativo: dos enums, cuerpo HTML y versión de
   plantilla en `ManualDocument`, y snapshot, estatus, overrides y procedencia en
   `CompanyDocument`, con las cuatro columnas de archivo relajadas a nullable.
2. `db push` aplicado sin `--accept-data-loss`, y el backfill deja como mucho una
   fila `VIGENTE` por (documento, empresa).
3. Ningún lector se rompe: `/files/company-document/[id]` responde 404 ante una
   fila sin archivo y conserva su autorización — 401 sin sesión, 403 desde otra
   empresa, 200 para la dueña y para el ADMIN del tenant.
4. La fixture existe en el seed y está aplicada a la base local sin haber borrado
   nada: dos empresas con marca distinta, manual publicado, sección, documento
   `PROCEDIMIENTO` con tabla, dos activaciones y una usuaria con sesión real en la
   segunda empresa.
5. Las dos evidencias de `form_snapshot` de la fase 1 siguen en la base local.
6. `check-types` limpio, `lint` en 0 errores y 81 advertencias, `build` verde.
</success_criteria>

<output>
Al terminar, crear
`.planning/phases/03-procedimientos-nativos/03-01-SUMMARY.md`.

Registrar en el SUMMARY:
- **Los ids de la fixture** (tenant, manual, capítulo, sección, documento, las dos
  empresas con sus `assignmentId`, la usuaria de Constructora Delta). Los planes
  03-02 a 03-07 los usan en todas sus verificaciones; sin ellos hay que volver a
  averiguarlos.
- El SQL exacto del backfill del invariante y su resultado (cuántas filas tocó).
  El plan 03-08 lo repite contra producción.
- La salida literal de `db push` (cuántas sentencias) y si pidió algo inesperado.
- La tabla de códigos HTTP de la tarea 3, con los dos controles positivos.
- Si `mammoth`, el sanitizador o cualquier otra pieza reveló una sorpresa al
  compilar, decirlo: el plan 03-03 la hereda.
</output>
</content>
</invoke>
